*.pdf
/*.txt
# 検索避けの robots.txt は上の /*.txt に飲まれるので、明示的に除外解除する
!/robots.txt
/画像素材/
/音声/
/.netlify/
# 開発用スモークテスト（Playwright）。実行時にだけ使う・デプロイには含めない。
/node_modules/
/test-results/

# 大きなzip等を誤ってコミットしない（画像素材.zip 1.36GBを誤commitした事故対策）
*.zip

# テストのログ（実行のたびに増える。共有する必要は無い）
/test-logs/
scratchpad/

# ストーリー原稿（部外秘）。ルート直下の *.txt は既に除外していたが、
# サブフォルダは対象外だったため 2026-09-06 に誤って公開リポジトリへ入った。
/ストーリー/

# 【2026-09-07】アナログ版の中身そのものなので公開対象から外した（手元には残る）。
# バックアップ: G:\マイドライブ\SEVEN非公開資料バックアップ\20260907\
# 注意: これらは実行時には読み込まれない（コード内のコメントから参照されるだけ）。
/説明書校正/
/docs/rulebook.md
/docs/cards.md
