# プロジェクト概要

ボードゲーム「7 SHADES OF S:EVEN」リメイク（仮）のデジタル版アプリ。
姉妹プロジェクトとして「7 SHADES OF S:EVEN 戦績管理システム」（対戦記録・ランキングの管理サイト、
単一の`index.html`）が別リポジトリで存在する。**2026-08-28時点の実際の場所は
`G:\マイドライブ\Cloud Code　作業フォルダ\`**（GitHub: `asobuzzasobazunihairarenai-dot/BATTLE-log`。
以前このファイルに書いていた「親フォルダ直下」は古い情報だった）。
**「無関係」ではなく密接に連携している**: 同じSupabaseプロジェクト（`prnddzrnblfysggiuzmo`）を共有し、
本アプリが `players` / `matches` / `match_feedback_replies` テーブルへ直接書き込む（対戦登録・コメント）。
`players.user_id` に本アプリのSupabaseアカウントが紐づく。両サイトとも同じGitHub Pagesのオリジンなので、
本アプリでログインしていれば戦績システム側も自動的にログイン状態になる（続き301の「マイ戦績」参照）。

## ゴールと段階

- 最終ベンチマークは MTGA（Magic: The Gathering Arena）＝カード効果を完全に自動処理する形。
- ただしいきなりそこは目指さず、まずはユドンリウムコネクト（ユドコネ）のような
  「アナログコンポーネントを画面に移植しただけ」の形を目指す。カード効果はテキスト表示のみで、
  処理はプレイヤーの自己申告に委ねる。
- ルールブックやカード効果は今後変更される可能性がある前提で設計する。

## 対戦形態（現状の合意事項）

- 最終的にはオンライン非同期対戦（同時接続ではなく、各プレイヤーが自分のタイミングで手番を進める形）を目指す。
- 実装は1人用（対戦相手なしのテストプレイ用）から始める。
- ただし「1人用→オンライン非同期」の移行で手戻りが出ないよう、最初から下記のアーキテクチャ方針を徹底する。

## アーキテクチャ方針

- ゲーム状態（盤面・手札・山札・フェーズなど）を1つのシリアライズ可能なオブジェクト（JSON）として持つ。
- 状態変更は全て「アクション」として表現し、`現在の状態 + アクション → 次の状態` の純粋な形にする
  （Reduxライクな設計）。1人用の段階ではアクションをローカルで即時適用するだけにしておき、
  オンライン非同期化する際は同じアクションをバックエンドに保存して同期する流れにそのまま乗せる。
- カード効果・カードデータはコードに埋め込まず、外部データ（JSON等）として持つ（変更に強くするため）。

## 技術スタック（現状の合意事項）

- 姉妹プロジェクト（戦績管理システム）に倣い、ビルド不要の単一HTML＋Tailwind CSS（CDN）＋Supabase
  （オンライン非同期対戦のバックエンド）という軽量構成を踏襲する。
- プラットフォームはデスクトップアプリを最終形としたいが、まずはブラウザで動くものを作り、
  将来的にTauri等でラップしてデスクトップアプリ化する想定。スマホとPCのクロスプレイも目標。
- GitHub上にPublicリポジトリを作成済み: https://github.com/asobuzzasobazunihairarenai-dot/7-SHADES-OF-S-EVEN-remake
  （デフォルトブランチは`main`）。**コミットするたびに`git push`まで行う運用**（ユーザーから明示的に指示済み）。
  実物のプレイマット画像(`assets/playmat.png`)はコピーされたくないという要望があり、`.gitignore`で
  除外した上でgit履歴からも完全に削除済み（`git filter-branch`で過去コミットからも消してある）。

## 作業ルール（Claude Code向け・新しいセッションでも必ず引き継ぐこと）

- **コミットのたびに`git push`まで行う**（上記参照、ユーザーから明示的に指示済み）。
- **プレイヤーから見える変更をしたら、アプリ内の「お知らせ／更新情報」（[src/changelog.js](src/changelog.js)）も
  随時更新する**（ユーザーから明示的に指示済み、2026-08-29）。CHANGELOG の先頭に `{ date, items, itemsEn }` を
  1件追記する。**`items`（日本語）と `itemsEn`（英語）の両方を、同じ順番・同じ件数で書く**。表示は
  現在の言語の方だけ（併記はしない）。内容は「プレイヤーに何が変わったか」を、内部用語を使わない
  言い方で書く（不具合番号・関数名・ファイル名は書かない）。開発者向けの詳細はこの CLAUDE.md の
  変更履歴の方に書く。
- **コミットの前に、このCLAUDE.md末尾へ変更履歴を1エントリ追記する**。フォーマットは
  `### YYYY-MM-DD（続きNN）：タイトル`（NNは既存の最大値+1、既存エントリを`grep 続き`等で
  確認してから採番する。過去分は[docs/dev-log.md](docs/dev-log.md)にもあるので、最大値は
  両方を見て採番すること）。何を・なぜ変えたかを、後から読む人（未来の自分含む）が経緯を
  追えるように書く。
- **CLAUDE.mdを肥大化させない（毎セッション全文が読み込まれコンテキストを圧迫するため。
  2026-08-28に続き290以前を、2026-09-05に続き385以前を[docs/dev-log.md](docs/dev-log.md)へ切り出し済み）**。本体には
  「概要・アーキテクチャ方針・作業ルール・Phase1の基盤教訓/ハマりどころ・直近の変更履歴」だけを
  残す。日付付き変更履歴（`### YYYY-MM-DD（続きNN）`）がおよそ40エントリを超えて溜まってきたら、
  古い方を[docs/dev-log.md](docs/dev-log.md)の**末尾（続き260の後）へ時系列順で追記する形で**移す
  （内容は消さず移動するだけ。dev-log.mdは古い順＝続き1が先頭・最新が末尾）。過去の経緯・
  不具合の詳細を調べる時はdocs/dev-log.mdをgrepする。
- **`supabase/functions/so7-apply-action.ts`を変更した場合は、ユーザーへの手動再デプロイが
  必要かどうかを必ず明示的に伝える**（Supabaseダッシュボードの Edge Functions 画面へ
  手動でコピペしてデプロイする運用のため、コード変更だけでは本番に反映されない）。
  単純なパススルー等で再デプロイ不要と判断した場合も、その判断自体をユーザーに伝えること。
- **公開済みの `src/*.js` は削除しない**（2026-09-05・続き423の実害）。「もう誰もimportしていない」のは
  こちらのリポジトリの話で、**利用者の端末のキャッシュには古いimportが生きている**。消すとその端末で
  404 →「Importing a module script failed」で**機能が丸ごと起動しなくなり**、しかも読み込みに失敗した
  ファイルは更新時の再取得の対象からも外れるので**永久に直らない**。使わなくなったら中身を空にするか、
  経緯をコメントで残して**ファイルは置いたままにする**。
- **`supabase_setup_so7.sql`にSQLを追記した場合は、ユーザーがSupabaseダッシュボードの
  SQL Editorで手動実行する必要があることを伝える**（このリポジトリからは直接適用できない）。
- 上記のルール自体は本ファイル（CLAUDE.md）に書かれているため、リポジトリ内で作業する
  限り、会話セッションが変わっても自動的に読み込まれ引き継がれる。

## 資料

- ルールブック: [docs/rulebook.md](docs/rulebook.md)（受領済み・テキスト化済み）
  - 元PDF `説明書.pdf`／`説明書.txt`はプロジェクト直下にあるが `.gitignore` でPDFはgit管理対象外（ローカル参照のみ）。
  - カード集（12〜14ページ）の補足説明は色ごとにまとまっているが、個別カード名との対応はcards.mdと突き合わせ済み。
- カード効果テキスト: [docs/cards.md](docs/cards.md)（受領完了・整理済み）
  - 全33種類（ファーストカード7種、エターナルカード7種、通常カード19種）。
  - 黒カードはルールブック記載（3種類）が誤りで、正しくは2種類（ユーザー確認済み）。
- 盤面レイアウト参考画像: [docs/board-reference/](docs/board-reference/)（ユドンリウムコネクト版の実際のスクリーンショット3枚。準備前/準備完了/ゲーム途中）
  - "Face Down Card Area" "Hands Area"（自分だけ中身が見える表示切替）はユドンリウムコネクトというツール自体の汎用機能であり、本ゲーム固有のルールではない（ユーザー確認済み、設計には反映しない）。

## 設計

- ゲーム状態データ構造: [docs/game-state-design.md](docs/game-state-design.md)
  - GameState / Board / Player / CardDefinition / CardInstance / Action の設計（v1、ユーザー合意済み）。
  - 乱数処理の同期方針: 結果をアクションのpayloadに確定値として含める（1人用⇄オンライン非同期で同じロジックを使うため）。

## Phase 1 実装状況

- 方針: CSS疑似3D（`perspective`+`rotateX`）で開始。3D化が必要になった場合は描画層（`src/main.js`の描画処理）だけを差し替える想定で、ゲームロジックとは分離して実装する。
- モバイル対応: PC（横長ブラウザ）優先。スマホ横向きは意識するが、実機での細かい調整はPhase1完了後に回す（現状、縦幅が狭い画面では上下のプレイヤーゾーンが見切れる）。
- ローカル確認用サーバー: ルートの `.claude/launch.json` に `7shades-digital` という設定名で追加済み（`npx serve 7shades-digital -l 3001`）。ルート直下の`static-serve`(3000番)は姉妹プロジェクト用なのでポートを分けている。
- 静的レイアウト（盤面7x7・4人分のロックエリア/手札ゾーン・共有山札等）を [index.html](index.html) / [src/style.css](src/style.css) / [src/main.js](src/main.js) に実装済み。
- **注意（ハマりどころ）**: CSS疑似3Dの箱に `class="table"` を付けたところ、Tailwind CDNの組み込みユーティリティクラス`table`（`display:table`）と衝突し、`display:grid`が上書きされるバグが発生した。Tailwind使用時はカスタムクラス名がTailwindの既存ユーティリティ名（`table`, `hidden`, `flex`, `grid`, `block`, `container`等）と衝突しないよう注意する（今回は`game-table`に変更して回避）。
- 座席配置: 手前(南)=A(自分)、左(西)=B、奥(北)=C、右(東)=D。盤面を上から見て時計回り(A→B→C→D)がルールブックの「時計回り」ターン順とそのまま一致するよう統一した。
- 手札は「テーブルに置く」のではなく「持ち上げて扇状に持っている」見た目。自分(A)は`rotateX(-40deg)+translateZ`で大きく持ち上げ扇状に開く（上下方向）。他プレイヤー(B/C/D)は逆に`rotateX(18deg)`で盤面側へ伏せ、裏向き柄（`.is-facedown`）にして密集させる（MTGA参考画像の対面プレイヤー表現に合わせた）。全員分カードごとに個別回転＋位置をJS側で計算する扇形式（`layoutFan`参照）。左右(B/D)も上下(A/C)と同じ考え方で縦方向に扇状に開く（扇コンテナ全体を90度回転させる旧方式はカード自体が横倒しになるバグがあったため、個々のカードにのみ回転をかける方式にした）。弧の向き（外側のカードが盤面から逃げる方向）は`ARC_SIGN`で座席ごとに定義。
- 手札・駒・山札系のカードはすべて正方形（このゲームの物理カードが正方形のため）。
- **駒の見た目は何度か作り直した**（試行錯誤の記録）:
  1. 3面をpreserve-3dで組む真の3D立方体 → 盤面のrotateX(42deg)との兼ね合いで正面以外がほぼ潰れる。打ち消し回転（ビルボード化）も試したが逆に正面が潰れて失敗。
  2. 2DのskewX/skewYで組む疑似アイソメトリック立方体（top/front/sideの3枚）→ 見た目は良くなったが、駒がセルより大きくはみ出すため「DOM順で後に来る隣のセル」に隠され、脚だけ残る崩れ方をした（`.cell`同士が同じスタッキング文脈でDOM順に描画されるため）。駒のあるセルに`z-index`を与えて一旦解決したが、複数面構成自体が壊れやすかった。
  3. ユドナリウム(TK11235/udonarium、Angular+TypeScript製、three.js等は不使用でCSSベース)の`game-character`コンポーネントを調査し、非対称な太さ・濃淡のボーダーだけで立体感を出す手法を試した（`.pedestal-grab-border`参考）。頑丈だが、ユーザーから「駒はユドナリウムでいう『地形』に該当する」と指摘があり、参照コンポーネントが違うと判明。
  4. **`terrain`（地形）コンポーネントを調査し、その技法をそのまま移植**。「床」を`translateZ(height)`で持ち上げ、4枚の壁を角ごとに異なる`transform-origin`+`rotateZ`/`rotateX`の組み合わせで側面に貼り付ける、正しい組み方のpreserve-3d立方体（`buildCubePiece`参照、CSSクラスは`.piece-top`/`.piece-wall-front`/`.piece-wall-back`/`.piece-wall-left`/`.piece-wall-right`）。移植直後、壁の高さが常にゼロに潰れる新たなバグが発生したが、原因は**`.piece`に付けていた`filter: drop-shadow()`**だった。CSSの`filter`は要素とその子孫を強制的に2D平坦化する仕様があり、`transform-style: preserve-3d`を無効化してしまう。影を`box-shadow`に変更して解決。この技法なら、駒がどの位置にあってもどの面がどれだけ見えるかはブラウザの3D計算が自動でやってくれるため、以前のようなセル位置に応じたJS側の手動調整（leanFactor）は不要になった。
  - 教訓1: 見た目を真似たい対象（ユドコネ）が実際どう実装されているかをオープンソースで確認する時は、コンポーネント名（駒＝character/terrainのどちらか）を早い段階でユーザーに確認する。
  - 教訓2: `transform-style: preserve-3d`を使う要素には`filter`（drop-shadow等）を付けない。影が欲しい場合は`box-shadow`を使う。
  - 教訓3: `backface-visibility: hidden`もユドナリウム本家では設定されていた（移植時に見落としていた）。真の3D構成を組む際は本家CSSを可能な限り忠実に移植する。
  - 教訓4: 強めの`perspective`（近いカメラ）の下では、駒の上面（カメラに近い＝拡大率が高い）と側面（奥行きがあり拡大率が異なる）の間にわずかな継ぎ目の隙間ができることがある。側面の高さを実際の駒の高さより少し（0.15rem程度）大きくして上面と重ねることで隙間を隠せる。
- 自分の手札カード（`.hand-card.is-self`）は視認性のため他プレイヤーの手札より大きく表示している。それに合わせて`.game-table`のgrid-template-rows（手前の行のみ拡大）、`.zone-bottom .hand-area`の高さ、扇の間隔(layoutFanのspacing)も調整済み。
- 盤面の奥行き感（台形っぽさ）は、`perspective`の値を小さくする（=カメラを近づける）ことで自然に強調できる。手動で盤面を台形に変形するのではなく、既存の3D構成（`perspective`+`rotateX`）のパラメータ調整で対応した（`perspective: 2200px→1150px`）。
- 山札は左上、エターナルカードは左下、捨て場は右下（山札と対角）に配置済み。
- 相手（B/C/D）の手札の弧（外側のカードを盤面から遠ざける演出）は、3D変形と組み合わさると非対称に見える不具合が出たため撤去し、回転のみのシンプルな扇にした（自分の手札は弧を維持）。
- 山札・エターナルカード・捨て場は、枚数に応じて厚みを持つ「積み重ね」表示にした（`.stack`/`.stack-top`/`.stack-front`、`buildCardStack`参照）。駒(`.piece`)と同じ「床+壁」の考え方だが面は上面と正面の2枚のみ。1枚あたり0.6px・最低2.4pxの厚み。初期山札は63枚（通常カード112枚－盤面49枚）、エターナルカードは7枚、捨て場は0枚。将来、盤面マスのカードスタックにも同じ`buildCardStack`を流用する想定。
- ウィンドウサイズが変わっても手札等が見切れないよう、`fitTableToViewport()`で`.game-table`の`scale`をJS側から動的に調整するようにした（`--table-tilt`というCSS変数でrotateX角度をCSS/JS間で共有し、scaleだけを画面サイズに応じて計算）。`resize`イベントでも再計算する。最大1.15倍まで拡大、それ以上大きい画面では頭打ちにしている。
  - **ハマりどころ**: `scale(s)`はX/Y軸の2軸しか縮小しない。駒の高さ等`translateZ`で表現している奥行き(Z軸)はscale()の対象外のため絶対サイズのまま変わらず、ウィンドウを小さくして盤面のX/Y footprintだけ縮むと、駒が奥行き方向にだけ間延びして「縦に伸びた柱」のように見えるバグが発生した。`scale(s)`を`scale3d(s, s, s)`に変えてZ軸も同じ倍率で縮小するようにして解決。3D変形が絡む要素をJSでリサイズする時は、`scale()`ではなく`scale3d()`を使うべき、という教訓。
- ローカルネットワーク内の実機（iPhone等）から確認する場合は `http://localhost:3001` ではなくPCのLAN IPアドレス（例: `http://192.168.10.137:3001`、環境により変わる）でアクセスする必要がある。`localhost`は「そのデバイス自身」を指すため他端末からは繋がらない。
- プレイヤーBの手札は`rotate(180deg)`で回転させている（`.zone-left .hand-fan.is-opponent`）。
- `.zone`（手札エリア・山札エリア等を囲っていた四角い枠）の背景・ボーダー・box-shadowは削除し、透明なレイアウト用コンテナのみにした。
- カラーパレットは実際のゲームの色番号に更新済み: 赤`#c70025` 橙`#ee781f` 黄`#fabe00` 緑`#22ac38` 青`#1bb8ce` 桃`#f19ec2` 紫`#915da3`（`:root`の`--color-*`参照）。
- **実物のプレイマット画像を使用**: `画像素材/プレイマット.png`（1080x1080、gitignore対象＝ローカルのみ）を`assets/playmat.png`にコピー（gitで管理、こちらはコミット対象）。画像はパレット形式(indexed PNG)でPythonが使えない環境だったため、Node.jsの`zlib`でPNGを自前デコードして構造を解析しようとしたが、画像内の細かい模様・陰影のせいで自動検出は不確実だった。最終的に「実際にブラウザで描画して目視確認・調整」という方法で位置合わせした。
  - 教訓5: 複雑なテクスチャを含む画像のレイアウト解析は、ピクセル解析より実際にブラウザで描画して目視確認・調整する方が早い場合がある。特に3D変形（rotateX）がかかった状態では正確な比較ができないので、位置合わせ時は`game-table`の`transform`を一時的に`none`にして平面化する。
  - **設計変更（重要）**: 当初`.arena`の`background-image`として実装したが、2つの問題が判明した。(1) `background-size`で拡大すると、はみ出した部分が要素の外側として切り取られ見切れる。(2) サイズ基準が「アリーナ全体（ロック帯の太さを含む可変サイズの箱）」だったため、`--lock-thickness`を変えるとアリーナの箱ごと変わり、画像も道連れで動いてしまう。これを解決するため、画像は`.playmat-bg`という独立した絶対配置要素にし、サイズ基準を「盤面(`.board`)の固定サイズ」（`--lock-thickness`に影響されない）にした上で、`background-size`ではなく`transform: scale()`で拡大するようにした（`transform`は要素の外側にはみ出しても切り取られない。`transform-origin: center`にしているので盤面中央が拡大縮小の起点になる）。
- **管理者モード（[src/admin.js](src/admin.js)）を追加**: 画面左上の「⚙ 管理者モード」ボタンから、プレイマット画像・山札・捨て場・エターナルカードそれぞれの位置合わせ用パラメータ（`拡大率`/`位置X`/`位置Y`、対応するCSS変数は`--playmat-*`/`--deck-*`/`--discard-*`/`--eternal-*`）とロックエリアの`--lock-thickness`/`--arena-gap`を、グループ分けされたスライダーでその場調整できるようにした。ユーザーが微調整して「出力をコピー」した`:root { ... }`のテキストをもらったら、その値を[src/style.css](src/style.css)の対応する`:root`変数にそのまま反映する運用。開発用ツールなので常時表示可能にしてあり、ゲーム本編のUIではない（将来的に本番ビルドでは非表示にするか検討）。
  - **注意（ハマりどころ、最終的に解決）**: `.playmat-bg`に`z-index: 0`を付けたところ、ロックエリアと盤面（`position`指定のない通常のグリッド要素）より手前に描画されてしまい、ロックエリアが完全に隠れて見えなくなるバグが発生した。`position: absolute`な要素に`z-index`を明示的に数値指定すると新しいスタッキングコンテキストができ、`position`指定のない通常のフロー要素より優先して手前に描画されるため（`z-index: auto`と`0`は同じに見えて挙動が違う）。一旦`z-index`を外し、DOM順（一番最初の子要素＝背面）に任せる方式に戻して解決したが、**これは対症療法に過ぎず、後に別の形で再発した**（詳細は下記「ロックエリアが見えない問題・真の決着」参照）。
  - 山札・捨て場・エターナルカードの`.stack`も同じ考え方（`transform: scale/translate`、`transform-origin: center`）で独立に拡大縮小・位置調整できるようにした。親の`.zone`が`overflow: hidden`だったため、`.pile-zone`に`overflow: visible`を明示して上書きしている（そうしないと拡大した山が見切れる）。
  - 山札等のpos-x/pos-yは当初`%`（自分のサイズ基準、3.2rem角の山では±100%でも±3.2rem程度しか動かせない）にしていたが、プレイマットの角まで動かすには全然足りないと分かったため`rem`（絶対量、±25rem）に変更した。中心からの移動量が大きい要素には`%`ではなく絶対単位を使うべき、という教訓。
  - **ロックエリアが「見えない」問題の真因（ハマりどころ）**: z-index修正後も「ロックエリアが見当たらない」という報告があった。調査の結果、要素自体は存在し正しい位置・色で描画されていたが（`elementsFromPoint`で最前面にあることを確認済み）、3D遠近感（`perspective: 1150px`の近いカメラ + `rotateX(42deg)`の傾き）によって薄い帯（`--lock-thickness`が数remでも）が画面上ではわずか数ピクセルの高さに圧縮され、実質見えなくなっていた。box-shadowでグローを加えても、影も要素と一緒に3D変形で圧縮されるため効果が薄かった。最終的に`--lock-thickness`のデフォルトを3.2remまで上げ、管理者モードのスライダー上限も6remまで拡張して、ユーザーが実際に見ながら十分な太さを選べるようにした（奥の辺＝カメラから遠い辺ほど強く圧縮されるため、上下と左右で必要な太さが異なる可能性がある）。
  - **山札・エターナル・捨て場がプレイマットの下に隠れる問題（ハマりどころ）**: `render()`内で`buildArena()`（プレイマット画像を含む）を各種ゾーンより「後」に`appendChild`していたため、`.arena`が`game-table`内でDOM順的に最後＝最前面に描画されていた。これは`.arena`自身が`position`指定のない通常のグリッド要素であり、`z-index`を使わないスタッキングでは兄弟要素間の重なりは単純にDOM順（後の要素が前面）で決まるため。管理者モードで山札等をプレイマットの位置まで動かすと、画面上で座標が重なった際にプレイマット側が前面に来て隠してしまっていた。`buildArena()`を`render()`内で一番最初に`appendChild`するよう順序を入れ替えて解決（プレイマットが常に一番背面＝盤面のマス目の枠線と同じ「高さ」で最前面に表示される、というユーザーの提案通りの設計にした）。
  - **手札の位置を4人独立で調整可能にした**: `--hand-a/b/c/d-pos-x/y`（範囲±10rem）を追加し、各プレイヤーの手札を盤面中心から個別にオフセットできるようにした（各`.zone-*`のセレクタで対応する`translate()`を追加）。
  - **ロックエリアをCSS Gridのトラックから完全に切り離し、独立配置の帯に再設計した（重要・アーキテクチャ変更）**: 当初は`.arena`のCSS Gridで`--lock-thickness-top/bottom/left/right`を列・行のトラックサイズとして使う実装だった（1つの変数から4辺独立の変数に分割済みだった）。しかしこの実装だと、ロック帯を1辺でも動かす（太さを変える）と`.arena`自体のグリッドサイズが変わり、それに連動して49マス・駒・他の3辺のロックエリア・両プレイヤーの手札・山札・捨て場・エターナルなど、画面上のほぼ全要素が一緒に動いてしまう不具合が発生した（ユーザー報告：「ロックエリアを調整しようとすると...それぞれの位置が追従して動いてしまいます」）。加えて、スロットの縦横比が正方形でなく「少し長方形」になってしまう問題もあった。
    - **根本原因**: ロック帯の太さという「見た目の調整値」を、CSS Gridの「レイアウトを決めるトラックサイズ」に直結させていたこと。トラックサイズを変えれば、当然そのグリッドに属する全要素・さらにその外側で暗黙に連動する要素（`.game-table`側の他のゾーン）まで再配置される。
    - **解決策**: ロックエリアを`.arena`のグリッドから完全に外し、`.playmat-bg`と同じ「`position: absolute` + `transform: translate()`」方式の独立レイヤーに変更した。デフォルト位置は`.board`の実サイズ（`--board-size`という共通変数に切り出した）を基準に、盤面の外側にちょうど1マス分（`--cell`）密着させる形で`margin-top`/`margin-left`により静的に計算する（マスと同じ大きさの帯が49マスの各辺にそのまま隙間なく並ぶ、というユーザーの要望通りのデフォルト）。ユーザーが動かす分は`transform: translate(var(--lock-*-pos-x), var(--lock-*-pos-y))`だけが担当する。`transform`は要素を「見た目上」動かすだけでレイアウト（他要素の位置計算）には一切影響しないため、1辺をどれだけ動かしても他の要素は完全に不動になる。
    - これにより「太さ」の概念自体が不要になった（デフォルトが常にマスと同じ正方形サイズのため）。管理者モードの「ロックエリア」グループは太さ4種＋間隔1種の計5スライダーから、辺ごとの位置X/Y計8スライダー（`--lock-top/bottom/left/right-pos-x/y`、範囲±10rem）に置き換えた。
    - **教訓**: 「調整可能にしたい値」が「レイアウトを決める値（グリッドのトラックサイズ、フレックスの基準サイズなど）」と一致していると、その値をいじるたびに無関係な要素まで連動して動く事故が起きやすい。見た目の微調整用途なら、最初から`transform`（レイアウトに影響しない）で実装するのが安全（プレイマット・山札・捨て場・エターナル・手札は最初からこの方式だったため無事だった。ロックエリアだけ後から同じ方式に作り直す形になった）。
  - 現在のデフォルト値（ユーザーが管理者モードで調整済み）: `--playmat-scale: 1.42`, `--playmat-pos-x: 0%`, `--playmat-pos-y: 0%`。山札・捨て場・エターナルは`scale:1`、`--deck-pos-x: 4.1rem; --deck-pos-y: 0.7rem; --discard-pos-x: -4.1rem; --discard-pos-y: -4.4rem; --eternal-pos-x: 4.1rem; --eternal-pos-y: -4.4rem;`。手札は`--hand-a-pos-y: -1.5rem; --hand-b-pos-x: -0.3rem; --hand-c-pos-y: -2.9rem; --hand-d-pos-x: 0.3rem;`（他は0）。ロックエリアは`--lock-top-pos-y: -1.8rem; --lock-bottom-pos-y: 1.8rem; --lock-left-pos-x: -1.9rem; --lock-right-pos-x: 1.8rem;`（盤面から少し離す方向、他は0）。
  - **手札がゾーンの見えない境界で上端を切られる問題**: `.zone`に共通の`overflow: hidden`があり、`.player-zone`（手札ゾーン）はそれを上書きしていなかったため、`--hand-*-pos-y`で手札を大きくずらすとゾーンの外にはみ出した部分（特に上端）が直線でスパッと切れて見えるバグがあった。`.pile-zone`は既に`overflow: visible`で上書き済みだったのと同じ理由・同じ直し方で、`.player-zone`にも`overflow: visible;`を追加して解決。
    - 副作用として、`overflow: visible`にしたことで手札がゾーン名ラベル（「プレイヤーC」等）の上に重なって隠すケースが出た（DOM順で名前ラベルが先、手札が後なので手札が手前に描画されるため）。`.label`に`position: relative; z-index: 5;`を追加し、常に手札より手前に出るようにして解決。
  - **山札・エターナル・捨て場の枚数表示をゾーン外ラベルから山自体の表示に統合**: 従来はゾーンの上に「山札（61枚）」のような独立した`<div class="label">`があったが、ロックエリアや手札を動かしてゾーン自体の位置がずれると、このラベルだけ置き去りになって浮いて見える問題が出た。ゾーン外のラベルは廃止し、枚数を`stack-top`（山の上面、名前が表示されている場所）に`.stack-count`という小さい行として統合表示するようにした（`buildPileZone`/`buildCardStack`参照。`buildPileZone`から`label`引数自体を削除しシグネチャを簡略化）。
  - **ロックエリアが見えない問題・真の決着（重要なハマりどころ）**: 上記のz-index除去やlock-thickness引き上げをやっても、ユーザーの環境では「ロックエリアだけが常にプレイマットの下に隠れる」という報告が最後まで残った（山札・捨て場・エターナルは`buildArena()`の呼び出し順序を先頭に変えたことで解決済みだったが、ロックエリアだけ直らなかった）。詳しく調査すると、`elementsFromPoint()`では`.lock-slot`が確かに最前面と判定され、`getComputedStyle()`でも色・ボーダー・box-shadowはすべて正常、祖先要素にも`clip-path`/`mask`/`overflow`/`opacity`等の怪しい設定は一切なかった。それでもスクリーンショット上は描画されない、という「ロジック上は正しいのに見た目だけ壊れている」状態だった。最終的に、`.playmat-bg`（z-index:1）・`.board`（z-index:2）・`.lock-area`（z-index:3）という具合に、重なり得る要素全部に一貫した明示的な`z-index`（＋`position: relative`）を割り振ることで即座に解決した。
    - **教訓（最重要）**: complex 3D transform階層（`perspective`+`rotateX`+`preserve-3d`が何重にもネストした構造）の奥深くにある、`position`指定のないflexコンテナは、DOM順・スタッキングコンテキストの理屈上は正しくても、ブラウザの合成（compositing）が期待通りにならないことがある。z-indexは「なるべく使わない」方が安全とは限らない。むしろ**「重なる可能性のある要素全部に、意図した重なり順を表す明示的なz-indexを必ず割り振る」方が安全**、というのがこのプロジェクトで得た結論（前述の「z-index: 0だけ足したら壊れた」という経験則は、"z-indexを一部の要素だけに足す"のが問題であって、"z-indexを使うこと自体"が問題ではなかった、というのが正しい理解）。
- **駒・カードのドラッグ操作を実装（[src/state.js](src/state.js) 新規追加）**: これがこのアプリで初めて「実際に状態が変わる」機能だったため、プロジェクト最初期に決めていたアーキテクチャ方針（状態変更は全てactionとして表現し、現在の状態+action→次の状態、という純粋な形にする）をここで導入した。
  - `state.js`: `{ tokens: [...], piles: { deck, eternal, discard } }`という最小限の状態。`tokens`は駒・手札カードを統一的に扱う配列（各要素は`{ id, kind: "piece"|"card", location, ... }`）。`location`は`{zone:"cell",row,col}` / `{zone:"lock",side,index}` / `{zone:"hand",player}`のいずれか。山札・エターナル・捨て場は個々のカードを持たず、残り枚数のカウンタのみ（実カードデータがまだ無いため）。`moveToken`/`sendTokenToPile`/`drawFromPile`という3つのaction関数のみを公開し、内部の`reduce()`でイミュータブルに次の状態を作る。将来docs/game-state-design.mdの本格的なGameStateへ拡張し、オンライン非同期化時も同じactionをサーバー同期にそのまま使う想定。
  - `main.js`: `render()`が`getState()`の中身に応じて盤面・手札・山の中身を毎回組み立て直す形に変更（ダミー固定データを渡す方式から、状態駆動の描画に変わった）。
  - **ドラッグの実装方式**: 盤面全体が`perspective`+`rotateX`の3D空間の中にあるため、ドラッグ中の要素をその3D空間の中で座標計算するのは複雑。そこで、ドラッグ中はカーソルに追従する「ゴースト」要素を3D空間の外（`document.body`直下）に浮かせて表示するだけにし、実体は動かさない。ドロップ時に`document.elementsFromPoint(clientX, clientY)`でその座標にある要素を調べ、`closest()`で盤面マス(`.cell`)／ロックスロット(`.lock-slot`)／手札エリア(`.hand-area`)／山(`.pile-zone`)のどれに該当するか判定し、該当すればactionをdispatchして`render()`で全体を再描画する（該当する要素が無ければ何もせず、ゴーストだけ消して元の位置に戻ったように見せる）。この方式なら3D変形を一切気にせず実装できる。
  - ルール適用は一切しない（Phase 1の方針通り）。駒はどの盤面マス・ロックスロットへも自由に移動でき、カードは手札↔盤面マス↔山（山札/エターナル/捨て場）のどこへでも自由に動かせる。山からのドラッグは「引く」動作として扱い、手札へ落とした時だけ有効（残り枚数を1減らし新しいカードトークンを生成）。
  - 自分(A)以外の手札・駒も含め、すべてドラッグ可能にしてある（1人でのテストプレイでは全座席を自分で操作するため）。
  - マウス操作は`pointerdown`/`pointermove`/`pointerup`で実装（`touch-action: none`も付与済みなのでタッチ操作でも動くはずだが、実機での確認はまだ）。
  - 検証時のハマりどころ: ブラウザ自動操作ツールの疑似ドラッグ（マウスの座標だけを動かす方式）では、スクリーンショット座標と実際のビューポート座標の縮尺がズレていて、狙った要素の外に離してしまい「何も起きない」ように見えることがあった。実際のアプリの不具合ではなく、検証コード側の座標計算ミス。`PointerEvent`を`dispatchEvent`で直接発行して座標をgetBoundingClientRect基準で厳密に計算し直したところ、駒の移動・手札→捨て場・山札からの引き（山札→手札）・駒→ロックスロットのすべてで正しく状態が更新されることを確認できた。
  - **ユーザー報告バグ1: 自分の手札だけドラッグできない（重要なハマりどころ）**: リリース後、自分(A)の手札カードだけがドラッグ操作を受け付けず、プレイヤーB/C/Dの手札は問題なく触れるという報告があった。原因は各`.piece`/`.hand-card`/`.stack`要素に直接`pointerdown`リスナーを付けていたこと。`document.elementFromPoint()`（ネイティブのヒットテスト、実際のpointerdownイベントがどの要素に届くかを決める仕組み）で自分の手札の座標を調べると、実際に画面に描画されている`.hand-card`ではなく、その奥にある何もリスナーの無い`.zone-bottom`（平坦なコンテナ）が返ってきた。一方`document.elementsFromPoint()`（複数形。見た目通りの重なり順のリストを返す）では正しく`.hand-card`が最前面と判定される——つまり**見た目の描画（compositing）は正しいのに、ネイティブのヒットテストだけが実際と食い違っていた**。自分の手札は`rotateX(-40deg)+translateZ(2.4rem)`で大きく持ち上げられ、テーブルの傾き(42deg)とほぼ打ち消し合ってカメラにほぼ正対する角度になっており、他の座席（`rotateX(18deg)`で盤面側へ伏せている）より3D変形の度合いが大きい。深くネストした3D transform階層でのヒットテストという、ロックエリアの表示バグ（同セッション内の別の問題、上記参照）よりさらに厄介な、ブラウザの別のバグ・限界に遭遇したと考えられる。
    - **解決策**: 個々の要素に`pointerdown`を直接付けるのをやめ、`#game-table`に1つだけリスナーを付け、その中で`document.elementsFromPoint(e.clientX, e.clientY)`（信頼できる方）を使って「実際に何がクリックされたか」を自前で判定する方式（`findDraggableAt()`）に統一した。ドロップ先の判定は元々この方式だったので、開始・終了の両方で一貫した仕組みになった。
    - **教訓**: 深い3D transform階層では、ブラウザの「見た目の描画」と「ネイティブのヒットテスト（クリック判定）」が食い違うことがある——しかも今回はどの辺が食い違うか予測しづらい（左手札は問題なかった）。ネイティブのイベント委譲（要素に直接listenerを付けて`e.target`に頼る）を信用せず、`elementsFromPoint()`で自前判定する方式の方が、この種の3D UIでは安全。
  - **ユーザー報告バグ2: 山から引いたカードが手札に反映されないことがある**: 「実際には引けているのに画面に反映されない、別の場所に成功する操作をした瞬間にまとめて反映される」という報告があった。原因は`onDragEnd()`内の`pileSource`（山からのドラッグ）の分岐で、`drawFromPile()`（state更新）は呼んでいたが、その後`render()`を呼び忘れていたこと（手札へ落とせなかった場合の`else`節にだけ`render()`があった）。stateは正しく更新されるがDOMには反映されず、次に別の操作が`render()`を呼んだ時に、それまで溜まっていた分がまとめて表示されていた。`pileSource`分岐の末尾で無条件に`render()`を呼ぶよう修正。
    - **教訓**: state+action方式では「dispatchしたら必ずrenderする」を徹底しないと、こういう「stateは正しいのに画面だけ古い」バグが起きる。分岐が増えるほど呼び忘れの経路ができやすいので注意する。
  - **ユーザー報告バグ3件（初回リリース直後）**: (1) 手札カードをドラッグしている間、掴んでいるはずのそのカードが手札に残ったまま(ゴーストと二重に)見える。(2) カードをロックスロットにドロップしても置かれず手札に戻ってしまう。(3) カードを盤面49マスに置いても何も描画されない。
    - (1)の原因と修正: ドラッグ中は元のDOM要素をそのまま残し、カーソルに追従するゴーストを別途重ねて表示していただけだった。`startTokenDrag()`でドラッグ開始時に元要素へ`visibility: hidden`を付けるようにした（`onDragEnd`は常に`render()`で全DOMを作り直すため、明示的な後始末は不要）。
    - (2)の原因と修正: `findDropTarget()`のカード分岐が`.hand-area`/`.pile-zone`/`.cell`しか判定しておらず、`.lock-slot`への該当が無かったため、ロックスロットへ落としても`dropTarget`が`null`になり「何も起きず手札に留まる」という結果になっていた。駒・カード共通で`.cell`と`.lock-slot`を先に判定し、カードだけ追加で`.hand-area`/`.pile-zone`も判定する形に統一。
    - (3)の原因と修正（一番本質的な欠落）: 盤面マス・ロックスロットの上の駒を描画する`renderPieceTokens()`が`token.kind === "piece"`のトークンしか処理しておらず、カードが盤面/ロックへ移動しても描画する処理が最初から存在しなかった（stateは正しく更新されるが、対応する描画コードが無かった）。カード用の簡易な見た目（`.board-card`、置かれたマス/スロットにフィットするだけの平たいカード）を追加し、`renderPieceTokens()`を`renderBoardTokens()`に一般化して駒・カード両方を描画するようにした。あわせて、ドラッグ開始判定（`findDraggableAt`）にも`.board-card`を追加し、一度盤面に出したカードを再度つまめるようにした。
    - **教訓**: 「stateのlocationとして定義したゾーン」と「実際に描画コードが対応しているゾーン」がズレていないか、機能追加のたびに両方を見比べて確認する必要がある。今回は駒についてだけ実装して満足し、カードの「盤面/ロックに置く」経路の描画を作り忘れていた。
  - **ドロップ先のハイライト表示を追加**: ドラッグ中、今離すとどこに置かれるかが分かるよう、対象のマス/ロックスロット/手札エリア/山札等を黄金色の枠と光彩でハイライトするようにした。`findDropTarget()`が返す値を`{ zone, ... }`から`{ location, el }`（実際に該当するDOM要素も含む）に拡張し、`pointermove`のたびに前回ハイライトしていた要素からクラスを外し、新しい対象に`.drop-target-active`を付け替える（`updateDropHighlight()`）。レイアウトに影響しないCSSクラスの付け外しだけなので副作用はない。
  - **ロックエリアと手札の干渉問題・真の原因と解決（重要なハマりどころ）**: カードをロックエリアへドロップしようとしても置かれず手札に留まる、という報告があった。ユーザーが`--hand-*-pos-*`で手札を盤面中心から遠ざける調整をしたが直らず、追加したドロップ先ハイライト機能（上記）でようやく可視化できた：手札の当たり判定（`.hand-area`）がロックエリアの帯とほぼ同じ画面位置に重なって表示されていた。
    - **根本原因**: 手札を持ち上げる`transform`（`translate(pos-x,pos-y) rotateX(...) translateZ(...)`）を、それまで子要素の`.hand-fan`にだけ付けていた。すると**見た目**（`.hand-fan`とその中の`.hand-card`）はカメラ側へ大きくリフトされて描画されるのに、ドラッグの当たり判定に使っている`.hand-area`自身は一切transformされない「平らな机の上に置かれたままの」位置に取り残される。つまり「見た目のカードの位置」と「クリック/ドロップ判定の位置」が別の場所にズレてしまい、`.hand-area`（見た目上は空っぽに見える場所）がたまたまロックエリアの帯とほぼ同じ画面位置に重なっていた。`--hand-*-pos-*`で見た目のカード位置を動かしても、当たり判定の位置（transformされていない`.hand-area`自体の位置）は変わらないため、直らなかった。
    - **解決策**: 持ち上げtransformを`.hand-fan`ではなく`.hand-area`自身に付け替えた。transformは子要素からのレイアウト計算には影響しない（親の`.player-zone`内でのフレックス配置は変わらない）ため、見た目は一切変わらず、当たり判定の箱だけが正しく「見た目通りの位置」に来るようになった。
    - あわせて、`.hand-area`のサイズも見直した。以前は「扇が伸びる方向」が常に`width:100%`（盤面と同じ幅）、「伸びない方向」も固定の`5.2rem`/`9rem`だったため、実際に見えている手札の範囲よりずっと広い当たり判定になっていた。`--hand-{a,b,c,d}-size`（扇が伸びる方向、手札3枚を基準に枚数へ比例して自動で伸縮、`main.js`の`buildPlayerZone`でJS側から計算）と`--hand-{a,b,c,d}-thickness`（伸びない方向、固定値）の2種類を管理者モードから調整できるようにし、カード1枚分程度まで絞れるようにした。
    - **教訓**: 3D transformを子要素に付けるか親要素に付けるかで、見た目は同じでも「その要素自身の当たり判定がどこに来るか」が変わる。当たり判定に使う予定の要素には、見た目のリフト用transformも一緒に持たせておくべき（別の要素に分散させない）。
  - **山札から盤面マス／ロックスロットへ直接ドロップできない**: `onDragEnd`の`pileSource`分岐が「手札へ落とした時だけ`drawFromPile`する」という設計になっていた（山から直接盤面へ置く操作は意味が曖昧だろうと判断し、意図的に制限していた）。ユーザーから「山札からカードをマスやロックエリアに置けない」という要望があり、他の操作は全てルール適用なしの自由な移動なのにここだけ制限されているのは一貫性がないと判断し、`zone !== "pile"`（山以外ならどこでもOK）に緩和した。山(pile)自体への直置きだけは今後も禁止のまま（piles型は個々のカードを保持しない残り枚数カウンタのみのため、置き場所として使うとカードが行方不明になる）。あわせて、山から盤面マス／ロックスロットへ直接引いたカードは表向き（`faceUp: true`）になるよう`state.js`の`drawFromPile`を調整した（手札への場合のみ、自分(A)かどうかで表/裏を分ける従来のロジックのまま）。
  - **山札・捨て場・エターナルのドロップハイライトが実物とズレて大きすぎる**: ハイライト対象を`.pile-zone`（グリッド上の枠、実際の山より広く、山自体は`transform: translate/scale`でズラして配置されているためズレる）にしていたのが原因。`findDropTarget`のpile分岐で、`.pile-zone`ではなくその中の実際の`.stack`要素（見た目の山そのもの）を返すように変更し、ハイライトのサイズ・位置が実物と完全に一致するようにした。
- **実際のカードデータを反映（[src/cards-data.js](src/cards-data.js) 新規追加）**: それまでカードは全て中身の分からない灰色の板だった。docs/cards.md・docs/rulebook.mdから実際のカード名・色・枚数を抽出し、外部データファイルとして持たせた（CLAUDE.mdの技術スタック方針「カードデータはコードに埋め込まず外部データとして持つ」に沿う）。
  - 通常カード19種・合計112枚: 赤橙黄緑青桃紫は各色2種×7枚＝98枚、虹（なないろの欠片）1種×7枚、無色（白黒）4種で合計7枚（ユーザー確認：色落ちキャットのみ1枚、他3種＝なないろの巨光/白の意思の覚醒/黒の契約の烙印は2枚ずつ）。ルールブックには「黒3種類・各1枚」とあるが、これは既知の誤記（実際は黒2種類）で、今回の枚数もその修正を反映している。
  - エターナルカード7種・ファーストカード7種（各色1種）もデータ化したが、今回はまだ実際のゲーム内で使っていない（ファーストカードの配布・ロックなどのセットアップ自動化はPhase1のスコープ外、エターナル山は実カード7種をシャッフルして保持するようにした）。
  - `state.js`: `createInitialState()`でNORMAL_CARDSをcount分だけ展開してシャッフルした配列を山札とし、そこから各プレイヤーの初期手札を実際に配るように変更（以前はダミー枚数のみで山札の枚数と無関係だった）。`piles`は単なる残り枚数のカウンタから、実カードidの配列（末尾＝一番上、popで引く・pushで積む）に変更。これにより捨て場に捨てたカードも身元を保ったまま、後で引き直すと同じカードが出てくるようになった（山札・エターナル・捨て場を合わせた合計は常に112/7に保たれる）。
  - 表示: 自分の手札・表向きの盤面/ロックのカードは実際のカード名＋色で表示する（`.is-color-*`、`--color-*`を流用）。相手の手札は引き続き中身を明かさない裏向き表示のまま（`token.faceUp`ではなく`isSelf`で判定、自分の手札は常に見える・他人の手札は常に見えない、という従来の割り切りを維持）。捨て場はルール通り「表向きに積む」場所なので、一番上のカードの名前・色をそのまま`stack-top`に表示するようにした（山札・エターナルは裏向き積みのため中身は明かさない）。
  - **今回のスコープ外だった効果テキストは、後述の実物画像反映で実質解決した**（画像自体に到達効果・手札効果・フレーバーテキストまで描かれているため）。
- **実物のカード画像を反映、カードの表裏をダブルクリックで切り替え可能に**: `画像素材/`配下に実物のカード画像（通常カード19種・エターナルカード7種・裏面3種）が用意されていたため、`assets/cards/`にカードidをファイル名としてコピーして使うようにした（プレイマットと同じ理由で、実際の絵柄はgit管理・公開リポジトリには含めない。`.gitignore`に`/assets/cards/`を追加）。
  - `cards-data.js`に`getCardImagePath(cardId)`/`getCardBackImagePath(cardId)`を追加（裏面は通常カード用・エターナルカード用の2種類があり、`cardId`が`"eternal-"`で始まるかで判別）。画像には既にタイトル・色・効果テキストが描き込まれているため、`.is-color-*`によるテキスト＋色分けの簡易表示は丸ごと廃止し、`background-image`で画像を敷くだけにした（`.hand-card`/`.board-card`/山の`.stack-top`・`.stack-front`）。山札・エターナルの山も裏面画像を敷いて、より物理カードの見た目に近づけた。
  - **表裏のデフォルトを見直した（ユーザー要望）**: 従来は山や手札から盤面/ロックへ移動すると自動的に表向きになっていたが、「手札や山札からドロップする時は基本裏面、自分の手札に加わる時だけ自分に見えるよう表面」という物理カードの直感に合わせて逆にした。`state.js`に`faceUpForLocation(location)`という共通関数を作り、`MOVE_TOKEN`（既存カードの移動）・`DRAW_FROM_PILE`（山から引く）の両方でこれを使って表裏を決めるようにした（移動先が`hand`かつ`player === "A"`の時だけ表向き、それ以外は裏向き）。
  - **場のカードはダブルクリックで表裏反転できるようにした（後に判定方式を修正、下記参照）**: 新しい`FLIP_TOKEN`アクション（`state.js`の`flipToken(tokenId)`）を追加。手札のカードは対象外（手札は常に「自分だけ見える／他人には見えない」という固定表示のため、個々にめくる概念がない）。
  - **ロックエリアの色を塗りつぶしから枠線のみに戻した**: 表示バグ調査時に視認性確保のため`slot.style.background`を色で塗りつぶしていたが、z-index修正で本質的な表示バグが解決していたため不要になっていた。塗りつぶしを外し、`.lock-slot`のCSS既定（暗い半透明背景）＋着色した枠線＋うっすらしたグロー(box-shadow)だけの控えめな配色に戻した（`main.js`の`buildLockArea`）。
- **「山札一覧」ビューアを追加（[src/deck-viewer.js](src/deck-viewer.js) 新規）**: テキストデータ(cards-data.js)と実際のカード画像がちゃんと紐づいているかをユーザー自身が目視確認できるようにする、開発用のデバッグツール。画面右上の「📋 山札一覧」ボタンから開くモーダルで、通常カード19種・エターナルカード7種それぞれについて「サムネイル画像・タイトル・枚数」を一覧表示し、各カードの「補足」ボタンを押すとdocs/cards.mdの「補足:」欄（画像には印刷されていないルール解釈の補足説明）を開閉できる。`cards-data.js`の各カード定義に`note`フィールドを追加してこの補足テキストを持たせた（効果本文自体は画像に描かれているため引き続きデータ化していない）。管理者モードと同様、ゲーム本編のUIではなく開発用ツールという位置づけ。
- **カード裏面画像が「角だけアップになる」バグの真因を特定・修正（重要なハマりどころ）**: 前回「ファイルは正しい、表示サイズの問題では」と暫定回答していたが、ユーザーが「裏面画像の角だけがアップして描画されている」と正確に症状を言語化してくれたことで根本原因を特定できた。
    - **原因**: CSSの`background`ショートハンドは、明示的に指定していないサブプロパティ（`background-size`/`background-position`/`background-repeat`等）も暗黙に初期値へリセットしてしまう。`.hand-card.is-facedown`・`.board-card`・`.board-card.is-facedown`・`.pile-deck`/`.pile-discard`/`.pile-eternal`が`background: <グラデーション>`という形でフォールバック背景を指定しており、これらは`.hand-card, .board-card, .stack-top { background-size: cover; ... }`という共通ルールより詳細度が高い（またはファイル内で後に書かれている）ため、後から効いていた`background-size: cover`を暗黙に`auto`へ戻してしまっていた。結果、JS側で`element.style.backgroundImage`だけを上書きしても、サイズは原寸(auto)・起点は左上(0% 0%)・繰り返しはrepeatのままになり、小さな枠の中に画像(433×433px)の左上の角だけが原寸大で表示される、という症状になっていた。
    - **解決策**: 該当箇所をすべて`background:`ショートハンドから`background-image:`ロングハンドに変更した。ロングハンドは指定したサブプロパティ(background-image)しか変更しないため、他のルールで設定した`background-size: cover`等が無事に効くようになる。
    - **教訓**: CSSショートハンドプロパティ（`background`/`font`/`border`等）は「指定していない部分は前の値を引き継ぐ」のではなく「指定していない部分も初期値にリセットする」という仕様のため、複数の場所で同じプロパティ系統を別々に指定する設計（今回のような「共通ルールでbackground-sizeだけ指定、個別ルールでbackground-imageだけ指定」）をする時は、個別ルール側は必ずロングハンドを使う。
  - **ダブルクリックでの表裏反転が効かない問題も同根のバグだった**: 手札の当たり判定ズレ（`.hand-area`の項参照）と全く同じ理由で、ネイティブの`dblclick`イベントも3D階層内でのヒットテストに頼っているため正しく発火しないことがあった。`dblclick`リスナー自体を廃止し、ドラッグ開始判定（`elementsFromPoint`ベースの`findDraggableAt`、確実に動作することが分かっている）に統合。同じ`.board-card`トークンに対して400ms以内に2回`pointerdown`があったら、ドラッグの代わりにflipToken()を呼ぶ、という自前のダブルクリック検出にした。
  - **ロックスロットの枠線太さを管理者モードで調整可能に**: `--lock-slot-border-width`（デフォルト0.1rem、以前は固定3px）を追加し、`border: 3px solid ...`だった記述を`border-width`/`border-style`/`border-color`に分割した。
  - **プレイヤーB/Dの名前ラベルを90度回転**: 盤面の左右の辺と平行に読めるよう、`.zone-left .label { transform: rotate(90deg); }` / `.zone-right .label { transform: rotate(-90deg); }`を追加（transformはレイアウトに影響しないため、回転してもゾーン内の配置自体は変わらない）。
- **プレイヤー名ラベルの位置を管理者モードで調整可能に**: `--label-{a,b,c,d}-pos-x/y`を追加。B/Dは回転(rotate)の後にtranslateを重ねる形（`rotate(90deg) translate(x, y)`）にし、回転済みの見た目の向きのままtranslateで動かせるようにした。
- **ドラッグ中のカード/駒が実物と違って見える問題を修正**:
  - カードのドラッグゴーストが常に紺色の汎用プレースホルダーになっていた。`createGhost()`/`startPileDrag()`のゴースト生成時にも`background-image`を実際のカード画像（表向きなら顔、裏向きなら裏面）で設定するようにした。捨て場からのドラッグは表向きに積まれている実物画像、山札・エターナルからのドラッグは裏面画像を使う。
  - 駒のドラッグゴーストが立方体ではなく平らな正方形(`.drag-ghost-piece`)になっていた。3D空間の外(document.body直下)に置く都合上、本物の`.piece`をそのまま持ってきても盤面のperspective/rotateXが無いと立体的に見えない。`perspective: 1150px`を持つouter要素と、盤面と同じ傾き(`--table-tilt`)を持つinner要素の入れ子(`.drag-ghost-piece-outer`/`-inner`)を新設し、その中に`buildCubePiece()`で作った本物の駒をそのまま入れることで、ドラッグ中も立方体の見た目を保てるようにした。
- **山札の厚みが枚数に応じて変化しているように見えない問題の真因を特定・修正（重要なハマりどころ）**: `--stack-height`の値自体は枚数に応じて正しく計算されていたが、それを表現する`.stack-front`（山の側面）が実際には高さ0に潰れて全く見えていなかった。
  - **原因1**: `.stack-front`に`filter: brightness(0.6)`が残っていた。これは駒の壁面で既に踏んだのと全く同じ「filterはpreserve-3dを無効化する」バグ。`box-shadow: inset 0 0 0 999px rgba(0,0,0,0.4)`（箱全体を覆う半透明の黒）に置き換えて解決。
  - **原因2（filterを直しても直らず、真因はこちら）**: `.stack`から`.game-table`に至る祖先の`.pile-zone`（山札/エターナル/捨て場ゾーンの共通クラス）が`transform-style`を明示指定しておらず既定値の`flat`のままだった。駒側の祖先チェーン（`.cell`→`.board`→`.arena`→`.game-table`）は全て`preserve-3d`なのに対し、山側だけこの`.pile-zone`が素通りできていなかった。`.pile-zone`自体はtransformを持たない（＝自分自身が回転等をしているわけではない）要素だが、それでも間に`transform-style: flat`の要素が挟まっていると子孫の3D変形（`.stack-front`のrotateX）が壊れて描画されなくなることが、駒との比較実験で判明した。`.pile-zone`に`transform-style: preserve-3d;`を追加して解決。実際に山札を15枚引いて厚みが目に見えて薄くなることを確認済み。
  - **教訓**: 「自分自身はtransformを持たない要素」であっても、`transform-style: flat`（既定値）のままだと3D階層の途中に挟まるだけで子孫の3D変形を壊すことがある——手札(`.hand-area`)の当たり判定のケースで学んだ「transformの無い要素は3Dに対して透過的」という理解は、正確には「他要素からの見た目上のヒットテストには影響しないが、preserve-3dの伝播そのものは別問題」だったとわかった。3Dで組んだ構造をコピー/流用する時は、祖先チェーンの`transform-style`が全て`preserve-3d`になっているかを逐一確認する必要がある。
- **場・ロックエリア間の移動でカードの表裏が変わってしまう問題を修正**: 前回導入した「移動先に応じて表裏を決める」ロジック(`faceUpForLocation`)を`MOVE_TOKEN`に単純適用していたため、マス→マス・マス→ロック・ロック→ロックなど「既に場に出ているカードを場の中で動かすだけ」の操作でも毎回裏向きにリセットされてしまっていた。`state.js`に`isTable(location)`（zoneがcellまたはlockかどうか）を追加し、「移動元と移動先が両方とも場/ロックエリア」の場合は表裏を変更しない（既存の`faceUp`をそのまま維持する）よう修正。手札↔場/ロックエリアの移動（新しくカードが場に出る／手札に加わる）の時だけ、引き続き`faceUpForLocation`で表裏を決め直す。
- **プレイヤー名ラベルの調整レンジを拡大**: `--label-*-pos-x/y`の可動域が±8remで、ユーザーがすでに上限(-8rem)まで使い切っていたため、±24remまで拡大した。
- **山札の側面を無地のライトグレーに変更**: `.stack-front`（山の側面）に一番上のカードと同じ画像を敷いていたため、薄い帯に画像が引き伸ばされて見苦しかった。`buildCardStack()`で側面には画像を敷かないようにし（`pileClass`も外し、常に`.stack-front`のみのクラスにした）、CSS側で無地のライトグレー(`#cbd5e1`)を背景色にした（既存の暗くする`box-shadow`はそのまま残るので、天面より少し暗いグレーの側面になる）。
- **駒・マス・カード・山札等にホバーハイライトを追加**: 駒がカードの上に乗っている時など、「今クリックしたら何が掴めるか」が分かりにくいという要望に対応。`findDraggableAt()`と同じ優先順位（駒＞カード＞山）で判定する`findHoverTarget()`を新設し（空のマス/ロックスロットもホバー対象になる点だけ異なる）、`#game-table`の`pointermove`でカーソル直下の対象に`.hover-active`（水色の枠線、ドラッグ中の黄金色ドロップ先ハイライトとは別役割）を付け外しする。ドラッグ中(`dragSession`がある間)はドロップ先ハイライトと役割が被るため休止する。
  - **ハマりどころ**: 当初`.hover-active`に`box-shadow`も付けていたが、`.piece`等ハイライト対象自身が既に別の`box-shadow`を持っていると、後から効いた方のルールで完全に上書きされ、光彩が全く見えなくなっていた（box-shadowは複数ルールの値を自動合成しない）。`outline`は他のどのルールも使っていないプロパティなので、確実に見えるよう`outline`のみで表現するようにした。
- **場のカードを同じマスに重ねると下に潜り込むことがある問題を修正**: `renderBoardTokens()`は`state.tokens`配列の順番通りに`appendChild`するため、配列内で後ろにあるトークンほど画面上で手前（一番上）になる。しかし`MOVE_TOKEN`の実装が`Array.prototype.map()`でその場更新するだけだったため、トークンの配列内での位置（＝重なり順）が「最後に動かした順」ではなく「元々作られた順」のままになっていた。後から同じマスに重ねたつもりのカードが、実は配列内では手前に居た別のカードより「後ろ」に留まり、見た目上潜り込んで見えることがあった。`MOVE_TOKEN`を「動かすトークンを配列から一旦取り除き、更新した上で配列の末尾に追加し直す」実装に変更し、常に「最後に動かしたものが一番上」になるようにした。
- **自分の手札が増えると盤面ごとレイアウトが崩れる問題を修正（重要なハマりどころ）**: `.hand-area`の幅は手札の枚数に比例して自動で広がる仕様（`--hand-a-size`）だが、これが`.player-zone`の通常のフロー内（フレックスの子要素）に置かれていたため、手札が多い時にその大きな幅が`.player-zone`自身の幅、さらには`.game-table`のgridトラック（auto幅の中央列。`.arena`・`.zone-top`・`.zone-bottom`が共有）にまで影響し、盤面(`.arena`)側が押し出される形でズレてしまっていた。`.hand-area`を`position: absolute`にして通常のフローから完全に外し、`.player-zone`自身の中心(50%, 50%)を基準に`translate(-50%, -50%)`で自己センタリングするよう変更（4方向すべての持ち上げtransformの先頭に追加）。これにより手札の幅がどれだけ変わっても周囲のレイアウト計算に一切影響しなくなり、手札12枚まで実際に増やして盤面中心とのズレが実質0になることを確認した。
- **山の側面が一部の角度で透明に見える問題を修正**: `.stack`は側面(前面)を1枚(`.stack-front`)しか持っておらず、駒(`.piece`)のような4面（前後左右）の壁が無かった。見る角度によっては存在しない面（特に右側）の奥が透けて見えていた。駒と同じ「床+4面の壁」の技法で`.stack-back`/`.stack-left`/`.stack-right`を追加し、正しい直方体にした（無地のライトグレー、面ごとに`box-shadow`の濃さを変えて陰影をつけている）。
- **駒のホバーハイライトを、外枠ではなく立方体の各面が光る表現に変更**: `.piece.hover-active`の外枠(outline)を無効化し、代わりに`.piece.hover-active .piece-face`（上面+4つの壁、個別のdiv）それぞれに`box-shadow`の光彩を付けた。`.piece-face`はそれ自体は他でbox-shadowを使っていないため、`.hover-active`本体で踏んだ「box-shadowは複数ルールの値を合成しない」問題を回避できる。
- **表向きカードのホバー拡大プレビューを追加**: 自分の手札・表向きの盤面/ロックのカード・捨て場の一番上のカード（表向き積みのため中身が分かる）にカーソルを乗せると、実物画像を拡大表示する`#card-preview`要素を新設した。`getVisibleCardId(el)`（`getPreviewImagePath`から切り出した共通ヘルパー、右クリックメニューとも共用）が対象の種類に応じてstateから実際のcardIdを引く（DOM上の`background-image`文字列を読み取るのではなく、必ずstateを正とする設計を踏襲）。サイズは`--card-preview-size`（デフォルト20rem、管理者モードで調整可）。位置決めは`left`+`bottom`（`top`ではなく）を使うことで、「拡大の起点は左下端に固定し、サイズが変わってもその起点はズレない」という要望を素直に満たせる。画面右端をはみ出す場合だけ表示方向を反転する。
- **カードの右クリックに専用メニューを追加**: `#game-table`上では常にブラウザ標準の右クリックメニューを出さないようにし（`contextmenu`を`preventDefault()`）、中身の分かるカード（`getVisibleCardId`が非nullを返す対象）を右クリックした時だけ「カード補足を見る」という専用メニューを表示するようにした。クリックすると`docs/cards.md`の補足テキストを表示するモーダルが開く。ダブルクリックと同じ理由で、`contextmenu`イベントのネイティブ`e.target`は信用せず、`findHoverTarget()`（elementsFromPoint()ベース）で対象を判定している。
- **駒のサイズを少し小さく、盤面のカードを少し大きく**: 駒がカードの上に乗ると下のカードがほぼ見えなくなるという指摘に対応。`--piece-size`を2.4rem→2.1remに、`.board-card`の`inset`を8%→3%にした。
- **駒にスキン画像を反映**: `画像素材/駒スキン/`（7色分）を`assets/pieces/`にコピーし（プレイマット・カード画像と同じ理由で、`.gitignore`の`/assets/pieces/`で公開リポジトリには含めない）、`buildCubePiece()`で単色塗りつぶし(`background`)ではなく`background-image`でスキン画像を敷くようにした。上面・4つの壁すべてに同じ画像を敷き、壁ごとの陰影（既存の`filter`/`box-shadow`）はそのまま効く。
- **プレイヤーCの名前ラベルを180度回転**: `.zone-top .label`のtransformに`rotate(180deg)`を追加（B/Dのrotate(90deg)/(−90deg)と同じ考え方）。
- **ロックエリアに置いたカードを、そのロックの持ち主から見て読みやすい向きに回転**: `.lock-top .board-card { transform: rotate(180deg); }`、`.lock-left`は90deg、`.lock-right`は-90deg（`.lock-bottom`＝自分Aは元々正しい向きなので変更不要）。座席配置（手前=A、左=B、奥=C、右=D）に合わせた回転角。
- **捨て場が空の時の塗りつぶしを透明に変更**: `.pile-discard`の紺色グラデーションを`background-image: none; background-color: transparent;`にした（ロックスロットの「うっすら枠だけ」と同じ考え方）。カードが入れば実物画像がinline styleで上書き表示される。
- **ファーストカードのパイルを新規追加（プレイマット右上）**: これまでデータ(`FIRST_CARDS`)だけ存在し実際のゲームには未接続だった（Phase1のセットアップ自動化スコープ外としていた）。ユーザーからの要望で、エターナルカードと同じ「シャッフルされた1つの山」として、盤面グリッドの空いていた右上（`.zone-first { grid-column: 3; grid-row: 1; }`。deck=左上, eternal=左下, discard=右下, first=右上で4隅が埋まった）に追加した。山札・エターナルと同様に裏向き積み（`assets/cards/back-first.png`、`getCardBackImagePath()`に`"first-"`接頭辞の判定を追加）。プレイヤーへの自動配布・ロックは引き続き未実装で、ドラッグで手動配布する運用。あわせて「山札一覧」ビューアにもファーストカードのセクションを追加し、`FIRST_CARDS`各定義に`note`（docs/cards.mdの補足）を追加した。
- **同じマスに重なったカードに「+N」バッジを追加、ホバーで一覧拡大表示**: `getCardStackGroups()`でトークンをlocationごとにグループ化し（駒は数えず`kind:"card"`のみ）、2枚以上重なっているグループの一番上のカードに`.stack-badge`（例:「+2」）を付けるようにした（`renderBoardTokens`）。バッジは`findHoverTarget()`で最優先に判定され、ホバーすると`#stack-preview`に重なっている全カードを横一列で拡大表示する。各カードは自分自身の`faceUp`に従って表向き/裏向きの画像を出す（下に潜んでいる裏向きカードの中身を、一覧表示によって覗けてしまわないようにするための配慮）。
- **「山札一覧」をスクロール不要のグリッド表示に再設計**: 1カード1行の縦積みレイアウト（33件で画面に収まらずスクロール必須だった）から、サムネイル中心のグリッド（`grid-template-columns: repeat(auto-fill, minmax(5rem, 1fr))`）に変更し、パネル幅も`min(68rem, 96vw)`に拡大した。タイルをクリックすると、そのタイルの直後（`grid-column: 1 / -1`で全幅）に補足テキストが開閉する。サブタイトルの「（データと画像の紐づき確認用）」は削除。
- **「+Nバッジのホバー一覧表示」機能を撤去し、右クリックメニュー方式に置き換え（ユーザー判断による仕様変更）**: ホバーで重なりカードを横一列拡大表示する機能が正しく動作しないと報告があり、かつ「何枚も重なっていると横一列表示自体が原理的に厳しい」との判断で、デバッグではなく設計変更として対応した。`getStackPreviewEl`/`showStackPreview`/`#stack-preview`/`.stack-preview-card`を全て削除。代わりに、右クリックメニューに「重なっているカードを見る」を追加し、クリックすると要クローズの`#stack-modal`（`flex-wrap`+`max-height`+`overflow-y: auto`で何枚重なっても画面内に収まる一覧）が開く方式にした。新設した`getStackTokensAt(el)`が、右クリック対象（`.board-card`または`.stack-badge`）の属する重なりグループ（2枚以上）の全トークンidを`getCardStackGroups()`から引く。
- **右クリックメニューを単一項目決め打ちから配列ベースに変更**: 「カード補足を見る」を出すか「重なっているカードを見る」を出すか（あるいは両方）を対象に応じて動的に決める必要が出たため、`showContextMenu(clientX, clientY, cardId)`だったシグネチャを`showContextMenu(clientX, clientY, items)`（`{label, onClick}`の配列）に変更した。`initContextMenuHandlers`側で`getVisibleCardId`と`getStackTokensAt`それぞれの結果に応じてitemsを組み立てる。
- **カード補足モーダルに拡大画像を追加**: `showCardNoteModal(cardId)`が今までタイトル+補足テキストのみだったのを、`getCardImagePath(cardId)`の画像(`.card-note-image`)を先頭に追加した。
- **「+N」バッジを`getVisibleCardId`の対象に追加**: バッジ自体を右クリック/ホバーした時にも、その下の一番上のカードと同じ扱い（表向きなら中身が分かる）になるよう、`getVisibleCardId`の先頭に`.stack-badge`の判定を追加した（バッジの`dataset.stackTokens`の最後のidが一番上のトークン）。これによりバッジへのホバーでも通常の`#card-preview`拡大表示が出るようになった（横一列プレビューの代替）。
- **駒スキン画像の角に隙間が見えるバグを修正**: `.piece-face`（駒の5面共通の基底クラス）に`border-radius: 0.2rem`が付いていたため、正方形のスキン画像がその分内側に丸められ、立方体の角で隣接面との間に隙間が見えていた。単色塗りつぶし時代の名残だったため、border-radiusごと削除した。
- **山札・エターナル・捨て場・ファーストカードの山の上面から、常時表示の名前・枚数テキストを撤去し、ホバー時のみ表示するツールチップに変更**: `buildCardStack()`から`nameEl`/`countEl`（常時表示だった`.stack-top`の子要素）を削除し、代わりに新設した`getPileTooltipText(el)`（`.stack[data-pile]`要素から`PILE_CONFIG`のラベルと`state.piles[pileKey].length`を組み立てる。捨て場は空でなければ一番上のカード名を表示）と、それを表示する`#pile-tooltip`（`updatePileTooltip()`、`updateHover`から呼ぶ）を追加した。
- **カード拡大プレビューが画面上端で見切れるバグを修正**: `positionPreviewPanel()`は元々画面右端のはみ出し（left/right反転）にしか対応しておらず、盤面奥（画面上部）のカードをホバーすると、`bottom`基準で上方向に伸びるプレビューパネルが画面上端(`y=0`)より上にはみ出て見切れていた。カーソルY座標とパネルの実測高さから「上方向に広げると画面上端を割るか」を判定し、割る場合だけ`bottom`基準ではなく`top`基準（カーソルの下方向に広げる）に切り替えるようにした。
- **「山札一覧」のカードタイルクリックを、インラインのテキスト展開から拡大画像+補足のモーダル表示に変更**: `deck-viewer.js`の`buildCardTile`が返していた`{tile, note}`（クリックでnoteのdisplayを切替）をやめ、クリックで`showCardModal(def)`（deck-viewer.js内に新設した専用モーダル、画像+タイトル+補足+閉じるボタン）を開く方式にした。

- **カード補足モーダル（右クリック・山札一覧共通）を、画像の下にテキストが続く縦積みから、画像の右隣にテキストが並ぶ横並びレイアウトに変更**: スクロールをなるべく減らしたいという要望に対応。`#card-note-modal`/山札一覧の専用モーダルとも、`display:flex; flex-wrap:wrap;`のcontent行の中に画像(固定幅14rem)とテキスト列(`flex:1`)を並べ、画面が狭い時だけ自動的に縦積みにフォールバックする。
- **エターナルの山の表示名を「永久」→「エターナルカード」に変更**: `PILE_CONFIG.eternal.label`のみの変更（ホバー時のパイルツールチップに使われる）。

- **ファーストの山の表示名を「ファースト」→「ファーストカード」に変更**: `PILE_CONFIG.first.label`のみの変更（エターナルと同じくホバー時のパイルツールチップに使われる）。
- **カード補足モーダル（右クリック・山札一覧の両方）の閉じるボタンを、下部の「閉じる」テキストボタンから右上の丸型✕ボタンに変更**: 新設した`.modal-close-x`（`position: absolute; top/right`、モーダル本体は元々`position: fixed`なのでそのまま基準にできる）を両モーダルで共用。画像+テキスト横並びレイアウトとの相性を考えて採用。

- **カード補足モーダル（右クリック）もモーダル外クリックで閉じられるように**: 山札一覧のカードモーダルは元々`#deck-card-modal-backdrop`という全画面backdropにclickリスナーを付けて閉じる仕組みが既にあったが、右クリックの`showCardNoteModal`にはbackdropが無く、外側クリックで閉じられなかった。同じ「透明な全画面backdrop＋そのclickでclose()」パターンを追加（`#card-note-modal-backdrop`、透明のままダイアログのdimはしていない）。

- **「セットアップウィザード」を追加（[src/game-setup.js](src/game-setup.js) 新規、docs/rulebook.mdの「Game Preparation」を再現）**: 画面右上に「🎲 セットアップ」ボタンを新設し、開くと以下5つのボタンを持つパネルが出る。
  - **０：プレイ人数選択、白黒カード確認**: プレイ人数(2/3/4)を選ぶダイアログ（管理者モードのトグルがONなら、人数の代わりにA/B/C/D各座席を自由にチェックボックスで選べるモードに切り替わる）。白黒（無色）カードを山札に含めるかのチェックボックスもここにあり、説明書の「初めてプレイする時は外すと簡単」という推奨に合わせてデフォルトは含めない。
  - **１：ファーストカードを配り、駒を配置する**: 実行の最初に必ず`resetGame()`（新設のRESET_GAMEアクション）で盤面を完全に空にする（ユーザー確認の上、ルールブック通り「初期手札なし」から作り直す設計にした。既存の起動時デフォルト値=`HAND_START`の仮配布はテスト用データとして手を付けず、あくまでこのウィザードを通した時だけ本来のルール通りの状態になる）。その後、参加座席の時計回り順に`ファースト`の山から1枚ずつ引き、そのカードと同色のロックエリアへ表向きでロックし（`SETUP_ASSIGN_FIRST_CARDS`）、同じ色の駒をそのプレイヤーのゲートに置く。
  - **２：盤面にカードを配置する**: 山札（白黒カード含む/含まないはステップ0の設定に従う）をシャッフルし、場の7×7＝49マスに1枚ずつ裏向きに配置する（`SETUP_FILL_BOARD`）。再実行しても安全なように、まず場のカード（駒は除く）だけ一旦取り除いてから配り直す設計。
  - **３：スタートプレイヤーを決める**: 参加座席から無作為に1人選び、モーダルで表示するだけ（状態には残さない。ターン管理の仕組み自体がまだ無いフェーズ1のため）。
  - **１〜３を一気に行う**: 上記を順番に実行する。
  - 座席(A/B/C/D)⇔盤面の辺(bottom/left/top/right)の対応・7色の並び順・ゲートの座標は、main.js/state.js両方から必要になったため`board-layout.js`（新規）に切り出し、重複によるズレを防いだ。
  - 配布時のアニメーションは今回は見送り（ユーザーとの合意により、まず配布ロジックを固めてから別途追加する）。
  - 座席の自動/自由選択の切り替えは、CSS変数のスライダー群とは性質が異なる「挙動の切り替え」のため、admin.jsに`manualSeatMode`という単純なbool値＋チェックボックスUIとして追加した（`isManualSeatMode()`をexport）。

- **セットアップウィザードのUIを「別ダイアログ」から「パネル内展開→決定で差し替え」方式に変更**: 「０：プレイ人数選択、白黒カード確認」を、以前は縮小されたボタンを押すと別のモーダルが開く形にしていたが、「設定しないと１〜３ができないことが分かりやすいように」という要望で、パネルを開くと最初から設定フォームが展開された状態になり、「決定」するまでは１〜３のボタン自体が存在しない（ensureConfigによる後追いチェックではなく、そもそも表示しないことで強制する）方式に変更した。決定後は「⚙ 設定を変更（０に戻る）」でいつでもフォーム表示に戻れる。`game-setup.js`内に`bodyEl`（パネル内の可変領域）と`renderPanelBody()`（フォーム/ステップボタンの差し替え）を新設。
- **ターン管理を導入（state.js）**: これまでセットアップウィザードの手順3「スタートプレイヤーを決める」は無作為に選んだ結果をモーダルで見せるだけで状態には残していなかったが、`state.activePlayers`（手順1で確定した参加座席）と`state.turnPlayer`（現在の手番）を新設し、実際に状態として持つようにした。新規アクション`SET_TURN_PLAYER`（手順3が呼ぶ）、`NEXT_TURN`（`activePlayers`を時計回り順に絞り込み、現在の`turnPlayer`の次へ進める。末尾の次は先頭に戻る）を追加。
- **手番プレイヤーを目立たせる演出を追加**: 手番プレイヤーのロックエリア全体に、ゆっくり柔らかく明滅するグロー（`@keyframes lock-area-turn-glow`、`.lock-area`自体は背景を持たないが`box-shadow`は背景の有無に関係なく外側に描画されるためこれだけで光る）を、名前ラベルには点滅＋文字色変更（`@keyframes label-turn-blink`）を追加。`buildLockArea(side)`は`SEAT_TO_SIDE[turnPlayer] === side`、`buildPlayerZone`は`player === turnPlayer`を見てそれぞれ`.is-turn-player`クラスを付け外しする。
- **「ターンを次のプレイヤーへ渡す」ボタンを新設**: 開発者向けツール（管理者モード＝左上、山札一覧/セットアップ＝右上）とは離し、プレイヤー自身が操作するゲームプレイ上のボタンとして画面右下に配置（`#end-turn-button`）。`state.turnPlayer`がnull（セットアップ手順3が未実行）の間は非表示。押すと`nextTurn()`を呼び、次のプレイヤー名を表示するボタン自体のテキストも毎回`render()`内の`updateEndTurnButton()`で更新する。
  - 座席⇔盤面辺の対応・7色の並び順に加え、時計回りの座席順を扱う`SEAT_ORDER`・座席ラベルの`SEAT_LABELS`も含め、これらは全て`board-layout.js`にまとめてあるため、ターン管理のような新しい機能追加でもmain.js/state.js/game-setup.js間でズレる心配がない。

- **手番グローの色を管理者モードから切り替え可能に**: `.lock-area.is-turn-player`の発光色をハードコードせず`--turn-glow-rgb`というCSS変数（`rgba(var(--turn-glow-rgb), 透明度)`という形でbox-shadowから参照）にし、admin.jsに「ロックエリアの手番グローを白色にする（オフ=黄色）」というチェックボックスを追加した（デフォルトは黄色寄り`255, 224, 130`、オンで`255, 255, 255`）。
- **「相手ゲート侵攻ボーナス」(docs/rulebook.md「Gate Invasion Bonus」)を自動処理化（[src/gate-invasion.js](src/gate-invasion.js) 新規）**: 「ターン終了」ボタンが押された瞬間、手番プレイヤーの駒が参加中の別プレイヤーのゲートに乗っていれば（自分のゲート・空席のゲートは対象外）、ルールブック通り①手札を半分（端数切り捨て）奪う→②エターナルカードを1枚無作為に獲得しロック→③自分のゲートのカードを回収してゲートに帰還、の3段階を、それぞれ内容を予告する確認ポップアップ（OK押下で実行）を挟みながら自動で順番に処理する。条件を満たさなければ何もせず通常通りすぐ次のプレイヤーへターンが渡る。
  - ②では、ロック先のスロットに既に何か（ファーストカードを除く）があれば先に手札へ加えるルールも再現した。エターナルカードの山が空の場合は「もう残っていません」という文言に切り替わる（ルール「無くなったら以降もらえない」）。
  - どの駒が誰のものかを判定するため、`SETUP_ASSIGN_FIRST_CARDS`が生成する駒トークンに新しく`player`フィールド（座席）を持たせるようにした（従来は`color`のみで、駒と座席の対応が取れていなかった）。
  - 新規state.jsアクション: `GATE_INVASION_STEAL_HAND`・`GATE_INVASION_ETERNAL`・`GATE_INVASION_RETURN_HOME`。乱数選択（奪うカードの抽選・エターナルの山の一番上）は呼び出し側(gate-invasion.js)が事前に行い、確認ポップアップの文言に反映してからreducerに渡す設計（reducer自体は受け取ったid通りに動くだけの純粋な処理）。
  - `board-layout.js`に、ゲートの辺からその持ち主の座席を逆引きする`SIDE_TO_SEAT`を追加。

- **駒のホバー/掴む当たり判定エリアの広さを管理者モードから微調整可能に**: 駒の見た目（立方体）はそのままに、判定だけを広げたり狭めたりしたいという要望に対応。`buildCubePiece()`が生成する`.piece`の中に、新しく透明な`.piece-hitbox`（`--piece-hitbox-scale`でサイズだけ独立に拡縮、中心基準）を追加した。findDraggableAt/findHoverTargetは`.closest(".piece")`で判定しているため、この子要素をクリック/ホバーしても正しく親の`.piece`（＝実際の駒トークン）に解決される。admin.jsに「駒の当たり判定（ホバーすると発光する範囲）」というスライダー（0.5〜2.5倍、デフォルト1＝見た目と同じ）を追加。
- **相手ゲート侵攻ボーナスの自動処理を「手番プレイヤー限定」から「参加中の全プレイヤー対象」に修正**: ルールブックに「あなたのターン、相手のターンどちらでも発生する」とある通り、何らかの効果で自分のターンでなくても相手ゲートに駒がいることはセブンではあり得るため、「ターン終了」ボタンを押した瞬間のチェック対象を手番プレイヤー1人に限定せず、参加している全プレイヤー（時計回り順）に広げた。該当者が複数いれば順番に1人ずつボーナス処理を行ってから、最後に１回だけターンを次のプレイヤーへ進める。`gate-invasion.js`の`runGateInvasionIfNeeded(attacker, done)`を`runGateInvasionsIfNeeded(done)`（引数なし、内部で全参加プレイヤーを走査）に変更。
- **手番プレイヤーの名前ラベルの点滅速度を、ロックエリアの明滅と同じ2.6秒に統一**: 別々のタイミングで点滅しているとちぐはぐに見えるため、`label-turn-blink`のアニメーション時間を`1.1s`→`2.6s`（`lock-area-turn-glow`と同じ）に変更。
- **カードが誰かの手札に加わった時、「何を得たか」を知らせるトースト通知を追加（[src/hand-announcer.js](src/hand-announcer.js) 新規）**: `announceHandPickups(player, pickups)`が、手札に入る直前にそのカードが「公開情報」（捨て場から取得した／既に表向きだった）だったか「非公開情報」（山札等から裏向きで引いた／相手の手札から奪った）だったかに応じて出し分ける。公開情報、または受け取った本人が自分(A)の場合は画像＋名前入りの詳細なトーストを、それ以外（本人以外が非公開情報を得た場合）は「プレイヤー●が非公開のカードをN枚手札に加えました」という事実だけのトーストにする（複数枚まとめて手に入る場合は公開/非公開が混在しても1つのトーストに収める）。3.2秒で自動的に消えるか、クリックで即座に消せる。
  - 呼び出し箇所: `main.js`の`onDragEnd`（山からのドロー・盤面/ロックからの移動、両方とも手札が行き先の時）、`gate-invasion.js`の3ステップ全て（①相手の手札を奪う＝常に非公開、②ロック先から弾かれた既存カード＝常に公開、③自ゲートのカード回収＝各カード自身のfaceUpに従う）。

- **ロックエリアに置いたカードは原則、表向きにするよう修正**: `faceUpForLocation(location)`が今まで`zone:"lock"`を「場」と同じ裏向き扱いにしていた（ルール「ロックする：カード1枚を...表向きで置くこと」に反していた）。ロック行き先は常に`faceUp:true`にするよう`MOVE_TOKEN`・`DRAW_FROM_PILE`双方の表裏決定ロジックを修正（`MOVE_TOKEN`は「場・ロック間の移動は表裏を変えない」という既存の例外より、ロック行きの表向き化を優先するよう条件を並べ替えた）。
- **駒の下にカードがあると駒の「下の方」が掴めなくなるバグを修正（重要なハマりどころ）**: `findDraggableAt`/`findHoverTarget`は`document.elementsFromPoint()`が返す要素配列を1回のループで「要素ごとに駒→カード→...の優先順位を判定して最初に一致したものを返す」実装だったため、たまたまその座標の一番手前（先頭）の要素が`.board-card`（駒の当たり判定を追加する前から存在する、セルにほぼ密着した大きな要素）で、かつその奥（配列の後ろの方）に`.piece`（今回追加した`.piece-hitbox`含む）があっても、ループが`.board-card`の時点で確定して返ってしまい、駒より優先度の低いはずのカードが先に選ばれてしまうことがあった。駒の可視な立方体は3D持ち上げ(`translateZ`)で上寄りにずれて描画されるため、見た目の「上の方」ではこの競合が起きにくく「下の方」でだけ再現していた。修正として、要素配列を種類（駒→盤面カード→手札カード→山）ごとに全体を舐めてから確定する2段階ループに変更し、描画順に関係なく常に優先順位通りに解決されるようにした。
- **駒の当たり判定の広さを管理者モードから調整可能に**: `buildCubePiece()`が生成する`.piece`の中に、見た目には影響しない透明な`.piece-hitbox`（`--piece-hitbox-scale`で中心基準に拡縮）を追加。管理者モードに「駒の当たり判定（ホバーすると発光する範囲）」スライダーを追加した。
- **管理者モードのパネルを大幅に整理**: 項目が増えて縦に長くなりすぎていたため、各セクションを`<details><summary>`で開閉できるようにし（デフォルトは全て閉じた状態）、パネルを開いた直後の見た目を大幅にコンパクトにした。
- **パネル/モーダルの閉じ方を「右上✕ボタン＋外クリックでも閉じる」に統一（[src/ui-helpers.js](src/ui-helpers.js) 新規、`createModalCloseX`/`createBackdrop`）**: 管理者モード・山札一覧・セットアップウィザード・重なりカード一覧モーダルの4つに適用（管理者モード・セットアップウィザードは常駐ツールなので背景は暗くしない透明backdrop、山札一覧は情報モーダルなので暗くするbackdropのまま）。以前の下部「閉じる」ボタンは撤去。今後追加するパネル/モーダルもこの部品を使って統一する方針。
- **カード獲得ポップアップの大きさ・表示時間を管理者モードから調整可能に、✕ボタンで即座に消せるように**: `--hand-pickup-toast-scale`（デフォルト1.3、以前より大きめ）・`--hand-pickup-toast-duration`（秒、デフォルト5秒、以前の3.2秒より長め）を追加し、管理者モードに「カード獲得ポップアップ」グループとして追加。トースト右上にも`.modal-close-x`を追加（クリックで即座に消せる。トースト全体クリックでの即消しも従来通り残している）。
- **画面右下に「1枚ドロー」ボタンを追加**: 手番プレイヤーが山札から1枚引いて自分の手札に加える簡易ショートカット。「ターンを次のプレイヤーへ渡す」ボタンと同じく、`state.turnPlayer`が無い間は非表示。
- **山札が空になった時の自動補充を実装（docs/rulebook.md「こんな時は」）**: 「捨て場のカードをそのまま裏向きにして山札とする。シャッフルはしない。」を再現。山札が空かつ捨て場にカードがある状態で「1枚ドロー」を押すと確認モーダルが出て、OKすると新規`REFILL_DECK_FROM_DISCARD`アクションが捨て場の山を（物理的に「ひっくり返す」動作にあたるため並び順を反転させて）山札に丸ごと移し、捨て場を空にしてからそのままドローを続行する。
- **7色ロック達成による勝利モーダルを実装（[src/victory.js](src/victory.js) 新規）**: `main.js`の`render()`終盤で毎回`checkForVictory()`を呼び、参加中の各プレイヤーについて自分のロックエリアの7色スロットが全て埋まっているかを判定、初めて達成した瞬間だけ金色に光るグロー＋トロフィー絵文字の勝利モーダルを表示する（一度出したプレイヤーは同じ対戦中は出し直さない。セットアップウィザードの手順1でリセットした時だけ`resetVictoryTracking()`で記録を忘れる）。

- **駒スキンをいつでも選び直せるように（[src/piece-skins.js](src/piece-skins.js) 新規）**: 画面右下に「🎨 駒スキン」ボタンを新設（自分の駒がまだ無い＝セットアップ未実施の間は非表示）。押すと自分の駒の色（ファーストカードで固定、変えられない）に対応する4種類（標準＋追加1〜3）のスキン画像をスワッチで選べるモーダルが開く。`画像素材/駒スキン追加/`にあった3セット×7色を`assets/pieces/${color}-{1,2,3}.png`としてコピーし（他の駒画像と同じ理由でgit管理外）、選択は色ごとに保持する（`skinIndexByColor`）ため、同じ色を引いた別のプレイヤーにもそのスキンが適用される。`buildCubePiece(color)`は`getSkinImagePath(color)`経由で画像パスを解決するよう変更。
- **山札・エターナル・ファーストの山が0枚でも満杯に見えるバグを修正**: `buildPileZone`が常に`config.backImage`をimagePathとして渡していたため（捨て場だけ0枚の時にnull扱いする特別分岐があったが、他の山には無かった）、0枚でも裏面画像がそのまま表示され続けていた。全ての山で0枚ならimagePathをnullにし、`buildCardStack`側でもnullの時は`background-image:none;background-color:transparent;`を明示設定してCSS側のフォールバック色（`.pile-deck`等の暗いグラデーション背景）を打ち消すようにした（捨て場が空の時と同じ「中身が無いと分かる」見た目に統一）。
- **山札切れの自動補充モーダルをOKを押すまで閉じないように変更**: 他のパネル/モーダルは「✕＋外クリックで閉じる」に統一したが、このモーダルはゲーム状態に関わる必須の確認のため、意図的にその対象から外し、`showConfirmModal`から✕ボタン・外クリックでの閉じる仕組みを削除した（`createBackdrop`のonClose引数にno-opを渡すことで、暗い背景は表示しつつクリックしても閉じないようにしている）。
- **相手の手札にカーソルを合わせると枚数だけ表示されるように**: 中身は非公開のまま、`getHandTooltipText(el)`（`.hand-card`かつ`is-self`でない時だけ）が「プレイヤー● 手札N枚」を返し、既存のピール用ツールチップ（`updatePileTooltip`）にそのまま乗せる形で統合した。
- **自分の手札枚数を画面左下に常時表示（`#self-hand-status`）**: 手札が扇状に重なって数えづらいため、自分（A）の手札枚数だけを常に見える小さなステータスとして追加した（他のプレイヤーの手札枚数はこれでは分からず、上記の相手手札ホバーでのみ分かる）。
- **自分のターン中、自分の駒も自分の駒の色でゆっくり柔らかく発光するように**: ロックエリア・名前ラベルの手番演出とは別に、`renderBoardTokens`で`token.player === "A" && turnPlayer === "A"`の時だけ駒に`.is-my-turn-glow`を付与し、`--piece-turn-glow-color`にその駒自身の色（`var(--color-${token.color})`）をJSから設定、CSSの`box-shadow`アニメーションで発光させる（ロックエリアと同じ2.6秒周期）。

- **「１〜３を一気に行う」完了時にセットアップウィザードを自動で閉じるように**: `game-setup.js`に`closePanel`という参照を持たせ、`initGameSetup()`内の`close()`をそこに登録、`runAll()`の最後に呼ぶようにした。
- **手番プレイヤーの駒の発光が自分(A)にしか効いていなかったバグを修正**: 前回「自ターンの時に自分の駒も発光」を実装した際、条件を`token.player === "A" && turnPlayer === "A"`と自分限定にしてしまっていた。ロックエリア/名前ラベルの手番演出と同じく「手番プレイヤー本人の駒」を対象にすべきだったため、`token.player === getState().turnPlayer`（座席を問わない）に修正。B/C/Dのターンでもそれぞれの駒が光ることを確認済み。
- **プレイヤー名・アバターを自由に変更可能に（[src/player-identity.js](src/player-identity.js) 新規）**: `getPlayerName`/`setPlayerName`・`getPlayerAvatar`/`setPlayerAvatar`を追加し、これまでコード各所にハードコードされていた`SEAT_LABELS[player]`の参照（main.js・game-setup.js・gate-invasion.js・hand-announcer.js・victory.js）を全てこれ経由に統一した。これにより名前を変更すると盤面のラベル・ターン終了ボタン・各種ポップアップ・トースト等、表示箇所すべてに反映される。アバターは画像素材が無いため、絵文字12種（🦊🐸🐙🦉🐲🦄🐧🦁🐺🐢🐬🦋）のダミーセットから選ぶ簡易実装（`AVATAR_OPTIONS`）。
  - 画面左下の自分専用ステータス（`#self-hand-status`）を、アバター（クリックで選択モーダル）＋名前（クリックでその場編集、Enter/blurで確定）＋手札枚数の3点セットに拡張した。
  - 盤面上にも各プレイヤーのアバターを追加（`buildPlayerZone`内、`.player-avatar`）。「相手のアバターは手札の後ろ側に」という要望に対応するため、DOM順を名前ラベル→アバター→手札の順にし（手札が後から描画される＝重なった時に手前）、位置・サイズを管理者モードの新グループ「プレイヤーアバターの位置・サイズ」から調整できるようにした（`--avatar-size`共通1つ＋`--avatar-{a,b,c,d}-pos-x/y`）。
- **「盤面拡大」ボタンを追加**: 画面右下に新設。押すと、プレイヤーA（手前）のロックエリアが画面下端、プレイヤーC（奥）のロックエリアが画面上端にほぼ収まる倍率まで拡大する。もう一度押すと通常の全体フィット表示に戻るトグル式。実装は「A側ロック〜C側ロックの中間点」を`transform-origin`に動的設定してからscale3d()で拡大する方式（中間点の画面上の位置を変えずに、その区間だけを上下対称に引き伸ばせる）。`fitTableToViewport()`を`applyNormalFit()`/`applyBoardZoomFit()`に分岐させ、`boardZoomed`フラグで管理。

- **「盤面拡大」ズームの位置を管理者モードから微調整可能に**: 理論上はA側ロック〜C側ロックの中間点を基準にscale3d()するだけでちょうど収まるはずだったが、手札・アバターの飛び出しやブラウザのレンダリング誤差で実際には手前のロックエリアが画面下端で見切れることがあった。`--board-zoom-margin`（拡大率の余白、小さいほど余白が増える）・`--board-zoom-offset-x/y`（transformにtranslateとして追加する微調整分）を新設し、管理者モードの新グループ「盤面拡大ボタンのズーム位置調整」から変更できるようにした。offset-yを変えると実際に見切れを解消できることを確認済み。
- **ロックエリアの色の存在感を弱める調整肢を拡充**: 枠線の太さ（既存の`--lock-slot-border-width`。範囲を0.02〜0.4remから0〜0.4rem・step0.01に広げ、枠線を完全に無くすことも選べるようにした）に加えて、新しく`--lock-slot-glow-scale`（色付きグローの強さ。枠線を細くしてもcurrentColorのbox-shadowグローだけ強いと結局目立ってしまっていたため）を追加。両方とも管理者モードの「ロックエリア」グループから調整できる。
- **プレイマットを複数用意し、セットアップ時に選べるように（[src/playmat.js](src/playmat.js) 新規）**: プレイマット画像の保存先が`画像素材/プレイマット/`に変わり、白・青・黒の3種類が用意されたため、`assets/playmats/{white,blue,black}.png`としてコピーした（他の実物画像素材と同じ理由でgit管理外、`.gitignore`に`/assets/playmats/`を追加）。セットアップウィザードの手順0のフォームに「プレイマット」選択（ラジオボタン、デフォルト白）を追加し、決定した瞬間に盤面へ即反映される（`.playmat-bg`の背景画像を固定のCSS指定からJS側のinline styleに変更）。
- **ロックしていても使えるカード（ファーストカード・エターナルカード）をロックエリア内で目立たせる**: 該当カードがロックゾーンにある間だけ`.is-usable-while-locked`を付与し、斜めの光の帯が定期的に（4秒周期で）カードを横切る「キラッ」演出と、同じタイミングでの控えめな金色グローの明滅を追加した（`buildFlatCard`でcardIdの接頭辞`first-`/`eternal-`かつ`location.zone === "lock"`の時だけ付与）。

- **管理者モードで調整された値をコードの新デフォルトとして反映**: ユーザーが管理者モードの「出力をコピー」で調整した内容（`--lock-slot-border-width: 0.01rem`・`--lock-slot-glow-scale: 0.25`・`--board-zoom-margin: 0.92`・`--board-zoom-offset-y: -1.7rem`）を、style.cssの`:root`デフォルト値とadmin.jsの各スライダーの`default`値の両方に反映した（「リセット」ボタンを押してもこの調整後の状態に戻るように）。なお「プレイヤーアバターの位置・サイズ」グループは前回のセッションで既に管理者モードに追加済みだったため、変更不要だった。
- **「盤面拡大」ボタンを2段階トグルに変更**: 押すたびに「盤面拡大」→「もっと拡大」→「元に戻す」と3段階を巡回するようにした。両レベルとも基準点（A側ロック〜C側ロックの中間点）は共通だが、倍率・位置の微調整分だけレベルごとに別のCSS変数（1段階目=`--board-zoom-{margin,offset-x,offset-y}`、2段階目=`--board-zoom-2-{margin,offset-x,offset-y}`）を持たせ、管理者モードにレベルごとの調整グループを分けて追加した（2段階目は「もっと拡大」なのでmarginのデフォルトを1.3＝画面より大きく引き伸ばす設定にしてある）。ボタンの見た目も2段階目だけ橙色にして1段階目（緑）と区別できるようにした。

- **駒スキンを左下の自分専用ステータスエリアに統合**: 以前は右下の独立したボタンだったが、アバター・名前と同じステータスエリアに移動した。実際の`buildCubePiece()`をそのまま使い、ドラッグ中のゴースト(`.drag-ghost-piece-outer/-inner`)と同じ「perspective+盤面と同じ傾きを持つ入れ子」のテクニックで、3D空間の外でも本物の駒を立方体のまま小さく表示する（クリックで従来通りのスキン選択モーダルが開く）。`piece-skins.js`から独自の呼び出しボタンを撤去し、データ管理とモーダルだけを持つ形にリファクタした。
- **画面左上にゲームタイトルを表示**: 「7 SHADES OF S:EVEN remake」（旧・管理者モードボタンがあった場所。オプションメニューへの統合で空いたスペースを使った）。
- **右上に「⚙ オプション」ボタンを新設し、その中に「管理者モード」を統合（[src/options-menu.js](src/options-menu.js) 新規）**: 以前は左上に専用の呼び出しボタンがあったが、オプションメニュー配下のドロップダウン項目にした。`admin.js`は自前の呼び出しボタンを持つのをやめ、`openAdminPanel()`という開閉トリガーだけを外部提供する形にリファクタした。
- **ロック中でも使えるカードの強調演出を2種類から選べるように**: 前回実装した「斜めに光る帯」は存在感が強すぎたため大幅に細く（35%→6%幅）した上で、新たに「そのロックスロットの色をした小さな球がカードのふちに沿ってゆっくり周回する」演出（`effect-orbit`、新デフォルト）を追加。球の動きはCSS`offset-path`等のモーションパスAPIに頼らず、4隅を等間隔のキーフレームにして直線的に結ぶ方式（確実に全ブラウザで同じ見た目になる）。管理者モードの「ロック中でも使えるカードの強調演出」でどちらか切り替えられる（`getUsableLockedEffect()`）。
- **管理者モードで新たに調整された値を反映**: `--board-zoom-2-offset-y: -2.5rem`、プレイヤーアバターの座席ごとの位置（A/B/C/Dそれぞれ）を、style.cssの`:root`デフォルトとadmin.jsの`default`値の両方に反映した。

> **注（コンテキスト削減のための整理・2026-08-28／2026-09-05）**: 続き385以前（2026-07-19〜2026-09-03）の詳細な変更履歴は [docs/dev-log.md](docs/dev-log.md) へ移動しました（内容は削除せず移動しただけ）。過去の経緯・不具合の詳細を調べる時はそこを grep してください。以下は続き386以降（直近の作業）です。

### 2026-09-03（続き386）：BGMを「常に1つだけ」に（重なって鳴る不具合の総点検）／ホームは待機中BGM／追色の脈動と勝利演出に効果音

ユーザー報告「マイページからホームに戻ったらタイトル時のBGMが鳴り始めて、CPU戦を開始しても
そのBGMが鳴りやみませんでした。BGM関連の総点検をしたほうがよくないですかね？」＋要望3件。
- **総点検の結果と根本対策**: `play*Bgm()` は**それぞれ自分のトラックを鳴らすだけ**で、
  **前のBGMを止めるのは呼び出し側の責任**という作りだった。呼び出しは16か所・7画面
  （タイトル/ホーム/マイページ/ロビー/対局/勝利/対戦終了…）に散っており、どこか1か所で
  止め忘れると重なって鳴り続ける。実際、続き379で `openHomeScreen()` にタイトルBGMを足した時、
  そこから対局へ移る経路で止める処理が無く、報告の通り重なった。
  **`stopOtherBgms(keep)` を sound.js に置き、4つの play*Bgm() の先頭で必ず他を止める**ように
  した（＝「BGMは常に1つだけ」を仕組みで保証。止め忘れという事故が構造的に起きない）。
  呼び出し側に残っている `stop*Bgm()` は無害な二重呼び出しになるだけなので、そのまま残した。
- **ホーム画面のBGMを待機中BGMに**（ユーザー指定「とりあえず」）。
- **追色演出の脈動に効果音**: `card-dissolve.js` のV5の脈動は2回（rawT ≒ 1.00 と 1.30 が山）。
  山の立ち上がりで**1回ずつだけ** `playPulseThump()` を鳴らす（毎フレーム鳴らさないよう
  `beatSfxFired` で管理）。2回目は少し軽く（ドクン・ドクッ）。
- **勝利演出に効果音**: 七色が1つ灯るごとに `playVictoryChime(i)`（ド レ ミ ソ ラ ド レ の
  7段で**音が上がっていく**＝集まっていく高揚感）、キューブの脈動に `playPulseThump(power)`
  （後ろの脈動ほど強く）、閃光に `playVictoryImpact()`（低い衝撃＋高く抜ける残響）。
  いずれも音源ファイルは持たず、鼓動と同じ合成（`schedulePartial`）で作る＝素材追加が要らない。
- **検証**: ヘッドレスで `HTMLMediaElement.prototype.play/pause` を差し替えて実際に鳴っている
  トラックを追跡し、①オープニング→待機→対局→勝利と切り替えても**常に1つだけ**②停止後は無音
  ③新しい効果音3種が例外を投げない、を実測。さらに**報告の経路そのもの**（ホームを開く→
  CPU戦を開始）を再現し、ホームでは waiting-bgm、対局開始後は game-bgm **だけ**が鳴ることを確認。
  `npm test` 54/54 PASS、`node test/smoke.mjs 2` PASS。実際の音の良し悪しは実機で聴いて調整したい。

### 2026-09-03（続き387）：はじめての人の「次にやること」カードを追加（[src/first-steps.js](src/first-steps.js) 新規）＝③新しく遊ぶ人の最初の10分

ユーザーと合意していた③に着手。**新しいモードは足さず、今ある入口に順番を付けて1つだけ強調する**
方針にした。
- **何が問題だったか**: ホームには8つのタイルが**全部同じ重さ**で並んでおり、初めて開いた人には
  どれから触ればいいか分からない（物語チュートリアルが入口として用意されているのに、ショップや
  ランキングと見た目の優先度が同じ）。チュートリアルを終えた後の「次に何を」も案内が無い。
- **実装**: ホームのタイル群の**上に1枚だけ**「次にやること」カードを出す。段階は3つ——
  ①遊び方を覚える（物語チュートリアル）→②CPUと1戦してみる（1対1で開始）→③誰かと対戦する
  （ランク戦＝相手が見つかる可能性が一番高く、待機中はCPU練習もできる）。3歩を終えると
  **自動的に出なくなる**。✕でいつでも二度と出さないようにできる。
- **進捗の持ち方**: 物語チュートリアルの進捗は eidos-story.js が既に localStorage＋アカウント同期で
  持っている（`tutorial_completed`）ので**そのまま使う**。CPU戦/オンラインを遊んだかだけ
  `so7-first-steps-v1` に薄く持つ（端末ローカル。「もう案内を出さない」ための目印でしかないため
  アカウント同期は不要と判断）。印を付けるのは、ホームのCPU戦タイル・カードの②ボタン・
  main.js のオンライン開始検知（`wasOnlineGameStarted`）の3か所。
- **カードは画面遷移の知識を持たない**（`buildFirstStepCard(actions)` に、ホームが既に持っている
  入口をそのまま渡してもらう）。導線を二重に持たないので、入口の実装が変わってもここは追随不要。
- **検証**: ヘッドレスで5状態を実測——①初回=「1 遊び方を覚える／はじめる」②チュートリアル完了後
  =「2 CPUと1戦してみる／対戦する」③CPU戦後=「3 誰かと対戦する／相手を探す」④オンライン後=
  **出ない** ⑤✕の後=**出ない**、例外0件。スクリーンショットで見た目も確認しユーザーへ送付。
  `npm test` 54/54 PASS。（スモークテストはユーザーがオンライン自動対戦を実行中だったため、
  同時に重いヘッドレスを走らせない方針＝続き364の教訓に従って見送り。アプリのブート自体は
  上記の検証で確認済み。）
- **申し送り**: 新規プロフィールでは初回だけ「音量設定」「α版のお知らせ」のモーダルが先に出るので、
  カードはその後ろに隠れる（閉じれば正面に出る）。順番としては妥当だが、実機で気になるようなら
  α版のお知らせを閉じた直後にカードを一瞬光らせる等の演出を足せる。

### 2026-09-03（続き388）：テストのログをファイルにも残すようにした／「まとめて実行」でオンラインテストが走らなかったバッチのバグを修正

ユーザー報告2件（「オンラインテストのログが出ていない」→「単独で回したら、気づいたらログが
ないです」）。どちらもテスト側の作りの問題だった。
- **「まとめて実行(7)」でオンラインテストが走らなかった**: 生成していたバッチは
  `set "CMD=A && B && C"` → `call %CMD%` という形だった。**cmd.exe は展開した変数の中の `&&` を
  演算子として扱わないことがあり**、実機で3本目（オンライン自動対戦）が走らなかった。
  `call :run 1` / `call :run 2` / `call :run 5` と**1つずつ呼ぶ**形に変更（途中で失敗しても
  残りが走る＝結果が全部見えるという利点もある）。
  - あわせて、生成物のコメント行から `&` を排除した。**`rem` 行の `&` は cmd がコマンドの
    区切りとして解釈してしまう**（`rem ... && ...` の後半が実行される）。生成するバッチの
    コメントは半角英数だけにする方針も明記（CP932変換表に無い漢字が "?" になる問題も同時に回避）。
- **ログが消える**: 端末の表示は消えることがある（バッファのクリア・ウィンドウを閉じた・
  スクロール）。`test/smoke.mjs` と `test/online-smoke.mjs` が**自分でログをファイルに残す**ように
  した（`console.log/error/warn` を横取りして `test-logs/<種別>-<日時>.log` へも書く）。開始時と
  バッチの終了時に保存先を表示する。`test-logs/` は `.gitignore` に追加。
  - これで「ログがない」が構造的に起きなくなり、不具合報告にもそのまま貼れる。
- **検証**: `node test/smoke.mjs 2` を実走させ、`test-logs/smoke-20260903-185801.log` が作られ
  末尾に `[smoke] PASS ✅ (1/1)` まで入っていることを確認。生成物に `&&` が1つも残らないことも
  ソース側の行配列から機械確認（0件）。
- **反省（作業の進め方）**: ユーザーがテストを回している最中に、私が同じソース（ホーム画面
  まわり）を編集していた。テストは常に手元のファイルを読むので、**編集中の状態を掴む**恐れが
  ある。テスト実行中はソースを触らないこと。

### 2026-09-03（続き389）：#223 iPhoneで対戦中にアプリが落ちてタイトルへ戻る不具合＝画像のメモリ過多を実測で特定し、全アセットを表示サイズに合わせて縮小（展開後 290MB→83MB）

ユーザー報告 #223「CPU戦中にアプリが落ちてタイトル画面にいってしまいました」（iPhone iOS 18.7・
4人CPU戦・932x430）。ブラックボックスが `suspectedCrash: true` / `navType: "back_forward"` /
`prevMode: "cpu"` / `secsSincePrev: 4` ＝**WebKitがページのプロセスごと落として Safari が復元した**
形跡を残していた（続き354で疑っていたメモリ不足が、今回ついに実際の報告として出た）。
- **実測で原因を確定させた**: ヘッドレスで**実際に4人CPU戦を60秒回し**、その時点で読み込まれている
  画像を1枚ずつ `naturalWidth/Height` から数え上げた（scratchpad の使い捨てスクリプト）。結果、
  **展開後 290MB**（うち表示中 122MB）。画像は**ファイルサイズではなく 幅×高さ×4バイト**でメモリを
  食うので、100KB のアバターでも 1254x1254 なら **6.0MB** になる。内訳はペット77MB・アイコン63MB・
  アバター60MB・駒20MB。さらに全アセットを棚卸ししたところ **`assets/playmats/black.webp` /
  `blue.webp` は 5000x5000＝1枚で展開95MB**（選んだ人はこれだけでアウト）だった。iOS Safari の
  1タブあたりの上限は数百MB程度なので、これは落ちて当然の数字。
  - なお最初の集計で「ペットだけで1004MB」と出たのは、`performance.getEntriesByType("resource")` が
    **同じURLの再取得を何度も数える**ため（ペットのコマ送りで同じ16枚を153回リクエストしていた）。
    URLで重複を除いて数え直したのが上記290MB。**メモリを見積もる時は必ずURLで一意化すること**。
- **対策（[tools/shrink-assets.mjs](tools/shrink-assets.mjs) 新規）**: 「そこに実際に表示される最大の大きさ × 高DPIぶんの
  余裕」を上限として、配信用の `assets/` だけを縮小した（原本は gitignore 下の `画像素材/` にある）。
  上限はフォルダごと: プレイマット2048 / アバター768（物語の立ち絵が最大およそ500px表示）/
  カード768（拡大プレビュー320px）/ 駒384 / ペット512（**既に5体が512で、それが元々の基準だった**）/
  アイコン256 / ランクバッジ256 / ランクゲージ1024 / オープニング1600。背景（1672、全画面表示）と
  カード面の素材（433）は表示サイズ相応なので**触っていない**。
  - 実装はライブラリを増やさず、テストで既に使っている **Playwright の Chromium の canvas** で
    リサイズ＋再エンコード（webpは品質0.92、pngはpngのまま＝**拡張子を変えないのでコード側の参照は
    1行も触らずに済む**）。`--dry` で下見できる。今後アセットを追加した時も同じコマンドで揃えられる。
  - **結果: 464枚を縮小し、展開後 2839MB→747MB（3.8分の1）／ファイル 355MB→85MB。
    実際の4人CPU戦での実測は 290MB→82.7MB（表示中は122MB→41.7MB）** ＝約3.5分の1。
    ファイルが270MB軽くなったので、初回の読み込み時間と通信量も大きく減る。
  - **見た目の確認**: 縮小の前後で「実際の表示サイズ（立ち絵384x500・マイページ300px・盤面アバター
    44px・カード拡大320px・駒45px・ペット40px・アイコン40px）」に並べた見本シートを **DPR 2倍**で
    撮って比較。カードの効果文の可読性を含め差は認められなかった。
- **次に落ちた時のために計測も足した**: 不具合報告の `context.weight` に、その瞬間の
  DOM要素数・`<img>` の枚数と展開後MB・canvas の枚数と面積・取得した画像の種類数・DPR を載せる
  （**カード名や伏せ情報は一切入らない**）。iOSは `performance.memory` を持たないため heapMB が
  0 のままで手掛かりが無かったが、これで「落ちた時に何をどれだけ抱えていたか」が分かる。
- **検証**: `npm test` 54/54 PASS、`node test/smoke.mjs 2` PASS、`node --input-type=module --check`
  （bug-report.js）通過。サーバー側（Supabase）の変更は無い。
- **あわせて確認**: 続き388で入れたテストのログ保存が効いており、ユーザー環境で画面に何も出ないまま
  終わった**オンライン自動対戦（2人）は実は PASS していた**（`test-logs/online-smoke-20260903-190935.log`
  に8ターン到達までの全ログが残っていた）。画面表示が消える件はコンソール側の問題で、テスト自体は
  正常に動いている。ただし8ターンでは決着しないため、**続き385の「最後のロックを全員で承認」は
  まだ実戦経路を通っていない**（`--full` で決着まで回す必要がある）。

### 2026-09-03（続き390）：#224 コノハナサクヤの「周囲」が前後左右の4マスしかハイライトされない不具合を修正／落ちる直前の「画面の重さ」を次回起動へ持ち越すようにした

- **#224「コノハナサクヤで相手を移動させる時、隣しかハイライトされていなかった。周囲とは8マスのこと」
  （原因確定・修正）**: `MOVE_CHOSEN_OPPONENT_ADJACENT_TO_SELF`（結ばれの一本桜 コノハナサクヤ）の
  移動先候補が `enumerateManhattanRing(1)`＝**前後左右の4マス**だった。カード文は「相手をあなたの
  **周囲**へ移動する」で、docs/rulebook.md の用語定義に **「周囲: 駒の周りの縦横斜めの計8マス」** と
  明記されている（＝斜めの4マスが丸ごと欠けていた）。`enumerateSurroundingOffsets()`（8マス）を新設して
  差し替えた。候補リストはそのままハイライトとCPUの選択にも使われるので、これ1か所で両方直る。
  - **「隣」と「周囲」は別の概念**（試練の儀式の補足に「『隣』とは前後左右のマスのことである」と明記）。
    同じ `enumerateManhattanRing(1)` を使っている残り2か所——**試練の儀式**（「あなたの隣に置く」）と
    **プレゼント**（「相手の隣に裏向きで置く」）——は**カード文が「隣」なので正しい**（触っていない）。
    カード文に「周囲」を使うのは全33種のうちコノハナサクヤだけであることを確認済み。
    ヘルパーのコメントに「カード文が『隣』なら Manhattan、『周囲』ならこちら」と書いて取り違えを防いだ。
  - **回帰テストを追加**（`npm test` 54→55件）。**ポイントは picks に `"index:7"`（8番目の候補）を
    使うこと**——テストランナーの `pickLocation` は「指定した座標が候補に無ければ指定通りに返す」
    フォールバックを持つため、`{row:4,col:4}` のように座標で書くと**候補が4マスのままでもテストが通って
    しまい回帰を検出できない**。番号指定なら候補が4つだと `undefined` になり必ず失敗する。実際に
    修正前のエンジンへ戻して**54/55 FAIL**、修正後**55/55 PASS**を確認した。
- **落ちる直前の「画面の重さ」を次回起動へ持ち越すようにした（[src/crash-blackbox.js](src/crash-blackbox.js)）**: 続き389で
  不具合報告に `context.weight` を載せたが、**アプリが落ちた場合はその報告を出す前にページが消える**ため
  肝心の瞬間の数字が残らなかった。ブラックボックスの心拍（5秒ごとのlocalStorage書き込み）に
  `imgMB`/`peakImgMB`/`imgCount`/`domNodes` を追加し、次回起動時の `bootReport` に
  `prevImgMB`/`prevPeakImgMB`/`prevImgCount`/`prevDomNodes` として出るようにした。iOS は
  `performance.memory` を持たない（heapMB が常に0）ので、**これが唯一の「落ちた時どれだけ重かったか」の
  記録**になる。実測: 4人CPU戦で `imgMB:30 / peakImgMB:33 / imgCount:35 / domNodes:4691`。
  - なお #224 の報告（縮小後の版 20260903-201832・iPhone・DPR3）には既に `weight: {imgMB:46,
    domNodes:4753, imgCount:63, fetchedImages:144}` が載っており、**続き389の計測が実機で機能している**
    ことも確認できた（縮小前の同等条件なら3倍以上になっていたはず）。
- **検証**: `npm test` **55/55 PASS**、`node test/smoke.mjs 2` PASS、構文チェック通過。
  サーバー側（Supabase）の変更は無い。

### 2026-09-03（続き391）：管理者ダッシュボードの不具合一覧に「対処済み」＋フィルターを追加（[src/handled-bug-reports.js](src/handled-bug-reports.js) 新規）／#225 「それぞれ別のマス」の文言・返事待ちモーダルを透かして手札を掴む問題

- **不具合一覧に「対処済み」（ユーザー要望）**: 列（`resolved`）は最初から存在したが、値を書き換える
  手段が無かった（`so7_bug_reports` は SELECT/UPDATE ポリシーを一切作らない設計＝読みも管理者RPC
  経由のため）。読みと同じ方式で `so7_set_bug_report_resolved(p_id, p_resolved)`（SECURITY DEFINER＋
  管理者メール判定）を追加し、**実際に更新された件数を返す**ようにした（0なら権限不足＝
  「エラー無し・0件で静かに失敗」を画面に出す。姉妹リポジトリでRLSのUPDATEポリシー欠落を踏んだ教訓）。
  → **ユーザーがSupabaseのSQL Editorで実行する必要あり**（未実行だとボタンが「更新0件」で失敗する）。
  - ダッシュボード側は「対処」列（押すとトグル）＋上部に**「対処済みを隠す」チェックボックス（既定ON）**。
    対処済みの行は薄く表示。列を1つ増やしたので `admin-dashboard.css` の `nth-child` 番号も
    6→7（詳細）に付け替えた（**table-layout:fixed の列番号は行の生成順と一致していないといけない**
    ——続き352で同じ間違いをして横スクロールが出ている）。
  - **私（Claude）が対処した印は [src/handled-bug-reports.js](src/handled-bug-reports.js) で持つ**。ユーザー要望「あなたが
    対処した時にあなたの方でマークするようにも心掛けて」への対応だが、**開発側は管理者として
    ログインできない＝DBに書けない**ため、「直したら番号をこのファイルに足してコミットする」方式に
    した（コードと一緒に履歴が残り、どの変更で対処したかgit logから追える）。ダッシュボードは
    DBのフラグとこの一覧の**どちらかが立っていれば対処済み**として扱い、コード側の分は
    「✅ コード」と表示してボタンを出さない（DBのフラグとは別管理であることを明示）。
    今回は続き377〜390で対処した #214〜#225 を登録した。
- **#225-1「罠を使った時の『それぞれ違うマス』ってどういう意味？」**: `PLACE_CARDS` の
  `destination.selection: CHOOSE` のマス選択が、**枚数に関係なく**「カードを置くマスを選択して
  ください（それぞれ別のマス）」を出していた。1枚しか置かないカード（選べる罠・ジャンプ台等）では
  意味不明なので、**1枚なら但し書きを出さず**、複数枚なら「（{i}枚目／全{n}枚・同じマスには
  置けません）」と具体的に出すようにした（ja/en）。
- **#225-2「スリカエで罠を返そうと思ったら罠を使用してしまった」（#211の再発・真因が別だった）**:
  続き368では「カード効果の選択待ち(activeEffectPicker)中は盤面のドラッグ開始をしない」ガードを
  入れたが、**選択待ちではない“返事待ちのモーダル”表示中は素通り**していた（スリカエは
  「奪った」モーダルを見せてから返す札を選ばせるので、その表示中のタップが手札効果の使用になる）。
  `findDraggableAt` は3D階層でネイティブのヒットテストが信用できないため `elementsFromPoint()` で
  自前に当たり判定する——つまり**モーダルの背景(backdrop)を透かして下の手札を掴む**のが根っこ。
  - **対策**: `ui-helpers.js` の `createBackdrop` が **dim付き（＝画面を覆って返事を待つモーダル）の
    背景にだけ** `so7-modal-backdrop` クラスを付け、`findDraggableAt` はそれを踏んでいたら
    何も返さない。54箇所のモーダルが同じヘルパー経由なので一律に効く。**dim:false（管理者モード・
    セットアップ等の常駐パネル）には付けない**＝それらを開いたまま盤面を触る従来の使い方は保たれる。
  - **一度失敗した実装（記録）**: 最初は「盤面(#game-table)より手前に盤面外の要素があれば無視」と
    いう一般則にしたが、実測すると**チュートリアルの暗幕**（`#tutorial-scrim`。穴は mask＝見た目
    だけで当たり判定は覆ったまま）や起動直後の案内モーダルの背景が常に手前にあり、**盤面操作が
    丸ごと死ぬ**ことが分かった（駒の位置で `boardIndex` が 0 ではなく 3 だった）。一般則が危ない
    ことを実測で掴めたので、目印クラス方式へ切り替えた。チュートリアル系は `createBackdrop` を
    使っていない（自前の `#tutorial-scrim`）ので、この方式ならチュートリアルは影響を受けない。
- **検証**: `npm test` **55/55 PASS**、`node test/smoke.mjs 2` PASS、CSSブレース平衡。ブラウザ実測で
  ①dim付きの背景に目印が付き、dimなしには付かない ②「奪った」モーダルの表示中だけ駒の位置で
  目印を踏む（前後は踏まない） ③ダッシュボードの見出しが「選択・日時・報告者・内容・画像・対処・
  詳細」になり、フィルターが既定ONで横スクロールも出ない ④コード側の対処済み判定（#224=true /
  #999=false・12件）。**ダッシュボードの行の実描画は管理者ログインが必要なため未確認**（前回同様、
  実際の表示は実機でお願いしたい）。
- **ユーザー作業**: `supabase_setup_so7.sql` 末尾の「追加(2026-09-03): 不具合報告に『対処済み』を…」を
  SQL Editor で実行する。Edge Function の変更は無いため再デプロイ不要。

### 2026-09-04（続き392）：#227 効果の処理中に「スキップ」が出てフェイズを進められてしまう／#226 CPUが場のパーティを回収しない（4回のゲート侵攻はルール通り）

- **#227「コノハナサクヤで俺の隣の罠に相手を到達させてる時に俺のところにスキップが表示されてた」
  （オンライン2人戦）**: `updateSkipButtonVisibility()` は「今がロック/ハンドフェイズで、そのフェイズの
  持ち主が疑似CPUでなく、最後のロックの承認待ち(#181)でもない」だけで表示を決めていた。**カード効果を
  解決している最中は考慮していなかった**ため、コノハナサクヤで相手を引き寄せ→相手が罠の到達効果を
  選んでいる間も、こちらの画面にスキップ（とマイデッキ）が出たままだった。押せば効果の途中で
  ハンド/ロックフェイズが終わって次へ進み得る（#212 と同じ形の事故）。
  - **判定は phase-automation の中で完結させた**（main.js を import すると循環するため）:
    ①`handEffectBusy`（このモジュールが持っている手札効果の処理中フラグ）②`#card-effect-picker-hint`
    が画面に出ている（＝どこかで選択待ちの案内が出ている）③`.so7-modal-backdrop` がある
    （＝返事待ちのモーダルが開いている。#225 で付けた目印を再利用）。どれかが真なら
    スキップ・マイデッキとも出さない。
  - 実測（ブラウザ）: 通常 block → 処理中 none → 解除で block → モーダル背景あり none → 外すと block。
- **#226「計4回もゲート侵攻できてしまっている」**: ログを追うと A は T5/T7/T9/T11 の**自分のターンに
  1回ずつ**侵攻しており（ジャンプ台2枚＋パーティの連鎖で毎ターン相手ゲートへ到達していた）、
  ルール上の不具合ではない（ゲート侵攻ボーナスは「ターン終了時に相手ゲートに乗っていれば発生」）。
  同じ報告の要望「**CPUは相手の踏んだパーティは積極的に回収していった方がいい。相手のリソースも
  減らせる**」に対応: `chooseEffectCell` の優先4（従来は red-jump-pad だけを積極回収）を
  `PICKUP_PRIORITY_CARDS = ["pink-party", "red-jump-pad"]` の**順番付き**に変え、パーティを最優先で
  回収するようにした。パーティは「伏せて置く→自分で踏んで手札に戻す」を繰り返せる使い回しカードなので、
  盤面にある間に奪えば相手のループを断てる（＝ユーザーの指摘通り）。
  - 実測: パーティ・ジャンプ台・罠が候補にある盤面で30回選ばせて**30回ともパーティのマス**。
    パーティが無ければ従来どおり20回ともジャンプ台（回帰なし）。
- **検証**: `npm test` 55/55 PASS、`node test/smoke.mjs 2` PASS、構文チェック通過。
  ダッシュボードの「対処済み（コード）」一覧に #226/#227 を追記。サーバー側の変更は無い。
- **申し送り（検証時のハマりどころ）**: ヘッドレスで `page.evaluate` が
  「Resulting promise was garbage collected」で落ちることがある（起動直後の自動更新でページが
  読み直されるため）。**数回リトライする**か、盤面が組まれるのを待ってから評価する。

### 2026-09-04（続き393）：コノハナサクヤは「移動」なので移動先はカードのあるマスだけ／#228 「このターン接触できない」を実際に強制／#229 オンラインでは接触の承認を必ず相手に出す／ライトモードの「受け取った」モーダル

- **【ユーザー指摘】コノハナサクヤの移動先はカードのあるマス**: 続き390で候補を8マス（周囲）に直したが、
  **空きマスも候補に含めていた**。rulebook の用語定義が決定的——「『移動』とは: …**カードの置かれた**
  別のマスに置き、そのカードが裏向きならオープンすること」「『強制移動』とは: **カードが無いマスにも**
  移動できる『移動』のこと」。コノハナサクヤは「移動する」なので**カードのあるマスだけ**。
  候補のフィルタに `findTopCardAtCell(row, col)` を追加した。
  - **回帰テストを更新・追加**（55→56件）。既存2件は盤面にカードが無い状態だったため、この修正で
    候補0件＝不発となり **54/56 FAIL** になった（＝仕様変更が効いている証拠）。移動先にカードを置く形に
    直し、さらに「周囲8マスのうちカードがあるのが1マスだけなら、選ばせずにそこへ移動する」テストを
    新設して**空きマスが候補にならないこと**を担保した。
- **#228「コノハナサクヤで呼び寄せた相手に接触できちゃった」**: カード文の「このターンあなたは接触
  できない。」は、これまで**案内モーダルで知らせるだけの自己申告**だった（Phase 1の「ルール適用は
  一切しない」方針の名残。engine のコメントにもその旨が書かれていた）。マルメゴの移動禁止
  （`movementDisabledUntilTurn`）と同じ「turnNumber が一致する間だけ有効」の自動失効パターンで
  `contactDisabledUntilTurn` を追加し、**接触の入口 `showContactPrompt`**（人間の「接触する」ボタン→
  確認モーダルも、疑似CPUの自動申し込みも必ずここを通る）で断る。理由が分かるようモーダルで知らせる
  （黙って何も起きないと「押せないバグ」に見えるため）。実際に成立する `submitContactProposal` にも
  安全網の再確認を置いた。
- **#229「接触する時、申し込み中モーダルが出るが相手に承認ボタンは多分出てない」（原因確定・修正）**:
  `checkCounterLockAutoApproval()` が「カウンターロックを持っていない防御側は、ボタンを見せずに自動承認」
  という作りだった（チラつき防止のため意図的にそうしていた）。オンラインではこれが2つの問題を生む——
  (1)相手からは「聞かれていない」ように見える (2)**即座に承認が返ること自体が「あの人はカウンター
  ロックを持っていない」という情報**になる。#222（最後のロックの承認）と全く同じ論点なので、同じ結論
  ——**オンラインでは全員に承認を出す**（`isOnlineMode()` なら自動承認しない／contact-approval.js の
  「使えない人には出さない」早期returnもオンラインでは無効）にした。放置対策として
  `checkContactApprovalTimeout()`（45秒で自動承認、`diag-contact-auto-approve` を残す）を既存の5秒
  ウォッチドッグに相乗りさせた。ローカルのCPU戦は隠す相手がいないので従来どおり自動。
- **ライトモードの「受け取った」モーダル（ユーザー報告「カウンタープレイ関係のモーダルでライトモード
  なのにダークのものがある」）**: `.card-received-modal`（接触・カウンターロックの流れの最後に出る
  「奪った」表示）だけ暗い緑の箱のままだった。**続き338/339のライト総点検が id 指定のモーダルを中心に
  洗っていたため、クラス指定のこのモーダルが漏れていた**。アイボリー地＋濃い文字に揃えつつ、
  「受け取った」ことを示す緑のアクセント（枠・見出し）は残した。
  - あわせて**クラス指定で暗いまま＆ライト上書きが無い箱**を機械的に洗い出し（18件）、その中で
    「明るい箱の中に入る」3つ（いつでも割り込みの行・基本設定の切り替えボタン・拡大プレビューの
    注意書き）にも上書きを足した。残りはタイトル画面・物語メニュー・盤面のカード裏・開発者用パネル
    等で、**暗いことに意味がある**ので意図的にそのまま。
- **検証**: `npm test` **56/56 PASS**、`node test/smoke.mjs 2` PASS、CSSブレース平衡（2792）、
  構文チェック通過。ライトモードの「受け取った」モーダルを実際に開いて、地がアイボリー
  （rgb(243,251,246)→rgb(253,251,245)）・枠が緑 rgb(28,122,58)・見出し rgb(21,128,61)・カード名
  rgb(58,51,38) になることを実測。#228/#229 はオンライン特有の経路のため実機での最終確認をお願いしたい。

### 2026-09-04（続き394）：ゲストが戦績システムに「プレイヤー」として登録され続けていた不具合（RLSで他人のゲスト判定が不可能だった）／#231 ゲート侵攻の演出を飛ばして勝利演出／コノハナサクヤの善処の原則／戦績システムの段位ランキングからブロンズ・ゲージ0を除外

ユーザー報告「戦績システムにプレイヤーという名前で大量に登録が行われています。おそらくスモークテストの
関係ですか？」ほか、確認事項2件と不具合報告2件（#230/#231）。

- **【本命】ゲストが戦績システムに「プレイヤー」という名前で登録され続けていた（原因確定・修正）**:
  本番DBを実際に問い合わせて確認したところ、`players` に名前「プレイヤー」の行が **30件**あり、
  作成日は **2026-07-25〜09-03に分散**していた——**スモークテストだけが原因ではなく、ゲストで
  遊んだ人が全員登録されていた**（テストはそれを増やしただけ）。全30件とも、どの試合(`matches.members`)にも
  コメント(`match_feedback_replies`)にも紐づかない孤立行だった。
  - **原因**: 「ゲストは戦績プレイヤーとして登録しない」の判定 `fetchGuestUserIds()` は
    `so7_user_profiles` の `is_guest` 列を読む実装だが、**同テーブルのRLSは
    `using (user_id = auth.uid())` ＝自分の行しか読めない**。つまり**他人のゲスト判定は常に
    空振り（空集合）**で、対局開始時にホストが走らせる `registerParticipantsAsStatsPlayers()` が
    全員を非ゲストとみなし `getOrCreateStatsPlayer()` していた。名前はゲスト席の既定表示名
    （`on.label.player`＝「プレイヤー」）。
  - **対策（追加SQL不要）**: 「他人のゲスト判定」をやめ、**各クライアントが自分自身だけを登録する**
    形に変えた（自分が匿名かどうかは `cachedUser.is_anonymous` で確実に分かる）。
    ・`registerParticipantsAsStatsPlayers(gameId)` → `registerSelfAsStatsPlayer()`（online.js）。
      呼ぶのはホストの `startGame` ではなく、**全クライアントが通るオンライン対局開始検知**
      （main.js の `wasOnlineGameStarted` の分岐）。観戦者・無座席・匿名は自分で弾く。
    ・`submitStatsMatchResult()` は自分以外の座席について `players` を**引くだけ（作らない）**に変更
      （`lookupStatsPlayerId`）。非ゲストは必ず自分で登録しているので、**引ければ実プレイヤー／
      引けなければゲスト**として `guest_names` へ回す。自分の座席だけは従来通り getOrCreate も行う
      （開始時の登録が通信エラー等で失敗していても、勝敗の記録には必ず載るように）。
    ・`fetchGuestUserIds()` は撤去（RLS上どうやっても機能しないため）。自分の `is_guest` を書く
      `persistMyGuestFlag()` は管理者向けの利用状況表示で使うので残した。
  - **教訓**: 「他人の行を読む前提の判定」を書く時は、**そのテーブルのRLSが `auth.uid()` 限定でないか**を
    必ず確認する。RLSで読めない場合、SELECTはエラーにならず**静かに0件**を返すため、判定が
    「常に false」に倒れていても気づけない（姉妹リポジトリのUPDATEポリシー欠落＝続き308と同じ形の罠）。
  - **残っている30件の孤立行**は削除していない（本番データの変更＝ユーザーの判断のため報告のみ）。
- **#231「エターナルで勝ったけど、ゲート侵攻演出をすっ飛ばして勝利演出になった」（原因確定・修正）**:
  実機ログで `diag-gate-invasion-enqueue` の**0.09秒後**に `diag-victory` が出ていた。ゲート侵攻
  ボーナス②で受け取ったエターナルカードがそのまま7色目のロックになるため、侵攻の演出（5ステップ・
  合計50秒前後）が1枚目を出した直後に勝利判定が成立し、勝利演出が上から覆いかぶさっていた。
  `victory.js` に `waitForGateInvasionToFinish()`（`isGateInvasionPending()` /
  `isGateInvasionQueueActive()` / `isLocalGateInvasionActive()` が全部落ちるまで200ms間隔で待つ、
  上限90秒）を追加し、**勝利演出の直前**で待つようにした。**勝敗の確定・戦績システムへの登録は
  従来どおり先に済ませたまま**（演出とは切り離す）で、見せる順番だけを直している。上限に達したら
  待たずに進むので、演出が終わらないせいで勝利画面が出なくなる事故は起こさない。
- **コノハナサクヤ（eternal-pink）に善処の原則を適用**（ユーザー確認事項）: 続き393で移動先を
  「カードのあるマスだけ」にしたため、**移動先の候補が無い時にコストだけ払って不発**になり得る
  状態だった。`hasKonohanasakuyaTarget(player)`（①相手の駒が盤面にいる ②自分の周囲8マスに
  「カードがあり駒が乗っていない」マスが1つ以上ある）を新設し、`isHandEffectOptionUsable` と
  `explainHandEffectOptionUnusable` の両方へ同じ順序で追加（セレナーデ・マスチェンジ等と同じ扱い）。
  文言 `ce.noKonohanaTarget` を ja/en 追加。
- **接触の承認モーダルは接触されたプレイヤーだけ**（ユーザー確認事項）: 現状のままで正しいことを
  コードで確認した——`contact-approval.js` の `canRespond = !isOnlineMode() || getSelfSeat() === pending.defender`
  でボタンは defender にしか出ず、他の人には「承認待ち」の文面だけが出る。45秒の自動承認
  （`checkContactApprovalTimeout`）も defender の画面でしか働かない。続き393で変えたのは
  「カウンターロックを持っていない defender にも、隠さず承認を出す」ところだけ。
  （全員に出すのは**最後のロック**＝ゴメンナサイが全員に関係する場面だけ、続き385。）
- **#230「なないろのかけら２枚使えず」（一部修正・原因は未特定）**: 手元では再現できなかった。
  確実に言えるバグとして、`LOCK_PAIR` の相方探索が `zone === "hand"` に限定されていて、
  使用可否の判定（`requiresPairInHand` → `getHandTokens`＝手札＋**手札公開エリア**）と**範囲が
  食い違っていた**のを揃えた（2枚目が公開エリアにあると「選べるのに何も起きない」状態になる）。
  ただし報告時のログでは公開ドローをしていないため、これが原因とは断定できない。次に同じ報告が
  来た時に推測せず判定できるよう、**選択肢がグレー表示になった瞬間の内訳**を行動ログに残す
  診断（`diag-hand-option-unusable`：各選択肢の可否／同名カードの枚数／それぞれがどのゾーンに
  あるか。**カード名は自分の分だけ＝伏せ情報は入らない**）を追加した。
- **姉妹リポジトリ（戦績システム）**: 段位ランキングから「ブロンズかつゲージ0かつLP0」を除外
  （`so7_ranked_players` の行はキューに入った時点で作られるため、**まだ1勝もしていない人**が
  並んでいた）。注記も「（ブロンズ ゲージ0 は対象外）」に更新。
- **検証**: 変更した全ファイルの構文チェック通過、`npm test` **56/56 PASS**、`node test/smoke.mjs 2` PASS。
  ブラウザ実測で、コノハナサクヤの使用可否を5通り確認——①周囲にカード無し=使えない②周囲にカード有り=
  使える③**斜めだけにカード有り=使える**（#224の8マスは維持）④相手の駒がいない=使えない
  ⑤唯一のカードのマスに対象の駒自身が乗っている=使えない。姉妹リポジトリはローカルの一時サーバーで
  ダミーの段位を流し込み、ブロンズ/ゲージ0の2人が除外され残り2人だけが並ぶことを実測（一時コピーは削除済み）。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Functionの再デプロイは不要。

### 2026-09-04（続き395）：#230 効果の選択モーダルが「開いた瞬間の合成クリック」で勝手に確定していた／#233 タイマーONボタンが出たまま／#232 もう一度遊ぶでブースト等が消える／ゲストの戦績プレイヤー行30件を削除

ユーザー訂正「#230 は１ドローor２枚ロックの選択モーダルが**出ずに**強制的に１ドローになった」＋
不具合報告#232/#233、および「30件は片づけていい／戦績には残してよいがプレイヤーにはゲストを入れない」。

- **#230（原因確定・修正）**: 選択肢モーダル（`showHandEffectOptionPicker`）のボタンは素の `click` を
  見ていた。スマホでは、手札のカードをタップして**指を離した位置に、その直後に作られる選択肢の
  ボタンがそのまま現れる**。ブラウザは pointerup のあとに click を合成して「その時点で指の下にある
  要素」へ送るため、**開いた瞬間に1つ目の選択肢（なないろの欠片なら「１枚ドロー」）が押され**、
  モーダルは一瞬も見えないまま確定していた。対戦終了パネルの事故クリック対策（続き296）と同じく、
  開いてから400msは選択肢と「盤面を見る」を受け付けないようにした。
  - ログからは判別できなかった理由: `hand-effect` の行は**選択肢が決まった後**（`announceUse`）に
    出るため、「モーダルが出たのか」はログに残らない。ユーザーの「モーダルが出ずに」という一言が
    決め手になった。**同じ形（開いた直後の合成クリック）は他のモーダルでも起こり得る**ので、
    「タップで開く→その場にボタンが出る」UIを作る時は必ずこのガードを入れること。
- **#233「タイマーをONにするボタンがずっと出っぱなし」（修正）**: これは基本時間アイコンを押すと
  出るポップオーバー（続き68）だが、**閉じる手段が「もう一度アイコンを押す」しか無かった**ため、
  うっかり触れると対局中ずっとフェイズ案内板の上に残っていた。普通のポップオーバーと同じく
  「他の場所を触ったら閉じる」（document の capture pointerdown。自分自身と基本時間アイコンの上は
  無視＝二重トグルを防ぐ）＋10秒で自動的に閉じる、を追加した。
- **#232「フリーマッチランク戦でもう一度遊ぶを選ぶとブーストなくなってる」（原因確定・修正）**:
  `maybeTriggerRematch` は前の対局から **timerEnabled と pseudoCpuModeEnabled しか引き継いで
  いなかった**（続き102で追加された時にこの2つだけだった）。ブースト・白黒カード・マイデッキ戦は
  毎回 startGame の既定値（全て false）へ巻き戻っていた。これらも「対局全体の固定ルール」なので、
  同じ仕組み（開始時に1回だけ送って全員が共有する `timerConfig`）に **`matchRules: {boost,
  includeBlackWhite, myDeckMode}` として相乗り**させ、再戦時に読み戻す。`so7_games.timer_config` は
  jsonb にそのまま保存される（Edge Function は `gamesPatch.timer_config = action.timerConfig`）ので
  **サーバー側の変更は不要**。matchRules を持たない古い対局からの再戦に備え、部屋がランク戦
  （`getRoomIsRanked`）なら固定ルール（全てON）で補う。`diag-rematch-rules` をログに残す。
- **ゲストの戦績プレイヤー行30件を削除（ユーザー承認済み）**: 続き394で原因（RLSで他人のゲスト判定が
  できず、ゲストも `players` に登録されていた）を直したうえで、残っていた孤立行30件を削除した。
  削除前に「どの試合の members / winner_id にも、コメント（match_feedback_replies / game_comments）にも、
  seed戦績・Discord ID・称号にも一切参照されていない」ことを確認済み。削除後は `players` 44件・
  名前「プレイヤー」0件。**ゲストは戦績システムのプレイヤーには登録せず、対戦記録の
  `guest_names` にだけ名前が残る**（ユーザー方針「戦績には残してもいいがプレイヤーには入れない」）。
- **検証**: 全編集ファイルの構文チェック通過、`npm test` **56/56 PASS**、`node test/smoke.mjs 2` PASS。
  ブラウザ実測——#230: 選択肢2つのピッカーを開いた直後のクリックは無視され（モーダルは開いたまま）、
  400ms後のクリックでは正しく "draw" が返って閉じる。#233: 開くと表示され、盤面のタップで閉じ、
  もう一度押すと開き直し、ボタン自身の上のタップでは閉じない。#232 はオンライン2台でしか通らない
  経路のため実機での確認をお願いしたい（`diag-rematch-rules` がログに残る）。サーバー側の変更は無い。

### 2026-09-04（続き396）：#234 スキップ/マイデッキのボタンが対局の途中から出なくなる（#227の対応で私が入れた回帰）

ユーザー報告「フェイズのスキップボタンが表示されなくなっちゃいました」。**続き392（#227）で私が入れた
回帰**だった。
- **原因（確定）**: `updateSkipButtonVisibility()` に足した「カード効果の解決中はボタンを出さない」判定
  (`busyResolving`) が、選択待ちの案内バナー `#card-effect-picker-hint` の表示を
  **`getClientRects().length`** で見ていた。ところがこのバナーは**一度作られたらDOMに残り続け、
  `.show` クラスの付け外し（`opacity: 0` ⇄ `1`）だけで見せ隠しする**要素で、`display` は block の
  まま。つまり閉じていても矩形を返すため、**その対局で一度でも案内が出たら、以後スキップ/マイデッキが
  二度と出てこない**状態になっていた（報告のログでも、到達効果が走った後のロックフェイズから出ていない）。
  実測でも、`.show` を外した直後の `getClientRects().length` は 1 のままだった。
- **修正**: 実際の表示は `.show` が付いているかで判定する。`.so7-modal-backdrop`（返事待ちモーダルの
  背景）の方は閉じる時にDOMから取り除かれるが、こちらも念のため「実際に矩形を持つものだけ」を数える形にした。
- **教訓**: 「この要素が画面に出ているか」を `getClientRects()` で判定してよいのは、**閉じる時にDOMから
  取り除く**か **`display:none` にする**要素だけ。このプロジェクトには「作りっぱなしで
  `opacity`／`.show` だけ切り替える」常駐要素（案内バナー・トースト・ツールチップ等）が多いので、
  その種の要素は**クラスで判定する**。
- **検証**: `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、構文チェック通過。ブラウザ実測で、
  `.show` を外した状態のバナーが `getClientRects().length === 1`（＝旧判定なら誤検知）である一方、
  `classList.contains("show")` は false になることを確認（修正はこの式の置き換えそのもの）。

### 2026-09-04（続き397）：フレンド機能を追加（[src/friends-ui.js](src/friends-ui.js) 新規）／管理者ダッシュボードに一括対処済み・ログイン回数・ゲスト非表示

ユーザー決定「①フレンドになったら何ができるか＝D（通算成績まで）／②申請の出し方＝C（対戦終了
パネル＋マイページの『最近対戦した人』）」に沿って実装した。

- **設計の要点**:
  - **申請できるのは「一度オンラインで対戦した相手」だけ**。名前で検索する導線は用意しない
    （知らない人から申請が飛んでこないようにする、というユーザーとの合意）。
  - **ゲスト（匿名ログイン）は対象外**。アカウントが残らず次に会えないため、そもそも候補に出ない
    （`players` 行を持たない＝「最近対戦した人」に出てこない、という形で自然に落ちる）。
  - **断られたことは相手にも自分にも見せない**。`so7_get_friends` が `declined` を返さないので、
    申請した側の画面からは静かに消える（再申請の導線も出ない＝気まずくならない）。
  - **名前・アバターは追加の仕組み無しで読める**が（`players` は誰でも読める）、**最終アクセス
    （＝今アプリを開いているか）は本人しか読めない**（`so7_user_profiles` のRLS。続き394のゲスト
    誤登録と同じ制限）。そのため `so7_get_friends()` を SECURITY DEFINER にして、**自分が当事者の
    行に出てくる相手の分だけ**表示名・アバター・last_seen_at を解禁する形にした。
- **SQL（`supabase_setup_so7.sql` 末尾、要実行）**: `so7_friendships`（1組1行・`user_a < user_b` に
  正規化・status は pending/accepted/declined）＋ `so7_request_friend` / `so7_respond_friend` /
  `so7_remove_friend` / `so7_get_friends`。**直接 insert は許可しない**（RLSはselectのみ）——
  「対戦したことのある相手にだけ申請できる」という導線を迂回されないようにするため、書き込みは
  必ずRPC経由にした。
- **クライアント**: `online.js` にAPI（`fetchFriends` / `requestFriend` / `respondFriend` /
  `removeFriend` / `fetchRecentOpponents` / `isFriendOnline`）。「最近対戦した人」と通算成績は
  戦績システムの `matches` / `players` から作るので追加SQLは不要。UIは `friends-ui.js`——
  マイページの「👥 フレンド」ボタン（**称号と同じくモーダル方式**。マイページのレイアウトは絶対配置＋
  `width:max-content` なので、幅の広いリストを直接置くと隣の実績を押し出す。続き316の教訓）と、
  対戦終了パネルの中の一行（モーダルを増やさない）。
  `PROFILE_LAYOUT.friends = { x: 60, y: 600, scale: 1.4 }`（実測で他ブロックと重ならず画面内に収まる
  位置。レイアウト編集モードで動かせる）。
- **管理者ダッシュボード（同日、続き396のコミットとは別）**: ①不具合報告に「すべて対処済みにする」
  （`so7_resolve_all_bug_reports`、確認あり・実際の更新件数を表示）②ログイン履歴に通算ログイン回数・
  初ログイン日・今月の回数を併記（`so7_get_admin_visit_log` を作り直し）③登録ユーザー一覧で
  **ゲスト（匿名）アカウントを既定で隠す**（`so7_get_admin_user_list` に `is_guest` を追加。
  「表示名未設定のユーザーが多数」＝スモークテストやお試しで作られる使い捨てアカウントだった）。
- **検証**: 全編集ファイルの構文チェック通過、`npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、
  CSSブレース平衡（2827）。ブラウザ実測——未ログインでフレンドモーダルが開き「ログインすると…」の
  案内が出て✕で閉じる／マイページにボタンが出て押すとモーダルが開く／ボタンの位置が他の6ブロックと
  **重ならず画面内**（実測 399,717〜540,762）／対戦終了パネルの一行は未ログインでは出ない（null）／
  ダッシュボードの見出しが「日時・表示名・メールアドレス・通算・初回・今月」になり横スクロールしない。
- **ユーザー作業（必須）**: `supabase_setup_so7.sql` 末尾の「追加(2026-09-04)」2ブロック
  （管理者ダッシュボード／フレンド機能）を SupabaseのSQL Editor で実行する。**実行するまで
  フレンド機能は動かない**（一覧が空・申請が失敗するだけで、他は壊れない）。Edge Function の
  変更は無いため再デプロイ不要。

### 2026-09-04（続き398）：盤面のWebGL描画（three.js）第1段——「CSS 3D → WebGL の変換器」として作る（[src/board-3d.js](src/board-3d.js) 新規、既定OFF）

ユーザー判断「おすすめの方法でthree.js化を進めましょう」。iPhoneのチカチカ／強制終了（#223）の
原因は**合成レイヤーとGPUテクスチャの枚数**にあり、2D表示で症状が止まることからも裏付けが取れて
いる。そこで「盤面だけ先にWebGLへ」の第1段を入れた。

- **設計の要点（この方針が今回いちばん重要）**: ゲームのロジックもDOM構造も作り直さない。
  `#game-table` の中で**インラインstyleで背景画像を描いている要素**（カード・駒の面・山）を走査し、
  「その要素が画面上のどこにどう映るはずか」を計算して**同じ絵をWebGLで描き、DOMからは絵だけ剥がす**。
  つまり盤面の作り直しではなく **CSS 3D → WebGL のラスタライザ**。結果として、
  ・タップ判定（`elementsFromPoint` ベースの自前方式）は**一切変更なしでそのまま動く**（箱は残す）
  ・管理者モードの位置調整スライダーも**そのまま効く**（CSSの計算結果を読んで描くため）
  ・カード面の文字・多言語・自動フィットは無関係（文字はDOMのまま）
  ・失敗しても設定を戻せば完全に元通り（既定OFF、管理者モードの「🧪 盤面をWebGLで描く」）。
- **投影を一致させる**: CSSの `perspective` は「(perspective-origin, P) にカメラを置き z=0 平面へ
  投影する」ピンホールカメラそのもの。three.js 側で同じ位置・**軸をずらした視錐台**
  （`makePerspective` を直接使う）を作れば一致する。要素の位置は `offsetLeft/offsetTop`（変形前の
  レイアウト座標）と computed transform を root まで積み上げて求める——**CSSの計算を二重に書かない**。
  実測で **DOMの矩形との差は±1px**（プレイマット・駒・カード・山で確認）。
- **踏んだ罠（3つとも実測で発見）**:
  1. **computed style から背景画像を読んではいけない**。3DがONの間はCSS側が
     `background-image: none` に上書きしているため、computed は常に none を返す（最初これで
     「板が0枚」になった）。盤面の絵は全て main.js が**インラインstyle**で入れているのでそちらを読む。
     ドラッグゴーストが computed を読んでいた2箇所も `paintedBackgroundImage()` 経由に直した。
  2. **near を P にすると手前半分が消える**。盤面は42度傾いていて手前側がカメラに近づくため、
     z=0 平面を near にすると手前が丸ごとクリップされる（画面の真ん中に水平の切れ目が出た）。
     near をずっと手前に置き、視錐台の縁も同じ比率で縮める。
  3. **キャンバスは1枚なので、DOMの重なり順の“途中”には割り込めない**。プレイマットはマスの枠線より
     奥、カードは手前——という順序を1枚のキャンバスでは再現できない。そこでキャンバスは盤面DOMより
     **手前**に置き、**手前に来るべきもの（カード・駒・山）だけ**をWebGLへ移した。プレイマットと床
     （`.table-background-bg`、4.75倍で地平線まで伸びる巨大な板）は画像1枚ずつでレイヤー数の問題には
     関係しないため、CSSのまま残す。
- **その他**: WebGLコンテキストが失われたら（iOSのメモリ不足で起こりうる）自動でCSS描画へ戻す
  （真っ黒な盤面で操作不能にしない）。解像度は devicePixelRatio 2倍で頭打ち。three.js r169 は
  `vendor/three.module.min.js` に**同梱**した（CDN依存にせず、オフライン/PWAでも動くように。
  更新チェッカーが取り直す対象にもなる）。
- **検証**: `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、CSSブレース平衡。ブラウザ実測で、
  ①ON にすると盤面で画像を描いていたDOM要素が **23→0**（全部WebGLへ移り、板23枚・画像7種）
  ②OFF で **23に戻る**（body クラスも外れる） ③DOMの矩形とWebGLの投影の差が**±1px**
  ④スクリーンショットでCSS版とほぼ同じ見た目（駒の立方体・山・ロック枠・プレイマットとも）
  ⑤コンソールエラー・未捕捉例外 0件。
- **次の段（未着手）**: マスの枠線・ロック枠・ハイライト・カード面の文字もWebGLへ移し、DOMの箱を
  「当たり判定だけの透明な板」にする。そこまでやると合成レイヤーがほぼ消える。実機で効果を測って
  から進めたい。

### 2026-09-04（続き399）：管理者モードの「🧪 盤面をWebGLで描く」が画面に出ていなかった（存在しないカテゴリを指定していた）

ユーザー報告「管理者モード →『🧪 盤面をWebGLで描く』はどこにある？」。**続き398で私が付けた
`category: "advanced"` が `CATEGORIES` に存在しないキー**だったため、振り分けループから漏れて
**どのカテゴリにも入らず、画面にまったく出ていなかった**（構文エラーにならず静かに消える）。
`"admin-only"`（🔐 管理者専用）へ変更した。
- **再発防止**: パネル構築時に「`CATEGORIES` に無い category を持つ項目」を `console.warn` する
  ガードを入れた。admin.js に項目を足す時は、**スライダーなら `GROUPS`・それ以外は `TOGGLE_SECTIONS`**
  （続き349の教訓）に加えて、**category が実在するキーか**も確認する。
- **検証**: ブラウザで管理者パネルを実際に開き、「🧪 盤面をWebGLで描く（three.js・試験中）」が
  **🔐 管理者専用の中に出る**こと、未知カテゴリの警告が0件であることを実測。
- **あわせて確認**: ユーザーが実行したSQLが本番に反映されていることを実測（`so7_get_friends` /
  `so7_request_friend` / `so7_respond_friend` / `so7_remove_friend` / `so7_resolve_all_bug_reports` /
  `so7_get_admin_user_list` / `so7_get_admin_visit_log` の7つとも、未ログイン・非管理者に対して
  関数内部の判定メッセージ（not_signed_in / not_authorized）を返す＝関数が存在して動いている。
  `so7_friendships` テーブルも読める）。

### 2026-09-04（続き400）：WebGL描画の見た目4件（#238/#240/#241）／#235・#239 ライトモードで対戦中のモーダルがダーク／#236 マス確認が勝手にオフになる／#237 iPhoneで鼓動が聞こえない

ユーザーから「チカチカ直りました！！！！！！！！！」（＝続き399のthree.js化第1段でiOSのちらつきが
解消）と同時に届いた報告7件（#235〜#241）への対応。

- **#238b「ロックエリアのカードがその席のプレイヤーに正対していない」＝テクスチャが上下逆さま
  （原因確定・修正）**: three.jsのテクスチャは既定 `flipY = true`（Yが上向きの座標系向け）。この
  レンダラーはCSSの座標系（Yが下向き）のまま板を並べるので、そのままだと**絵だけが上下反転**して
  貼られていた。位置は±1pxで合っていたので気づけず、カード裏面はほぼ上下対称の柄なので見逃して
  いた——**表向きのロックカード**（各席の最初のカード）で初めて分かった。`tex.flipY = false` で解決。
  実測: OFF/ONのスクリーンショットを同じ場所で切り出して比較し、修正前は "YELLOW" の文字と隅の点が
  上下逆、修正後は完全に一致することを確認（差分タイル 337→214）。
  - **教訓**: 3Dの検証で「位置が合っている」ことと「絵が正しく貼れている」ことは別。位置は
    getBoundingClientRect で数値比較できるが、**向きは数値に出ない**ので、必ず画像で見比べる。
- **#240「PCでCPU戦開始時に駒が変化形に」＝隠れているはずの駒を描いていた（原因確定・修正）**:
  CSSの `opacity` / `visibility` は**祖先から継承して効く**が、WebGL側はその要素自身しか見て
  いなかった。セットアップ中の駒は `.piece { opacity: 0; transform: scale(0.2) }` で隠れているのに、
  子の `.piece-face` は opacity 1 なので**全部描いてしまい**、まだ出ていないはずの駒が小さく歪んで
  見えていた。`effectiveVisual()` で root まで遡って実効値を求めるようにした（display:none /
  visibility:hidden の祖先も同様に除外）。**ドラッグ中に元要素を `visibility:hidden` で隠す処理
  （駒はこれを `.piece` に付ける）も同じ理由で効いていなかったので、あわせて直っている**。
  実測: セットアップ中に描かれていた駒の板が 0 枚になることを確認。
- **#241「駒がカードに隠れている」＝重なり順をDOM順で決めていた（原因確定・修正）**: 板は半透明を
  含むので深度バッファ（depthWrite）を使わず**描く順番**だけで前後を決めているが、その順番をDOMの
  並び順にしていた。CSSの3D（preserve-3d）は**実際のZ位置**で前後を決めるため、駒（translateZで
  持ち上がっている）より後ろのDOMにあるカードが駒の上に描かれていた。毎フレーム、板の中心の
  ワールドZでソートして renderOrder を振り直す（同じ高さのものはDOM順）。実測: 駒とカードが同居
  する4マスすべてで「駒の板 > カードの板」になることを確認（例: カード6 に対し駒 11〜15）。
- **#238a「ムーブフェイズの時に盤面のカードのドロップシャドーが強くなる」＝暗転の膜がカードに
  かからなくなっていた（原因確定・修正）**: 選べないマスは `.cell::after` の黒い半透明の膜で
  暗くしているが、WebGLのキャンバスは盤面のDOMより**手前**にあるため、カードの絵だけが膜の上に
  出て「カードだけ明るく、周りが真っ暗＝濃い影が付いた」ように見えていた。`overlayTint()` で
  マス（.cell / .lock-slot）の `::after` の背景色を読み、膜が重なった時の色を板の色として先に
  作る（`getComputedStyle(el, "::after")` は擬似要素にも使えるので、暗転ルールが増えても自動で
  追随する）。実測: ムーブフェイズ中に 64枚中59枚が着色され、抜けると 0 に戻る。
  - あわせて、3D描画中に剥がしていた `box-shadow` の `!important` を外した。基本の影（iOSで重い
    合成の元）は今までどおり消えるが、`.cell.card-effect-just-placed .board-card` のような
    **詳細度の高いハイライトの光は残る**。実測: 3D ON で 通常=none / ハイライト=金の光 / 3D OFF=通常の影。
- **#235・#239「ライトモードなのにモーダルがダーク」（原因確定・修正）**: 対戦画面のライト化は
  `body.theme-light-ingame`、メニュー等のライトは `body.theme-light` と**完全に独立**していた
  （続き338当時、対戦中にワンクリックで見比べるための作り）。非管理者は `ensureThemeForRole` で
  両方ONに強制されるので実害が無く、**管理者（＝ユーザー本人）だけ**が「ライトモードなのに盤面の
  上のモーダルはダーク」という状態になっていた。ライト化の作業自体はもう終わっているので、
  `theme-light` なら対戦画面もライトにする（`themeLightIngame || themeLightMode`）。管理者向けの
  トグルは「メニューはダークのまま対戦画面だけライトにする」上書きとして残す。実測: 到達モーダル・
  効果説明モーダルの地がアイボリー（255,253,247）になることを確認。
- **#236「ジャンプ台の手札効果で『このマスでいいですか？』が出なかった」＝設定が勝手にオフに
  なっていた（原因確定・修正）**: 報告 #235（15:54）では `cellConfirm: true`、#236（15:58）では
  `false`。**#230 と同じ「開いた瞬間の合成クリック」**——このモーダルは選んだマスのすぐ隣に出るので、
  マスをタップして指を離した位置に「今後このモーダルを表示しない」が現れ、そのタップがそのまま
  ボタンを押していた。`createOpenGuard()`（ui-helpers.js に新設、既定400ms）を使い、開いた直後は
  はい／選び直す／今後表示しない のどれも受け付けないようにした。**「総点検して」の指示どおり
  `confirmTouchAction`（使う前の確認）にも同じガードを入れた**（capture フェーズの pointerdown 経路
  ＝#200 の対策側にも）。あわせて、この設定をオフにした時は画面に一言お知らせする（「押した覚えが
  ないのに出なくなった」を防ぐ）。実測: 開いた直後のクリックは無視されモーダルが開いたまま設定も
  維持され、400ms後は正しく確定することを確認。
- **#237「iPhoneでザ・ギャンブルの心臓の鼓動が聞こえなかった」（対策・要実機確認）**: 続き381で
  中音（×2.6＝約160Hz）を足したが、**iPhone本体のスピーカーは300Hz以下がほとんど出ない**ため、
  まだ本体では鳴っていないに等しかった（イヤホン・PCでは低音が聞こえるので気づきにくい）。
  OfflineAudioContext で合成し**400Hzのハイパスを通して「スマホのスピーカーで実際に出る帯域」だけ**
  のRMSを比べて倍率と音量を決め（×7・0.5倍・60ms）、その帯域で**約3.0倍**にした（全帯域のピークは
  0.87で歪まない）。あわせて、AudioContext が running になるまで 900ms 待っていた立ち上がりを
  120ms間隔で見に行くようにし（最初の数拍が無音になっていた）、`diag-heartbeat`（state / 音量 /
  画面が隠れているか / audioSession）を1回残すようにした——次に「聞こえない」と報告された時、
  推測せずに切り分けられる。
- **検証**: 変更した全ファイルの `node --input-type=module --check` 通過・CSSブレース平衡（2829）。
  `npm test` **56/56 PASS**、`node test/smoke.mjs 2` PASS。ブラウザ実測は上記各項のとおり
  （テーマ・マス確認ガード・WebGLの板の枚数と着色・影の3状態・エラー0件）。#237 の実機での
  聞こえ方の確認だけお願いしたい。サーバー側（Supabase）の変更は無い。

### 2026-09-05（続き401）：盤面のWebGL描画・第2段（マス・ロックスロット・カードの枠もWebGLへ）／「2D表示がおすすめ」の案内を既定OFF／管理者ダッシュボードに「読み込み直す」

ユーザー判断「①（WebGL描画を既定ONに）はもっと触ってから。②（第2段）を進めましょう」＋
「スマホ用の2Dの勧め警告も不要。一応戻せるようにして、デフォルトでは表示しない」＋不具合報告
「管理者ダッシュボードで『すべて対処済みにする』を押したら、登録ユーザー一覧もログイン履歴も
なくなった」。

- **WebGL描画・第2段（[src/board-3d.js](src/board-3d.js)）**: 第1段は「絵（カード・駒・山の画像）」だけを
  WebGLへ移したが、マス(49)・ロックスロット(28)・カードの枠線は**まだDOMが塗っていた**。iOSは
  preserve-3d の中の要素ひとつひとつに合成レイヤーを作るので、ここを「何も塗らない箱」にできれば
  その分の描画コストが丸ごと消える。**実測: 盤面の中で何かを塗っているDOM要素が 187（OFF）→
  156（第1段）→ 30（第2段）**。
  - **画像を持たない要素は、CSSの値からその場でテクスチャを作る**（角丸の枠＋背景色＋外側のグローを
    小さなcanvasに描く）。同じ見た目のものは使い回すので、実際のテクスチャは十数種類で済む。
  - **剥がした値をどうやって読むか**が肝。WebGL描画中は枠線・背景色をこちら側で消しているため、
    普通に getComputedStyle しても「透明」しか返ってこない。**board-3d.js が自分で持つ `<style>` を
    一瞬だけ無効化して読む**方式にした（`readShapeSpecs`）。CSSの計算を二重に書かずに済み、
    管理者モードのスライダー等で値が変わっても自動的に追随する。スタイルの再計算が2回走るので
    400msに1回までに間引いている。
  - **剥がすのは「背景色・枠線の色」だけで、枠線の太さは残す**（box-sizing の都合でレイアウトが
    変わってしまうため）。**outline（選択中のマスの光る枠）と、カードのハイライトの光は剥がさない**
    ——箱の内側/外側に描かれるもので、WebGLの板（枠だけ・中は透明）に隠されないため、今までどおり
    DOMのまま動く。ロックスロットのグローだけは板と重なるのでWebGL側で描く。
  - 1つの要素が「絵の板」と「形の板」を**両方**持てるようにした（盤面のカードは絵も枠もWebGL）。
    最初これを排他にしてしまい、**盤面のカードが全部消えた**（形の板だけが作られ、絵の板が作られ
    なかった）。ON/OFFのスクリーンショット比較で即座に分かった——**3D関連の変更は必ず画像で見比べる**。
  - **【重要なバグ・実測で発見】CSSの色は rgb/rgba とは限らない**。アニメーションやトランジションの
    最中、Chromeは computed値を `oklab(0.83 0.013 0.17 / 0.11)` のような形で返す。自前で文字列を
    解釈していたため取りこぼし、**1フレームごとに新しいテクスチャが増える**状態になっていた
    （実測でテクスチャ78種まで増殖。iOSのために減らしているのに本末転倒）。**1x1のcanvasに実際に
    塗って読み返す**（＝ブラウザ自身に解釈させる）方式に変え、色は8段階・不透明度は20段階に丸めて
    から見分けに使うようにした。上限48種を超えたら古い順に捨てる保険も入れた（実測22〜34種で安定）。
  - あわせて、深さ順の並べ替えは「板か視点が変わった時だけ」に変更（毎フレーム206枚を並べ替えていた）。
  - **計測を仕込んだ**（管理者パネルの表示に出る）: 板の枚数・うち枠の枚数・テクスチャ種類数・
    1フレームの時間・描画の時間・作り直しの時間。**ヘッドレスのテスト環境はGPUが無くWebGLをCPUで
    描くため、そこでの数値（3D ONで約1.4倍遅い）は実機の参考にならない**——必ず実機で見ること。
- **「2D表示がおすすめ」の案内を既定OFFに（[src/tablet-2d-warning.js](src/tablet-2d-warning.js)）**: 盤面のWebGL描画で
  iOSのチカチカが解消したため、最初に「2D表示にしてください」とお願いする必要が無くなった。
  `so7-tablet-2d-warning-enabled`（既定OFF）を新設し、管理者モードの「📱 『2D表示がおすすめ』の案内」
  からいつでも戻せる（「表示済み」の記録を消すボタンも用意）。この案内が兼ねていた「端末の種類を
  聞く」質問は、画面サイズからの推測（device-detect.js の getDeviceType）にフォールバックするので
  出さなくても壊れない。
- **管理者ダッシュボードの報告について**: 偽のSupabaseクライアントを差し込んでヘッドレスで再現を
  試みたが、**「すべて対処済みにする」を押しても登録ユーザー一覧・ログイン履歴の行は消えなかった**
  （押す前後とも5行のまま・例外0件）。JS側にそのような結び付きは無いので、一覧の取得そのものが
  失敗していたか、認証の復元に一時的に失敗して中身が丸ごと隠れた（`init()` は `getUser()` が null
  だと `#dashboard-content` を出さないまま終わる）かのどちらかと考えられる。次に起きた時に原因が
  分かるよう、また**ページを開き直さずに復旧できる**ように以下を入れた:
  - ヘッダーに「🔄 読み込み直す」ボタン（4つの一覧をまとめて取り直す）。
  - `getUser()` が null なら800ms待って1回だけ再試行してから諦める（通信の一時的な失敗でダッシュ
    ボード全体が消えないように）。
  - 取得失敗を画面だけでなく `console.error` にも出す（どの一覧が何で失敗したかが分かる）。
  - 「すべて対処済み」の後は「（『対処済みを隠す』がONなので、この一覧は空になります）」と添える
    （不具合一覧が空になるのは正常だが「消えた」と誤解しやすいため）。
  - ついでの不具合: 「読み込み直す」でログイン履歴が二重に並ぶのを修正（`loadVisitLogInitial` が
    追記するだけだったので、必ず空にしてから読むようにした）。
- **検証**: 変更した全ファイルの `node --input-type=module --check` 通過・CSSブレース平衡。
  `npm test` **56/56 PASS**、`node test/smoke.mjs 2` PASS。WebGLの第2段は ON/OFF のスクリーン
  ショットを撮って盤面全体・マス・ロックエリアを見比べ、見た目が一致することを確認（カードの
  ドロップシャドウが無いのは第1段からの既知の差）。塗っているDOM要素の数は上記のとおり実測。

### 2026-09-05（続き402）：管理者ダッシュボードで一覧が取れない原因を特定（plpgsqlの返す型が厳密＝auth.users.emailはvarchar）／WebGLの数値を行動ログにも残す

ユーザーがダッシュボードのスクリーンショットを送ってくれたことで、続き401で「再現できなかった」
件の原因が判明した。画面には **「取得に失敗しました: structure of query does not match function
result type」**（ログイン履歴）と **「0件（直近0件を表示）」**（登録ユーザー一覧）が出ていた。

- **原因（確定）**: plpgsql の `returns table (...)` は、返す列の型が**厳密に一致**していないと
  実行時にこのエラーになる。**`auth.users.email` は `text` ではなく `character varying(255)`**
  なので、`email text` として返そうとすると落ちる（Supabaseでよくある落とし穴）。続き397で
  この2つの関数を作り直した時に入り込んだ。つまり**「すべて対処済みにする」ボタンとは無関係**で、
  SQLを実行した時点からこの2つの一覧はずっと取れていなかった（ボタンを押した時に不具合一覧も
  空になったので、3つとも消えたように見えた）。
  - **修正**: `supabase_setup_so7.sql` の末尾に、**返す列すべてに明示的なキャストを付けた**版を
    追記した（`u.email::text` / `coalesce(...)::bigint` / `...::timestamptz` 等）。
    → **ユーザーがSupabaseのSQL Editorで実行する必要がある**（この2つの関数を置き換えるだけ。
    何度実行しても安全）。
  - **教訓**: `returns table` で `auth.users` の列を返す時は必ず `::text` 等を明示する。
    型が違うだけで「クエリの構造が関数の戻り値型と一致しない」という、原因が読み取りにくい
    実行時エラーになる。
- **なぜ登録ユーザー一覧は「0件」に見えたのか（こちらも修正）**: `loadUserList` は失敗時に説明行へ
  理由を出していたが、その後**「ゲストを隠す」チェックを触ると `renderUserList` が説明行を
  「0件（直近0件を表示）」で上書き**していた。＝失敗が見えなくなる。直近の失敗理由を保持し、
  再描画しても消えないようにした（ログイン履歴側はチェックが無いので理由が残っていた——
  だからそちらだけ原因が読み取れた）。
- **WebGLの数値を行動ログにも残す（ユーザー要望「ログに出力するようにするのはどうでしょう？」）**:
  管理者パネルを開いていなくても実機の数値が読めるよう、`diag-board3d`（板の枚数・うち枠・
  テクスチャ種類数・1フレーム/描画/作り直しの各ms・dpr・キャンバスの実ピクセル）を
  **20秒に1回**アクションログへ残すようにした（最初の1回は動き出して5秒後——起動直後は全部0で
  意味が無いため）。不具合報告にもそのまま載るので、実機の重さを推測せずに判断できる。
- **検証**: `node --input-type=module --check`（board-3d/admin-dashboard）通過、`npm test` 56/56 PASS、
  `node test/smoke.mjs 2` PASS。ブラウザ実測で、①`diag-board3d` が実際の数値（板192・枠128・
  1フレーム182ms・描画17.6ms・作り直し74.1ms ※ヘッドレスはGPU無しの参考値）で1件記録される
  ②ダッシュボードで取得に失敗させると理由が説明行に出て、**チェックを触っても消えない**、を確認。

### 2026-09-05（続き403）：端末にだけ保存されていた設定をアカウントにも保存（[src/pref-registry.js](src/pref-registry.js) 新規）

ユーザー要望「アカウントに保存すべき情報について再精査してください」。設定には2種類あり、
**アカウントに保存（so7_user_profiles の列）**＝音量・アニメ削減・表示時間・名前/アバター・
2D表示・自動処理・使う前の確認・言語・マイデッキ、**この端末にだけ保存（localStorage）**＝
CPUの速さ/強さ/人数/自動送り・フェイズの自動スキップ・マスの確認・カード拡大の大きさ/向き・
手札の固定・絵だけ表示・ドラッグ制限・ランク戦の通知・振動、という分かれ方だった。
後者が「設定がリセットされた」の正体（閲覧データの削除／別ブラウザ・別端末／iOSが古い保存
データを自動削除）。今回、後者のうち**「好み」に当たる14件をアカウントにも保存**するようにした。

- **設計①: 列を増やさず1つの jsonb にまとめる（`so7_user_profiles.extra_prefs`）**。設定ごとに列を
  足す方式だと、**列が1つ足りないだけでSELECT文全体が失敗し、他の設定まで丸ごと読めなくなる**
  （online.js の loadMyPreferences のコメントに残っている既往事故）。読み書きも**独立したクエリ**に
  したので、この列がまだ無い環境でも他の設定に一切影響しない（lang / my_deck と同じ作法）。
  → **`supabase_setup_so7.sql` 末尾の `alter table so7_user_profiles add column if not exists
  extra_prefs jsonb;` をユーザーがSQL Editorで実行する必要がある**（実行するまでは今までどおり
  端末保存のまま動く＝壊れない）。
- **設計②: 登録制にして循環importを避けた**。[src/pref-registry.js](src/pref-registry.js) は**importを一切持たない葉
  モジュール**で、各設定モジュール（cpu-battle-state.js 等）が自分から `registerSyncedPref(key,
  get, set)` を呼ぶ。online.js はこのファイルだけを見ればよい——逆向き（online.js が各設定モジュールを
  import）にすると、ranked-notify.js が online.js を import しているので循環する。
- **設計③: 遅れて読み込まれるモジュールに対応**。CPU戦の設定は対局を始めるまで読み込まれない
  ことがあるので、アカウントから読んだ値は `pendingValues` に取っておき、そのモジュールが登録
  された瞬間に適用する。
- **設計④: setter を書き換えず「値の変化」を見て保存する**。設定を変える経路はモーダルの
  「今後表示しない」等あちこちにあり、保存処理を1つずつ足すと**呼び忘れの経路ができる**
  （このプロジェクトで何度も踏んでいる形）。15秒ごと＋画面を離れる時に
  `takeChangedSyncedPrefs()`（前回保存した内容とJSON比較）で拾う。読み込んだ直後は「保存済み」
  扱いにして書き戻さない。ログアウト時は追跡をリセットする（別アカウントで入り直した時に
  前の人の設定を書き戻さないため）。
- **同期する14件**: cpuSpeed / cpuDifficulty / cpuPlayerCount / cpuAutoSkip / autoPhaseSkip /
  cellConfirm / cardPreviewSize / cardPreviewSide / boardIllustOnly / fixedHand /
  autoDragRestriction / rankedNotify（ON/OFFと時間帯） / vibration / firstSteps（「次にやること」の進み具合）。
- **あえて同期しないもの**: 端末の種類（スマホ/タブレット）・テーマ（管理者専用）・
  実験用の描画モード（lite-render / iso25d）・各種「今後表示しない」「既読」・管理者モードの
  調整値・部屋のパスワードや待機状態などの一時情報。いずれも「その端末の事情」か開発用のため。
  - **申し送り**: 2D表示（flatten_2d_mode）は以前からアカウント同期されているが、本来は端末の
    性能に依る設定なので、端末ごとの方が自然かもしれない（既存の挙動は変えていない）。
- **検証**: 変更した全ファイルの構文チェック通過、`npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS。
  ブラウザ実測で、①14件すべてが登録される ②アカウントから読んだ値が全部適用される
  ③読み込んだ直後は保存が発生しない ④1つ変えると変化として拾える ⑤エラー0件、を確認。

### 2026-09-05（続き404）：接触のタックル演出を短く／駒の上の「🤝 接触する」浮遊ボタンを廃止（確認モーダルへ直行）

ユーザー指示（管理者モードのスクリーンショットで秒数を指定）＋「接触するとき駒の上に『接触する』
ボタンが出るが、その次にちゃんとしたモーダルが出るためそのボタンを無くしましょう」。

- **タックル演出の秒数を焼き込み**: `--contact-anim-runup-duration` 1.5→**0.5**、
  `--contact-anim-tackle-duration` 1→**0.5**、`--contact-anim-flight-duration` 1→**0.5**
  （①承認から演出開始までの間は1のまま）。style.css の `:root` と admin.js のスライダーの
  `default:` の両方に反映（「リセット」でもこの値に戻る）。
- **浮遊ボタンの廃止**: `showContactPrompt` が駒の上に `.card-open-prompt` の小さなボタンを出し、
  それを押すと確認モーダル、という2段階だった。同じことを2回聞いているだけなので、
  **確認モーダルへ直行**するようにした。使われなくなった `contactPromptEl` /
  `closeContactPrompt` / その外側クリック用の document リスナーは撤去（引数
  `anchorPieceTokenId` は浮遊ボタンの位置決め専用だったので未使用になったが、呼び出し側3箇所の
  シグネチャは変えずに残してある）。疑似CPU席の自動申し込み・#228の接触禁止チェックは
  今までどおりこの関数の入口を通る。
- **あわせて確認モーダルに「開いた直後の合成クリック除け」を追加**（#230/#236 と同じ
  `createOpenGuard`）。**駒を離した直後にモーダルが出るようになったため**、指の下でボタンが
  押される事故が起こり得る位置関係になった。
- ヘルプの本文（ja/en とも「『接触する』ボタンが浮かびます」）も実際の流れに合わせて更新した。
- **検証**: `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、CSSブレース平衡（2829）。
  ブラウザで**実際にドラッグして**確認——隣の相手の駒へ自分の駒をドラッグして離すと、
  浮遊ボタンは0個で `#contact-confirm-modal`（「本当に接触しますか？」）が直接出る。
  ※この経路は「自動処理中のドラッグ制限」がONだと駒を掴めないため、検証では一時的にOFFにした
  （自動処理モードでは、ムーブフェイズにハイライトされた相手のマスをタップする経路で同じ
  `showContactPrompt` に入る）。

### 2026-09-05（続き405）：ダッシュボードの2つの一覧を「型合わせ」から解放（jsonbを1つ返す形へ）

続き402で `returns table (...)` の各列に明示キャストを付けたが、ユーザー報告では**まだ**
「取得に失敗しました: structure of query does not match function result type」のまま
（今度は登録ユーザー一覧にも同じ理由が出た＝続き402の「失敗の理由が消えない」修正は効いている）。

- **方針変更**: plpgsql の `returns table` は**列の型・数・順番を1つでも取り違えると実行時に
  落ちる**という仕組みそのものが弱い（原因も読み取りにくい）。型合わせを頑張るのをやめ、
  **結果を jsonb（オブジェクトの配列）1つとして返す**形に書き換えた。一致しようがないので
  この種のエラーは二度と起きない。クライアントは今までどおり「オブジェクトの配列」として
  受け取れる（キー名は同じ）ので、`Array.isArray` のガードを足すだけで済んだ。
- **どちらの版が入っているか確認する方法**もSQLのコメントに書いた:
  `select p.proname, pg_get_function_result(p.oid) from pg_proc p join pg_namespace n
  on n.oid=p.pronamespace where n.nspname='public' and p.proname in (...)` →
  `jsonb` ならこの版。**なぜ前回の修正が効かなかったのかは未確認**（ファイル全体を実行した際に
  どこかでエラーが出て、末尾の修正まで到達していなかった可能性がある。次はこの2つの関数だけを
  貼って実行してもらう）。
- **教訓**: SECURITY DEFINER の集計系RPCは、最初から `returns jsonb` にしておくのが安全。
  列を1つ足すたびに型合わせの事故が起きる `returns table` を、管理画面のような
  「そのまま画面に出すだけ」の用途で使う必要はない。
- **検証**: 構文チェック通過。偽のクライアントで「オブジェクトの配列」を返した時に、登録ユーザー
  一覧・ログイン履歴とも正しく描画される（通算/初回/今月の列も含む）ことをブラウザで実測。

### 2026-09-05（続き406）：駒の移動を「実際に動いて見える」ように／盤面のWebGL描画を既定ONに／全コピペで実行できるSQLファイルを用意

ユーザーからの3点（①「全コピペでいいようにできますか？」 ②「WebGLは既定をONで行こう」
③「駒が一瞬で次のマスに行っているような挙動。しっかり移動している感じにしたい」）。

- **駒の移動アニメーション（[src/main.js](src/main.js) `playPieceMoveAnimation`）**: 移動が `moveToken()` →
  `render()` だけだったので、駒は一瞬で次のマスに現れていた。**オンラインで他人の移動を見せる時
  （remote-move-animator.js）と全く同じ方式**——移動先の駒をいったん隠し（`setSetupPendingTokenIds`）、
  飛翔ゴーストを飛ばし、着いたら現す——を自分・CPUの移動にも使う。これで自分の移動と他人の移動が
  同じ見え方になり、実装も既存の仕組みに乗るだけで済む。
  - **なぜ実物にCSSトランジションを掛けないのか（検討して却下した案）**: 駒のDOMは `render()` の
    たびに作り直されるので、移動中に render() が走った瞬間（＝ほぼ必ず走る）にアニメーションが
    消えてしまう。飛翔ゴーストは body 直下の独立要素なので render() の影響を受けない。
  - 出発点は「元のマスの中心に、駒と同じ大きさで」置く（マスの大きさで飛ばすと出発の瞬間だけ
    駒が大きく見えるため）。時間は `--piece-move-duration`（既定0.45秒）で、管理者モードの
    新グループ「駒の移動アニメーション」から調整できる。「駒やカードが飛ぶ動きをやめる」設定は
    そのまま尊重する（その時は従来どおり一瞬で移る）。
  - 今回つないだのは通常移動（`performPhaseMoveToCell`＝自分の移動もCPUの移動もここを通る）。
    カード効果による強制移動などは従来どおり（必要なら同じ関数を呼ぶだけで足せる）。
- **盤面のWebGL描画を既定ONに**: 設定値だけを持つ極小モジュール [src/board-3d-setting.js](src/board-3d-setting.js) を新設
  （board-3d.js は three.js を静的importしているので、設定を読むだけのために options-menu.js から
  importすると、オプションを開くだけで700KB弱を読み込むことになる）。既定ONで、main.js の起動
  処理から `applyStoredBoard3d()` を**動的import**で呼ぶ。基本設定の「動きが重い・カクつくとき」に
  プレイヤー向けのチェックも追加した（うまく描けない端末で戻せるように）。**この設定はアカウントに
  同期しない**——端末の性能に依るため（2D表示と同じ考え方）。
- **接触のタックル演出の秒数**（助走/タックル/帰還を各0.5秒）と**駒の上の「🤝 接触する」浮遊
  ボタンの廃止**は続き404を参照。
- **全コピペで実行できるSQL**: ユーザーが「codeが苦手なので全コピペでいいようにできますか？」と
  言うので、`supabase_今すぐ実行するSQL.sql` を新設した（今やってほしい2件だけ＝ダッシュボードの
  2関数と `extra_prefs` 列）。あわせて、**`supabase_setup_so7.sql` 全体を貼っても途中で止まらない
  よう**、権限で失敗し得る storage まわり（bug-shots バケットとそのポリシー）を
  `do $$ ... exception when others then raise notice ... end $$;` で包んだ。SQL Editor は1つでも
  エラーが出るとそこで止まり、**それ以降の定義が反映されない**——前回の修正が効かなかったのは
  これが原因の可能性が高い。
- **検証**: `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、CSSブレース平衡（2830）。
  ブラウザ実測で、①起動直後に `body.board-3d-on` が付き板100枚で描画されている（既定ON）
  ②CPU自己対戦を20秒観察して**駒の飛翔ゴーストが5回・移動中に駒が隠れているのが2回**観測できた
  （＝移動が見えるようになった） ③新しい設定モジュールが読める ④エラー0件。

### 2026-09-05（続き407）：オンラインの決着まで自動対戦で「自分の手番なのにフェイズが始まらず止まる」を発見／保険を実装

ユーザー選択「②（オンラインの自動対戦を決着まで回す）お願いします」。`test/online-smoke.mjs 2 --full`
を初めて通した（これまでは8ターンで打ち切っていたので、最後のロックの全員承認などは一度も
通っていなかった）。

- **まず自分で入れた2つの雑音を潰した**:
  - `extra_prefs` 列がまだ無い環境（SQL未実行）で `console.error` を毎回出していたため、
    **オンライン自動対戦が即FAIL**した。SQL未実行は「想定内の状態」なので、行動ログに1回だけ
    残して以後は読み書きを諦める（`diag-extra-prefs-unavailable`）。ログイン中ずっと出続けて
    本物のエラーが埋もれるのも防げる。
  - **テストではWebGL描画を切る**ようにした（`so7-board-3d-enabled=0`）。ヘッドレスChromiumは
    GPUが無くWebGLをCPUで描くため、実測で **1フレーム655ms**まで落ち、進行が遅いだけなのに
    「45秒進展なし＝停止」と誤検知していた（turn 12でFAIL）。テストで見たいのはゲームの進行で
    あって描画性能ではない。切ったら同じ条件で turn 19 まで進んだ。
- **【新しい不具合・未解決】自分の手番なのにフェイズが始まらず、対局が完全に止まる**:
  共有ブラウザで turn 19、**別ブラウザ(`--isolated`)でも turn 24** で再現。症状は毎回同じ——
  **手番のクライアントだけ `getCurrentPhase()` が null のまま**で、相手のクライアントは
  `phase=lock` と認識している。持ち時間はとっくに切れており（deadlineIn=-47s）、
  相手側は10秒ごとに `diag-timeout-latch-retry` → `diag-pseudo-cpu lockAutoPlay-check` を
  出しているが、**自分の席ではないので当然何もしない**。＝誰も動かせない。
  - フェイズ自動進行(`reconcilePhaseAutomation`)は render()（＝状態の変化）をきっかけに走るので、
    **ターンが切り替わった通知を取りこぼすと次に何かが起きるまで永久に始まらない**、という
    構造が原因と考えられる（根本原因は未特定）。
  - **保険を実装した**（main.jsの5秒ウォッチドッグに追加、`rescuePhaseIfNeverStarted`）:
    「自分の手番・自分に優先権・フェイズ未開始」が5秒続き、かつ何も表示中・処理中でなければ、
    `reconcilePhaseAutomation()` を呼び直す（`diag-phase-restart` を残す）。直らなければ5秒ごとに
    再試行する。**対局が完全に止まるのが一番まずい**ので、原因究明より先に自力復帰を入れた。
- **申し送り**: オンラインの自動対戦は、共有ブラウザ（既定）だと**前面以外のタブがブラウザ側で
  抑制される**ため停止に見えることがある。停止を疑う時は `--isolated`（人数分の別ブラウザ）で
  確認すること。今回の不具合は `--isolated` でも再現したので本物。
- **検証**: `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS。保険そのものが効いて決着まで
  到達できるかは、次に `--full --isolated` を回して確認する。

### 2026-09-05（続き408）：オンラインの停止「移動フェイズで候補が1つも無いと何もせず止まる」を特定・修正

続き407で入れた保険（フェイズ未開始の救済）では止まらなかった（3回目は turn 9 で停止、
`diag-phase-restart` は0件＝フェイズは始まっていた）。ログを読み直して**別の経路**だと分かった。

- **原因（特定）**: `performPriorityTimeoutAutoAction` のムーブフェイズ分岐は、移動先/接触相手の
  候補を**DOMのハイライト（`.phase-move-highlight` / `.phase-contact-highlight`）から集めている**。
  候補が0件だと `chosen` が undefined になり、**そのまま何もせず素通り**していた（`if (chosen)`
  の外に何も無い）。持ち時間が切れても誰も動かせず、対局が永久に止まる。
  - 通常は phase-automation.js の救済（`reconcileMovePhase` の「移動先も接触相手も無ければ、
    隣の1マスへ山札から1枚置いてターン終了」）が働くが、**置ける隣のマスすら無い**時
    （`emptyCells.length === 0`）は何もしないまま抜けるため、ここも素通りになる。
  - 実測: 停止時の状態は毎回同じ——手番のクライアントが `phase=move`・自分に優先権・
    `deadlineIn=-44s`・`diag-timeout-latch-retry` は10秒ごとに出ているのに何も起きない。
- **修正**: 候補が0件なら「ルール上、動けないならそのターンは終わり」として**必ず前へ進める**。
  phase-automation.js の救済処理を `location = null`（カードを置く部分だけ飛ばす）でも呼べるように
  分岐を足し、`endTurnBecauseNothingToDo(player)` として公開。main.js のムーブ分岐から呼ぶ
  （`diag-move-auto-noop` を残す）。優先権の引き継ぎ・ゲート侵攻の確認・NEXT_TURN は
  既存の救済処理と全く同じ経路を通るので、挙動の食い違いが生まれない。
- **あわせて自動ロックにも診断を追加**: `performLockPhaseClick` の早期return（`not-lockable` /
  `pending-final-lock` / `no-token`）を `diag-lock-click-skip` として残し、await していない
  自動ロックの失敗も `diag-lock-click-failed` で拾うようにした（turn 19 の停止では
  「ロック候補1枚あるのにロックされない」形だったため、次に再現した時に切り分けられる）。
- **検証**: `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS。オンラインの決着まで自動対戦で
  実際に最後まで行けるかは、この後もう一度回して確認する。

### 2026-09-05（続き409）：PWA案内の位置と文言／「作った部屋が一覧に出ない」の原因を実測で切り分け

- **「ホーム画面に追加」の案内がホームのタイルに重なる（ユーザー報告・スクリーンショット）**:
  `#pwa-install-banner` をホーム画面の流れの中（`margin-top` で下に置くつもり）に入れていたが、
  ホームは中央寄せのレイアウトなのでタイル群と重なっていた。ユーザー提案どおり
  **右上（オプションアイコン列のすぐ下）に固定**した（`position: fixed; top: calc(帯の高さ + 0.6rem);
  right: 1rem; z-index: 1600`）。実測: 1600x900で `[1280, 58, 304, 92]`・ホームのタイルとの重なり0件。
  - **PCでの文言も直した**（ユーザー報告「スマホではないのにホーム画面への追加推奨モーダルが
    出ています」）。デスクトップChromeも `beforeinstallprompt` を出すので**表示自体は正常**だが、
    PCで「ホーム画面に追加」と言われても意味が通らない。タッチ端末かどうかで
    「ホーム画面に追加」/「アプリとしてインストール」を出し分ける。
- **「フレンドリーマッチで部屋を作ったのに、もう一方のアカウントで一覧が空」の切り分け**:
  ヘッドレスの別ブラウザ2つ（別アカウント）で実際に試して、**アプリ側の不具合ではない**ことを
  確認した。
  - 普通の部屋 → **相手のアカウントからすぐ一覧に出る**（実測）。作成者がその部屋に居続ければ
    100秒後も残る（実測）。
  - **「🏆 ランク戦にする」にチェックを入れて作った部屋は、仕様として一覧に出ない**
    （`so7_games_list` は `not is_ranked`。部屋コードを共有して入る私的な部屋）。実測で確認。
  - もう1つの消え方も実測で確認: **作成者が別の部屋を作る/移ると、前の部屋の座席の
    `last_seen` が更新されなくなり、90秒で掃除されて部屋ごと消える**（`so7_cleanup_stale_rooms`）。
  - → 一番ありそうなのは「ランク戦にチェックが入っていた」。気づけるように、**部屋が0件の時の
    案内文に「ランク戦にした部屋はここに出ない・部屋コードで入る」を書き添えた**（ja/en）。
- **検証**: `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、CSSブレース平衡（2830）。
  PWAバナーはブラウザ実測（位置・重なり0件・PC文言）。部屋一覧は上記の実測。

### 2026-09-05（続き410）：「作った部屋が相手の一覧に出ない」を双方のログから特定できるようにした／PWA案内の文言が2文字だけ折り返される件を修正

ユーザー報告「『🏆 ランク戦にする』にチェックを入れてないのに出ません。…双方で不具合報告するので
そこであなたが状況が分かるようにログ出るようにしてください」＋「『すぐに開けます』の改行がダサいです」。
- **部屋の診断ログを4つ追加（[src/online.js](src/online.js)）**。続き409で「普通の部屋なら相手の一覧に出る」ことは
  2アカウントで実測できているので、次に再現した時に**推測せず**突き合わせられるようにした。
  - `diag-room-created` … 作った部屋の素性（gameId / ranked / パスワード有無 / 名前）。
    「ランク戦にチェックが入っていなかったか」が作った側のログで確定する。
  - `diag-room-seat-check` … **参加して座席ができたか**（`so7_game_seats` の件数）。ここが**今回いちばん
    効くはず**の記録——サーバー側の掃除 `so7_cleanup_stale_rooms` は「開いている部屋のうち座席が1つも
    無いもの」を**即座に**消すため、`createRoom` の後の `joinRoom` が失敗して座席ができていないと、
    相手が一覧を開いた瞬間（一覧取得の前に必ず掃除が走る）にその部屋が消える＝「作ったのに出ない」に
    なる。seats:0 が残っていれば一発で分かる。
  - `diag-room-list` … 見る側の記録（件数・部屋idの先頭10件・ログイン状態・掃除の失敗理由）。
    0件だった時に「そもそも取得できていないのか／本当に0件なのか」を判定できる。
  - `diag-room-heartbeat` … 座席の生存報告が実際に飛んでいるか（60秒に1回だけ・`document.hidden` 付き）。
    ロビーの座席は**90秒**更新が途絶えると掃除されるが、ブラウザは**裏に回ったタブのタイマーを間引く**ため、
    「部屋を作ってから別のブラウザ／別のウィンドウへ移った」時に届いていない恐れがある。
- **あわせてハートビートを堅くした**: 画面が表に戻った瞬間にも1回送る（間引かれていた分の窓を狭める）。
  実装上の注意として、`visibilitychange` のハンドラは**その時の最新の送信関数**を経由して呼ぶ
  （`currentHeartbeatBeat`）——ハンドラを1回しか登録しない作りなので、素直に書くと部屋を移った後も
  **古い部屋のIDへ送り続ける**。宣言は全て使用箇所より前に置いた（このプロジェクトで何度も踏んでいるTDZ事故の予防）。
- **PWA案内の折り返し**: 案内文とボタンが同じ行を取り合っていたため、日本語が「すぐに開け／ます」のように
  2文字だけ次の行へこぼれていた。**案内文に1行まるごと使わせ**（`.pwa-install-text { flex: 1 0 100% }`）、
  文言自体も短くした（「アプリとしてインストールすると、すぐに開けます」→「アプリとして入れると、すぐ開けます」、
  「ホーム画面に追加すると、アプリのようにすぐ遊べます」→「ホーム画面に追加すると、すぐ遊べます」。英語も同様）。
  幅も 19rem→21rem。**実測**: 1600x900 で案内文は1行（幅218px）・バーは右上(1248,58,336x73)・ホームのタイルとの重なり0件。
- **検証**: `node --input-type=module --check`（online.js/ui-text.js）通過・CSSブレース平衡。`npm test` 56/56 PASS、
  `node test/smoke.mjs 2` PASS。サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Functionの再デプロイは不要。

### 2026-09-05（続き411）：#242/#243「参加できる部屋が空」「ロビーが白紙」の真因＝英語化の一括置換が壊した17箇所（オンラインの部屋UIが約1週間まるごと死んでいた）

続き410で入れた診断ログが、その日のうちに効いた。**双方の報告を突き合わせたら、サーバー側は完全に正常**だった:
- 作った側（YGM）: `diag-room-created {gameId:"ICJZJ7", ranked:false}` → `diag-room-seat-check {seats:1}` → `diag-room-heartbeat {hidden:false}`
- 見た側（SHO）: `diag-room-list {count:2, ids:["ICJZJ7","BGN4JL"]}` ＝**その部屋はちゃんと返ってきていた**
- そして**同じミリ秒**に `UNHANDLED_REJECTION: Cannot create property 'textContent' on string ''`

つまり「部屋が消えていた」のではなく、**一覧を描く側が例外で死んで空になっていた**。
- **真因**: UI英語化フェーズ11（`50359c8`、続き327）の一括置換が、`online-ui.js` の**行ごと差し替えの際に周りのコードを壊していた**。
  置換前の実装（`git show 50359c8^`）と突き合わせて、**17箇所**が別物になっていた——
  `countEl`→`metaEl` / `waiting`→`waitEl` / `codeHint`→`codeEl` / `label`→`btn` / `info`→`btn` /
  `listStatus`・`specStatus`・`alert(...)`→`status` / `readyText`・`tooManyText`→`rankedNote`、
  さらに **`const startBtn = textButton(...)` が3箇所・`const resumeBtn = textButton(...)` が1箇所、宣言ごと消えていた**。
  - エラー文が「文字列に textContent を作れない」だったのは、`status` がどこにも宣言されておらず
    **ブラウザの `window.status`（値は空文字列）に解決していた**ため。一覧の描画が投げた例外を catch した先で
    その `status.textContent = …` を踏み、未処理の Promise 拒否として表に出ていた。
  - 影響範囲は「部屋一覧」「観戦一覧」「進行中の対局の再開」「対戦ロビー」＝**オンラインの入口ほぼ全て**。
    2026-08-29 のフェーズ11以降ずっと壊れていたが、`node --check` も `npm test` もスモークも素通りするため
    誰も気づけなかった（`test/online-smoke.mjs` は `online.js` を直接呼ぶのでUIを通らない）。
- **同じ形をもう3件、別ファイルで発見・修正**（いずれもフェーズ10 `956d639`）:
  `my-deck-list.js` の `label`→`note`、`my-deck-builder.js` の `entry.setAttribute(...)`→`tile.title`、
  `my-page.js` の `statusEl`→`status`。
- **常設の検査を追加（[tools/check-undeclared.mjs](tools/check-undeclared.mjs)）**: 「宣言されていない変数・別の関数の変数への代入」を
  全 src から静的に洗う。トップレベル関数ごとにブロックを切り出し、その中＋モジュール直下の宣言だけを
  「見える名前」として突き合わせる。`node tools/check-undeclared.mjs` で実行（問題があれば終了コード1）。
  **この事故はこれで4回目**（続き325 t()のimport漏れ／続き342 tを隠すローカル変数／続き347 存在しないaddRow／今回）なので、
  使い捨てスクリプトではなく tools/ に常設した。実行結果: 159ファイル・0件。
- **`renderRoomStatus` は死んだコードだった**（ロビー刷新で表示経路を失っていた）。壊れていた4箇所は直したうえで、
  その旨をコメントに明記した。あわせて `oui.roomHeader` が座席を必ず出す形だったのを直し（ロビー中は `mySeat` が null で
  「（座席null）」になる）、`oui.roomHeaderNoSeat` を追加した。
- **検証**: 実アプリをヘッドレスでゲストログインさせ、**本番の部屋一覧が実際に描画される**ことを確認
  （「セブンの部屋-2（1人）」の行が出る・エラー0件）。部屋を作って対戦ロビーを開き、
  「🌐 対戦ロビー／参加人数: 1人／ターンタイマー ON…／あと1人以上でゲーム開始できます／この部屋を離れる」が
  正しく出ることも実測（`oui.L312` の分岐も復活）。`npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS。
- **申し送り**: `test/online-smoke.mjs` は `online.js` を直接叩くため、この種の**UIの壊れを検出できない**。
  部屋一覧・ロビーを画面から操作する経路を1つ足しておくと、同じ見落としを防げる。

### 2026-09-05（続き412）：#244 盤面カードの色味が端末で違う（WebGLキャンバスがステージ倍率を勘定に入れず引き伸ばされていた）

ユーザー報告「YGMの方と私の方で盤面のカードの裏面の色味が違う。私の方がダークグレイ、YGMの方がライトグレイ。
YGMの方が実際の色味に近い」。同じ対局を2人で見ているのに見え方が違う＝端末に依る要因を探した。
- **原因（実測で確定）**: `board-3d.js` がキャンバスの解像度を `devicePixelRatio` だけで決めていた。
  このアプリは **1600x900 の「ステージ」を body ごと拡大／縮小して画面に合わせる**（`applyViewportStage`）ので、
  大きな画面では盤面が実寸より大きく引き伸ばされて表示される。CSSの描画は最終的な倍率で描き直されるので
  問題にならないが、**WebGLのキャンバスは固定サイズのビットマップ**なので、そのまま引き伸ばされてぼやける。
  実測（報告者2人の環境を再現）:

  | | 盤面のCSSサイズ | 画面上の大きさ | キャンバス | 実質 |
  |---|---|---|---|---|
  | SHO（2560x1276・dpr0.75） | 1600x900 | 2268x1276（**1.42倍**） | **1200x675** | 約1.9倍に拡大 |
  | YGM（1600x867・dpr1.2） | 1600x900 | 1541x867（0.96倍） | 1920x1080 | ほぼ等倍 |

  カード裏は細かい模様の入った明るいグレーなので、拡大されるほど模様がつぶれて**一様な濃いグレー**に見える。
  「YGMの方が実際の色味に近い」という報告とも一致する（あちらは1:1で描けていた）。
- **修正**: 「画面上の大きさ ÷ レイアウト上の大きさ」＝ステージ倍率を `getBoundingClientRect()` から求めて
  `devicePixelRatio` に掛ける。上限2倍はiOSのメモリ対策としてそのまま。実測で**4通りとも 1:1** になった:
  SHO相当 1200x675→**1701x956**／YGM相当 1920x1080→1849x1040（画面と完全一致・わずかに軽くなる）／
  小さい窓 1024x576／4K 3200x1800（上限2で0.9倍）。
  - **スマホはむしろ軽くなる**: dpr3・ステージ倍率0.58 の端末なら 3→1.75 で、従来の上限2より少ないピクセル数になる。
    大きな画面だけ増えるが、それは実際にその大きさで表示されているぶんなので妥当。
- **`diag-board3d` に `stage`（ステージ倍率）と `onScreen`（画面上の実ピクセル）を追加**した。
  次に「片方だけ見え方が違う」と言われた時、キャンバスと表示サイズの比を推測せずに読める。
- **検証の限界（記録）**: ヘッドレスのスクリーンショットには**WebGLの描画が写らない**
  （`preserveDrawingBuffer` が既定オフのため、撮影時にはバッファが空）。見た目そのものの確認は実機に頼るしかない。
  今回は「キャンバスの実ピクセル数 ÷ 画面上の実ピクセル数」を4条件で測って1:1になったことをもって確認とした。
- **検証**: `node --input-type=module --check`（board-3d.js）通過、`node tools/check-undeclared.mjs` 0件、
  `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS。サーバー側（Supabase）の変更は無い。

### 2026-09-05（続き413）：#245 山の側面が消える／手番の駒のグローがWebGLの板に隠れる（キャンバスが盤面DOMより手前にある構造の問題）／#246 色味は三次元描画のせいではないと実測

- **#245「山札の側面がなくなっちゃってます」（原因確定・修正）**: 山の側面（`.stack-front/back/left/right`）は
  **画像を持たず、CSSの色と inset の影だけ**で描いている。ところがWebGL描画中の「DOMの塗りを剥がす」対象
  （`body.board-3d-on` の `background-image/color: none`）にこの4クラスまで入れていたため、**代わりに描かれる
  ものが無く側面がまるごと消えていた**（WebGL側は「インラインの background-image がある要素」しか板を作らない）。
  剥がす対象からも `PAINT_SELECTOR` からも外し、従来どおりDOMで描かせる（4つの山×4面＝16要素だけなので、
  合成レイヤーを減らす目的にはほとんど影響しない）。山の**上面**だけは実際のカード画像を持つのでWebGLのまま。
  実測: `.stack-front` の背景色が `rgb(203,213,225)` に戻り、陰影の inset 影も残ることを確認。
- **「自ターン時の駒のカラーEFFECTがしっかり光っていない」（原因確定・修正）**: これは駒だけの問題ではなく、
  **キャンバスが盤面のDOMより手前（z-index:5）にある**という構造から来る。DOMの box-shadow で描いている光は
  必ずキャンバスの裏側になるので、WebGLが描くカード・マスの板に覆われ、**マスの隙間にしか見えない**
  ＝「なんとなく光っている」状態になる。
  - **対処**: 光っている要素と同じ位置・同じ大きさの**空の箱**を、キャンバスより手前の層
    （`#board-3d-glow-layer`、z-index:6）に置き、既存のCSSアニメーションをそこで再生する
    （`syncGlowLayer()`。毎フレーム同期するのでホバーの付け外しにも即座に追随する）。WebGL側で光を
    描き直すより確実で、「常時光る演出をやめる」設定もそのまま効く。位置は駒の**上面**（`.piece-top`）に
    合わせる（`.piece` 自身の箱は translateZ で持ち上げる前の位置なので、そこに合わせると下にずれる）。
  - 実測: 手番の駒1つに対し逃がし先が1つ作られ、位置・大きさが**誤差0px**で一致、アニメーション
    `piece-own-turn-glow` が動き、駒の色（`--piece-turn-glow-color: #915da3`）も引き継ぐ。例外0件。
  - **同じ理由で隠れている演出が他にもある（申し送り）**: ロック中でも使えるカードの周回する光
    （`.board-card.is-usable-while-locked.effect-orbit::before`）はカードの板の裏なので見えない。
    到達バースト・ロックの刻印など、盤面のDOMに載せている一時的な演出も同様。同じ逃がし層に
    載せれば直せるが、今回は報告のあった駒のグローに絞った。
- **#246「色味は基本まだ暗いグレーのまま」（三次元描画のせいではない、と実測で確認）**: 続き412の解像度修正は
  実機に届いている（`diag-board3d` が `stage:1.42 / canvas:1704x958 / onScreen:2272x1278` ＝ 1704 = 2272×0.75 で
  ちょうど等倍）。そのうえで、**three.js のテクスチャ処理そのものが色を暗くしていないか**を切り分けた——
  同じカード裏を90pxで描いた平均色は **素の2Dキャンバス rgb(219,217,213) / three.js（ミップマップあり）
  rgb(221,219,216) / ミップマップなし rgb(221,219,216)** で、**three.js の方がむしろわずかに明るく、
  ミップマップの有無でも差が無い**。つまり sRGB のミップマップ生成による暗化でも、色空間の設定漏れでもない。
  - 残る可能性は**表示側の色管理**（HDR有効時のSDR合成、ディスプレイのプロファイル）で、これは手元では
    再現できない。ユーザーに **基本設定→「動きが重い・カクつくとき」→「盤面をWebGLで描く」をOFFにして
    見比べてもらう**のが決定的（色が変わるならWebGL側、変わらないなら元の画像がその環境でそう見えている）。
  - 「たまに一瞬明るいグレーに見える」は、読み込み直後にDOMが先に描き（明るい）→WebGLが引き継ぐ（暗い）
    という順番と一致するので、上のA/Bで裏が取れるはず。
- **質問への回答**: 駒の立方体は**現在three.jsで描いている**（5面それぞれが `.piece-face` で、インラインの
  スキン画像を持つのでWebGLの板になる）。当たり判定と位置決めは今までどおりDOM。
- **検証**: `node --input-type=module --check`（board-3d.js）通過・CSSブレース平衡・`node tools/check-undeclared.mjs` 0件、
  `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS。サーバー側（Supabase）の変更は無い。

### 2026-09-05（続き414）：#247 管理者モードのWebGLチェックが「既定ONなのにOFFに見える」（映すものを間違えていた）／影の切り分け用にログを追加

- **#247「『盤面の絵をWebGLで描く』が最初オフになってました。既定でオンにしましたよね？？」（原因確定・修正）**:
  設定はONのままだった（`diag-board3d` が起動直後から出ている＝WebGLは動いていた）。管理者モードの
  チェックが `getBoard3dStats().active`＝**「今まさに描画中か」**を映していたため、盤面が組まれる前の
  一瞬に開くとOFFに見えていた。**しかもそれを見て押し直すと、設定が本当にOFFで保存される**
  （`setBoard3dEnabled` は続き406から localStorage に書くようになっている）ので、実害のある見せ方だった。
  チェックは `isBoard3dEnabled()`＝**保存されている設定**を映すようにし、実際に描けているかは下の説明文へ。
  開いている間は1秒ごとに更新する（描き始めるまでの一瞬も追いかける）。描画の開始に失敗した時だけ
  設定もOFFに戻す。実測: 設定を切り替えると1.4秒以内にチェックが追随（true→false→true）・エラー0件。
  - **教訓**: 設定のチェックボックスは「設定値」を映す。「その設定の結果いま何が起きているか」を映すと、
    起動途中・失敗中・遅延中に**嘘の状態**を見せ、ユーザーがそれを直そうとして本当に設定を壊す。
- **#247「カードにドロップシャドウのようなものがある。もう一つのアカウントではならない」（未解決・材料を追加）**:
  ユーザーは「#238a と全く同じ症状」と言っている（＝移動できるマスを選んでいる間だけ、暗転の膜のせいで
  カードが浮いて見える）。手元では再現できなかった——移動フェイズの暗転を作って WebGL ON/OFF で比べたが、
  カード中央226 / マス全体227（ON）と 226/226（OFF）で**差が出なかった**。盤面内で box-shadow を持つ要素も
  洗ったが、`.board-card` は `none`（strip が効いている）で、残るのは `.piece`（DOMの影・strip対象外）と
  山の側面の inset 影だけ。`diag-board3d` に **`tinted`（膜の色を混ぜた板の枚数）と `dim`（暗転の膜が
  出ている状況か＝move / +effect）** を追加したので、次の報告からはログで判定できる。スクリーンショットも依頼した。
- **「アカウントで症状が変わるのはなぜか」への回答（コード変更なし）**: アカウントの違いではなく
  **ウィンドウの大きさ＝盤面の引き伸ばし倍率**の違い。SHO（ブラウザ 2560x1276・dpr0.75・ステージ1.42倍）と
  YGM（アプリ 1600x867・dpr1.2・0.89〜0.96倍）。#244 の色味の違いもこれが原因だった。
  「アプリかブラウザか」自体はほぼ無関係（同じChromeのエンジン）。ただし続き403から14件の設定が
  アカウント同期されるので、そちらの差が出る可能性は理論上ある（今回のログでは該当なし）。
- **設計の相談への回答（コード変更なし・申し送り）**:
  - **山の側面をWebGLへ移すか** → 今はDOMのままでよい。16要素しかなく軽量化に効かない／側面は画像を持たず
    「色＋内側の陰影」だけで、今の「形の板」は角丸の枠＋背景色＋外向きの光しか描けないため新規処理が要る。
    ただし**上面WebGL・側面DOMの混在は前後関係の事故を生みやすい**（キャンバスが盤面DOMより手前）ので、
    将来 山を傾ける・演出で動かすなら、その時は側面も揃えること。
  - **演出をWebGLへ移すか** → 移す価値は大きい（立方体の形に沿った光・床に落ちる光・加算合成・粒子は
    CSSでは原理的に出せない）。ただし順番として ①まず「隠れて見えない」演出を全部逃がし層に載せる
    （周回の光・到達バースト・ロックの刻印）→ ②手番の駒のグローだけWebGLの本格的な光にして見比べる →
    ③良ければ他も移す、を提案した。いきなり全部移すと「見えるようになったが前より変」の切り分けができない。
- **検証**: `node --input-type=module --check`（admin.js/board-3d.js）通過、`node tools/check-undeclared.mjs` 0件、
  `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS。サーバー側（Supabase）の変更は無い。

### 2026-09-05（続き415）：#251 ゲートのカードが黄色く透ける＝描画順の並べ替えが浮動小数の厳密比較だった／#250 返事待ちモーダルの背後を盤面がタップしてしまう（総点検）／色味の差はディスプレイ違いと判明

- **#251「ゲートに置かれたカードの裏面が黄色っぽく透ける」（原因確定・修正）**: `sortByDepth` の並べ替えが
  `a.z !== b.z ? a.z - b.z : a.i - b.i` ＝ **Zの厳密比較**だった。マスとその中のカードは同じ平面にあるが、
  行列を積み上げた結果のZには必ず 1e-9 程度の誤差が乗るため**毎回「別の高さ」と判定され、
  「同じ高さならDOMの並び順で」という意図した分岐に一度も入っていなかった**（＝重なり順が誤差で決まっていた）。
  実測: ゲートのマスの板が描画順100・カードが85でマスが上。`.cell.is-gate` は
  `background: rgba(250,204,21,0.12)` の黄色を持つので、その膜がカード全面に被っていた。
  0.5px 以内は「同じ高さ」とみなすようにして解決（駒は translateZ で数px以上持ち上がっているので #241 の
  「駒がカードの下に隠れる」修正はそのまま維持される）。**実測: 49マス全部でマス<カード・駒がカードより奥0件**。
  - 検証用に `debugMeshInfo(el)`（絵の板／形の板の 描画順・色・不透明度・DOM順）を追加した。
  - **教訓**: 3Dの重なり順で「同じ高さならDOM順」をやりたい時、Zの厳密比較は**必ず**外れる。
    許容幅（このプロジェクトでは0.5px）を持たせること。
- **#250「接触するを押したら背後のマスも選択されてマス確認モーダルが出た」（修正・総点検）**: 盤面のタップ判定は
  3D階層のため `elementsFromPoint` で自前に行っており、**返事待ちモーダルの背景を透かして下の盤面を触る**。
  ドラッグ開始側は #225 で塞いだが、**capture フェーズのタップ経路が残っていた**（移動先のタップ・アバターの
  メニュー・カード効果の候補選択）。盤面処理の入口1か所に「見えている `.so7-modal-backdrop` があれば何もしない」
  ガードを入れた。**preventDefault はしない**——握り潰すとモーダルのボタンまで効かなくなる（続き76の教訓）。
  - **【重要・危うく壊すところだった】最初は `document.querySelector(".so7-modal-backdrop")` の存在で判定したが、
    山札一覧・マイページ・ヘルプ・行動ログ・優先権譲渡など**7個の背景は作りっぱなしで display の切り替えだけ**で
    見せ隠ししており、閉じている間もDOMに残る（実測: 起動直後から常に7個）。存在で判定すると**盤面が永久に
    触れなくなる**。続き396とまったく同じ罠。`getClientRects().length > 0` で実際に出ているものだけを見る。**
  - 実測: 通常のタップ→マス確認が出る(true)／見えている背景がある時→出ない(false)／常駐の7個では止まらない。
- **#248/#249「盤面カードの色味が2台で違う」= ディスプレイ違い（アプリの不具合ではない）**: ユーザーから
  「PCは一緒だがディスプレイは違った」と訂正があり、測定とも一致した——**WebGLではないDOMの床の明るさも
  同じだけ差があった**（178 vs 169。WebGLが原因なら床は一致するはず）。カード裏画像の透明度も測って
  完全に不透明（半透明の点0個）であることを確認済み。同じディスプレイでの見比べを依頼した。
- **検証**: `node --input-type=module --check`（main.js/board-3d.js）通過、`node tools/check-undeclared.mjs` 0件、
  `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS。サーバー側（Supabase）の変更は無い。
- **次にやること（ユーザー承認済み）**: 演出のWebGL化の第1段＝「隠れて見えない演出を逃がし層に載せる」
  （ロック中カードの周回の光・到達バースト・ロックの刻印）。

### 2026-09-05（続き416）：WebGL描画で隠れていた演出を、キャンバスの手前へ逃がす層に集約（[src/board-3d-overlay.js](src/board-3d-overlay.js) 新規）

ユーザー承認済みの「演出のWebGL化・順番1」＝**まず「隠れて見えない」を全部なくす**（確実な改善・低リスク）。
- **背景**: キャンバスは盤面のDOMより**手前**（z-index:5）にある（1枚のキャンバスはDOMの重なり順の途中に
  割り込めないため、手前に来るべきものだけをWebGLへ移す設計）。その結果、盤面のDOMに載せている演出は
  必ずキャンバスの裏になり、WebGLが描くカード・マスの板に覆われて見えない。続き413では駒のグローだけを
  逃がしたが、周回する光・到達バースト・ロックの刻印が残っていた。
- **[src/board-3d-overlay.js](src/board-3d-overlay.js)（新規・依存ゼロの葉モジュール）に逃がし層を集約**した。board-3d.js は three.js を
  700KB弱**静的import**しているので、main.js からこの層を使うためにあちらを静的importすると、起動しただけで
  three.js を読み込むことになる（board-3d-setting.js を作ったのと同じ理由）。
  - `mountOverlayEffect(hostEl, effectEl, ttlMs)` … 一時的な演出を、元の置き場所と同じ位置・大きさで手前へ出す。
  - `syncOverlayMirrors(sources)` … 常時出ている演出の「空の箱」を毎フレーム合わせ直す。
  - `sceneLocalBox(el)` … **実画面座標をそのまま left/top に入れてはいけない**（ステージ変形が二重に掛かる。
    続き351で勝利演出が丸ごとズレた原因）。「画面上の大きさ ÷ レイアウト上の大きさ」で割り戻す。
- **一時的な演出は `appendEffectHost` の1か所を通っていた**（到達バースト・ロックの刻印・ロックの収束・
  柱状オーラの4経路とも）。そこで分岐するだけで全部まとめて表に出せた。
  - **見た目の変化（意図的）**: 逃がし層は3Dの外なので、演出に盤面の傾きも祖先の180度回転も掛からず、
    常に画面の上向きに描かれる。これらの演出は元々「常に画面の上方向に伸びる」設計で、奥(C)・右(D)の
    ロックエリアの180度回転を打ち消す `.effect-side-flip` まで用意されていたので、むしろ意図通りになる。
    逃がし先ではその入れ子は付けない。
- **ロック中でも使えるカードの光**（`.board-card.is-usable-while-locked` の ::before/::after）は、
  ミラー用のクラスを既存ルールのセレクタに**並べて書く**形にした（同じ見た目を二重に書かない）。
- **検証**（ブラウザ実測）: 逃がしたミラー3件（手番の駒＋使えるカード2枚）／周回の光 `usable-locked-orbit` が
  動き色も引き継ぐ（桃 rgb(241,158,194)・橙 rgb(238,120,31)）／一時演出の位置が元のマスと **dx=0・dy=0・dw=0**／
  「常時光る演出をやめる」ONで全部 `none`／WebGL描画OFFで層ごと片付き `isBoardOverlayActive()` が false
  （＝従来どおりDOMへ入る）／再度ONで復活／例外0件。
- **検証**: `node tools/check-undeclared.mjs` 0件、CSSブレース平衡、`npm test` 56/56 PASS、
  `node test/smoke.mjs 2` PASS。サーバー側（Supabase）の変更は無い。
- **次（順番2）**: 手番の駒のグローだけをWebGLの本格的な光（立方体の形に沿う・床に落ちる・加算合成）にして、
  今の平らな光と見比べる。良ければ他も移す。

### 2026-09-05（続き417）：#253 ロックエリアの色枠が点滅する＝DOMを作り直すとWebGL側の「形」キャッシュが空になっていた／戦績システムの「直近のアクセス」にプレイヤー名

- **#253「たまに処理中にロックエリアのカラー枠が全部消えたりついたりします」（原因確定・修正）**:
  マスとロックスロットの「形」（角丸の枠＋背景色）は、読むのにスタイルの全再計算が2回かかるため
  **400msに1回だけ読み直す**キャッシュにしてある。ところが `render()` は盤面のDOMを毎回作り直すので、
  その直後はキャッシュの要素が**全部 isConnected:false**になる。以前はそれを消すだけで次の読み直しまで
  待っていたため、**最大400msのあいだ枠が丸ごと0枚**になっていた。
  **ユーザーのログにそのまま残っていた**——`{"quads":59,"shapes":0}` `{"quads":80,"shapes":77}` のように、
  絵の板はあるのに形の板だけ0。続き402で `diag-board3d` にこの数値を出しておいたのが効いた。
  - **修正**: 前回読んだ時に見た要素を覚えておき（塗りが無くて板を作らなかったものも含む）、
    **見覚えのない要素が1つでもあれば＝DOMが作り直された**とみなして間隔を待たずに読み直す。
    実測: 30秒・307フレームを監視して**枠が0枚になったフレーム0件**（最小127枚）。
  - **教訓**: 「要素をキーにしたキャッシュ」は、DOMを作り直す描画方式とは相性が悪い。
    要素が入れ替わったことを検知して作り直す仕組みを必ず用意する。
- **戦績システム（姉妹リポジトリ、push済み `a29156d`）: 管理者コンソールの「直近のアクセス」に**
  **プレイヤー名を出す**（ユーザー要望）。`page_visits` は端末ごとのランダムな `visitor_id` しか
  持っておらず「誰が見に来たか」が分からなかった。ログイン中なら `user_id` も記録し、表示時に
  `players.user_id`（誰でも読める）と突き合わせて名前を出す。未ログインは「ゲスト（未ログイン）」、
  アカウントはあるが戦績プレイヤー未連携なら「ログイン中（プレイヤー未連携）」。
  - **列が無い環境でも壊れない**ようにしてある（書き込みは `user_id` 付きで失敗したら外して再試行、
    読み取りも同様）。SQLを実行するまでは今までどおり日時だけが並ぶ。
  - 名前は本人が自由に入力するので、HTMLへ差し込む前に無害化する（`escapeHtml` を新設）。
  - 実測: 実データで44人中16人がアカウント連携済み、アカウントから名前を引けること、無害化、例外0件。
  - **ユーザー作業**: 姉妹リポジトリの `supabase_setup.sql` 末尾
    「追加(2026-09-05): 管理者コンソールの『直近のアクセス』に…」の1行をSQL Editorで実行する。
- **設計相談への回答（コード変更なし）**「three.js化を進めれば治るのでは？DOMとの混在が不具合を生んでいる？」:
  今日の3件を分けると——#251（ゲートが黄色く透ける）は**混在のせいではない**（WebGL側の並べ替えの
  書き方の誤りで、全部three.jsでも同じ）／#250・#252（背後を押してしまう）は**CSS 3Dのせい**で、
  ブラウザの当たり判定が信用できず `elementsFromPoint` で自前判定していることが根っこ（WebGL化の前から
  ある問題）／#253 は**混在のせい**（DOMを作り直すとキャッシュが空になる）。
  - 当たり判定まで three.js のレイキャストに変えれば押し間違いの系統は根絶できるが、カード面の文字・
    ルビ・多言語・自動フィットが全部HTMLなので作り直しが大きい。
  - **提案した中間案**: プレイマットと床もWebGLへ移し、**キャンバスを盤面DOMの一番下へ置く**。そうすると
    光る演出・枠・刻印はDOMのまま自然に手前になり（逃がし層が不要になる）、重なり順の事故が構造的に
    起きなくなる。順番2（駒のグローをWebGLの光にする）より先にやる価値がある、と伝えた。
- **未対応（次）**: #252（手札を押したのに後ろのファーストカードが使われた。#250と同系だがモーダルは
  出ていないので別経路）、#254（烙印ドローの選択中に勝手にムーブフェイズへ）。
- **検証**: `node --input-type=module --check`（board-3d.js）通過、`node tools/check-undeclared.mjs` 0件、
  `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS。デジタル版のサーバー側の変更は無い。

### 2026-09-05（続き418）：#258/#257 盤面が見えない・iPhoneでチカチカ再発 → 第3段（キャンバスを盤面DOMの奥へ）を取り消した／SQLが「戻り値の型は変えられない」で止まる問題を修正

デプロイ直後に2機種から報告。**どちらも直前のコミット `d984c4e`（盤面のWebGL描画・第3段）が原因**と判断し、
`git revert` で丸ごと取り消した（`903f279`）。第3段は**プレイヤーから見た利点がほぼ無い**変更
（ON/OFFの見た目の差1.17%＝内部の重なり順を整理するためのもの）だったので、原因を突き止めてから直すのではなく、
**先に戻して配信し、原因はその後で調べる**判断をした。

- **#258（SHO・Windows Chrome・2560x1278 / dpr0.75 / ステージ倍率1.42）「完全にいろいろ見えなくなりました。
  スマホは大丈夫みたいです」**: スクリーンショットでは、盤面の中まで一面が**床のテクスチャ**で覆われ、
  プレイマット・49マス・カードが見えない（DOMのまま残っている `.lock-area-bar` だけが手前に見えている）。
  一方でアクションログの `diag-board3d` は `quads:194 / shapes:128 / tinted:148` と**正常な枚数**を出しており、
  カードの板はWebGL側に確かに存在していた。＝**作れてはいるが、画面に出ていない／何かに覆われている**。
  **正確な仕組みは未確認**（配信を止めないことを優先し、原因究明より先に取り消した）。第3段で変えたのは
  (a) 画面より大きい床（4.75倍）とプレイマットをWebGLへ移したこと (b) キャンバスを盤面DOMより奥へ動かしたこと
  の2点なので、疑わしいのは「床の板が、こちらの深さ順の並べ替えで盤面より後に描かれて全部覆った」か、
  「奥へ動かしたキャンバス（傾いていない平面）と、42度傾いた盤面の面との前後関係が環境によって崩れた」のどちらか。
  後者ならステージ倍率で結果が変わるので、PC（倍率1.42で全滅）とスマホ（0.48で無事）の差と辻褄が合う。
  ＝**「1枚のキャンバスに全部描く」ようにした瞬間、CSSの z-index による重なりが効かなくなり、
  こちらの深さ順の並べ替えだけが頼りになる**という設計上のリスクが表に出た形。
- **#257（YGM・iPhone iOS18.7）「ロックエリアバーがチカチカするやつになっちゃった」**: コンソールに
  `THREE.WebGLRenderer: Context Lost` → 続き398で入れた保険が働いて**CSS描画へ自動で戻り**、その結果
  以前のチカチカが再発していた（＝WebGLが落ちたことの二次症状で、チカチカ自体の再発ではない）。
  第3段でプレイマット（2048px級）と床の大きなテクスチャが増えたことがGPUメモリの一押しになったと考えられる
  （報告時の `weight` は imgMB:46 / canvasMB:11、キャンバスは 2293x1290）。
- **教訓**: 盤面の描画をWebGLへ移す時は、**「1枚でも多く移す」ことを目的にしない**。移した瞬間にその要素は
  CSSの重なり順から外れ、こちらの深さ計算の正しさに命運を預けることになる。特に
  **画面より大きい板（床・プレイマット）は、1回の並べ替えミスで盤面全体を隠す**ので割に合わない。
  第2段まで（カード・駒・山の上面・マスとロックの枠）が、いまのところ得と損の釣り合う範囲。
- **SQLが途中で止まる問題（ユーザー報告 `ERROR: 42P13: cannot change return type of existing function`）**:
  `supabase_setup_so7.sql` には `so7_get_admin_user_list` / `so7_get_admin_visit_log` の定義が**5回ずつ**あり、
  最後（続き405）だけ `returns jsonb`、それ以前は `returns table (...)`。既に新しい版が入っているDBに
  ファイル全体を貼ると、**前半の古い定義に差しかかった時点で42P13が出て、そこから後ろが全部実行されない**。
  古い方4箇所の直前にも `drop function if exists` を入れて、**全文コピペで最後まで通る**ようにした。
  なお今回ユーザーが実行したのは戦績システム側ではなくこのファイル。**戦績システムの
  `supabase_setup.sql` には関数定義が1つも無く（テーブル・列・ポリシーのみ）、全文コピペで安全**なのは変わらない。
- **検証**: `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、`node tools/check-undeclared.mjs` 0件、
  取り消し後に `PAINT_SELECTOR` が3種へ戻り・キャンバスが `z-index:5`／`appendChild`（＝盤面DOMより手前）へ戻り・
  逃がし層（[src/board-3d-overlay.js](src/board-3d-overlay.js)）が復活していることを確認。
  お知らせ（changelog.js）にも1項目追記（日英）。サーバー側（Supabase）のコード変更は無い。

### 2026-09-05（続き419）：#259 iPhoneで「ロックエリアバーだけ」チカチカが残る → バーの画像もWebGLで描くようにした

続き418の取り消し後（版 20260905-103739）にユーザーから「まだロックエリアバーがチカチカしてます」。
**今回のログには `Context Lost` が無く、`diag-board3d` は `quads:192 / shapes:128` と正常**＝WebGL描画は
生きたまま、その状態でバーだけがチカチカしている。つまり #257 とは原因が違う。

- **原因（強い状況証拠）**: `.lock-area-bar-image`（盤面とロックエリアの間の装飾バー・4辺で4枚）は
  **背景画像を CSS の `url()` で指定していた**ため、WebGL描画の対象から漏れていた——盤面のレンダラーは
  **インラインstyleの background-image しか読まない**（続き398の罠1。3D中はCSS側が none に上書きするので
  computed からは読めない、という理由でそう作ってある）。結果、WebGL描画がONの盤面の中で
  **唯一「DOMが絵を描いたまま」残っていた要素がこのバー**だった。iPhoneでは以前は盤面全体がチカチカし、
  WebGL化でそれが止まった経緯（続き400のユーザー報告「チカチカ直りました！！！」）と合わせると、
  **DOMが絵を描いている要素だけが今もチカチカする**と読める。
- **修正**: 画像の指定を `style.css` から外し、`buildLockAreaBar` が**インラインstyleで敷く**ようにして
  （＝指定元を1か所に集約）、`PAINT_SELECTOR` と「3D中に剥がす」対象の両方に `.lock-area-bar-image` を
  追加した。板は4枚・テクスチャは1024x45（展開0.2MB）増えるだけ。
- **続き418の教訓（画面より大きい板は移さない）との線引き**: あちらで問題になったのは
  **床（4.75倍）とプレイマット**＝1回の並べ替えミスで盤面全体を隠す大きさのもの。今回のバーは
  盤面の平面に収まる薄い帯で、カード・駒と同じ「第2段の範囲」。ただし**見た目は必ず画像で確かめる**という
  今回の教訓は守り、下記の通り実際の対局盤面で撮って確認した。
- **検証**: ①バー4枚とも `computedBg: none`（DOMは描かない）＋インラインに画像が入る ②WebGL側に
  画像の板ができる（`debugMeshInfo` で `visible:true`・板 100→104枚） ③**実際にCPU戦をセットアップした
  盤面**を撮り、WebGL ON で 赤橙黄緑青桃紫の帯が正しい位置・正しい色で描かれ、**その上のロックスロットを
  隠していない**ことを目視確認 ④WebGL OFF に戻すと従来どおりCSSが描く（`computedBg` に url が戻る）
  ⑤例外0件。`npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、`node tools/check-undeclared.mjs` 0件、
  CSSブレース平衡（2840）。
- **実機での確認をお願いしたい**（iOSのチカチカは手元で再現できないため）。**直らなかった場合の
  当座の逃げ道**として、基本設定 →「画面の見え方」→「ロックエリアバーを表示する」を切れば
  バーごと消せる（機能には一切影響しない装飾）。
- **申し送り**: 描画順は「同じ高さならDOM順」（続き415の0.5px許容）で決まるが、実測ではバーの板の
  描画順が ロックスロットの板より**後**（＝手前）になっている。今回の配置では両者の塗り面が重ならない
  ので実害は無いが、将来バーを太くする・ロックエリアへ寄せる調整をした時は**必ず画像で見比べる**こと。
- サーバー側（Supabase）の変更は無い。

### 2026-09-05（続き420）：#260 盤面のカードの「ドロップシャドウ」の正体＝暗転を色空間を間違えて適用していた（#238a/#247 の積み残しもこれ）／手番のロックエリアの光を逃がし層へ

続き419（バーの修正）でバーのチカチカは止まり、残り2件の報告。**どちらも根っこは「WebGLがONの盤面で
DOMが描いたまま残っている／DOMとWebGLで見え方が食い違っている」ところ**だった。

- **【本命】「ムーブフェイズでの移動先選択時、右下のマスのカードのドロップシャドウだけチカチカ」**:
  調べる前にまず**盤面で今も塗っているDOM要素を全部数え上げた**（推測で当てにいかない）。ムーブ選択中は
  `.cell::after`（暗転の膜）46件・`.cell` の outline 3件・`.lock-area.is-turn-player` の光1件・駒の影6件・
  山の側面16件・プレイマットと床2件、だった。
  - **最初の仮説は外れた**: 「DOM の膜とWebGLの着色で二重に暗くなっている」と考えて DOM の膜を止めて
    みたところ、**暗転がほぼ消えた**（＝実際に効いていたのはDOMの膜の方）。3通り（WebGL OFF／ON＋DOMの膜／
    ON＋膜なし）を実際の盤面で撮って並べたことで、**WebGL ON の時だけ暗転が明らかに弱い**と分かった。
  - **真因（実測で確定）**: 暗転は「膜が重なった時の色」を板の色として先に作る方式
    （`mesh.material.color.setRGB(1-a, 1-a, 1-a)`）だが、**three.js の既定の作業色空間はリニア**。
    第4引数を省いていたため 0.45 が**リニア値**として扱われ、画面には sRGB の約0.70 として出ていた
    ——実測でカードの板の色が `#b3b3b3`（0.70）。つまり**55%暗くするつもりが30%程度しか効いていなかった**。
    周囲（DOMの膜が正しく効いている）だけがきちんと暗く、**カードだけ明るく残る**ので、
    カードのふちに暗い縁ができて「カードに影が付いている」ように見えていた。
    `THREE.SRGBColorSpace` を明示して解決＝実測 `#737373`（0.45）になり、**WebGL OFF の見え方と一致**した
    （スクリーンショットでも確認）。
  - **これは #238a（2026-09-05「ムーブフェイズの時に盤面のカードのドロップシャドウが強くなる」）と
    #247（「カードにドロップシャドウのようなものがある」）の積み残しでもある**。あの時は「膜の色を板に
    混ぜる」ところまでは作ったが、混ぜた値が薄まっていることに気づけなかった（`tinted:148` という
    **件数**だけを見て「効いている」と判断したのが誤り。**量**を測るべきだった）。
  - **教訓**: three.js で `Color.setRGB` / `setHex` に「CSSで見えている値」を渡す時は、必ず色空間を明示する
    （省略＝リニア扱い）。そして「効いているか」は件数ではなく**実際の出力値**で確かめる。
- **「手番プレイヤーを示すロックエリアの光がたまにチカチカ」**: `.lock-area.is-turn-player` の
  box-shadow アニメーションは**キャンバス(z-index:5)の裏**にあるため、WebGLが描くカード・マスの板に
  覆われて途切れ途切れに見えていた（続き413で駒のグローに対して直したのと同じ構図で、こちらが残っていた）。
  駒のグローと同じ**逃がし層**（`board-3d-overlay.js` / `#board-3d-glow-layer`）へ、同じ位置・同じ大きさの
  空の箱を置いて同じアニメーションを再生する形にした。実測: 元 `[602,141,396,40]` と逃がし先が**完全一致**、
  `lock-area-turn-glow` が動き、色（`--turn-glow-rgb: 255,224,130`）も引き継ぎ、
  「ずっと光り続ける演出をやめる」設定でも止まる。
- **検証**: `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、`node tools/check-undeclared.mjs` 0件、
  CSSブレース平衡。暗転は実際の対局盤面で ①WebGL OFF ②修正前ON ③修正後ON を撮って見比べ、
  ③が①と一致することを目視＋板の色の数値で確認。例外0件。サーバー側（Supabase）の変更は無い。
- **申し送り**: ムーブ選択中の暗転は今も「DOMの膜（`.cell::after` 46件）」と「WebGLの着色」の**両方**が
  効いている（キャンバスが手前なので、カードの上はWebGL・マスの隙間はDOM、と分担されている）。
  DOMの膜を止めると隙間の暗転が消えるので、**止めてはいけない**（今回実測で確認）。iOSの合成レイヤーを
  さらに減らしたいなら、マスの板そのものが隙間まで塗るようにしてから外す、という順番になる。

### 2026-09-05（続き421）：#261 演出の最中に情報モーダルが出るのをやめた（[src/anim-gate.js](src/anim-gate.js) 新規）／#262 チカチカの切り分け用にテクスチャの内訳を計測

- **#261「接触演出、カード効果演出（マスチェンジなど）の最中にモーダルが出るようになっちゃってます。
  演出はしっかり見せたいのでモーダルは演出後にしてください。また演出は画面のどこかをクリックで
  飛ばせるように。演出系を総点検しよう」**:
  - **まず調べたこと**: 接触のタックルもマスチェンジの入れ替えも、演出自体は呼び出し側で
    ちゃんと `await` されていた（`await playMassChangeSwapAnimation(...)` / `await playContactLunge(...)`）。
    つまり**演出と別の経路からモーダルが出ている**（効果の告知は演出の進行とは独立に呼ばれる）。
    個々の呼び出し順を直して回るとキリが無く、新しい効果を足すたびに同じ事故が起きる。
  - **仕組みで止める形にした**: [src/anim-gate.js](src/anim-gate.js)（import を一切持たない葉モジュール。
    `celebration-state.js` と同じ作り）に「いま盤面の演出が再生中か」を置き、
    **情報を見せるだけのモーダル**（`showHandEffectUseModal` / `showEffectReasonModal` /
    `showCardReceivedModal` / `showMultipleCardsReceivedModal`）は演出が終わるまで待ってから出す。
    **選択を求めるモーダル（マス選択・手札選択・選択肢）は待たせない**——止めると効果の解決が
    進まなくなるため（続き369で決めた線引きをそのまま踏襲）。
  - **演出側は中身を触らず、同名の薄い包みを足して本体を `__inner` へ改名**しただけ（呼び出し側は
    一切変更不要）。対象7つ: 接触のタックル（助走／ゲートへ飛ばす／観戦側）・マスチェンジの入れ替え・
    駒の移動・エターナル獲得・ゲート侵攻の手札奪取。
  - **止まらない作りにしてある（今回いちばん気を使った点）**: 待ちには**上限4秒**、さらに演出フラグが
    15秒立ちっぱなしなら**自力で下ろす**。演出側が `end` を呼び忘れても「モーダルが少し遅れて出る」
    だけで、対局が固まることはない。実際、演出の中から情報モーダルを await する経路があれば
    理屈の上では相互待ちになるが、この上限で必ず抜ける。
  - **実測**: 演出中はモーダル0件 → 演出終了で表示される／待ちの上限600ms指定で642msで抜ける／
    呼び出し側の await も1.6秒で解決（固まらない）／例外0件。`npm test` 56/56 PASS。
  - **「タップで演出を飛ばす」は次回に回した**（今回は入れていない）。飛ばすには演出ごとに
    「途中で終わらせても後片付けが残らない」ことを個別に確かめる必要があり、順番の修正と
    同時に入れると、どちらが原因で崩れたか分からなくなるため。追色の演出（`card-dissolve.js`）は
    続き359で既にタップでスキップできる。
- **#262「まだスマホでチカチカします」（今回は場所の指定なし・未解決）**: 前3回（バー・カードの影・
  ロックエリアの光）はいずれも「DOMが描いたまま残っていたもの」で、直すたびにその項目は報告から
  消えている。今回は対象が書かれていないため**推測で触らず、次の報告で切り分けられる計測を足した**。
  - `diag-board3d` に **`texImg`（画像テクスチャ数）/ `texShape`（形テクスチャ数）/ `texEvicted`
    （捨てて作り直した累計）** を追加。`texShape` が上限48に張り付いて `texEvicted` が増え続けて
    いれば、**同じ見た目のテクスチャを作り直し続けてGPUへ転送し続けている**＝チカチカの原因として
    濃厚、と判定できる（続き401で一度踏んだ形の再発かどうかが分かる）。今回のログは PC が
    21→44→54→65→67→68 で**頭打ちになっている**ので、単なるキャッシュの温まりに見える（＝別要因）。
  - **盤面でまだDOMが描いているもの（実測・ムーブ選択中）**: `.cell::after` の暗転46件／マスの
    outline 3件／駒の影6件／山の側面16件／プレイマットと床2件。次に報告をもらう時は、この中の
    どれかを指してもらえば一発で絞れる。
- **検証**: `node --input-type=module --check`（main/hand-effect-ui/board-3d/anim-gate）通過、
  `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` 56/56 PASS、
  `node test/smoke.mjs 2` / `4` PASS。サーバー側（Supabase）の変更は無い。

### 2026-09-05（続き422）：#258 の真因を特定（並べ替えの鍵が「板の中心の奥行き」だった）／盤面のWebGL描画・第3段をやり直して再投入

ユーザー指示「やはりチカチカ解消のために three.js 化をしたほうがいいのでは？以前その際に色々消えて
しまった原因をしっかり調査して！」。続き418で「未確認」のまま取り消していた #258 を、**実際に再現して
切り分け**、真因を突き止めたうえで第3段をやり直した。

- **切り分けの手順（推測せず、1つずつ戻して測った）**: 報告者と同じ画面条件（2560x1278 / dpr 0.75 /
  ステージ倍率1.42）をヘッドレスで再現するハーネスを作り、①今の状態（基準）②第3段の変更のうち
  **キャンバスを奥へ置く**だけ ③さらに**プレイマットと床もWebGLへ**（＝第3段そのもの）、の3通りを撮った。
  - ②の時点で盤面のカードが消えた。これは当然で、プレイマットがまだDOM側で塗っていたから
    （キャンバスが奥＝プレイマットが上に乗る）。第3段はそれを見越して③をセットで入れていた。
  - ③（第3段そのもの）で**報告と同じ絵**——盤面の中まで床のテクスチャで覆われる——を再現できた。
- **【真因】並べ替えの鍵に「ワールド座標での板の中心のZ」を使っていたこと**。実測値が決定的だった:

  | | 描画順 | DOM順 | 中心のワールドZ |
  |---|---|---|---|
  | 床（4.75倍） | **197＝最後（最前面）** | 0 | **+324.5** |
  | プレイマット | 86 | 1 | -28.3 |
  | 盤面のカード（奥の列） | 14 | 23 | -165.8 |

  盤面は42度傾いているので、**同じ平面に載っているだけの板でも、手前寄りにあるものほどZが大きい**。
  つまりこの鍵は事実上「盤面のどの列にいるか」を見ていただけで、重なりの前後とは無関係だった。
  小さくて同じ大きさのタイル同士（マスとその中のカード）は中心がほぼ一致するため今まで破綻せず、
  **盤面全体を覆うほど大きい板を入れた瞬間に**その中心が手前寄りにあるせいでZが最大になり、
  最後に描かれて盤面を丸ごと隠した。＝続き418で書いた2つの仮説のうち「床の板が最後に描かれた」方が正解。
- **直し方**: 鍵を「**盤面の平面からどれだけ持ち上がっているか**」＝板自身の translateZ
  （`mesh.matrix.elements[14]`）に変えた。駒だけが立方体として数px以上持ち上がっており、カード・マス・
  山の上面・プレイマット・床はすべて0＝同じ高さ。同じ高さのものはCSSと同じく**DOMの並び順**で決まる。
  これで「盤面のどこにあるか・どれだけ大きいか」に一切左右されなくなる。
  - 実測: 直後に 床0 / プレイマット1 / カード23 と**DOM順そのもの**になり、盤面が正しく描かれた。
  - **この鍵は #251（ゲートのマスがカードより手前に描かれて黄色く透ける）の直し方より根本的**。
    あちらは「0.5pxの許容幅」で誤差を吸収する対症だったが、今回の鍵なら誤差自体が入らない
    （許容幅は行列の積み上げ誤差よけとして残してある）。
- **第3段を再投入**: プレイマットと床もWebGLで描き、キャンバスを `.scene` の先頭・z-index:0 へ。
  盤面で塗っているものは全部WebGLへ移ったので、DOM側は「大きさと当たり判定だけの透明な箱」になり、
  奥に敷いても何も隠れない。逆に光・枠・刻印・ハイライトは**何もしなくても自然に手前**になるため、
  続き416で作った「手前へ逃がす層」（board-3d-overlay.js）は役目を終えたので撤去した
  （残すと元の光と二重に描かれる）。`.board-3d-glow` 等のCSSは**あえて残してある**——万一キャンバスを
  手前へ戻す判断をした時に、また同じものを書き直さずに済むため（未使用だが害は無い）。
- **検証（PC条件とiPhone条件の両方で実測）**: キャンバスが奥（z-index:0・先頭）／逃がし層の残り0／
  **駒がカードの下に潜る 0件**（#241の回帰なし）／**ゲートのマスがカードより手前 0件**（#251の回帰なし）／
  プレイマットと床のDOM塗りが両方 false（WebGLへ移った）／手番の駒の光あり／盤面のスクリーンショットが
  基準と一致／例外0件。`npm test` 56/56 PASS、`node test/smoke.mjs 2` / `4` PASS、
  `node tools/check-undeclared.mjs` 0件、CSSブレース平衡。
- **教訓（このプロジェクトで一番効く形）**: 「CSSの見え方をWebGLで描き直す」レンダラーでは、
  **重なり順の鍵はCSSと同じもの（持ち上がり＋DOM順）にする**。カメラからの距離で並べ替えると、
  傾いた盤面では「位置」と「前後」が混ざり、大きい板を足した瞬間に破綻する。
- **調査に使った道具を残した**: `debugMeshInfo` に `worldZ`（並べ替えに実際に使う値）を追加。
  次に重なり順を疑う時は、`z`（板自身の持ち上がり）と `worldZ`（中心の奥行き）の両方を見れば
  どちらの意味でズレているのか一目で分かる。

### 2026-09-05（続き423）：#264 iPhoneでWebGL描画が起動しなくなった真因＝**削除したモジュールの404**（自己増殖して永久に直らない状態だった）／#263 暗転の二重／ロック演出中のモーダル

ユーザー報告2件。**#264 のコンソールログが決定的**だった——`board-3d の起動に失敗` に続いて
`UNHANDLED_REJECTION: Importing a module script failed.` が**毎秒**数百行。つまり iPhone では
**WebGL描画がそもそも起動していなかった**（＝CSS描画に落ちていたのでバーがチカチカする）。

- **【真因】続き422で `src/board-3d-overlay.js` を削除したこと**（実測で確定）。
  - 一つ前の版の `board-3d.js`（4c718c4）は `import { syncOverlayMirrors, … } from "./board-3d-overlay.js"`
    を持っている。続き422（9f174f3）でそのファイルを消したので、本番で **404**（`curl` で確認: 404 / text/html）。
  - iPhone は `board-3d.js` の**古いキャッシュ**を持っており、そこから 404 のモジュールを読もうとして
    「Importing a module script failed」。PCは board-3d.js が新しいので無傷——**端末ごとに壊れ方が違う**のはこのため。
  - **さらに悪いことに自己増殖する**: 読み込みに失敗したファイルは「読み込み済みリソース」に載らないので、
    更新チェッカーの再取得（`refreshCachedAssets` は現在読み込まれている同一オリジンのJS/CSSを
    `cache:"reload"` で取り直す、続き249/372）の**対象から外れる**。＝更新しても古いキャッシュのまま
    永久に直らない袋小路。実際 `bootReport.prevLastError` が前のセッションでも同じ失敗を記録していた。
  - **対策**: 消したファイルを**そのまま復活**させた（スタブではなく元の実装。古いキャッシュの
    board-3d.js が読み込んだ時に、続き421と同じ正しい見え方で動くため）。新しい board-3d.js は
    このファイルを import しないので、本番では**ロードすらされない**（数KBのファイルが1つ増えるだけ）。
    ファイル冒頭に「消してはいけない理由」を経緯ごと明記した。
  - **【原則・今回の一番の教訓】公開済みのモジュールファイルは消さない**。使わなくなっても中身を空にして
    残す。「もう誰も import していない」はこちらのリポジトリの話であって、**利用者の端末のキャッシュには
    まだ古い import が生きている**。数KBのファイルと、端末が起動不能になり自力で復帰もできない事故は
    釣り合わない。
- **原因が読めるようにした**: `console.error("board-3d の起動に失敗", err)` は中身が `{}` としか出ず
  役に立たなかった。`err.message` を出し、**不具合報告に必ず載る行動ログ**へ
  `diag-board3d-import-failed`（name / message）を残すようにした。
- **ログが数百行埋まっていた件も修正**: 管理者パネルの「🧪 盤面をWebGLで描く」は開いている間 1秒ごとに
  `import("./board-3d.js")` して数値を更新する（続き414）。この rejection を拾っていなかったため、
  import が失敗する端末では**毎秒**未処理の例外が出て、本物のエラーが埋もれていた。catch して理由を
  表示し、失敗したら繰り返しも止める。
- **#263-1「移動先選択時の暗転がPCだと濃すぎる。スマホだといい感じ。なんで差があるの？」（原因確定・修正）**:
  暗転は `.cell::after` の `rgba(0,0,0,0.55)`（DOM）と、WebGL側で板の色に混ぜる着色（`overlayTint`）の
  **二重**になっていた＝ 0.45 × 0.45 ≒ **0.20**（本来 0.45）。続き422で**キャンバスを盤面DOMの奥へ**
  移した時点で、DOMの膜はWebGLが描いたカード・駒・山の上にもそのまま掛かるようになっており
  （`#game-table` が z-index:1 / キャンバスが 0）、**WebGL側の着色はもう不要になっていた**。
  `overlayTint` ごと撤去（実測: `tinted` が 146 → **0**）。
  - **PCとスマホで違った理由**も同じ根っこ: スマホはWebGL描画が起動に失敗していて**DOMの膜だけ**が
    効いていた＝そちらが本来の濃さだった。ユーザーの「スマホだといい感じ」がそのまま正解を指していた。
  - なお続き420で色空間（`THREE.SRGBColorSpace`）を直した苦労は、キャンバスを奥へ移した時点で
    役目を終えていた。**描画の構造を変えたら、その前提で入れた補正が不要になっていないか見直す**こと。
- **#263-2「ロック演出時に次のモーダルが出ちゃってます」（修正）**: 続き421の演出ゲート（anim-gate）は
  接触・マスチェンジ等7つの演出に入れたが、**ロックの演出が漏れていた**。ロック演出は
  `triggerLockEffect`（オーラの収束→焼き付く閃光→刻印、計2.2秒）の1か所に集約されているので、
  そこを `withBoardAnimation` で包めば全経路（通常のロック・セットアップ演出・チュートリアル）に効く。
- **#264-2「到達拡大モーダルにカードがしっかり表示されてません」（未解決・材料のみ）**: カード面の素材
  （`assets/cards-blank/*.webp`）は本番で正常に配信されている（200・49KB）ことを確認済み。報告時の
  iPhone は module の読み込み失敗が毎秒続き `suspectedCrash: true` の直後という壊れた状態だったので、
  その二次症状（画像の読み込み／デコードの失敗）の可能性が高い。**推測で触らず**、上のWebGL起動の
  修正が届いた後に再確認をお願いする。
- **検証**: `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` **56/56 PASS**、
  CSSブレース平衡（2842）、`node test/smoke.mjs 2` / `4` PASS。ブラウザ実測で——
  ①復活させた `board-3d-overlay.js` が読め、古い board-3d.js が必要とする3つのexportが揃っている
  ②WebGL描画は正常（板198・枠128・キャンバス z-index 0 かつ `.scene` の先頭・`#game-table` が z-index 1
  ＝DOMの膜がWebGLの上に掛かることの裏付け）③暗転中の `tinted` が **0**（二重解消。DOMの膜
  `rgba(0,0,0,0.55)` だけが効く）④演出ゲートで情報モーダルが演出中0件→終了後1件
  ⑤**実際のロック演出中に毎50msで採取し、演出が見えた18サンプルすべてでゲートが立っていた**
  （終了後に立ちっぱなしも無し）⑥例外0件。サーバー側（Supabase）の変更は無い。
- **申し送り（今回はあえてやらなかったこと）**: 「モジュールの読み込みに失敗したら、そのURLだけ
  `cache:"reload"` で取り直す」自己修復も考えたが、**同じページの中では module map が失敗を覚えている
  ため import をやり直しても直らない**（次に開いた時に効くだけ）で、しかも今回のような状況を手元で
  再現して確かめる手段が無い。検証できない自己修復を入れるより、**そもそも消さない**（上のルール）と
  **失敗したら理由がログに残る**（`diag-board3d-import-failed`）で十分と判断した。次に同じ形の報告が
  来たら、そのログに「どのファイルが読めなかったか」が出る。

### 2026-09-05（続き424）：#265 駒が透けて見える＝立方体の面ごとの陰影がWebGL描画で失われていた／演出中の中央お知らせ／お知らせが一斉に出て読めない

ユーザー報告 #265（iPhone・版20260905-134650＝続き423の修正が届いた後）3件。**3件とも「iPhoneで初めて
WebGL描画が動き出した」ことで表に出た**——#264でこの端末は起動に失敗し続けていたので、PCでは数日前から
そう見えていたはずの差が、ここで一気に報告された。

- **①「なんか駒が少し透けている？ように見えます」（原因確定・修正）**: 駒の立方体の陰影は box-shadow
  ではなく **`filter: brightness()`** で付けている（`.piece-wall-back` 0.35 / `-left` 0.55 / `-right` 0.8 /
  `-front` と `-top` は等倍）。WebGL側はこれを一切読んでいなかったため、**5面すべてが同じ明るさの同じ
  スキン画像**で描かれ、立体の陰影が丸ごと失われていた。CSSの filter はDOMには残っているが、その要素は
  もう何も塗らない（背景画像を剥がしてある）ので効き目が無い。陰影の無い立方体は平べったく、輪郭の
  頼りない塊に見える——それが「少し透けている」の正体だった。
  - **修正**: `effectiveVisual()` が opacity と同じ要領で（＝要素から `#game-table` まで遡って）
    `filter: brightness()` を掛け合わせ、その値を板の色に入れる。**色空間に `THREE.SRGBColorSpace` を
    渡すのが要点**——sRGBはおおむねべき乗なので、sRGB空間で指定した係数を掛けると CSS の brightness() と
    **数学的に同じ結果**になる（リニアのまま渡すと掛け算の意味が変わって暗くなりすぎる。続き420で
    踏んだのと同じ話）。実測: back `#595959`(0.35) / left `#8c8c8c`(0.55) / right `#cccccc`(0.8) /
    front・top は白＝CSSの指定と完全一致。
  - **ハマりどころ（自分のミス）**: 最初 `cssBrightness()` を正規表現で書いたら、書き込みの過程で
    **バックスラッシュが全部落ちて** `/brightness(s*([0-9.]+)(%?)s*)/g` になっており、静かに常に1を
    返していた（構文エラーにならないので気づけない）。`getComputedStyle` は正しく
    `brightness(0.35)` を返しているのに板が白のまま、という食い違いから発覚。**正規表現をやめて
    indexOf/slice の素朴な走査に書き直した**（このファイルはCRLFで、エスケープの取り違えが起きやすい）。
- **②「モーダルが次から次へと勝手に消えては次のモーダルで切り替わりが早すぎて何が起こってるのか
  よくわかりません。以前はそんなことなかったのに！」（原因確定・修正）**: 続き421の演出ゲート
  （`anim-gate.js`）の `flush()` は、演出が終わった瞬間に**待っている全員を同じ一瞬で起こす**。
  演出中に溜まったお知らせ（使用告知・理由告知・獲得/ドロー/ロックのトースト…）がそこで一斉に出て、
  しかもこれらは中央の同じ場所に出る／前のものを消してから出る作りなので、**1つも読めないまま次々に
  差し替わっていた**。ゲートを入れる前は待たされなかったぶん自然にばらけていたので、
  **「以前はそんなことなかった」というユーザーの感覚は正しい（＝私が入れた回帰）**。
  - **修正**: `anim-gate.js` に**お知らせの順番待ちの列**（`waitForNoticeSlot(holdMs)`）を新設した。
    ①演出の終わりを待ち ②前のお知らせから `holdMs` 空くまで待つ、を1本の列で順に行う。
    holdMs は「そのお知らせが場所を占める長さ」を呼ぶ側が渡す（中央のトーストはフラッシュの長さ
    1.8秒、右のモーダルは1.3秒）。**単発のお知らせには一切の遅れが出ない**（列が空なら即通過）。
  - **止まらないための作り**: 5つ以上並んだら間隔を半分（下限0.5秒）に詰める。お知らせを await して
    いる効果処理があるので、待ちが積み上がって対局が止まる方が実害が大きい。実測: 演出中に5つ
    投げ込んでも「一斉」は解消し、それぞれ0.65〜2.6秒ずつ表示される（修正前は実質0秒で差し替わっていた）。
- **③「中央に出るミニモーダルはいまだに演出中に表示されます。例えば、到達時にボワーンとオーラが出る
  演出とかロック演出とかいろんな演出です！」（原因確定・修正）**: 2つの穴があった。
  - **`hand-announcer.js` の `showToast` が演出ゲートを一切見ていなかった**（続き421で入れたのは
    `hand-effect-ui.js` の4つだけ。トーストは獲得・ドロー・ロック・捨て・ゲート侵攻の全部を担うので、
    実際に一番よく中央に出るのはこちらだった）。上記の順番待ちに乗せた。
  - **到達のオーラ（`spawnArrivalBurst`）が演出として数えられていなかった**。続き421で7つ、続き423で
    ロック演出を包んだが、ユーザーが名指しした「ボワーンとオーラが出る演出」だけが漏れていた。
    この関数は同期で要素を返す作り（呼び出し側が戻り値を使う）なので、`withBoardAnimation` で包む
    代わりに `beginBoardAnimation()` ＋ 演出の長さ（`ARRIVAL_BURST_DURATION_MS`＝1400ms）後に
    `endBoardAnimation()` で手で押さえる形にした。
  - **フラッシュの途中で演出が始まる場合**が残るので、`anim-gate.js` に「演出が始まった瞬間に呼ばれる
    後片付け」（`registerBoardAnimationStart`、`celebration-state.js` と同じ考え方）を足し、
    中央に残っているフラッシュはその場で右下のストックへ畳む。**中身は失われずチップとして積まれる**。
- **検証**: `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` **56/56 PASS**、
  `node test/smoke.mjs 2` / `4` とも PASS、CSSブレース平衡（2842）。ヘッドレスで実測——
  ①駒の5面の板の色が上記のとおりCSSの brightness と一致 ②演出中に投げ込んだお知らせは演出中0件、
  終了後に間隔を空けて1つずつ出る ③**実際にCPU自己対戦を55秒回して**、到達のオーラが見えていた
  113サンプル・ロック演出の27サンプルとも**全てゲートが立っており**、その間に中央のフラッシュが
  出ていたサンプルは**0件**（修正前は13件） ④例外0件。サーバー側（Supabase）の変更は無い。
- **申し送り**: #264-2（到達拡大モーダルにカードが表示されない）は、報告時のセッションが壊れた状態
  （毎秒のモジュール読み込み失敗・`suspectedCrash`）だったため二次症状の疑いが濃く、まだ手を付けて
  いない。WebGL描画が正しく起動するようになった今の版で再確認をお願いしている。

### 2026-09-05（続き425）：#266 演出の最中にフェイズが切り替わる／モーダルが一つ前の表示に重なって出る（「画面の中央は一度に1つ」を仕組みで保証した）

ユーザー報告 #266「フェイズの切り替えが演出中（ロック演出とか）に起きます。ミニモーダルが中央に出て
いるときにもフェイズが切り替わります。スリカエの相手一人なのでモーダルの最中に手札奪う儀式モーダルが
出ます。といった具合にモーダルが一つ前の処理にラップして出てしまっている。そういった表示タイミングを
ログに出るようにしてもいいし、総点検してもいいし、何とか対策を講じてください」。

**個々の呼び出し順を直して回るのはもう限界**（#261→#265→#266 と、直すたびに別の経路が出てくる）と
判断し、**「画面の中央は一度に1つだけ・中央がふさがっている間はゲームを進めない」という1本の規則**を
仕組みとして入れた。

- **①「中央の占有物」の定義を anim-gate.js に集約**（`isBlockingModalVisible()`。このファイルは
  import を一切持たない葉モジュールなので、DOMを見るだけで済ませている）。占有物は3種類——
  (a) `.so7-modal-backdrop`（画面を覆って返事を待つモーダルの背景。#225 で付けた目印。**儀式の
  奪取ピッカー・「受け取った」表示・各種確認モーダルが全部これ**）(b) `#card-effect-picker-hint.show`
  （マス/手札を選ばせている時の上部の案内）(c) `.phase-announce-toast.show`（フェイズ告知）。
  - **実際に見えているものだけ**を数える（`getClientRects().length > 0`）。このアプリには
    「作りっぱなしで display / .show を切り替えるだけ」の常駐要素が多く、存在の有無で判定すると
    **永久に待って盤面が死ぬ**（続き396・415で2回踏んだ罠）。
- **②お知らせは「中央が空くまで」待つ**（`waitForNoticeSlot`）。続き424で入れた順番待ちの列に、
  「演出が途切れるまで」と「返事待ちのモーダルが閉じるまで」を足した。
  - **演出は連続する**（到達のオーラが何度も出る等）ので、1回 flush を待っただけでは直後に始まった
    次の演出に重なる。**途切れるまで繰り返し待つ**ようにした（実測でここが効いた）。
  - これで **#266 の「スリカエの儀式モーダルの最中にお知らせが出る」が直る**——儀式ピッカーは
    dim backdrop を持つので、その間はお知らせが出ない。
  - 上限は必ず設ける（演出待ち9秒・モーダル待ち10秒）。**待ち続けて対局が止まる方が実害が大きい**
    （続き421と同じ考え方）。
- **③フェイズの切り替えを「中央が空くまで」待たせる**。ここが今回の本丸。
  - まず `reconcilePhaseAutomation()` の先頭で待たせた（既にある「ゲート侵攻の演出中は進めない」と
    **まったく同じ形**）。ただし実測すると **それだけでは足りなかった**（55秒で1回すり抜けた）。
    フェイズの開始は reconcile 以外からも呼ばれるため——自動スキップの1.5秒タイマー
    （`advancePhaseAfterSkip`）・スキップボタン・マイデッキで引いた時・カード効果の
    「このフェイズを終了する」。
  - そこで**入口である `enterPhase` 自体**を包み（本体は `enterPhase__inner` へ改名）、
    中央がふさがっていれば予約だけして 150ms ごとに再挑戦する形にした。予約は常に1件だけ。
  - **止まらないための作り**: reconcile 側は待っている間 200ms ごとに自分を呼び直す
    （render() が来ないと待ちっぱなしになる。続き407で踏んだ形）。enterPhase 側は上限8秒で
    必ず進む。演出フラグ自体も15秒で自力解除される（anim-gate の STUCK_MS）。
- **④待たせた時間をログに残す**（ユーザー要望「表示タイミングをログに出るようにしてもいい」）:
  `diag-notice-wait`（お知らせを何ms待たせたか・理由が anim / queue / modal のどれか・待ち行列の数）と
  `diag-phase-deferred`（フェイズを何ms待たせたか・どのフェイズか・reconcile と enterPhase の
  どちらで止めたか）。anim-gate.js は import を持たない葉モジュールなので、記録する関数は
  `setAnimGateLogger()` で main.js から差し込む。
- **検証（ヘッドレスでCPU自己対戦を55秒、50msごとに採取）**:

  | | 修正前 | 修正後 |
  |---|---|---|
  | 演出中にフェイズが切り替わった回数 | 1 | **0** |
  | 演出中に中央のお知らせが出ていたサンプル | 24 | **0** |
  | 返事待ちモーダルの上にお知らせが出ていたサンプル | — | **0** |
  | フェイズ告知とお知らせが重なっていたサンプル | 15 | 8（＝告知が消えていく0.4秒の間だけ） |

  あわせて、①演出中に `reconcilePhaseAutomation()` を何度呼んでもフェイズが進まない ②見えている
  backdrop がある間は `waitForNoticeSlot` が解決せず、消した0.9秒後に解決する ③何も出ていなければ
  待ち時間0ms（＝単発のお知らせは今までどおり即出る）④`diag-notice-wait` /
  `diag-phase-deferred` が実際に記録される（例: waited 1390ms / phase "move" / via "reconcile"）、
  を実測。ターンは 55秒で3〜4ターン進み、止まらないことも確認した。
- **検証**: `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` **56/56 PASS**、
  `node test/smoke.mjs 2` / `4` とも PASS、CSSブレース平衡（2842）、例外0件。
  サーバー側（Supabase）の変更は無い。
- **申し送り（速さとのトレードオフ）**: 中央を1つずつ順番に見せる分、対局は**確実に遅くなる**
  （実測で同じ55秒に進むターン数が 4→3 になった場面がある）。ユーザーの要望が一貫して
  「早すぎて何が起こったか分からない」側なので今回はこの方向で振ったが、「もたつく」と感じたら
  お知らせの表示時間（`--hand-pickup-toast-duration`）とフェイズ告知の2.6秒を短くするのが
  効く（どちらも中央の占有時間そのもの）。

### 2026-09-05（続き426）：#267 ロック中にハンドフェイズの告知が出る／プレゼント後にターンが猛スピードで飛ぶ（#266で私が入れた回帰）／#268 フェニックスの二重「捨て」／#269 手札の背面のロック中カードが使われる／#270 フェイズ告知が重なる／#271 最小化案内がダークのまま

#266 の直後に届いた実機報告 #267〜#271。**#267 の後半は続き425で私が入れた回帰**だった。

- **#267-2「CPUの処理をゆっくりに設定しているのにすごく早く感じるときがある。特にプレゼントに到達した
  後とか」（真因確定・修正）**: 実機ログの T6/T7 が**中身ゼロで1.5秒ずつ**流れていた
  （`gate-invasion check` → `NEXT_TURN` だけ、フェイズ告知もCPUの手も無い）。原因は、続き425で
  `reconcilePhaseAutomation()` の**関数の先頭付近**（ゲート侵攻の待ちの直後）に「中央がふさがっている
  間は進めない」早期returnを置いたこと。待っている間はその下にある**不具合#19の後始末**
  （`phaseOwner` が今の駆動対象と違えば clearPhase＝前のターンの残骸を捨てる）まで飛ばしてしまい、
  前のターンの `currentPhase="move"＋moveActionTaken` が残ったまま次のターンに入る。すると
  `computeShouldEmphasize()` が即 true になり、**自動ターン終了が1.5秒ごとに走り続けて**空のターンが
  猛スピードで流れる。プレゼントは全員がドローする＝お知らせが並ぶので、その間ずっとこの早期returnに
  入っていた（ユーザーの「特にプレゼントに到達した後」と完全に一致）。
  - **修正**: この待ちを**後始末より後**へ移した。あわせて「なぜここに置くのか」を長めのコメントで
    残した（同じ場所へ戻すと再発する）。
  - **教訓**: 「毎回呼ばれる調整関数」の**先頭に早期returnを足す**時は、その下に**待っている間も必ず
    やらなければいけない後始末**が無いかを必ず確認する。待ちは「次へ進む処理」の直前に置く。
- **#267-1「ロックしている最中に、ハンドフェイズのモーダルが出ました」（真因確定・修正）**: 2つ重なっていた。
  1. **ロックの告知が飛んでいく 0.42 秒が「中央を占めている時間」に入っていなかった**。中央のフラッシュは
     表示（最大1.8秒）のあと右下のストックへ飛ぶが、`waitForNoticeSlot` には表示分しか渡していなかった。
     実測でもフェイズ告知はフラッシュが切れた **0.08 秒後**に出ていた＝ちょうど飛翔の最中。飛翔分
     （`STOCK_FLIGHT_MS`）も hold に含めた。
  2. **ロックの検知と演出の開始が1タックずれていた**。ロックは「トークンを動かす → その dispatch で
     render() → reconcile が次のフェイズへ → 呼び出し側に戻ってから `maybeAnnounceLock` →
     `triggerLockEffect`（演出ゲートを上げる）」という順番なので、**演出が始まる前のほんの一瞬**だけ
     ゲートが下りたまま。実測で dispatch と告知が同じ一瞬、その **27ms 後**に演出開始。ロックを検知したら
     `LOCK_ADVANCE_GRACE_MS`（400ms）だけ間を置いてから次へ進むようにした——その頃には演出ゲートが
     立っているので、あとは既存の待ちに任せられる。
- **#267 の一般化: 中央がふさがっている間はターンも終わらせない**: #266 で決めた「画面の中央は一度に
  1つだけ・中央がふさがっている間はゲームを進めない」を、フェイズの開始（`enterPhase`）だけでなく
  **自動ターン終了**にも適用した（`fireAutoEndTurn`、上限6秒＋`diag-turn-end-deferred`）。
- **#270「CPUがハンドフェイズをスキップするとき、まだハンドフェイズモーダルが出ているのにムーブ
  フェイズモーダルがラップしてくる」（修正）**: フェイズ告知は 2.6 秒表示なのに、ハンドフェイズの自動
  スキップは 1.5 秒で次へ行くため、同じ場所に2枚重なっていた。`anim-gate.js` に
  `isPhaseAnnounceVisible()` を足し、`phaseDisplayBusy()`（enterPhase の待ち条件）に含めた。
  上限（8秒）があるので告知が消えなくても止まらない。
- **#268「フェニックスの効果で追色コストに捨てたカードが、このターンの出来事では2回捨てたことに
  なっている」（原因確定・修正）**: 赤のキューブ フェニックスは「捨て場の上から2番目」を取るために
  **一番上を一度引いて退避し、あとで捨て場へ戻す**（捨て場には途中を直接指す手段が無いため）。この
  「戻す」を普通の捨てと同じ経路で行っていたので、追色コストで捨てたカード（＝そのまま一番上にある）が
  もう一度「捨てた」と告知されていた。`discardFromHandReveal(tokenId, { silent: true })` を追加し、
  戻す時は焼失演出も出来事ストックも出さない。
- **#269「手札を選択したのに手札の背面のエターナルカードをクリックしちゃってます」（原因確定・修正）**:
  自分の手札の扇は自分のロックエリアの帯に**重なって**描かれる。「ロック中でも使えるカード」のタップ
  経路（capture フェーズ、main.js）が `elementsFromPoint()` の**最初に見つかった** `.board-card.
  is-usable-while-locked` を無条件に拾っていたため、手札が手前にあってもロック中のカードが使われていた。
  #189 でホバー拡大とドラッグ開始には入れたガード（手札が手前ならそちらを優先）が、**この経路だけ
  抜けていた**ので同じものを足した。
- **#271「このモーダルがダークのままです」（修正）**: 対戦結果パネルの「最小化して盤面を確認します」の
  案内モーダルだけ、**インラインstyleで暗い色を直に書いていた**（CSS側のライト上書きはインライン
  styleには届かない）。ライトモードならアイボリー地＋濃い文字（続き338/339の配色）に切り替える。
- **検証**: `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` 56/56 PASS、
  `node test/smoke.mjs 2` PASS、構文チェック通過、翻訳キーの増減なし。#267/#270 は実機の見え方の
  最終確認をお願いしたい（速さの感じ方は数値では測れないため）。サーバー側（Supabase）の変更は無い。
- **申し送り（速さ）**: 中央を1つずつ順番に見せる規則を「フェイズの開始」だけでなく「ターンの終わり」
  にも広げたので、対局はさらにゆっくりになる。もたつくと感じたら、お知らせの表示時間
  （`--hand-pickup-toast-duration`）とフェイズ告知の 2.6 秒を短くするのが一番効く（どちらも中央の
  占有時間そのもの）。

### 2026-09-05（続き427）：#272/#273 CPUが1ターンに2回ロックする（「1フェイズに1枚だけ」をフェイズの切り替えだけで担保していたのが、待ちを入れて崩れた）

ユーザー報告「なぜCPUはT3で二回ロックした？？？」「T5でもCPUが２回ロックしている。なんで？」。
**#266/#267 の副作用**で、原因ははっきりしている。

- **ログ（T3）**: `lockAutoPlay-check lockableCount:2` → `lock green-growing-trees`（16:39:42.015）→
  `diag-notice-wait 2219 anim` → **3.4秒後に** `lockAutoPlay-check lockableCount:1` →
  `lock yellow-gamble`（16:39:45.429）→ さらに 4.4 秒後にようやく
  `diag-phase-deferred {phase:"hand", via:"enter"}`。T5 も同じ形（purple-sorry → orange-mass-change）。
- **原因**: ロックが「1フェイズに1枚だけ」であることを、**ロックした直後にフェイズがハンドへ
  進むこと**だけで担保していた。#266/#267 でフェイズの切り替えを「演出とお知らせが終わるまで待つ」
  ようにしたため、ロックしてから次のフェイズへ進むまでに **3〜4秒の窓**ができ、その間に疑似CPUの
  自動ロック（持ち時間のタイマーで回る）がもう一度走って2枚目をロックしていた。
  **人間でもその窓で2回タップすれば同じことが起きる**（ドラッグでも同じ）。
- **修正**: フェイズが進んだかどうかに頼らず、**このロックフェイズで新しくロックしたか**そのもので
  判定する。`phase-automation.js` に `hasPlacedNewLockThisPhase(player)` を新設（既にある
  `lockedIdsAtPhaseStart` との差分＝reconcile の placedNewLock と同じ見方）し、
  - `performLockPhaseClick`（**人間のタップも疑似CPUの自動ロックも必ず通る1か所**）
  - ドラッグ&ドロップでロックエリアへ落とす経路（onDragEnd）
  の2つで弾く。ロックフェイズ中で持ち主が一致する時しか true にならないので、自由配置や
  セットアップのドラッグには影響しない。人間の操作の時だけ理由を一言出す
  （`game.lock.alreadyLockedThisPhase`、自動ロックには出さない）。弾いたことは
  `diag-lock-click-skip {reason:"already-locked-this-phase"}` に残る。
- **教訓（このプロジェクトで繰り返している形）**: 「1回だけ」という制約を、**別の処理が直後に走る
  ことを前提に**担保してはいけない（今回は「ロックしたらすぐフェイズが進むから2枚目は選べない」）。
  その別の処理に待ちや非同期を1つ足した瞬間に窓が開く。制約は**その事実そのもの**
  （＝このフェイズで既にロックしたか）で判定する。続き321のラッチと同じ話。
- **検証**: `node tools/check-undeclared.mjs` 0件、`npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、
  翻訳キー ja/en とも 1786 で一致。サーバー側（Supabase）の変更は無い。

### 2026-09-05（続き428）：駒の移動を「立体のまま跳ねる」動きに／中央のお知らせはどこを押しても畳める／「前のターンの出来事」の行を追加

ユーザー要望3件。いずれも不具合ではなく体験の改善。

- **駒の移動が平たかったのを、立方体のまま跳ねる動きにした（[src/main.js](src/main.js) `playPieceHopGhost`）**:
  従来は汎用の飛翔ゴースト（`flyGhost` ＝スキン画像を1枚貼っただけの平たい四角）を使っていたので、
  盤面では立方体なのに**移動中だけ板**になっていた。ドラッグ中のゴースト
  （`.drag-ghost-piece-outer/-inner`）で実績のある「perspective ＋ 盤面と同じ傾き(`--table-tilt`)の
  入れ子に、本物の `buildCubePiece()` をそのまま入れる」やり方へ揃えた。
  - **役割を3枚に分けたのが要点**: `.piece-hop-ghost`（横の移動だけ・CSSトランジション）／
    `.piece-hop-lift`（上下の跳ねと潰れだけ・CSSアニメ）／`.piece-hop-inner`（盤面と同じ傾き）。
    横と縦を別の要素にすると**それぞれ別のイージングをかけられる**ので、素直な放物線になる
    （1つの transform に混ぜると、どちらかのイージングに引きずられて弧が歪む）。
  - あわせて **踏み切りと着地の潰れ（squash & stretch）／盤面に落ちる影（浮いている間だけ薄く
    ぼやける＝高さが伝わる）／着地の小さな輪**（`.piece-land-ring`、マスの子なので盤面に寝て広がる）
    を足した。影は移動を transition で駒と一緒に動かすので、**キーフレーム側では transform を触らない**
    （同じプロパティをアニメとトランジションの両方で動かすと取り合いになる）。
  - 3D空間の外（body直下）に置くので、**盤面のWebGL描画のON/OFFに関係なく同じに見える**。
    「駒やカードが飛ぶ動きをやめる」設定は従来どおり尊重する（その時は一瞬で移る）。
- **中央のお知らせを、画面のどこを押しても畳めるようにした（[src/hand-announcer.js](src/hand-announcer.js)）**:
  ユーザー要望「せっかちな人用」。document の capture フェーズで pointerdown を拾って `stow()` する。
  - **握り潰さない**（preventDefault も stopPropagation もしない）——ここで止めると、同じタップで
    ボタンや盤面を操作したかった人の入力まで消える（続き76の教訓）。
  - **出した直後の300msは受け付けない**（`createOpenGuard`）。タップして指を離した位置にフラッシュが
    現れると、その同じタップで即座に消えてしまう（#230/#236 と同じ「開いた瞬間の合成クリック」）。
- **「このターンの出来事」の下に「前のターン」の行を追加（[src/turn-event-stock.js](src/turn-event-stock.js)）**:
  `clearTurnEventStock()` が捨てていた分を **`prevEntries` へ移して残す**（残すのは直前の1ターン分だけ。
  それ以上は帯が長くなって盤面を隠す）。普段は畳んで**先頭3枚だけを重ねて薄く**表示し、隠れている
  枚数を「+N」で出す。ラベル（▸/▾）か、畳んでいる間は行のどこを押しても開く。
  - 帯は画面下端からの距離で位置が決まっているので、**行が増えても下のボタン列を押し下げず上へ伸びる**。
  - 畳んでいる枚数は CSS の `:nth-child(n + 4)` と JS の `COLLAPSED_PREVIEW_COUNT` の**両方**にあるので、
    変える時は必ず揃えること（コメントにも明記）。
  - **危うかった点（自分で見つけて直した）**: 「前のターンの分はあるが今のターンはまだ0件」という
    新しい状態ができたため、`getTurnEventStockTargetRect()` が **listEl が display:none のまま**測って
    0x0 の矩形を返し得るようになっていた。そのまま使うと中央のフラッシュが**画面の左上へ飛ぶ**
    （#195/#197 と同じ形）。帯だけでなく listEl の display も一時的に戻してから測るようにした。
    実測: 何も無い時 (1571,626,10x10)／今のターンに3件 (1537,593,29x29)／前のターンだけ
    (1571,579,10x10) と**どれも0にならず**、測った後も listEl は隠れたまま。
- **検証**: `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` 56/56 PASS、
  `node test/smoke.mjs 2` PASS、CSSブレース平衡、翻訳キー ja/en とも追加2件で一致。
  ヘッドレス実測——①CPU自己対戦中に実際の移動を MutationObserver で捕まえ、ゴーストが
  **面5枚の立方体**（`brightness(0.35)/0.55/0.8` の面ごとの陰影がそのまま乗っている）・
  perspective 1090px・`preserve-3d`・`rotateX(42deg)`・影と着地の輪も出る／**平たい旧ゴースト
  （.setup-fly-piece）は0件** ②中央フラッシュは出した直後の押下では畳まれず、300ms後の押下で
  `is-flying` になる ③前のターンの行は 5件→畳んで3枚表示（opacity 0.42）＋「+2」、押すと5枚全部、
  もう一度押すと畳む ④例外0件。
- **申し送り（提案したが今回は入れていない案）**: 移動方向へ少し前のめりに傾く／空中でゆっくり
  半回転（ただし面ごとの陰影が固定なので、暗い面が正面に来て不自然になる恐れがある）／半透明の
  残像が2〜3個ついてくる／2マス以上の移動（ジャンプ台）は跳ねを高く長くする／通り道のマスが順に
  一瞬光る。前のターンの行に「誰のターンだったか」を出すのは、`player-identity.js` を import すると
  `online.js` を経由して循環しかねないので見送った（必要なら名前を渡す形にする）。

### 2026-09-05（続き429）：#276 効果の解決中にフェイズが進んでしまう／移動候補を画面のハイライトから数えていた／#274 使用演出（追色なし）を待っていなかった／#275 **使用中のテクスチャを捨てていた**チカチカ／移動モーションの拡張・前のターンの持ち主表示

ユーザー報告 #274〜#276 と、要望2件（前のターンの行に「誰のターンだったか」／移動モーションの追加案）。

- **#276「CPUがドムスネロの効果を使ってその後ムーブフェイズをせずにターンを終わった気がします」
  （原因2つ・両方修正）**: 実機ログの T8 が決定的だった——`hand-effect eternal-yellow`(20:19:03.897) の
  **1秒後**に `diag-phase-deferred {phase:"move", via:"enter"}`(04.874)、効果が終わるのは 10.582、そして
  11.626 に `diag-move-auto-noop {reason:"no-candidates"}` → 隣へ1枚置いてターン終了。
  1. **効果の解決中にムーブフェイズが始まっていた**。ロック/ハンドの自動スキップは
     `advancePhaseAfterSkip()` が **1.5秒後に** `advancePhase()` を予約する作りで、予約した後に手札効果が
     始まると、その最中に enterPhase("move") が発火する。`reconcilePhaseAutomation` は前から
     `handEffectBusy` を見て止まっていたが、**入口である `enterPhase` の待ち条件（`phaseDisplayBusy`）
     だけが素通り**していた（続き427と同じ「別の処理が直後に走ることを前提にした担保」の形）。
     `phaseDisplayBusy()` に `handEffectBusy` を追加。上限は効果の解決だけ別枠にした
     （`ENTER_PHASE_DEFER_MAX_HAND_EFFECT_MS = 30000`。人が選ぶ時間を含むので8秒では足りない——
     試練の儀式で実測14秒。handEffectBusy 自身が #93/#214 のウォッチドッグで必ず解除されるので、
     ここが永久に待つことはない）。
  2. **移動候補を「画面のハイライト」から数えていた**。持ち時間切れの自動処理（main.js
     `performPriorityTimeoutAutoAction`）は `.cell.phase-move-highlight` を数えるが、ハイライトは
     render() のたびに作り直され、その**再適用は reconcile が演出・お知らせ・効果処理で止まっている間は
     行われない**。つまり「候補が無い」のではなく「まだ貼られていないだけ」の瞬間に当たると、
     続き408で入れた救済（動けないならターン終了）が誤って発動していた。`phase-automation.js` に
     `computePhaseMoveCandidates(player)` を新設し、**盤面の状態から数え直す**ようにした（マスのDOM要素は
     ハイライトの有無に関係なく引ける）。ハイライトが取れない時だけ従来のDOM経路にフォールバックする。
  - **教訓（このプロジェクトで繰り返し）**: 判定の材料に**画面（DOMのクラス）**を使うと、「まだ描かれて
    いない／もう剥がされた」瞬間に嘘の答えを返す。事実（状態）から数えること。#253（形のキャッシュが
    render で空になる）・#272（ロック回数をフェイズ遷移で担保）と同じ形。
- **#274「試練の儀式の手札効果使用時、効果使用ボタン押した後の演出中に色選択モーダルが出ました」
  （原因確定・修正）**: 追色コストあり(V5)の霧散演出は 2026-09-01 に await するよう直していたが、
  **追色なし(V4)は投げっぱなしのまま**だった（`announceHandEffectUseForEffect` が何も返していなかった）。
  試練の儀式は自身を捨てるだけで追色コストが無いので V4 経路＝演出の開始直後に色宣言モーダルが出ていた。
  V4 も Promise を返し、`runHandEffectOption` で `await` する（万一演出が終わらなくても止まらないよう
  8秒の上限付き）。あわせて、**効果カード自身を捨てる分の焼失演出を止めた**（`discardAndSync` に
  `noBurn` を追加）——待つようにすると「使用演出で霧散→続けて焼失演出でもう一度霧散」と、同じカードが
  2回消えて見えるため。
- **#275「3D時にまだ盤面のカードの一部とロックエリアバーの一部が不規則にチカチカしてます」
  （真因確定・修正）**: PC側のログに答えが出ていた——`texShape` が上限48に張り付き、`texEvicted` が
  0→13→41→…→**180** と増え続けていた（続き421でこの内訳をログに足しておいたのが効いた）。
  - **真因**: 形テクスチャの片付けが `shapeTexture()` の中で「**入れた順に**古いものから dispose」だった。
    ところが**見た目が変わらない板は shapeTexture() を呼び直さない**（key が同じなら素通り）ので、
    **まだ板が参照しているテクスチャを dispose** してしまい、中身の無いテクスチャを貼ったまま描き続ける
    ことになる。しかも押し出す側は毎フレーム増える（マスの点滅 `move-highlight-blink` は背景色を
    `color-mix` で連続的に変えるので、細かく見分けると1フレームごとに新しい見た目になる）ため、
    49マス＋28スロットの分まで巻き添えで捨てられ、次のフレームで作り直し→GPUへ転送、を延々と繰り返す。
  - **修正**: 片付けを作り直しの最後にまとめて行う `pruneShapeTextures()` に移し、**今どの板も使って
    いないものだけ**を捨てる。あわせて churn そのものを減らした——色の段階を 8→16 に、不透明度を
    20段階→10段階に、グローのぼかし/広がりを2px刻みに丸め、上限を48→80、使ったものは列の最後尾へ
    入れ直す（LRU）。
  - **教訓**: 共有キャッシュから要素を捨てる時は、**まだ誰かが参照していないか**を必ず確かめる。
    参照カウントを持たないなら「今使っているものの集合」を作ってから捨てる。
- **【要望】前のターンの行に「誰のターンだったか」**: `clearTurnEventStock(ownerName)` に名前を渡す形に
  した（turn-event-stock.js から `player-identity.js` を import すると `online.js` 経由で循環しかねない、
  という続き428の申し送りどおり）。main.js が直前の手番の席を覚えておいて `getPlayerName()` で解決する。
  文言は `tes.prevClosedBy` / `tes.prevOpenBy`（ja/en）。
- **【要望】移動モーションの拡張**: ユーザー選択「移動方向に前方を持ち上げて、よっこいしょ／ジャンプ台の
  時は跳ねを大きく／ディメンションの時はワープ／試練の儀式などの連続移動では通り道に光の軌跡」。
  - **よっこいしょ**: `.piece-hop-yaw`（進行方向を +X に合わせる）→ `.piece-hop-pitch`（その辺を
    持ち上げる。ここだけアニメ）→ `.piece-hop-yaw`（向きを戻す）の3枚重ね。**立方体そのものは回さない**
    ——面ごとの陰影が固定なので、回すと暗い面が正面に来て光の向きが破綻する（続き428で「半回転は
    おすすめしない」と書いたのと同じ理由）。盤面は rotateX で傾いているので、画面上の縦の差は
    cos(傾き) で割って**盤面の上での方向**に直してから角度を出す（そうしないと斜め移動で持ち上がる
    向きがずれる）。
  - **移動の見え方を3種類に**（`PIECE_MOVE_STYLES`）: hop（1マス）／jump（跳ねの高さ2.1倍・時間1.5倍）／
    warp（跳ばずに縦へ伸びて消え、移動先で戻る。位置の瞬間移動は「消えている一瞬」に行う）。
    振り分けは、通常移動＝ディメンションで移動範囲が伸びていれば warp・それ以外 hop、
    カード効果の移動＝距離1で hop・2〜3で jump・4以上で warp（自分のゲートへ戻る等、盤面を横断する
    移動を跳ねさせると間延びするため）。
  - **カード効果による駒の移動にも演出を付けた**（続き406では通常移動だけだった）。`moveAndSyncForEffect`
    ——engine の駒移動が必ず通る1か所——で、駒のマス→マス移動なら `playPieceMoveAnimation` を await する。
    マスチェンジの入れ替えは専用演出があるので `animOpts.skipPieceMove` で外す。
  - **光の軌跡**: 1回の移動ごとに細い光の帯を1本置いてゆっくり消す（`.piece-move-trail`）。連続で動くと
    帯が重なって「通ってきた道」になる。同じ駒が 2.6 秒以内に続けて動いた時だけ濃く・長く残す
    （＝試練の儀式のような繰り返し移動で自然に道になる。単発の移動はうっすら）。
- **テストの直し**: `test/effect-runner.mjs` の `discardAndSync` スタブが第2引数を「捨て先の山」として
  受けていたが、本物（`discardFromHandReveal`）ではオプション（`{silent}`/`{noBurn}`）。文字列の時だけ
  山名として扱うようにした（そのままだと 12件 FAIL）。
- **検証**: `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` **56/56 PASS**、
  `node test/smoke.mjs 2` PASS、CSSブレース平衡、お知らせ ja/en とも10件。ヘッドレスでの実測は
  下記コミットメッセージ／会話ログ参照。サーバー側（Supabase）の変更は無い。
- **【自分で見つけた別の不具合・同時に修正】WebGL描画がONの間、ドラッグ中と移動中の駒が
  「まっさらな箱」になっていた**: `body.board-3d-on .piece-face { background-image: none !important }`
  等の「絵を剥がす」ルールに `#game-table` の限定が無かったため、**盤面の外**に置く駒——ドラッグの
  ゴースト（`.drag-ghost-piece-inner`）・今回の移動ゴースト（`.piece-hop-ghost`）・左下の駒スキンの
  プレビュー——の絵まで消していた。WebGLが描くのは `#game-table` の中を走査して見つけた要素だけなので、
  盤面の外の同名要素には**代わりに描くものが無い**＝無地になる。実測（3D ON）で、盤面の外に置いた
  `.piece-face` の背景が `none`（修正後は `url(.../assets/pieces/...)`、盤面の中は `none` のまま）。
  続き406で3D描画を既定ONにした時から続いていたはず。CSS側と board-3d.js の剥がすルール（stripSheet）の
  両方を `#game-table` の中に限定した。
  - **教訓**: 「WebGLが描くから、CSSからは絵を剥がす」という対の作りでは、**剥がす範囲と描く範囲を
    必ず同じにする**。描く側が `#game-table` を走査しているなら、剥がす側のセレクタにも同じ限定を書く。
- **実測（ヘッドレス）**:
  - CSS単体: `piece-hop-lean`/`piece-warp-lift`/`piece-move-trail` がいずれも実際に効く
    （lean は 0.9秒時点で rotateY≒12.9°、warp は 2.1秒時点で 横0.097倍・縦2.25倍、trail の不透明度は
    `--trail-peak` 0.55 のとおり 0.548）。
  - CPU自己対戦260秒（turn 6 まで）: 移動ゴースト7回すべてに yaw と lean が入り、面は5枚。
    継続時間は 450ms（1マス）と **675ms（=450×1.5、2マス以上のジャンプ）** の2種類、跳ねの高さは
    27/28/34px（hop）と **57px（jump）**。光の軌跡7本。使用演出と選択モーダルの重なり **0**。
    前のターンの行の見出しが「▸ 前のターン（CPU）」「▸ 前のターン（プレイヤーA）」になることを確認。例外0件。
  - #276-(2): ムーブフェイズ中に画面のハイライトを強制的に剥がしても、状態から数えた候補は
    **3件のまま**（DOMは 3→0）。
  - #276-(1): `handEffectBusy` を立てて `forceEndCurrentPhase()` を呼ぶと、フェイズは "hand" のまま
    動かず、busy を下ろすと "move" へ進む。
  - #275: WebGL描画ONで75秒回して `texEvicted` **0**（報告時のログでは180まで増え続けていた）。
  - **未確認**: ワープ（ディメンションでの移動・4マス以上の移動）は自己対戦中に発生しなかったため、
    CSS単体でしか確かめられていない。実機での見え方の確認をお願いしたい。
