# 説明書 組版メモ（PDFレイアウト版）

作成日：2026-08-21
対象：アナログ版（ボードゲーム本体）の説明書

| ファイル | 内容 |
| --- | --- |
| `説明書_組版.pdf` | **完成イメージのPDF（全24ページ）** |
| `img/` | PDFに貼り込んでいる画像。`画像素材/` の実データを、印刷に必要なサイズまで縮小してJPEGに変換したコピーです（PDFを軽くするため。元データは触っていません） |
| `説明書_組版.html` | 上記PDFの元データ。ブラウザで開けば同じ見た目で確認できます |
| `説明書_校正版.txt` | 流し込んだ原稿（テキストのみ） |

---

## 判型・仕様

- **216 × 216 mm の正方形**（旧版PDFと同じ判型に合わせました）
- 全24ページ（本文23ページ ＋ 追加ページ案1ページ）
- 表紙は含んでいません（別デザインのため）
- 背景の紙テクスチャ・フッターの飾り罫はCSSで描いています
- 中央の透かしは**通常カードの裏面の紋様**（`img/watermark.png`）を薄く敷いています

## 盤面の比率（実物どおり）

プレイマットの実データを解析して、図の比率を実物に合わせました。

- 場（7×7）：盤面全体の70％、1マス＝10％
- ロックエリアバー：3.33％（＝場の1辺の1/21。実物のバーは 5000×218px ＝ 約23:1）
- ロックエリア（カードを置く場所）：11.67％（各辺7スロット）

## 旧版から踏襲した見た目

- **大見出し**：セリフ体のスモールキャップス（`LAYOUT` のように頭文字だけ大きい形）＋ 下に明朝の日本語サブタイトル
- **7色のピル型ラベル**（`①ロックエリア` など）
- **フローチャート**（角丸＋矢印）
- **吹き出し**（※注意！／※ポイント！）
- **フッター**：左右に小さな飾り＋中央にページ番号
- **本文はゴシック体、物語・情緒的な文面は明朝体**

## 図（実際のカード・駒・バーの画像＋描き起こしのベクター図）

| ページ | 図 |
| --- | --- |
| 3 | 7色のキューブ（実物の紋様画像） |
| 4 | 内容物（実物のカード裏面3種・駒7個・ロックエリアバー）、カードの各部名称（実物のカード画像＋引き出し線） |
| 5 | レイアウト図（実物のカード裏面・バーをモノクロで敷き、指し示す箇所だけ色枠）、準備完了図（カラー＋4色の駒＋ファーストカード） |
| 6 | 山札から49枚を配置する図（実物の画像） |
| 7 | 斜め俯瞰の盤面で7色そろった状態（駒は立体に起こしています）、ロックの正誤3パターン（実物のロックエリアバー＋カード置き場） |
| 8 | ターンのフローチャート、時計回りの手番循環図（実物の駒） |
| 9 | 3フェイズの流れ帯、ロック（実物のバー＋カード置き場）／ハンドの挿絵 |
| 10 | 移動できる4マス、接触の図（実物のカード裏面・駒） |
| 11・12 | プレイ例の挿絵（実カード・ミニ盤面・ロックエリア） |
| 13 | 手札半分を奪う図、ゲートへの強制移動図（盤面全体で表示） |
| 14〜17 | カード集（実物のカード画像＋効果シンボルの実アイコン） |
| 22 | QRコード（**ダミーです。実物と差し替えてください**） |

---

## ご確認・ご相談したいこと

1. **配色の方向性**
   旧版に合わせて「生成りの紙＋黒文字＋7色アクセント」にしています。
   世界観（色が消えた世界）を出すダーク基調にすることもできます。

2. **フォント**
   いまはWindows標準の游明朝／游ゴシック＋Palatinoで組んでいます。
   本番で使いたい書体（旧版はTrajan系の英字書体に見えます）が決まっていれば教えてください。

3. **12〜15ページ（カード集）について**
   `画像素材/`のカード画像と `カード効果　テキスト.txt` から自動生成しています
   （12：ファーストカード7枚／13：エターナルカード7枚／14〜15：通常カード19種）。
   効果テキストを直したら再生成できます。フレーバーテキスト（Ω）は誌面の都合で省いています。

4. **カードの各部名称（4ページ）の番号**
   実物のカード画像を見て番号を配置しました。⑨エディションマーク・⑩カード種別表示の
   位置がこちらの推測です。違っていたら教えてください。

4. **19ページのQRコードはダミー**です。公式サイトのURLをいただければ本物を生成します。

5. **20ページのクレジット**
   旧版（リメイク前）のものをそのまま置いています。リメイク版の正しい内容に差し替えるか、
   削除するかをご指示ください。

6. **21ページ（重要な原則）の差し込み位置**
   いまは最終ページに置いています。7ページ「勝利条件」の後や、15ページ「基本用語集」の前に
   移すこともできます。

---

## この後の使い方

- **イラレでの本組みのラフとして使う**：レイアウトの当たりや文字量の目安として
- **このまま清刷りに近づける**：写真・イラストの差し替え、フォント指定、色調整を続ける
- **PDFの再生成**：`説明書_組版.html` を直せば、同じ手順でPDFを作り直せます

修正したい箇所（文字を大きく、図を差し替え、ページを入れ替え など）を教えていただければ、
その場で直してPDFを作り直します。

### 図の色分け（5ページ）

- 5ページ 図1 の引き出し枠は、①赤 ②橙 ③黄 ④緑 ⑤青 ⑥桃 ⑦紫 ⑧灰 の順で、7色を番号順（虹の順）に使い切っている。
- 3ページのキューブ紋様は 4・8ページと同じ `img/piece-*.jpg`（0thリメイク）に統一。

### 4ページの図

- 図2＝通常カード「なないろの欠片」（①〜⑪）／図3＝通常カード「マスチェンジ」（⑧ 追色シンボル）／図4＝ファーストカード「フェニックス」（⑫ 効果名・⑬ ロックエリア使用可能表示）。
- ⑫⑬はファースト／エターナルにのみ存在する項目。

### 10・11ページの図

- 10ページ 図3 は「移動できるマス（緑）」と「移動できないマス（赤✕＝カードが無いマス／相手の駒がいるマス）」を併記。
- 11ページ 図1 はゲートからのスタートなので、駒の手前にロックエリアバー（`img/bar-mid.jpg`＝中央3色）を敷いている。

### カード集（14〜17ページ）のテキストの出どころ

- カード名・効果・補足は、プロジェクト直下の **「カード効果　テキスト.txt」が正**（アプリ側の docs/cards.md とは別に、この説明書はこの txt を参照する）。
- 2026-08-22：ファースト／エターナル全14枚の★基本効果に「**その際にこれは捨てない。**」が追加されたため反映。あわせて4ページ⑬の注記とFAQ（ロックしたカードの手札効果）にも同じ趣旨を補記した。
- 反映後は txt と HTML を全カード照合して差分ゼロを確認している（通常カード19枚は変更なし）。

### 2026-08-22　構成の変更（24→26ページ）

- **24ページ Credits（クレジット）** を新設（23ページ Special Thanks の本文と分けた）。テストプレイヤー名は戦績管理システムの `players`（status=approved）から作成。0戦の方も Special Thanks に掲載、ダミー名「プレイヤー」は除外。名前を増やす／削る時はこのページの `.credit` を直す。
- **25ページ Quick Reference（早見表）** を新設。ターンの流れ／ムーブ／ロック／勝利条件／侵攻ボーナス／こんな時は／カードの効果の見かた／覚えておきたい原則を1ページに。詰まりやすいので `.qref` のフォントと `.qr-box` の余白で調整する。
- 目次（1ページ）は行間を詰めて15項目に対応。
- **説明書_校正版.txt は「説明書_組版.html」から自動生成**する（scratchpad の `gen_txt.mjs`）。手で直さず、HTMLを直してから再生成すること。

## 2026-08-27　0th EDITION復活・原則の注記移動・早見表並べ替え・クレジット仮名・FAQ追加

- 用語集の【国宝キューブ】【駒】に「0th EDITION」を復活（ファーストカードのみ削除のまま）。
- 「原則」の意味の注記を1ページ→**5ページ**へ移動（凡例の最後にダークピルで追加）。
  ※ユーザーの想定はP7だったが、本文で「原則」が最初に出るのは実測で**5ページ**（次いで6・7）。
- 早見表（25ページ）：**ロック**の説明を**ムーブフェイズ**の上に並べ替え。
- クレジット（24ページ）：Testing Team／Special Thanks を仮名「○○〇」に差し替え（16名分／24名分）。
  **Crowdfunding Supporters**（ご支援プラン別）を新設＝Rainbow / Seven / Color の3ランク（6／12／20名分、いずれも仮名）。
  ランク名は仮。実プラン名が決まったら `.crhead` 直後の3行の `.ck` を差し替える。
- FAQを5件追加：
  - 【ロック】ロックしたカードを自分の意思で手札に戻せますか？→ 原則、戻せない。
  - 【手札】山札も捨て場も無くなったらドローは？→ 引けない（善処の原則で引ける分だけ）。
  - 【その他】誰も座っていない席のゲートに侵攻→ ボーナスは発生しない。
  - 【その他】2人同時勝利→ ない（7色ロックの瞬間に確定）。
  - 【その他】**同時ゲート侵攻**→ すべて成立。1〜3は処理順の原則で1人ずつ、**4「ゲートに戻る」は成功者全員が同時**。
- 13ページ「4 ゲートに戻る」にも同じ注記を追記（例外条項を作らず「同時に行う」で解決＝
  ゲートの持ち主は1人ずつなので、1マスに2駒が乗る状態が生じない）。
- 溢れ対策：P5凡例7.4→7.2pt＋原則行7.1pt、FAQ 7.1→6.8pt・`.qa` margin1.4mm/line-height1.4・
  `.faqcat` 7.8pt、P24の余白（credit margin 3mm／リード4mm／末尾5mm）。
- `説明書_校正版.txt` を再生成（26ページ／1335行）。

## 2026-08-27（続き）　原則注記のさりげなく化・空席ゲートの言い回し・奥付追加・追加ページ廃止

- **P5の「原則」**：黒ピルの項目をやめ、凡例の最後に「※」書きの小さな注記（6.7pt／#5a5344）に変更。
- **FAQ（空席のゲート）**：「ボーナスは発生しません」→「**そこはゲートではありません**。ゲートは参加している
  プレイヤーの分だけ存在します。参加していない席の場所は、ゲートではないただのマスとして扱います。」
  設問も「2人・3人プレイのとき、空いている席の場所に侵攻すると…」に変更。
- **奥付（権利表記）をP24クレジット下部に追加**（`.colophon`）。内容は仮：
  `© 2026 ○○○　7 SHADES OF S:EVEN　All Rights Reserved.` ／ `発行：○○○　発行年月：2026年○月　お問い合わせ：○○○＠○○○`
  → 発行元・連絡先・発行年月が決まったらこの1ブロックを差し替える。
- **Key Principles（追加ページ案）を廃止**し、差分を**早見表（25ページ）**へ寄せた：
  - 「カードの効果の見かた」に、到達効果＝**乗った瞬間に自動発動・処理後は原則手札に加わる**、
    手札効果＝**原則そのカードを捨ててから発動**、※効果文にカードの処遇が書かれていればそちらに従う、を追記。
  - 新ボックス「**まちがえやすい用語**」＝強制移動の定義／「接触」と「移動」は別物、を追加。
  - 原則の一覧（1番上・句点・処理順・善処）は従来どおり早見表の「覚えておきたい原則」に集約。
- **総ページ数 26 → 25**（`build.mjs` のページ数ガードも 25 に変更）。目次は元々この案を載せていないため変更不要。
- 溢れ対策：P24（`.cr` margin1.8mm／names 8.2pt・行間1.55／各余白）、P25（`.qref` 7.2pt／`.qr-box` padding1.3mm・
  margin1.6mm／`.qr-b` margin.8mm・行間1.5）。
- `説明書_校正版.txt` を再生成（**25ページ**）。

## 2026-08-27（続き2）　遊び心の仕掛け ― 採用は「S:EVENは7回だけ」のみ

### 採用：② 「S:EVEN」は説明書全体で7回だけ
**表紙のロゴ（1回）を見込んで、本文は6回に調整済み。**（表紙1＋本文6＝7）
本文の出現箇所（**この6か所以外で S:EVEN 表記を増やさないこと**。増やす時はどれかを「セブン」に置換して数を保つ）：
1. 3ページ 概要「7 SHADES OF S:EVEN（シェイズオブセブン - 通称：セブン）の世界では…」
2. 18ページ 用語集【セブン】
3. 23ページ Special Thanks「7 SHADES OF S:EVENは、多くの皆様の応援によって…」
4. 24ページ クレジットの大きなロゴ表記
5. 24ページ 奥付の © 行
6. 24ページ 種明かし「※「S:EVEN」の文字は、この説明書に7回だけ現れます。」（`.sevenote`）
- 3ページ末尾はもともと「さあ7 SHADES OF S:EVENの世界へ飛び込もう」だったが、表紙ぶんを空けるため
  **「さあセブンの世界へ飛び込もう」に変更**（直前で「通称：セブン」と説明済みなので自然）。
- **表紙に S:EVEN を使わないことになった場合**は、ここを元に戻して7回にすること。
- `<head>` の `<title>` は印刷されないので数に入れない（HTML全体のgrepでは1件多くヒットする）。

### 不採用（実装したが撤去）
- ① なないろの欠片49個（全ページの余白に七色の菱形）… ユーザー判断で不要。
- ⑤ 第8の枠（23ページ末尾の「まだ見ぬカード」の伏せ枠）… ユーザー判断で不要。
- ④ 記憶を失った青年の手記 … アナログ版に青年が登場しないため不採用。
  （どうしても入れるなら「ある国王の手記」に置き換える案は残っている）

## 2026-08-28　8pt版（`説明書_組版_8pt.html` / `.pdf`）＝アートディレクター確認用

**目的**：AD指摘「現実的な文字サイズ（8pt以上）にすると余白がなくなる」を実物で検証するための版。
**元版（25ページ）はそのまま維持**し、別ファイルで作成している。以後もこの2系統を分けて扱うこと。

### 実測（元版）
- 全22,831字のうち **79%が8pt未満**。本文6.8〜7.2pt、カード集5.6〜6.2pt、用語集6.4pt、FAQ6.8pt。
- 全文字の**64%が後半9ページ**（カード集4・用語集2・FAQ2・早見表1）に集中している。

### 8pt版の作り方（再現手順）
1. `説明書_組版.html` をコピー → `説明書_組版_8pt.html`
2. `up8.mjs`：`font-size:Xpt` のうち8pt未満を8ptへ（51箇所）。見出し格（.cc-name 9pt／.term .tt・.faqcat・.cc-cat 8.8pt）だけ本文より上に戻す。
3. `resplit.mjs`：カード集4→8ページ（4/3・4/3・6/4/4/5枚）、用語集2→3ページ（15語ずつ）、FAQ2→3ページ。
4. `faqfit.mjs`：FAQは実測高さで再配分（11/13/9項目）。
5. `splitp4.mjs`：内容物ページを「Components」と「Card Anatomy（カードの見かた）」の2ページへ分割。
   ※右列（各部名称）は幅104mm→170mmに広げて凡例2列を活かすと収まる。
6. `tighten.mjs` ほか：レイアウト図を78mm→68mmに縮小（ラベルのmm座標も同率で変換）、準備完了図・早見表の余白調整。
7. SVG内の `font-size` 属性も実測8pt以上になるよう拡大（P9は1.24倍、P11は7.9→10.7）。`.tref` の `.78em` は 8pt 固定に。
8. `renum.mjs`：フッターのページ番号を通しで振り直し。`toc8.mjs`：目次のページ番号を更新。本文の「18ページ」→「22ページ」。
- 検証：`check8.mjs`（溢れ）／`fontaudit8.mjs`（8pt未満の字数）。**8pt未満 0字**、溢れは6・8ページに各1mm程度（実際のフッターとは重ならない）。

### 結果
| 区画 | 元版 | 8pt版 |
|---|---|---|
| 目次〜概要 | 1〜3 | 1〜3 |
| 内容物 | 4 | 4（内容物）＋5（カードの見かた） |
| レイアウト〜ゲート侵攻 | 5〜13 | 6〜14 |
| **カード集** | 14〜17（4p） | **15〜22（8p）** |
| **用語集** | 18〜19（2p） | **23〜25（3p）** |
| **FAQ** | 20〜21（2p） | **26〜28（3p）** |
| オンライン〜早見表 | 22〜25 | 29〜32 |
| **合計** | **25ページ** | **32ページ** |

### 8pt版の検証方法（重要）
- **HTMLのcomputed styleではなく、書き出したPDFから直接フォントサイズを読む**こと。
  `pdfsize.mjs`（pdfjs-dist の `getTextContent()` の `transform` から実効ptを算出）。
  元版を同じスクリプトで測ると 5.5〜7.9pt が大量に出るので、測定自体の妥当性も確認できる。
- 現状：8pt版は**最小8.0pt／8pt未満0件**（32ページ・23,121字）。
- **測定対象外**＝カード画像(.jpg)に印刷されている効果文（画像なので測れない）。これはカード現物の話で、
  ADも「カードは5ptでもギリ許される」としているため冊子の文字サイズとは別扱い。
- 全ページ右上に確認用の見本（`.ptcheck`／赤字「8pt あア亜Ag ← 本文の最小サイズ」）を入れてある。
  `.inner` 直下の絶対配置なのでレイアウト・溢れ判定には影響しない。**納品前には外すこと。**

### 8pt版の改訂（32 → 30ページ）
1. **図中の文字は8pt厳守の対象外に戻した**（P9のフローチャート・P11の「ゲートへ」）。
   8ptまで拡大すると枠から文字が溢れ、図が壊れる。図の記号ラベルは「読む文章」ではないので元のサイズを維持する。
   → 現在8pt未満は **81字（全体の0.3%）＝図中のラベルのみ**。本文・説明・カード効果文・用語集・FAQ は全て8pt以上。
   ※「1ターンの流れ」の図が見にくくなったというユーザー指摘もこれが原因だった。
2. **用語集 3 → 2ページ**：`.term .tt` `.tb` を `display:block` から **`display:inline`** に。
   用語名と説明が同じ行から始まるので1語あたり1行減る（合計707mm → 602mm）。あわせて margin 1.35→0.95mm、
   line-height 1.4→1.36、column-gap 6→4.5mm。
3. **カード集 8 → 7ページ**：`ccpack.mjs` で**カテゴリをまたいで貪欲に詰める**方式に変更
   （1ページに「ファースト（つづき）」＋「エターナル」が同居できる）。あわせて `.cc-img` 15.5→13.5mm、
   `.cc-ef` 行間1.36→1.3、`.cc-note` 1.28→1.24、grid gap 1→0.6mm、`.cc` gap 2→1.6mm。
   最終ページが1枚だけになるのを避けるバランス処理も入れてある（4枚/3枚に分散）。
- 検証スクリプト：`ccuse.mjs`（カード集ページの余白）／`gmeasure.mjs`（用語集の総高さ）。

### PDFの書き出し（v版の運用）
`build.mjs` / `build8.mjs` は、**本体PDFが開かれていて上書きできない場合、自動で `_v2` `_v3` … として書き出す**
（例：`説明書_組版_8pt_v2.pdf`）。ユーザーがPDFを閉じたら、v版を消して本体を書き出し直すこと。

### 8pt版の改訂2（30 → 28ページ）
1. **6ページ 図1の崩れを修正**：レイアウト図を68mmに縮めていたため、8ptにしたラベルが引き出し線を覆い、
   ラベル同士も重なっていた。**図1を元の150×78mmへ戻し**、ラベル座標も元に戻した（`.lf-lb` の mm を ×78/68）。
   その分の高さは、図2（`.setupfig`）を62→54mmに縮め、**凡例を1列→2列**にして吸収した。
2. **6ページ 凡例の折り返し**：`.legend2 .it` を `flex` から `block` に変え、ピルを `inline-block` にして
   **テキストがピルの下にも回り込む**ようにした（数文字だけ次行に落ちるのを軽減）。
3. **用語集**：ユーザー指示で**用語名は独立行に戻した**（`.tt` を `display:block`／8pt・太字）。
   代わりに**説明文の `<br>` を「句点・）・」の直後以外は削除**（34件削除・37件維持）。
   `.term` margin 0.75mm・line-height 1.32。結果 **3ページ**（用語名を独立行にすると2ページには収まらない）。
4. **カード集**：ユーザー指示で**効果テキスト（`.cc-ef`）を全削除し、補足（`.cc-note`）だけの構成**にした。
   効果はカード現物に印刷されているため。**7 → 4ページ**（33枚中3枚は補足なし＝カード名と画像のみ）。
- 結果：**28ページ**（元版25ページ）。8pt未満は9ページのフローチャート内 77字のみ。

### 早見表（Quick Reference）の改善
- **このページだけヘッダーのデザイン規則から外し、内容を上から載せる**（ユーザー指示）。
  section に `page-qref` クラスを付け、`.page-qref .head` で h1 を15pt・sub 8.6pt・余白を圧縮（約30mm節約）。
- **項目名を独立行に**：`.qr-k{display:block;}` を新設し、**段落の先頭にある `<b>` だけ**にこのクラスを付ける（13件）。
  アイコン（`span.ef` / `img.efic`）で始まる3件は `</b>` の直後に `<br>` を入れて同じ見た目にした。
  ※`.qr-b > b:first-child` では**文中の `<b>` にも当たる**（CSSの `:first-child` はテキストノードを数えないため）。
  「カードと**同じ色**の…」の「同じ色」まで改行されてしまったので、専用クラス方式に変更した。

### 用語集・FAQの削減（28 → 26ページ）
**判断の基準に「その言葉が実際に使われているか」を追加した**（`usage.mjs`：カード効果テキストと
説明書本文〈用語集ページを除く〉での出現回数を数える）。「周囲」を調べたところ、エターナルカードの
コノハナサクヤ《恋の訪れ》の1箇所だけで使われており、**【周囲】は必要／【相手の周囲】は一度も
使われていない**と分かった。この基準で洗い直すのは有効なので、用語を足す時も同じ確認をすること。

**用語集 45 → 36語**
- 削除9語：相手の周囲・場の中央（**どこにも出てこない**）／相手・自分・プレイヤー・通常の移動・
  何もないマス（自明）／国宝キューブ（【駒】に既出）／セブン（3ページで説明済み）
- **表記を実物に合わせた**（カードを読んで用語集を引いた時に見つかるように）：
  【1番少なくロックしているプレイヤー】→【1番少なくロックしている】、【手札の半分】→【半分】、
  【プレイヤー全員】→【全員】。改名した2語は五十音順の正しい位置へ移動（全員＝接触の後／半分＝場の後）。
  ※**【追色S】はユーザー指示でそのまま**（今後「追色３」等が出るため、Sは変数の意味）。
- 533mm → **2ページ**（20/16語）

**FAQ 28 → 20問**（削除8問。**Q25「1マスに駒は複数置けるか」はユーザー指示で残した**）
- 削除：Q2（用語集と完全重複）／Q9・Q11・Q12・Q14・Q17（本文・早見表に同じ答え）／
  Q22（フェイズの考え方。61mmと最大で、9〜11ページと早見表に三重に記載）／Q23（用語集＋早見表）
- 552mm → **2ページ**（11/14項目）

**目次と本文の参照を更新**：目次7項目、本文の「22ページ」→「19ページ」4箇所。
- 結果：**26ページ**（元版25ページ／8pt化前の初版32ページ）。

### 9ページのフローチャートも8pt以上に（完全達成）
SVG（`viewBox="0 0 530 104"`）の**表示幅を 172mm → 176mm（+2.3%）にしただけ**。
viewBox はそのままなので、枠・矢印・文字がすべて同じ比率で拡大され、レイアウトは一切崩れない。
→ 実測 7.9pt → 8.1pt。**これで説明書全体の 8pt 未満が 0 件**になった。
※ 以前 font-size 属性だけを1.24倍して枠から文字が溢れたのは誤りで、**SVGは表示サイズごと拡げるのが正解**。
