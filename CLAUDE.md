# プロジェクト概要

ボードゲーム「7 SHADES OF S:EVEN」リメイク（仮）のデジタル版アプリ。
姉妹プロジェクトとして「7 SHADES OF S:EVEN 戦績管理システム」（対戦記録・ランキングの管理サイト、
単一の`index.html`）が別リポジトリで存在する。**2026-08-28時点の実際の場所は
`G:\マイドライブ\Cloud Code　作業フォルダ\`**（GitHub: `asobuzzasobazunihairarenai-dot/BATTLE-log`。
以前このファイルに書いていた「親フォルダ直下」は古い情報だった）。
**「無関係」ではなく密接に連携している**: 同じSupabaseプロジェクト（`prnddzrnblfysggiuzmo`）を共有し、
本アプリが `players` / `matches` / `match_feedback_replies` テーブルへ直接書き込む（対戦登録・コメント）。
`players.user_id` に本アプリのSupabaseアカウントが紐づく。両サイトとも同じGitHub Pagesのオリジンなので、
本アプリでログインしていれば戦績システム側も自動的にログイン状態になる（続き301の「マイ戦績」参照）。

## ゴールと段階

- 最終ベンチマークは MTGA（Magic: The Gathering Arena）＝カード効果を完全に自動処理する形。
- ただしいきなりそこは目指さず、まずはユドンリウムコネクト（ユドコネ）のような
  「アナログコンポーネントを画面に移植しただけ」の形を目指す。カード効果はテキスト表示のみで、
  処理はプレイヤーの自己申告に委ねる。
- ルールブックやカード効果は今後変更される可能性がある前提で設計する。

## 対戦形態（現状の合意事項）

- 最終的にはオンライン非同期対戦（同時接続ではなく、各プレイヤーが自分のタイミングで手番を進める形）を目指す。
- 実装は1人用（対戦相手なしのテストプレイ用）から始める。
- ただし「1人用→オンライン非同期」の移行で手戻りが出ないよう、最初から下記のアーキテクチャ方針を徹底する。

## アーキテクチャ方針

- ゲーム状態（盤面・手札・山札・フェーズなど）を1つのシリアライズ可能なオブジェクト（JSON）として持つ。
- 状態変更は全て「アクション」として表現し、`現在の状態 + アクション → 次の状態` の純粋な形にする
  （Reduxライクな設計）。1人用の段階ではアクションをローカルで即時適用するだけにしておき、
  オンライン非同期化する際は同じアクションをバックエンドに保存して同期する流れにそのまま乗せる。
- カード効果・カードデータはコードに埋め込まず、外部データ（JSON等）として持つ（変更に強くするため）。

## 技術スタック（現状の合意事項）

- 姉妹プロジェクト（戦績管理システム）に倣い、ビルド不要の単一HTML＋Tailwind CSS（CDN）＋Supabase
  （オンライン非同期対戦のバックエンド）という軽量構成を踏襲する。
- プラットフォームはデスクトップアプリを最終形としたいが、まずはブラウザで動くものを作り、
  将来的にTauri等でラップしてデスクトップアプリ化する想定。スマホとPCのクロスプレイも目標。
- GitHubリポジトリ: https://github.com/asobuzzasobazunihairarenai-dot/asobuzzasobazunihairarenai-dot.github.io
  （公開URLは **https://seven.asobuzz.net/**。2026-09-07に独自ドメインへ移行し、リポジトリ名からゲーム名を外した＝続き474〜476）
  （デフォルトブランチは`main`）。**コミットするたびに`git push`まで行う運用**（ユーザーから明示的に指示済み）。
  実物のプレイマット画像(`assets/playmat.png`)はコピーされたくないという要望があり、`.gitignore`で
  除外した上でgit履歴からも完全に削除済み（`git filter-branch`で過去コミットからも消してある）。

## 作業ルール（Claude Code向け・新しいセッションでも必ず引き継ぐこと）

- **コミットのたびに`git push`まで行う**（上記参照、ユーザーから明示的に指示済み）。
- **`git add -A` / `git add .` を使わない。コミットするファイルは必ず1つずつ明示して `git add` する**
  （2026-09-06・続き452の実害）。このリポジトリは**公開**で、作業フォルダには gitignore し切れていない
  非公開の資料（原稿・素材・メモ）が置かれることがある。実際に `git add -A` が
  `ストーリー/STORY（部外秘）.txt` を巻き込んで公開リポジトリへ push してしまい、履歴の書き換えと
  強制pushで消す羽目になった。**`.gitignore` に書いてあるから大丈夫、と考えないこと**——
  ルート直下の `/*.txt` は**サブフォルダには効かない**（これが今回の抜け道だった）。
  コミット前に `git status --short` を必ず読み、**身に覚えのないファイルが1つでもあれば手を止めて
  ユーザーに確認する**。
- **プレイヤーから見える変更をしたら、アプリ内の「お知らせ／更新情報」（[src/changelog.js](src/changelog.js)）も
  随時更新する**（ユーザーから明示的に指示済み、2026-08-29）。CHANGELOG の先頭に `{ date, items, itemsEn }` を
  1件追記する。**`items`（日本語）と `itemsEn`（英語）の両方を、同じ順番・同じ件数で書く**。表示は
  現在の言語の方だけ（併記はしない）。内容は「プレイヤーに何が変わったか」を、内部用語を使わない
  言い方で書く（不具合番号・関数名・ファイル名は書かない）。開発者向けの詳細はこの CLAUDE.md の
  変更履歴の方に書く。
- **コミットの前に、このCLAUDE.md末尾へ変更履歴を1エントリ追記する**。フォーマットは
  `### YYYY-MM-DD（続きNN）：タイトル`（NNは既存の最大値+1、既存エントリを`grep 続き`等で
  確認してから採番する。過去分は[docs/dev-log.md](docs/dev-log.md)にもあるので、最大値は
  両方を見て採番すること）。何を・なぜ変えたかを、後から読む人（未来の自分含む）が経緯を
  追えるように書く。
- **CLAUDE.mdを肥大化させない（毎セッション全文が読み込まれコンテキストを圧迫するため。
  2026-08-28に続き290以前を、2026-09-05に続き385以前を、2026-09-06に続き432以前を[docs/dev-log.md](docs/dev-log.md)へ切り出し済み）**。本体には
  「概要・アーキテクチャ方針・作業ルール・Phase1の基盤教訓/ハマりどころ・直近の変更履歴」だけを
  残す。日付付き変更履歴（`### YYYY-MM-DD（続きNN）`）がおよそ40エントリを超えて溜まってきたら、
  古い方を[docs/dev-log.md](docs/dev-log.md)の**末尾（続き260の後）へ時系列順で追記する形で**移す
  （内容は消さず移動するだけ。dev-log.mdは古い順＝続き1が先頭・最新が末尾）。過去の経緯・
  不具合の詳細を調べる時はdocs/dev-log.mdをgrepする。
- **`supabase/functions/so7-apply-action.ts`を変更した場合は、ユーザーへの手動再デプロイが
  必要かどうかを必ず明示的に伝える**（Supabaseダッシュボードの Edge Functions 画面へ
  手動でコピペしてデプロイする運用のため、コード変更だけでは本番に反映されない）。
  単純なパススルー等で再デプロイ不要と判断した場合も、その判断自体をユーザーに伝えること。
- **公開済みの `src/*.js` は削除しない**（2026-09-05・続き423の実害）。「もう誰もimportしていない」のは
  こちらのリポジトリの話で、**利用者の端末のキャッシュには古いimportが生きている**。消すとその端末で
  404 →「Importing a module script failed」で**機能が丸ごと起動しなくなり**、しかも読み込みに失敗した
  ファイルは更新時の再取得の対象からも外れるので**永久に直らない**。使わなくなったら中身を空にするか、
  経緯をコメントで残して**ファイルは置いたままにする**。
- **`supabase_setup_so7.sql`にSQLを追記した場合は、ユーザーがSupabaseダッシュボードの
  SQL Editorで手動実行する必要があることを伝える**（このリポジトリからは直接適用できない）。
- 上記のルール自体は本ファイル（CLAUDE.md）に書かれているため、リポジトリ内で作業する
  限り、会話セッションが変わっても自動的に読み込まれ引き継がれる。

## 資料

- **注（2026-09-07・続き474）**: `docs/rulebook.md` と `docs/cards.md`、および `説明書校正/` は
  **git管理から外した**（アナログ版の中身そのものなので公開リポジトリに置かない）。**手元には
  そのまま在る**ので今までどおり読める。控えは `G:\マイドライブ\SEVEN非公開資料バックアップ\`。

- ルールブック: [docs/rulebook.md](docs/rulebook.md)（受領済み・テキスト化済み）
  - 元PDF `説明書.pdf`／`説明書.txt`はプロジェクト直下にあるが `.gitignore` でPDFはgit管理対象外（ローカル参照のみ）。
  - カード集（12〜14ページ）の補足説明は色ごとにまとまっているが、個別カード名との対応はcards.mdと突き合わせ済み。
- カード効果テキスト: [docs/cards.md](docs/cards.md)（受領完了・整理済み）
  - 全33種類（ファーストカード7種、エターナルカード7種、通常カード19種）。
  - 黒カードはルールブック記載（3種類）が誤りで、正しくは2種類（ユーザー確認済み）。
- 盤面レイアウト参考画像: [docs/board-reference/](docs/board-reference/)（ユドンリウムコネクト版の実際のスクリーンショット3枚。準備前/準備完了/ゲーム途中）
  - "Face Down Card Area" "Hands Area"（自分だけ中身が見える表示切替）はユドンリウムコネクトというツール自体の汎用機能であり、本ゲーム固有のルールではない（ユーザー確認済み、設計には反映しない）。

## 設計

- ゲーム状態データ構造: [docs/game-state-design.md](docs/game-state-design.md)
  - GameState / Board / Player / CardDefinition / CardInstance / Action の設計（v1、ユーザー合意済み）。
  - 乱数処理の同期方針: 結果をアクションのpayloadに確定値として含める（1人用⇄オンライン非同期で同じロジックを使うため）。

## Phase 1 実装状況

- 方針: CSS疑似3D（`perspective`+`rotateX`）で開始。3D化が必要になった場合は描画層（`src/main.js`の描画処理）だけを差し替える想定で、ゲームロジックとは分離して実装する。
- モバイル対応: PC（横長ブラウザ）優先。スマホ横向きは意識するが、実機での細かい調整はPhase1完了後に回す（現状、縦幅が狭い画面では上下のプレイヤーゾーンが見切れる）。
- ローカル確認用サーバー: ルートの `.claude/launch.json` に `7shades-digital` という設定名で追加済み（`npx serve 7shades-digital -l 3001`）。ルート直下の`static-serve`(3000番)は姉妹プロジェクト用なのでポートを分けている。
- 静的レイアウト（盤面7x7・4人分のロックエリア/手札ゾーン・共有山札等）を [index.html](index.html) / [src/style.css](src/style.css) / [src/main.js](src/main.js) に実装済み。
- **注意（ハマりどころ）**: CSS疑似3Dの箱に `class="table"` を付けたところ、Tailwind CDNの組み込みユーティリティクラス`table`（`display:table`）と衝突し、`display:grid`が上書きされるバグが発生した。Tailwind使用時はカスタムクラス名がTailwindの既存ユーティリティ名（`table`, `hidden`, `flex`, `grid`, `block`, `container`等）と衝突しないよう注意する（今回は`game-table`に変更して回避）。
- 座席配置: 手前(南)=A(自分)、左(西)=B、奥(北)=C、右(東)=D。盤面を上から見て時計回り(A→B→C→D)がルールブックの「時計回り」ターン順とそのまま一致するよう統一した。
- 手札は「テーブルに置く」のではなく「持ち上げて扇状に持っている」見た目。自分(A)は`rotateX(-40deg)+translateZ`で大きく持ち上げ扇状に開く（上下方向）。他プレイヤー(B/C/D)は逆に`rotateX(18deg)`で盤面側へ伏せ、裏向き柄（`.is-facedown`）にして密集させる（MTGA参考画像の対面プレイヤー表現に合わせた）。全員分カードごとに個別回転＋位置をJS側で計算する扇形式（`layoutFan`参照）。左右(B/D)も上下(A/C)と同じ考え方で縦方向に扇状に開く（扇コンテナ全体を90度回転させる旧方式はカード自体が横倒しになるバグがあったため、個々のカードにのみ回転をかける方式にした）。弧の向き（外側のカードが盤面から逃げる方向）は`ARC_SIGN`で座席ごとに定義。
- 手札・駒・山札系のカードはすべて正方形（このゲームの物理カードが正方形のため）。
- **駒の見た目は何度か作り直した**（試行錯誤の記録）:
  1. 3面をpreserve-3dで組む真の3D立方体 → 盤面のrotateX(42deg)との兼ね合いで正面以外がほぼ潰れる。打ち消し回転（ビルボード化）も試したが逆に正面が潰れて失敗。
  2. 2DのskewX/skewYで組む疑似アイソメトリック立方体（top/front/sideの3枚）→ 見た目は良くなったが、駒がセルより大きくはみ出すため「DOM順で後に来る隣のセル」に隠され、脚だけ残る崩れ方をした（`.cell`同士が同じスタッキング文脈でDOM順に描画されるため）。駒のあるセルに`z-index`を与えて一旦解決したが、複数面構成自体が壊れやすかった。
  3. ユドナリウム(TK11235/udonarium、Angular+TypeScript製、three.js等は不使用でCSSベース)の`game-character`コンポーネントを調査し、非対称な太さ・濃淡のボーダーだけで立体感を出す手法を試した（`.pedestal-grab-border`参考）。頑丈だが、ユーザーから「駒はユドナリウムでいう『地形』に該当する」と指摘があり、参照コンポーネントが違うと判明。
  4. **`terrain`（地形）コンポーネントを調査し、その技法をそのまま移植**。「床」を`translateZ(height)`で持ち上げ、4枚の壁を角ごとに異なる`transform-origin`+`rotateZ`/`rotateX`の組み合わせで側面に貼り付ける、正しい組み方のpreserve-3d立方体（`buildCubePiece`参照、CSSクラスは`.piece-top`/`.piece-wall-front`/`.piece-wall-back`/`.piece-wall-left`/`.piece-wall-right`）。移植直後、壁の高さが常にゼロに潰れる新たなバグが発生したが、原因は**`.piece`に付けていた`filter: drop-shadow()`**だった。CSSの`filter`は要素とその子孫を強制的に2D平坦化する仕様があり、`transform-style: preserve-3d`を無効化してしまう。影を`box-shadow`に変更して解決。この技法なら、駒がどの位置にあってもどの面がどれだけ見えるかはブラウザの3D計算が自動でやってくれるため、以前のようなセル位置に応じたJS側の手動調整（leanFactor）は不要になった。
  - 教訓1: 見た目を真似たい対象（ユドコネ）が実際どう実装されているかをオープンソースで確認する時は、コンポーネント名（駒＝character/terrainのどちらか）を早い段階でユーザーに確認する。
  - 教訓2: `transform-style: preserve-3d`を使う要素には`filter`（drop-shadow等）を付けない。影が欲しい場合は`box-shadow`を使う。
  - 教訓3: `backface-visibility: hidden`もユドナリウム本家では設定されていた（移植時に見落としていた）。真の3D構成を組む際は本家CSSを可能な限り忠実に移植する。
  - 教訓4: 強めの`perspective`（近いカメラ）の下では、駒の上面（カメラに近い＝拡大率が高い）と側面（奥行きがあり拡大率が異なる）の間にわずかな継ぎ目の隙間ができることがある。側面の高さを実際の駒の高さより少し（0.15rem程度）大きくして上面と重ねることで隙間を隠せる。
- 自分の手札カード（`.hand-card.is-self`）は視認性のため他プレイヤーの手札より大きく表示している。それに合わせて`.game-table`のgrid-template-rows（手前の行のみ拡大）、`.zone-bottom .hand-area`の高さ、扇の間隔(layoutFanのspacing)も調整済み。
- 盤面の奥行き感（台形っぽさ）は、`perspective`の値を小さくする（=カメラを近づける）ことで自然に強調できる。手動で盤面を台形に変形するのではなく、既存の3D構成（`perspective`+`rotateX`）のパラメータ調整で対応した（`perspective: 2200px→1150px`）。
- 山札は左上、エターナルカードは左下、捨て場は右下（山札と対角）に配置済み。
- 相手（B/C/D）の手札の弧（外側のカードを盤面から遠ざける演出）は、3D変形と組み合わさると非対称に見える不具合が出たため撤去し、回転のみのシンプルな扇にした（自分の手札は弧を維持）。
- 山札・エターナルカード・捨て場は、枚数に応じて厚みを持つ「積み重ね」表示にした（`.stack`/`.stack-top`/`.stack-front`、`buildCardStack`参照）。駒(`.piece`)と同じ「床+壁」の考え方だが面は上面と正面の2枚のみ。1枚あたり0.6px・最低2.4pxの厚み。初期山札は63枚（通常カード112枚－盤面49枚）、エターナルカードは7枚、捨て場は0枚。将来、盤面マスのカードスタックにも同じ`buildCardStack`を流用する想定。
- ウィンドウサイズが変わっても手札等が見切れないよう、`fitTableToViewport()`で`.game-table`の`scale`をJS側から動的に調整するようにした（`--table-tilt`というCSS変数でrotateX角度をCSS/JS間で共有し、scaleだけを画面サイズに応じて計算）。`resize`イベントでも再計算する。最大1.15倍まで拡大、それ以上大きい画面では頭打ちにしている。
  - **ハマりどころ**: `scale(s)`はX/Y軸の2軸しか縮小しない。駒の高さ等`translateZ`で表現している奥行き(Z軸)はscale()の対象外のため絶対サイズのまま変わらず、ウィンドウを小さくして盤面のX/Y footprintだけ縮むと、駒が奥行き方向にだけ間延びして「縦に伸びた柱」のように見えるバグが発生した。`scale(s)`を`scale3d(s, s, s)`に変えてZ軸も同じ倍率で縮小するようにして解決。3D変形が絡む要素をJSでリサイズする時は、`scale()`ではなく`scale3d()`を使うべき、という教訓。
- ローカルネットワーク内の実機（iPhone等）から確認する場合は `http://localhost:3001` ではなくPCのLAN IPアドレス（例: `http://192.168.10.137:3001`、環境により変わる）でアクセスする必要がある。`localhost`は「そのデバイス自身」を指すため他端末からは繋がらない。
- プレイヤーBの手札は`rotate(180deg)`で回転させている（`.zone-left .hand-fan.is-opponent`）。
- `.zone`（手札エリア・山札エリア等を囲っていた四角い枠）の背景・ボーダー・box-shadowは削除し、透明なレイアウト用コンテナのみにした。
- カラーパレットは実際のゲームの色番号に更新済み: 赤`#c70025` 橙`#ee781f` 黄`#fabe00` 緑`#22ac38` 青`#1bb8ce` 桃`#f19ec2` 紫`#915da3`（`:root`の`--color-*`参照）。
- **実物のプレイマット画像を使用**: `画像素材/プレイマット.png`（1080x1080、gitignore対象＝ローカルのみ）を`assets/playmat.png`にコピー（gitで管理、こちらはコミット対象）。画像はパレット形式(indexed PNG)でPythonが使えない環境だったため、Node.jsの`zlib`でPNGを自前デコードして構造を解析しようとしたが、画像内の細かい模様・陰影のせいで自動検出は不確実だった。最終的に「実際にブラウザで描画して目視確認・調整」という方法で位置合わせした。
  - 教訓5: 複雑なテクスチャを含む画像のレイアウト解析は、ピクセル解析より実際にブラウザで描画して目視確認・調整する方が早い場合がある。特に3D変形（rotateX）がかかった状態では正確な比較ができないので、位置合わせ時は`game-table`の`transform`を一時的に`none`にして平面化する。
  - **設計変更（重要）**: 当初`.arena`の`background-image`として実装したが、2つの問題が判明した。(1) `background-size`で拡大すると、はみ出した部分が要素の外側として切り取られ見切れる。(2) サイズ基準が「アリーナ全体（ロック帯の太さを含む可変サイズの箱）」だったため、`--lock-thickness`を変えるとアリーナの箱ごと変わり、画像も道連れで動いてしまう。これを解決するため、画像は`.playmat-bg`という独立した絶対配置要素にし、サイズ基準を「盤面(`.board`)の固定サイズ」（`--lock-thickness`に影響されない）にした上で、`background-size`ではなく`transform: scale()`で拡大するようにした（`transform`は要素の外側にはみ出しても切り取られない。`transform-origin: center`にしているので盤面中央が拡大縮小の起点になる）。
- **管理者モード（[src/admin.js](src/admin.js)）を追加**: 画面左上の「⚙ 管理者モード」ボタンから、プレイマット画像・山札・捨て場・エターナルカードそれぞれの位置合わせ用パラメータ（`拡大率`/`位置X`/`位置Y`、対応するCSS変数は`--playmat-*`/`--deck-*`/`--discard-*`/`--eternal-*`）とロックエリアの`--lock-thickness`/`--arena-gap`を、グループ分けされたスライダーでその場調整できるようにした。ユーザーが微調整して「出力をコピー」した`:root { ... }`のテキストをもらったら、その値を[src/style.css](src/style.css)の対応する`:root`変数にそのまま反映する運用。開発用ツールなので常時表示可能にしてあり、ゲーム本編のUIではない（将来的に本番ビルドでは非表示にするか検討）。
  - **注意（ハマりどころ、最終的に解決）**: `.playmat-bg`に`z-index: 0`を付けたところ、ロックエリアと盤面（`position`指定のない通常のグリッド要素）より手前に描画されてしまい、ロックエリアが完全に隠れて見えなくなるバグが発生した。`position: absolute`な要素に`z-index`を明示的に数値指定すると新しいスタッキングコンテキストができ、`position`指定のない通常のフロー要素より優先して手前に描画されるため（`z-index: auto`と`0`は同じに見えて挙動が違う）。一旦`z-index`を外し、DOM順（一番最初の子要素＝背面）に任せる方式に戻して解決したが、**これは対症療法に過ぎず、後に別の形で再発した**（詳細は下記「ロックエリアが見えない問題・真の決着」参照）。
  - 山札・捨て場・エターナルカードの`.stack`も同じ考え方（`transform: scale/translate`、`transform-origin: center`）で独立に拡大縮小・位置調整できるようにした。親の`.zone`が`overflow: hidden`だったため、`.pile-zone`に`overflow: visible`を明示して上書きしている（そうしないと拡大した山が見切れる）。
  - 山札等のpos-x/pos-yは当初`%`（自分のサイズ基準、3.2rem角の山では±100%でも±3.2rem程度しか動かせない）にしていたが、プレイマットの角まで動かすには全然足りないと分かったため`rem`（絶対量、±25rem）に変更した。中心からの移動量が大きい要素には`%`ではなく絶対単位を使うべき、という教訓。
  - **ロックエリアが「見えない」問題の真因（ハマりどころ）**: z-index修正後も「ロックエリアが見当たらない」という報告があった。調査の結果、要素自体は存在し正しい位置・色で描画されていたが（`elementsFromPoint`で最前面にあることを確認済み）、3D遠近感（`perspective: 1150px`の近いカメラ + `rotateX(42deg)`の傾き）によって薄い帯（`--lock-thickness`が数remでも）が画面上ではわずか数ピクセルの高さに圧縮され、実質見えなくなっていた。box-shadowでグローを加えても、影も要素と一緒に3D変形で圧縮されるため効果が薄かった。最終的に`--lock-thickness`のデフォルトを3.2remまで上げ、管理者モードのスライダー上限も6remまで拡張して、ユーザーが実際に見ながら十分な太さを選べるようにした（奥の辺＝カメラから遠い辺ほど強く圧縮されるため、上下と左右で必要な太さが異なる可能性がある）。
  - **山札・エターナル・捨て場がプレイマットの下に隠れる問題（ハマりどころ）**: `render()`内で`buildArena()`（プレイマット画像を含む）を各種ゾーンより「後」に`appendChild`していたため、`.arena`が`game-table`内でDOM順的に最後＝最前面に描画されていた。これは`.arena`自身が`position`指定のない通常のグリッド要素であり、`z-index`を使わないスタッキングでは兄弟要素間の重なりは単純にDOM順（後の要素が前面）で決まるため。管理者モードで山札等をプレイマットの位置まで動かすと、画面上で座標が重なった際にプレイマット側が前面に来て隠してしまっていた。`buildArena()`を`render()`内で一番最初に`appendChild`するよう順序を入れ替えて解決（プレイマットが常に一番背面＝盤面のマス目の枠線と同じ「高さ」で最前面に表示される、というユーザーの提案通りの設計にした）。
  - **手札の位置を4人独立で調整可能にした**: `--hand-a/b/c/d-pos-x/y`（範囲±10rem）を追加し、各プレイヤーの手札を盤面中心から個別にオフセットできるようにした（各`.zone-*`のセレクタで対応する`translate()`を追加）。
  - **ロックエリアをCSS Gridのトラックから完全に切り離し、独立配置の帯に再設計した（重要・アーキテクチャ変更）**: 当初は`.arena`のCSS Gridで`--lock-thickness-top/bottom/left/right`を列・行のトラックサイズとして使う実装だった（1つの変数から4辺独立の変数に分割済みだった）。しかしこの実装だと、ロック帯を1辺でも動かす（太さを変える）と`.arena`自体のグリッドサイズが変わり、それに連動して49マス・駒・他の3辺のロックエリア・両プレイヤーの手札・山札・捨て場・エターナルなど、画面上のほぼ全要素が一緒に動いてしまう不具合が発生した（ユーザー報告：「ロックエリアを調整しようとすると...それぞれの位置が追従して動いてしまいます」）。加えて、スロットの縦横比が正方形でなく「少し長方形」になってしまう問題もあった。
    - **根本原因**: ロック帯の太さという「見た目の調整値」を、CSS Gridの「レイアウトを決めるトラックサイズ」に直結させていたこと。トラックサイズを変えれば、当然そのグリッドに属する全要素・さらにその外側で暗黙に連動する要素（`.game-table`側の他のゾーン）まで再配置される。
    - **解決策**: ロックエリアを`.arena`のグリッドから完全に外し、`.playmat-bg`と同じ「`position: absolute` + `transform: translate()`」方式の独立レイヤーに変更した。デフォルト位置は`.board`の実サイズ（`--board-size`という共通変数に切り出した）を基準に、盤面の外側にちょうど1マス分（`--cell`）密着させる形で`margin-top`/`margin-left`により静的に計算する（マスと同じ大きさの帯が49マスの各辺にそのまま隙間なく並ぶ、というユーザーの要望通りのデフォルト）。ユーザーが動かす分は`transform: translate(var(--lock-*-pos-x), var(--lock-*-pos-y))`だけが担当する。`transform`は要素を「見た目上」動かすだけでレイアウト（他要素の位置計算）には一切影響しないため、1辺をどれだけ動かしても他の要素は完全に不動になる。
    - これにより「太さ」の概念自体が不要になった（デフォルトが常にマスと同じ正方形サイズのため）。管理者モードの「ロックエリア」グループは太さ4種＋間隔1種の計5スライダーから、辺ごとの位置X/Y計8スライダー（`--lock-top/bottom/left/right-pos-x/y`、範囲±10rem）に置き換えた。
    - **教訓**: 「調整可能にしたい値」が「レイアウトを決める値（グリッドのトラックサイズ、フレックスの基準サイズなど）」と一致していると、その値をいじるたびに無関係な要素まで連動して動く事故が起きやすい。見た目の微調整用途なら、最初から`transform`（レイアウトに影響しない）で実装するのが安全（プレイマット・山札・捨て場・エターナル・手札は最初からこの方式だったため無事だった。ロックエリアだけ後から同じ方式に作り直す形になった）。
  - 現在のデフォルト値（ユーザーが管理者モードで調整済み）: `--playmat-scale: 1.42`, `--playmat-pos-x: 0%`, `--playmat-pos-y: 0%`。山札・捨て場・エターナルは`scale:1`、`--deck-pos-x: 4.1rem; --deck-pos-y: 0.7rem; --discard-pos-x: -4.1rem; --discard-pos-y: -4.4rem; --eternal-pos-x: 4.1rem; --eternal-pos-y: -4.4rem;`。手札は`--hand-a-pos-y: -1.5rem; --hand-b-pos-x: -0.3rem; --hand-c-pos-y: -2.9rem; --hand-d-pos-x: 0.3rem;`（他は0）。ロックエリアは`--lock-top-pos-y: -1.8rem; --lock-bottom-pos-y: 1.8rem; --lock-left-pos-x: -1.9rem; --lock-right-pos-x: 1.8rem;`（盤面から少し離す方向、他は0）。
  - **手札がゾーンの見えない境界で上端を切られる問題**: `.zone`に共通の`overflow: hidden`があり、`.player-zone`（手札ゾーン）はそれを上書きしていなかったため、`--hand-*-pos-y`で手札を大きくずらすとゾーンの外にはみ出した部分（特に上端）が直線でスパッと切れて見えるバグがあった。`.pile-zone`は既に`overflow: visible`で上書き済みだったのと同じ理由・同じ直し方で、`.player-zone`にも`overflow: visible;`を追加して解決。
    - 副作用として、`overflow: visible`にしたことで手札がゾーン名ラベル（「プレイヤーC」等）の上に重なって隠すケースが出た（DOM順で名前ラベルが先、手札が後なので手札が手前に描画されるため）。`.label`に`position: relative; z-index: 5;`を追加し、常に手札より手前に出るようにして解決。
  - **山札・エターナル・捨て場の枚数表示をゾーン外ラベルから山自体の表示に統合**: 従来はゾーンの上に「山札（61枚）」のような独立した`<div class="label">`があったが、ロックエリアや手札を動かしてゾーン自体の位置がずれると、このラベルだけ置き去りになって浮いて見える問題が出た。ゾーン外のラベルは廃止し、枚数を`stack-top`（山の上面、名前が表示されている場所）に`.stack-count`という小さい行として統合表示するようにした（`buildPileZone`/`buildCardStack`参照。`buildPileZone`から`label`引数自体を削除しシグネチャを簡略化）。
  - **ロックエリアが見えない問題・真の決着（重要なハマりどころ）**: 上記のz-index除去やlock-thickness引き上げをやっても、ユーザーの環境では「ロックエリアだけが常にプレイマットの下に隠れる」という報告が最後まで残った（山札・捨て場・エターナルは`buildArena()`の呼び出し順序を先頭に変えたことで解決済みだったが、ロックエリアだけ直らなかった）。詳しく調査すると、`elementsFromPoint()`では`.lock-slot`が確かに最前面と判定され、`getComputedStyle()`でも色・ボーダー・box-shadowはすべて正常、祖先要素にも`clip-path`/`mask`/`overflow`/`opacity`等の怪しい設定は一切なかった。それでもスクリーンショット上は描画されない、という「ロジック上は正しいのに見た目だけ壊れている」状態だった。最終的に、`.playmat-bg`（z-index:1）・`.board`（z-index:2）・`.lock-area`（z-index:3）という具合に、重なり得る要素全部に一貫した明示的な`z-index`（＋`position: relative`）を割り振ることで即座に解決した。
    - **教訓（最重要）**: complex 3D transform階層（`perspective`+`rotateX`+`preserve-3d`が何重にもネストした構造）の奥深くにある、`position`指定のないflexコンテナは、DOM順・スタッキングコンテキストの理屈上は正しくても、ブラウザの合成（compositing）が期待通りにならないことがある。z-indexは「なるべく使わない」方が安全とは限らない。むしろ**「重なる可能性のある要素全部に、意図した重なり順を表す明示的なz-indexを必ず割り振る」方が安全**、というのがこのプロジェクトで得た結論（前述の「z-index: 0だけ足したら壊れた」という経験則は、"z-indexを一部の要素だけに足す"のが問題であって、"z-indexを使うこと自体"が問題ではなかった、というのが正しい理解）。
- **駒・カードのドラッグ操作を実装（[src/state.js](src/state.js) 新規追加）**: これがこのアプリで初めて「実際に状態が変わる」機能だったため、プロジェクト最初期に決めていたアーキテクチャ方針（状態変更は全てactionとして表現し、現在の状態+action→次の状態、という純粋な形にする）をここで導入した。
  - `state.js`: `{ tokens: [...], piles: { deck, eternal, discard } }`という最小限の状態。`tokens`は駒・手札カードを統一的に扱う配列（各要素は`{ id, kind: "piece"|"card", location, ... }`）。`location`は`{zone:"cell",row,col}` / `{zone:"lock",side,index}` / `{zone:"hand",player}`のいずれか。山札・エターナル・捨て場は個々のカードを持たず、残り枚数のカウンタのみ（実カードデータがまだ無いため）。`moveToken`/`sendTokenToPile`/`drawFromPile`という3つのaction関数のみを公開し、内部の`reduce()`でイミュータブルに次の状態を作る。将来docs/game-state-design.mdの本格的なGameStateへ拡張し、オンライン非同期化時も同じactionをサーバー同期にそのまま使う想定。
  - `main.js`: `render()`が`getState()`の中身に応じて盤面・手札・山の中身を毎回組み立て直す形に変更（ダミー固定データを渡す方式から、状態駆動の描画に変わった）。
  - **ドラッグの実装方式**: 盤面全体が`perspective`+`rotateX`の3D空間の中にあるため、ドラッグ中の要素をその3D空間の中で座標計算するのは複雑。そこで、ドラッグ中はカーソルに追従する「ゴースト」要素を3D空間の外（`document.body`直下）に浮かせて表示するだけにし、実体は動かさない。ドロップ時に`document.elementsFromPoint(clientX, clientY)`でその座標にある要素を調べ、`closest()`で盤面マス(`.cell`)／ロックスロット(`.lock-slot`)／手札エリア(`.hand-area`)／山(`.pile-zone`)のどれに該当するか判定し、該当すればactionをdispatchして`render()`で全体を再描画する（該当する要素が無ければ何もせず、ゴーストだけ消して元の位置に戻ったように見せる）。この方式なら3D変形を一切気にせず実装できる。
  - ルール適用は一切しない（Phase 1の方針通り）。駒はどの盤面マス・ロックスロットへも自由に移動でき、カードは手札↔盤面マス↔山（山札/エターナル/捨て場）のどこへでも自由に動かせる。山からのドラッグは「引く」動作として扱い、手札へ落とした時だけ有効（残り枚数を1減らし新しいカードトークンを生成）。
  - 自分(A)以外の手札・駒も含め、すべてドラッグ可能にしてある（1人でのテストプレイでは全座席を自分で操作するため）。
  - マウス操作は`pointerdown`/`pointermove`/`pointerup`で実装（`touch-action: none`も付与済みなのでタッチ操作でも動くはずだが、実機での確認はまだ）。
  - 検証時のハマりどころ: ブラウザ自動操作ツールの疑似ドラッグ（マウスの座標だけを動かす方式）では、スクリーンショット座標と実際のビューポート座標の縮尺がズレていて、狙った要素の外に離してしまい「何も起きない」ように見えることがあった。実際のアプリの不具合ではなく、検証コード側の座標計算ミス。`PointerEvent`を`dispatchEvent`で直接発行して座標をgetBoundingClientRect基準で厳密に計算し直したところ、駒の移動・手札→捨て場・山札からの引き（山札→手札）・駒→ロックスロットのすべてで正しく状態が更新されることを確認できた。
  - **ユーザー報告バグ1: 自分の手札だけドラッグできない（重要なハマりどころ）**: リリース後、自分(A)の手札カードだけがドラッグ操作を受け付けず、プレイヤーB/C/Dの手札は問題なく触れるという報告があった。原因は各`.piece`/`.hand-card`/`.stack`要素に直接`pointerdown`リスナーを付けていたこと。`document.elementFromPoint()`（ネイティブのヒットテスト、実際のpointerdownイベントがどの要素に届くかを決める仕組み）で自分の手札の座標を調べると、実際に画面に描画されている`.hand-card`ではなく、その奥にある何もリスナーの無い`.zone-bottom`（平坦なコンテナ）が返ってきた。一方`document.elementsFromPoint()`（複数形。見た目通りの重なり順のリストを返す）では正しく`.hand-card`が最前面と判定される——つまり**見た目の描画（compositing）は正しいのに、ネイティブのヒットテストだけが実際と食い違っていた**。自分の手札は`rotateX(-40deg)+translateZ(2.4rem)`で大きく持ち上げられ、テーブルの傾き(42deg)とほぼ打ち消し合ってカメラにほぼ正対する角度になっており、他の座席（`rotateX(18deg)`で盤面側へ伏せている）より3D変形の度合いが大きい。深くネストした3D transform階層でのヒットテストという、ロックエリアの表示バグ（同セッション内の別の問題、上記参照）よりさらに厄介な、ブラウザの別のバグ・限界に遭遇したと考えられる。
    - **解決策**: 個々の要素に`pointerdown`を直接付けるのをやめ、`#game-table`に1つだけリスナーを付け、その中で`document.elementsFromPoint(e.clientX, e.clientY)`（信頼できる方）を使って「実際に何がクリックされたか」を自前で判定する方式（`findDraggableAt()`）に統一した。ドロップ先の判定は元々この方式だったので、開始・終了の両方で一貫した仕組みになった。
    - **教訓**: 深い3D transform階層では、ブラウザの「見た目の描画」と「ネイティブのヒットテスト（クリック判定）」が食い違うことがある——しかも今回はどの辺が食い違うか予測しづらい（左手札は問題なかった）。ネイティブのイベント委譲（要素に直接listenerを付けて`e.target`に頼る）を信用せず、`elementsFromPoint()`で自前判定する方式の方が、この種の3D UIでは安全。
  - **ユーザー報告バグ2: 山から引いたカードが手札に反映されないことがある**: 「実際には引けているのに画面に反映されない、別の場所に成功する操作をした瞬間にまとめて反映される」という報告があった。原因は`onDragEnd()`内の`pileSource`（山からのドラッグ）の分岐で、`drawFromPile()`（state更新）は呼んでいたが、その後`render()`を呼び忘れていたこと（手札へ落とせなかった場合の`else`節にだけ`render()`があった）。stateは正しく更新されるがDOMには反映されず、次に別の操作が`render()`を呼んだ時に、それまで溜まっていた分がまとめて表示されていた。`pileSource`分岐の末尾で無条件に`render()`を呼ぶよう修正。
    - **教訓**: state+action方式では「dispatchしたら必ずrenderする」を徹底しないと、こういう「stateは正しいのに画面だけ古い」バグが起きる。分岐が増えるほど呼び忘れの経路ができやすいので注意する。
  - **ユーザー報告バグ3件（初回リリース直後）**: (1) 手札カードをドラッグしている間、掴んでいるはずのそのカードが手札に残ったまま(ゴーストと二重に)見える。(2) カードをロックスロットにドロップしても置かれず手札に戻ってしまう。(3) カードを盤面49マスに置いても何も描画されない。
    - (1)の原因と修正: ドラッグ中は元のDOM要素をそのまま残し、カーソルに追従するゴーストを別途重ねて表示していただけだった。`startTokenDrag()`でドラッグ開始時に元要素へ`visibility: hidden`を付けるようにした（`onDragEnd`は常に`render()`で全DOMを作り直すため、明示的な後始末は不要）。
    - (2)の原因と修正: `findDropTarget()`のカード分岐が`.hand-area`/`.pile-zone`/`.cell`しか判定しておらず、`.lock-slot`への該当が無かったため、ロックスロットへ落としても`dropTarget`が`null`になり「何も起きず手札に留まる」という結果になっていた。駒・カード共通で`.cell`と`.lock-slot`を先に判定し、カードだけ追加で`.hand-area`/`.pile-zone`も判定する形に統一。
    - (3)の原因と修正（一番本質的な欠落）: 盤面マス・ロックスロットの上の駒を描画する`renderPieceTokens()`が`token.kind === "piece"`のトークンしか処理しておらず、カードが盤面/ロックへ移動しても描画する処理が最初から存在しなかった（stateは正しく更新されるが、対応する描画コードが無かった）。カード用の簡易な見た目（`.board-card`、置かれたマス/スロットにフィットするだけの平たいカード）を追加し、`renderPieceTokens()`を`renderBoardTokens()`に一般化して駒・カード両方を描画するようにした。あわせて、ドラッグ開始判定（`findDraggableAt`）にも`.board-card`を追加し、一度盤面に出したカードを再度つまめるようにした。
    - **教訓**: 「stateのlocationとして定義したゾーン」と「実際に描画コードが対応しているゾーン」がズレていないか、機能追加のたびに両方を見比べて確認する必要がある。今回は駒についてだけ実装して満足し、カードの「盤面/ロックに置く」経路の描画を作り忘れていた。
  - **ドロップ先のハイライト表示を追加**: ドラッグ中、今離すとどこに置かれるかが分かるよう、対象のマス/ロックスロット/手札エリア/山札等を黄金色の枠と光彩でハイライトするようにした。`findDropTarget()`が返す値を`{ zone, ... }`から`{ location, el }`（実際に該当するDOM要素も含む）に拡張し、`pointermove`のたびに前回ハイライトしていた要素からクラスを外し、新しい対象に`.drop-target-active`を付け替える（`updateDropHighlight()`）。レイアウトに影響しないCSSクラスの付け外しだけなので副作用はない。
  - **ロックエリアと手札の干渉問題・真の原因と解決（重要なハマりどころ）**: カードをロックエリアへドロップしようとしても置かれず手札に留まる、という報告があった。ユーザーが`--hand-*-pos-*`で手札を盤面中心から遠ざける調整をしたが直らず、追加したドロップ先ハイライト機能（上記）でようやく可視化できた：手札の当たり判定（`.hand-area`）がロックエリアの帯とほぼ同じ画面位置に重なって表示されていた。
    - **根本原因**: 手札を持ち上げる`transform`（`translate(pos-x,pos-y) rotateX(...) translateZ(...)`）を、それまで子要素の`.hand-fan`にだけ付けていた。すると**見た目**（`.hand-fan`とその中の`.hand-card`）はカメラ側へ大きくリフトされて描画されるのに、ドラッグの当たり判定に使っている`.hand-area`自身は一切transformされない「平らな机の上に置かれたままの」位置に取り残される。つまり「見た目のカードの位置」と「クリック/ドロップ判定の位置」が別の場所にズレてしまい、`.hand-area`（見た目上は空っぽに見える場所）がたまたまロックエリアの帯とほぼ同じ画面位置に重なっていた。`--hand-*-pos-*`で見た目のカード位置を動かしても、当たり判定の位置（transformされていない`.hand-area`自体の位置）は変わらないため、直らなかった。
    - **解決策**: 持ち上げtransformを`.hand-fan`ではなく`.hand-area`自身に付け替えた。transformは子要素からのレイアウト計算には影響しない（親の`.player-zone`内でのフレックス配置は変わらない）ため、見た目は一切変わらず、当たり判定の箱だけが正しく「見た目通りの位置」に来るようになった。
    - あわせて、`.hand-area`のサイズも見直した。以前は「扇が伸びる方向」が常に`width:100%`（盤面と同じ幅）、「伸びない方向」も固定の`5.2rem`/`9rem`だったため、実際に見えている手札の範囲よりずっと広い当たり判定になっていた。`--hand-{a,b,c,d}-size`（扇が伸びる方向、手札3枚を基準に枚数へ比例して自動で伸縮、`main.js`の`buildPlayerZone`でJS側から計算）と`--hand-{a,b,c,d}-thickness`（伸びない方向、固定値）の2種類を管理者モードから調整できるようにし、カード1枚分程度まで絞れるようにした。
    - **教訓**: 3D transformを子要素に付けるか親要素に付けるかで、見た目は同じでも「その要素自身の当たり判定がどこに来るか」が変わる。当たり判定に使う予定の要素には、見た目のリフト用transformも一緒に持たせておくべき（別の要素に分散させない）。
  - **山札から盤面マス／ロックスロットへ直接ドロップできない**: `onDragEnd`の`pileSource`分岐が「手札へ落とした時だけ`drawFromPile`する」という設計になっていた（山から直接盤面へ置く操作は意味が曖昧だろうと判断し、意図的に制限していた）。ユーザーから「山札からカードをマスやロックエリアに置けない」という要望があり、他の操作は全てルール適用なしの自由な移動なのにここだけ制限されているのは一貫性がないと判断し、`zone !== "pile"`（山以外ならどこでもOK）に緩和した。山(pile)自体への直置きだけは今後も禁止のまま（piles型は個々のカードを保持しない残り枚数カウンタのみのため、置き場所として使うとカードが行方不明になる）。あわせて、山から盤面マス／ロックスロットへ直接引いたカードは表向き（`faceUp: true`）になるよう`state.js`の`drawFromPile`を調整した（手札への場合のみ、自分(A)かどうかで表/裏を分ける従来のロジックのまま）。
  - **山札・捨て場・エターナルのドロップハイライトが実物とズレて大きすぎる**: ハイライト対象を`.pile-zone`（グリッド上の枠、実際の山より広く、山自体は`transform: translate/scale`でズラして配置されているためズレる）にしていたのが原因。`findDropTarget`のpile分岐で、`.pile-zone`ではなくその中の実際の`.stack`要素（見た目の山そのもの）を返すように変更し、ハイライトのサイズ・位置が実物と完全に一致するようにした。
- **実際のカードデータを反映（[src/cards-data.js](src/cards-data.js) 新規追加）**: それまでカードは全て中身の分からない灰色の板だった。docs/cards.md・docs/rulebook.mdから実際のカード名・色・枚数を抽出し、外部データファイルとして持たせた（CLAUDE.mdの技術スタック方針「カードデータはコードに埋め込まず外部データとして持つ」に沿う）。
  - 通常カード19種・合計112枚: 赤橙黄緑青桃紫は各色2種×7枚＝98枚、虹（なないろの欠片）1種×7枚、無色（白黒）4種で合計7枚（ユーザー確認：色落ちキャットのみ1枚、他3種＝なないろの巨光/白の意思の覚醒/黒の契約の烙印は2枚ずつ）。ルールブックには「黒3種類・各1枚」とあるが、これは既知の誤記（実際は黒2種類）で、今回の枚数もその修正を反映している。
  - エターナルカード7種・ファーストカード7種（各色1種）もデータ化したが、今回はまだ実際のゲーム内で使っていない（ファーストカードの配布・ロックなどのセットアップ自動化はPhase1のスコープ外、エターナル山は実カード7種をシャッフルして保持するようにした）。
  - `state.js`: `createInitialState()`でNORMAL_CARDSをcount分だけ展開してシャッフルした配列を山札とし、そこから各プレイヤーの初期手札を実際に配るように変更（以前はダミー枚数のみで山札の枚数と無関係だった）。`piles`は単なる残り枚数のカウンタから、実カードidの配列（末尾＝一番上、popで引く・pushで積む）に変更。これにより捨て場に捨てたカードも身元を保ったまま、後で引き直すと同じカードが出てくるようになった（山札・エターナル・捨て場を合わせた合計は常に112/7に保たれる）。
  - 表示: 自分の手札・表向きの盤面/ロックのカードは実際のカード名＋色で表示する（`.is-color-*`、`--color-*`を流用）。相手の手札は引き続き中身を明かさない裏向き表示のまま（`token.faceUp`ではなく`isSelf`で判定、自分の手札は常に見える・他人の手札は常に見えない、という従来の割り切りを維持）。捨て場はルール通り「表向きに積む」場所なので、一番上のカードの名前・色をそのまま`stack-top`に表示するようにした（山札・エターナルは裏向き積みのため中身は明かさない）。
  - **今回のスコープ外だった効果テキストは、後述の実物画像反映で実質解決した**（画像自体に到達効果・手札効果・フレーバーテキストまで描かれているため）。
- **実物のカード画像を反映、カードの表裏をダブルクリックで切り替え可能に**: `画像素材/`配下に実物のカード画像（通常カード19種・エターナルカード7種・裏面3種）が用意されていたため、`assets/cards/`にカードidをファイル名としてコピーして使うようにした（プレイマットと同じ理由で、実際の絵柄はgit管理・公開リポジトリには含めない。`.gitignore`に`/assets/cards/`を追加）。
  - `cards-data.js`に`getCardImagePath(cardId)`/`getCardBackImagePath(cardId)`を追加（裏面は通常カード用・エターナルカード用の2種類があり、`cardId`が`"eternal-"`で始まるかで判別）。画像には既にタイトル・色・効果テキストが描き込まれているため、`.is-color-*`によるテキスト＋色分けの簡易表示は丸ごと廃止し、`background-image`で画像を敷くだけにした（`.hand-card`/`.board-card`/山の`.stack-top`・`.stack-front`）。山札・エターナルの山も裏面画像を敷いて、より物理カードの見た目に近づけた。
  - **表裏のデフォルトを見直した（ユーザー要望）**: 従来は山や手札から盤面/ロックへ移動すると自動的に表向きになっていたが、「手札や山札からドロップする時は基本裏面、自分の手札に加わる時だけ自分に見えるよう表面」という物理カードの直感に合わせて逆にした。`state.js`に`faceUpForLocation(location)`という共通関数を作り、`MOVE_TOKEN`（既存カードの移動）・`DRAW_FROM_PILE`（山から引く）の両方でこれを使って表裏を決めるようにした（移動先が`hand`かつ`player === "A"`の時だけ表向き、それ以外は裏向き）。
  - **場のカードはダブルクリックで表裏反転できるようにした（後に判定方式を修正、下記参照）**: 新しい`FLIP_TOKEN`アクション（`state.js`の`flipToken(tokenId)`）を追加。手札のカードは対象外（手札は常に「自分だけ見える／他人には見えない」という固定表示のため、個々にめくる概念がない）。
  - **ロックエリアの色を塗りつぶしから枠線のみに戻した**: 表示バグ調査時に視認性確保のため`slot.style.background`を色で塗りつぶしていたが、z-index修正で本質的な表示バグが解決していたため不要になっていた。塗りつぶしを外し、`.lock-slot`のCSS既定（暗い半透明背景）＋着色した枠線＋うっすらしたグロー(box-shadow)だけの控えめな配色に戻した（`main.js`の`buildLockArea`）。
- **「山札一覧」ビューアを追加（[src/deck-viewer.js](src/deck-viewer.js) 新規）**: テキストデータ(cards-data.js)と実際のカード画像がちゃんと紐づいているかをユーザー自身が目視確認できるようにする、開発用のデバッグツール。画面右上の「📋 山札一覧」ボタンから開くモーダルで、通常カード19種・エターナルカード7種それぞれについて「サムネイル画像・タイトル・枚数」を一覧表示し、各カードの「補足」ボタンを押すとdocs/cards.mdの「補足:」欄（画像には印刷されていないルール解釈の補足説明）を開閉できる。`cards-data.js`の各カード定義に`note`フィールドを追加してこの補足テキストを持たせた（効果本文自体は画像に描かれているため引き続きデータ化していない）。管理者モードと同様、ゲーム本編のUIではなく開発用ツールという位置づけ。
- **カード裏面画像が「角だけアップになる」バグの真因を特定・修正（重要なハマりどころ）**: 前回「ファイルは正しい、表示サイズの問題では」と暫定回答していたが、ユーザーが「裏面画像の角だけがアップして描画されている」と正確に症状を言語化してくれたことで根本原因を特定できた。
    - **原因**: CSSの`background`ショートハンドは、明示的に指定していないサブプロパティ（`background-size`/`background-position`/`background-repeat`等）も暗黙に初期値へリセットしてしまう。`.hand-card.is-facedown`・`.board-card`・`.board-card.is-facedown`・`.pile-deck`/`.pile-discard`/`.pile-eternal`が`background: <グラデーション>`という形でフォールバック背景を指定しており、これらは`.hand-card, .board-card, .stack-top { background-size: cover; ... }`という共通ルールより詳細度が高い（またはファイル内で後に書かれている）ため、後から効いていた`background-size: cover`を暗黙に`auto`へ戻してしまっていた。結果、JS側で`element.style.backgroundImage`だけを上書きしても、サイズは原寸(auto)・起点は左上(0% 0%)・繰り返しはrepeatのままになり、小さな枠の中に画像(433×433px)の左上の角だけが原寸大で表示される、という症状になっていた。
    - **解決策**: 該当箇所をすべて`background:`ショートハンドから`background-image:`ロングハンドに変更した。ロングハンドは指定したサブプロパティ(background-image)しか変更しないため、他のルールで設定した`background-size: cover`等が無事に効くようになる。
    - **教訓**: CSSショートハンドプロパティ（`background`/`font`/`border`等）は「指定していない部分は前の値を引き継ぐ」のではなく「指定していない部分も初期値にリセットする」という仕様のため、複数の場所で同じプロパティ系統を別々に指定する設計（今回のような「共通ルールでbackground-sizeだけ指定、個別ルールでbackground-imageだけ指定」）をする時は、個別ルール側は必ずロングハンドを使う。
  - **ダブルクリックでの表裏反転が効かない問題も同根のバグだった**: 手札の当たり判定ズレ（`.hand-area`の項参照）と全く同じ理由で、ネイティブの`dblclick`イベントも3D階層内でのヒットテストに頼っているため正しく発火しないことがあった。`dblclick`リスナー自体を廃止し、ドラッグ開始判定（`elementsFromPoint`ベースの`findDraggableAt`、確実に動作することが分かっている）に統合。同じ`.board-card`トークンに対して400ms以内に2回`pointerdown`があったら、ドラッグの代わりにflipToken()を呼ぶ、という自前のダブルクリック検出にした。
  - **ロックスロットの枠線太さを管理者モードで調整可能に**: `--lock-slot-border-width`（デフォルト0.1rem、以前は固定3px）を追加し、`border: 3px solid ...`だった記述を`border-width`/`border-style`/`border-color`に分割した。
  - **プレイヤーB/Dの名前ラベルを90度回転**: 盤面の左右の辺と平行に読めるよう、`.zone-left .label { transform: rotate(90deg); }` / `.zone-right .label { transform: rotate(-90deg); }`を追加（transformはレイアウトに影響しないため、回転してもゾーン内の配置自体は変わらない）。
- **プレイヤー名ラベルの位置を管理者モードで調整可能に**: `--label-{a,b,c,d}-pos-x/y`を追加。B/Dは回転(rotate)の後にtranslateを重ねる形（`rotate(90deg) translate(x, y)`）にし、回転済みの見た目の向きのままtranslateで動かせるようにした。
- **ドラッグ中のカード/駒が実物と違って見える問題を修正**:
  - カードのドラッグゴーストが常に紺色の汎用プレースホルダーになっていた。`createGhost()`/`startPileDrag()`のゴースト生成時にも`background-image`を実際のカード画像（表向きなら顔、裏向きなら裏面）で設定するようにした。捨て場からのドラッグは表向きに積まれている実物画像、山札・エターナルからのドラッグは裏面画像を使う。
  - 駒のドラッグゴーストが立方体ではなく平らな正方形(`.drag-ghost-piece`)になっていた。3D空間の外(document.body直下)に置く都合上、本物の`.piece`をそのまま持ってきても盤面のperspective/rotateXが無いと立体的に見えない。`perspective: 1150px`を持つouter要素と、盤面と同じ傾き(`--table-tilt`)を持つinner要素の入れ子(`.drag-ghost-piece-outer`/`-inner`)を新設し、その中に`buildCubePiece()`で作った本物の駒をそのまま入れることで、ドラッグ中も立方体の見た目を保てるようにした。
- **山札の厚みが枚数に応じて変化しているように見えない問題の真因を特定・修正（重要なハマりどころ）**: `--stack-height`の値自体は枚数に応じて正しく計算されていたが、それを表現する`.stack-front`（山の側面）が実際には高さ0に潰れて全く見えていなかった。
  - **原因1**: `.stack-front`に`filter: brightness(0.6)`が残っていた。これは駒の壁面で既に踏んだのと全く同じ「filterはpreserve-3dを無効化する」バグ。`box-shadow: inset 0 0 0 999px rgba(0,0,0,0.4)`（箱全体を覆う半透明の黒）に置き換えて解決。
  - **原因2（filterを直しても直らず、真因はこちら）**: `.stack`から`.game-table`に至る祖先の`.pile-zone`（山札/エターナル/捨て場ゾーンの共通クラス）が`transform-style`を明示指定しておらず既定値の`flat`のままだった。駒側の祖先チェーン（`.cell`→`.board`→`.arena`→`.game-table`）は全て`preserve-3d`なのに対し、山側だけこの`.pile-zone`が素通りできていなかった。`.pile-zone`自体はtransformを持たない（＝自分自身が回転等をしているわけではない）要素だが、それでも間に`transform-style: flat`の要素が挟まっていると子孫の3D変形（`.stack-front`のrotateX）が壊れて描画されなくなることが、駒との比較実験で判明した。`.pile-zone`に`transform-style: preserve-3d;`を追加して解決。実際に山札を15枚引いて厚みが目に見えて薄くなることを確認済み。
  - **教訓**: 「自分自身はtransformを持たない要素」であっても、`transform-style: flat`（既定値）のままだと3D階層の途中に挟まるだけで子孫の3D変形を壊すことがある——手札(`.hand-area`)の当たり判定のケースで学んだ「transformの無い要素は3Dに対して透過的」という理解は、正確には「他要素からの見た目上のヒットテストには影響しないが、preserve-3dの伝播そのものは別問題」だったとわかった。3Dで組んだ構造をコピー/流用する時は、祖先チェーンの`transform-style`が全て`preserve-3d`になっているかを逐一確認する必要がある。
- **場・ロックエリア間の移動でカードの表裏が変わってしまう問題を修正**: 前回導入した「移動先に応じて表裏を決める」ロジック(`faceUpForLocation`)を`MOVE_TOKEN`に単純適用していたため、マス→マス・マス→ロック・ロック→ロックなど「既に場に出ているカードを場の中で動かすだけ」の操作でも毎回裏向きにリセットされてしまっていた。`state.js`に`isTable(location)`（zoneがcellまたはlockかどうか）を追加し、「移動元と移動先が両方とも場/ロックエリア」の場合は表裏を変更しない（既存の`faceUp`をそのまま維持する）よう修正。手札↔場/ロックエリアの移動（新しくカードが場に出る／手札に加わる）の時だけ、引き続き`faceUpForLocation`で表裏を決め直す。
- **プレイヤー名ラベルの調整レンジを拡大**: `--label-*-pos-x/y`の可動域が±8remで、ユーザーがすでに上限(-8rem)まで使い切っていたため、±24remまで拡大した。
- **山札の側面を無地のライトグレーに変更**: `.stack-front`（山の側面）に一番上のカードと同じ画像を敷いていたため、薄い帯に画像が引き伸ばされて見苦しかった。`buildCardStack()`で側面には画像を敷かないようにし（`pileClass`も外し、常に`.stack-front`のみのクラスにした）、CSS側で無地のライトグレー(`#cbd5e1`)を背景色にした（既存の暗くする`box-shadow`はそのまま残るので、天面より少し暗いグレーの側面になる）。
- **駒・マス・カード・山札等にホバーハイライトを追加**: 駒がカードの上に乗っている時など、「今クリックしたら何が掴めるか」が分かりにくいという要望に対応。`findDraggableAt()`と同じ優先順位（駒＞カード＞山）で判定する`findHoverTarget()`を新設し（空のマス/ロックスロットもホバー対象になる点だけ異なる）、`#game-table`の`pointermove`でカーソル直下の対象に`.hover-active`（水色の枠線、ドラッグ中の黄金色ドロップ先ハイライトとは別役割）を付け外しする。ドラッグ中(`dragSession`がある間)はドロップ先ハイライトと役割が被るため休止する。
  - **ハマりどころ**: 当初`.hover-active`に`box-shadow`も付けていたが、`.piece`等ハイライト対象自身が既に別の`box-shadow`を持っていると、後から効いた方のルールで完全に上書きされ、光彩が全く見えなくなっていた（box-shadowは複数ルールの値を自動合成しない）。`outline`は他のどのルールも使っていないプロパティなので、確実に見えるよう`outline`のみで表現するようにした。
- **場のカードを同じマスに重ねると下に潜り込むことがある問題を修正**: `renderBoardTokens()`は`state.tokens`配列の順番通りに`appendChild`するため、配列内で後ろにあるトークンほど画面上で手前（一番上）になる。しかし`MOVE_TOKEN`の実装が`Array.prototype.map()`でその場更新するだけだったため、トークンの配列内での位置（＝重なり順）が「最後に動かした順」ではなく「元々作られた順」のままになっていた。後から同じマスに重ねたつもりのカードが、実は配列内では手前に居た別のカードより「後ろ」に留まり、見た目上潜り込んで見えることがあった。`MOVE_TOKEN`を「動かすトークンを配列から一旦取り除き、更新した上で配列の末尾に追加し直す」実装に変更し、常に「最後に動かしたものが一番上」になるようにした。
- **自分の手札が増えると盤面ごとレイアウトが崩れる問題を修正（重要なハマりどころ）**: `.hand-area`の幅は手札の枚数に比例して自動で広がる仕様（`--hand-a-size`）だが、これが`.player-zone`の通常のフロー内（フレックスの子要素）に置かれていたため、手札が多い時にその大きな幅が`.player-zone`自身の幅、さらには`.game-table`のgridトラック（auto幅の中央列。`.arena`・`.zone-top`・`.zone-bottom`が共有）にまで影響し、盤面(`.arena`)側が押し出される形でズレてしまっていた。`.hand-area`を`position: absolute`にして通常のフローから完全に外し、`.player-zone`自身の中心(50%, 50%)を基準に`translate(-50%, -50%)`で自己センタリングするよう変更（4方向すべての持ち上げtransformの先頭に追加）。これにより手札の幅がどれだけ変わっても周囲のレイアウト計算に一切影響しなくなり、手札12枚まで実際に増やして盤面中心とのズレが実質0になることを確認した。
- **山の側面が一部の角度で透明に見える問題を修正**: `.stack`は側面(前面)を1枚(`.stack-front`)しか持っておらず、駒(`.piece`)のような4面（前後左右）の壁が無かった。見る角度によっては存在しない面（特に右側）の奥が透けて見えていた。駒と同じ「床+4面の壁」の技法で`.stack-back`/`.stack-left`/`.stack-right`を追加し、正しい直方体にした（無地のライトグレー、面ごとに`box-shadow`の濃さを変えて陰影をつけている）。
- **駒のホバーハイライトを、外枠ではなく立方体の各面が光る表現に変更**: `.piece.hover-active`の外枠(outline)を無効化し、代わりに`.piece.hover-active .piece-face`（上面+4つの壁、個別のdiv）それぞれに`box-shadow`の光彩を付けた。`.piece-face`はそれ自体は他でbox-shadowを使っていないため、`.hover-active`本体で踏んだ「box-shadowは複数ルールの値を合成しない」問題を回避できる。
- **表向きカードのホバー拡大プレビューを追加**: 自分の手札・表向きの盤面/ロックのカード・捨て場の一番上のカード（表向き積みのため中身が分かる）にカーソルを乗せると、実物画像を拡大表示する`#card-preview`要素を新設した。`getVisibleCardId(el)`（`getPreviewImagePath`から切り出した共通ヘルパー、右クリックメニューとも共用）が対象の種類に応じてstateから実際のcardIdを引く（DOM上の`background-image`文字列を読み取るのではなく、必ずstateを正とする設計を踏襲）。サイズは`--card-preview-size`（デフォルト20rem、管理者モードで調整可）。位置決めは`left`+`bottom`（`top`ではなく）を使うことで、「拡大の起点は左下端に固定し、サイズが変わってもその起点はズレない」という要望を素直に満たせる。画面右端をはみ出す場合だけ表示方向を反転する。
- **カードの右クリックに専用メニューを追加**: `#game-table`上では常にブラウザ標準の右クリックメニューを出さないようにし（`contextmenu`を`preventDefault()`）、中身の分かるカード（`getVisibleCardId`が非nullを返す対象）を右クリックした時だけ「カード補足を見る」という専用メニューを表示するようにした。クリックすると`docs/cards.md`の補足テキストを表示するモーダルが開く。ダブルクリックと同じ理由で、`contextmenu`イベントのネイティブ`e.target`は信用せず、`findHoverTarget()`（elementsFromPoint()ベース）で対象を判定している。
- **駒のサイズを少し小さく、盤面のカードを少し大きく**: 駒がカードの上に乗ると下のカードがほぼ見えなくなるという指摘に対応。`--piece-size`を2.4rem→2.1remに、`.board-card`の`inset`を8%→3%にした。
- **駒にスキン画像を反映**: `画像素材/駒スキン/`（7色分）を`assets/pieces/`にコピーし（プレイマット・カード画像と同じ理由で、`.gitignore`の`/assets/pieces/`で公開リポジトリには含めない）、`buildCubePiece()`で単色塗りつぶし(`background`)ではなく`background-image`でスキン画像を敷くようにした。上面・4つの壁すべてに同じ画像を敷き、壁ごとの陰影（既存の`filter`/`box-shadow`）はそのまま効く。
- **プレイヤーCの名前ラベルを180度回転**: `.zone-top .label`のtransformに`rotate(180deg)`を追加（B/Dのrotate(90deg)/(−90deg)と同じ考え方）。
- **ロックエリアに置いたカードを、そのロックの持ち主から見て読みやすい向きに回転**: `.lock-top .board-card { transform: rotate(180deg); }`、`.lock-left`は90deg、`.lock-right`は-90deg（`.lock-bottom`＝自分Aは元々正しい向きなので変更不要）。座席配置（手前=A、左=B、奥=C、右=D）に合わせた回転角。
- **捨て場が空の時の塗りつぶしを透明に変更**: `.pile-discard`の紺色グラデーションを`background-image: none; background-color: transparent;`にした（ロックスロットの「うっすら枠だけ」と同じ考え方）。カードが入れば実物画像がinline styleで上書き表示される。
- **ファーストカードのパイルを新規追加（プレイマット右上）**: これまでデータ(`FIRST_CARDS`)だけ存在し実際のゲームには未接続だった（Phase1のセットアップ自動化スコープ外としていた）。ユーザーからの要望で、エターナルカードと同じ「シャッフルされた1つの山」として、盤面グリッドの空いていた右上（`.zone-first { grid-column: 3; grid-row: 1; }`。deck=左上, eternal=左下, discard=右下, first=右上で4隅が埋まった）に追加した。山札・エターナルと同様に裏向き積み（`assets/cards/back-first.png`、`getCardBackImagePath()`に`"first-"`接頭辞の判定を追加）。プレイヤーへの自動配布・ロックは引き続き未実装で、ドラッグで手動配布する運用。あわせて「山札一覧」ビューアにもファーストカードのセクションを追加し、`FIRST_CARDS`各定義に`note`（docs/cards.mdの補足）を追加した。
- **同じマスに重なったカードに「+N」バッジを追加、ホバーで一覧拡大表示**: `getCardStackGroups()`でトークンをlocationごとにグループ化し（駒は数えず`kind:"card"`のみ）、2枚以上重なっているグループの一番上のカードに`.stack-badge`（例:「+2」）を付けるようにした（`renderBoardTokens`）。バッジは`findHoverTarget()`で最優先に判定され、ホバーすると`#stack-preview`に重なっている全カードを横一列で拡大表示する。各カードは自分自身の`faceUp`に従って表向き/裏向きの画像を出す（下に潜んでいる裏向きカードの中身を、一覧表示によって覗けてしまわないようにするための配慮）。
- **「山札一覧」をスクロール不要のグリッド表示に再設計**: 1カード1行の縦積みレイアウト（33件で画面に収まらずスクロール必須だった）から、サムネイル中心のグリッド（`grid-template-columns: repeat(auto-fill, minmax(5rem, 1fr))`）に変更し、パネル幅も`min(68rem, 96vw)`に拡大した。タイルをクリックすると、そのタイルの直後（`grid-column: 1 / -1`で全幅）に補足テキストが開閉する。サブタイトルの「（データと画像の紐づき確認用）」は削除。
- **「+Nバッジのホバー一覧表示」機能を撤去し、右クリックメニュー方式に置き換え（ユーザー判断による仕様変更）**: ホバーで重なりカードを横一列拡大表示する機能が正しく動作しないと報告があり、かつ「何枚も重なっていると横一列表示自体が原理的に厳しい」との判断で、デバッグではなく設計変更として対応した。`getStackPreviewEl`/`showStackPreview`/`#stack-preview`/`.stack-preview-card`を全て削除。代わりに、右クリックメニューに「重なっているカードを見る」を追加し、クリックすると要クローズの`#stack-modal`（`flex-wrap`+`max-height`+`overflow-y: auto`で何枚重なっても画面内に収まる一覧）が開く方式にした。新設した`getStackTokensAt(el)`が、右クリック対象（`.board-card`または`.stack-badge`）の属する重なりグループ（2枚以上）の全トークンidを`getCardStackGroups()`から引く。
- **右クリックメニューを単一項目決め打ちから配列ベースに変更**: 「カード補足を見る」を出すか「重なっているカードを見る」を出すか（あるいは両方）を対象に応じて動的に決める必要が出たため、`showContextMenu(clientX, clientY, cardId)`だったシグネチャを`showContextMenu(clientX, clientY, items)`（`{label, onClick}`の配列）に変更した。`initContextMenuHandlers`側で`getVisibleCardId`と`getStackTokensAt`それぞれの結果に応じてitemsを組み立てる。
- **カード補足モーダルに拡大画像を追加**: `showCardNoteModal(cardId)`が今までタイトル+補足テキストのみだったのを、`getCardImagePath(cardId)`の画像(`.card-note-image`)を先頭に追加した。
- **「+N」バッジを`getVisibleCardId`の対象に追加**: バッジ自体を右クリック/ホバーした時にも、その下の一番上のカードと同じ扱い（表向きなら中身が分かる）になるよう、`getVisibleCardId`の先頭に`.stack-badge`の判定を追加した（バッジの`dataset.stackTokens`の最後のidが一番上のトークン）。これによりバッジへのホバーでも通常の`#card-preview`拡大表示が出るようになった（横一列プレビューの代替）。
- **駒スキン画像の角に隙間が見えるバグを修正**: `.piece-face`（駒の5面共通の基底クラス）に`border-radius: 0.2rem`が付いていたため、正方形のスキン画像がその分内側に丸められ、立方体の角で隣接面との間に隙間が見えていた。単色塗りつぶし時代の名残だったため、border-radiusごと削除した。
- **山札・エターナル・捨て場・ファーストカードの山の上面から、常時表示の名前・枚数テキストを撤去し、ホバー時のみ表示するツールチップに変更**: `buildCardStack()`から`nameEl`/`countEl`（常時表示だった`.stack-top`の子要素）を削除し、代わりに新設した`getPileTooltipText(el)`（`.stack[data-pile]`要素から`PILE_CONFIG`のラベルと`state.piles[pileKey].length`を組み立てる。捨て場は空でなければ一番上のカード名を表示）と、それを表示する`#pile-tooltip`（`updatePileTooltip()`、`updateHover`から呼ぶ）を追加した。
- **カード拡大プレビューが画面上端で見切れるバグを修正**: `positionPreviewPanel()`は元々画面右端のはみ出し（left/right反転）にしか対応しておらず、盤面奥（画面上部）のカードをホバーすると、`bottom`基準で上方向に伸びるプレビューパネルが画面上端(`y=0`)より上にはみ出て見切れていた。カーソルY座標とパネルの実測高さから「上方向に広げると画面上端を割るか」を判定し、割る場合だけ`bottom`基準ではなく`top`基準（カーソルの下方向に広げる）に切り替えるようにした。
- **「山札一覧」のカードタイルクリックを、インラインのテキスト展開から拡大画像+補足のモーダル表示に変更**: `deck-viewer.js`の`buildCardTile`が返していた`{tile, note}`（クリックでnoteのdisplayを切替）をやめ、クリックで`showCardModal(def)`（deck-viewer.js内に新設した専用モーダル、画像+タイトル+補足+閉じるボタン）を開く方式にした。

- **カード補足モーダル（右クリック・山札一覧共通）を、画像の下にテキストが続く縦積みから、画像の右隣にテキストが並ぶ横並びレイアウトに変更**: スクロールをなるべく減らしたいという要望に対応。`#card-note-modal`/山札一覧の専用モーダルとも、`display:flex; flex-wrap:wrap;`のcontent行の中に画像(固定幅14rem)とテキスト列(`flex:1`)を並べ、画面が狭い時だけ自動的に縦積みにフォールバックする。
- **エターナルの山の表示名を「永久」→「エターナルカード」に変更**: `PILE_CONFIG.eternal.label`のみの変更（ホバー時のパイルツールチップに使われる）。

- **ファーストの山の表示名を「ファースト」→「ファーストカード」に変更**: `PILE_CONFIG.first.label`のみの変更（エターナルと同じくホバー時のパイルツールチップに使われる）。
- **カード補足モーダル（右クリック・山札一覧の両方）の閉じるボタンを、下部の「閉じる」テキストボタンから右上の丸型✕ボタンに変更**: 新設した`.modal-close-x`（`position: absolute; top/right`、モーダル本体は元々`position: fixed`なのでそのまま基準にできる）を両モーダルで共用。画像+テキスト横並びレイアウトとの相性を考えて採用。

- **カード補足モーダル（右クリック）もモーダル外クリックで閉じられるように**: 山札一覧のカードモーダルは元々`#deck-card-modal-backdrop`という全画面backdropにclickリスナーを付けて閉じる仕組みが既にあったが、右クリックの`showCardNoteModal`にはbackdropが無く、外側クリックで閉じられなかった。同じ「透明な全画面backdrop＋そのclickでclose()」パターンを追加（`#card-note-modal-backdrop`、透明のままダイアログのdimはしていない）。

- **「セットアップウィザード」を追加（[src/game-setup.js](src/game-setup.js) 新規、docs/rulebook.mdの「Game Preparation」を再現）**: 画面右上に「🎲 セットアップ」ボタンを新設し、開くと以下5つのボタンを持つパネルが出る。
  - **０：プレイ人数選択、白黒カード確認**: プレイ人数(2/3/4)を選ぶダイアログ（管理者モードのトグルがONなら、人数の代わりにA/B/C/D各座席を自由にチェックボックスで選べるモードに切り替わる）。白黒（無色）カードを山札に含めるかのチェックボックスもここにあり、説明書の「初めてプレイする時は外すと簡単」という推奨に合わせてデフォルトは含めない。
  - **１：ファーストカードを配り、駒を配置する**: 実行の最初に必ず`resetGame()`（新設のRESET_GAMEアクション）で盤面を完全に空にする（ユーザー確認の上、ルールブック通り「初期手札なし」から作り直す設計にした。既存の起動時デフォルト値=`HAND_START`の仮配布はテスト用データとして手を付けず、あくまでこのウィザードを通した時だけ本来のルール通りの状態になる）。その後、参加座席の時計回り順に`ファースト`の山から1枚ずつ引き、そのカードと同色のロックエリアへ表向きでロックし（`SETUP_ASSIGN_FIRST_CARDS`）、同じ色の駒をそのプレイヤーのゲートに置く。
  - **２：盤面にカードを配置する**: 山札（白黒カード含む/含まないはステップ0の設定に従う）をシャッフルし、場の7×7＝49マスに1枚ずつ裏向きに配置する（`SETUP_FILL_BOARD`）。再実行しても安全なように、まず場のカード（駒は除く）だけ一旦取り除いてから配り直す設計。
  - **３：スタートプレイヤーを決める**: 参加座席から無作為に1人選び、モーダルで表示するだけ（状態には残さない。ターン管理の仕組み自体がまだ無いフェーズ1のため）。
  - **１〜３を一気に行う**: 上記を順番に実行する。
  - 座席(A/B/C/D)⇔盤面の辺(bottom/left/top/right)の対応・7色の並び順・ゲートの座標は、main.js/state.js両方から必要になったため`board-layout.js`（新規）に切り出し、重複によるズレを防いだ。
  - 配布時のアニメーションは今回は見送り（ユーザーとの合意により、まず配布ロジックを固めてから別途追加する）。
  - 座席の自動/自由選択の切り替えは、CSS変数のスライダー群とは性質が異なる「挙動の切り替え」のため、admin.jsに`manualSeatMode`という単純なbool値＋チェックボックスUIとして追加した（`isManualSeatMode()`をexport）。

- **セットアップウィザードのUIを「別ダイアログ」から「パネル内展開→決定で差し替え」方式に変更**: 「０：プレイ人数選択、白黒カード確認」を、以前は縮小されたボタンを押すと別のモーダルが開く形にしていたが、「設定しないと１〜３ができないことが分かりやすいように」という要望で、パネルを開くと最初から設定フォームが展開された状態になり、「決定」するまでは１〜３のボタン自体が存在しない（ensureConfigによる後追いチェックではなく、そもそも表示しないことで強制する）方式に変更した。決定後は「⚙ 設定を変更（０に戻る）」でいつでもフォーム表示に戻れる。`game-setup.js`内に`bodyEl`（パネル内の可変領域）と`renderPanelBody()`（フォーム/ステップボタンの差し替え）を新設。
- **ターン管理を導入（state.js）**: これまでセットアップウィザードの手順3「スタートプレイヤーを決める」は無作為に選んだ結果をモーダルで見せるだけで状態には残していなかったが、`state.activePlayers`（手順1で確定した参加座席）と`state.turnPlayer`（現在の手番）を新設し、実際に状態として持つようにした。新規アクション`SET_TURN_PLAYER`（手順3が呼ぶ）、`NEXT_TURN`（`activePlayers`を時計回り順に絞り込み、現在の`turnPlayer`の次へ進める。末尾の次は先頭に戻る）を追加。
- **手番プレイヤーを目立たせる演出を追加**: 手番プレイヤーのロックエリア全体に、ゆっくり柔らかく明滅するグロー（`@keyframes lock-area-turn-glow`、`.lock-area`自体は背景を持たないが`box-shadow`は背景の有無に関係なく外側に描画されるためこれだけで光る）を、名前ラベルには点滅＋文字色変更（`@keyframes label-turn-blink`）を追加。`buildLockArea(side)`は`SEAT_TO_SIDE[turnPlayer] === side`、`buildPlayerZone`は`player === turnPlayer`を見てそれぞれ`.is-turn-player`クラスを付け外しする。
- **「ターンを次のプレイヤーへ渡す」ボタンを新設**: 開発者向けツール（管理者モード＝左上、山札一覧/セットアップ＝右上）とは離し、プレイヤー自身が操作するゲームプレイ上のボタンとして画面右下に配置（`#end-turn-button`）。`state.turnPlayer`がnull（セットアップ手順3が未実行）の間は非表示。押すと`nextTurn()`を呼び、次のプレイヤー名を表示するボタン自体のテキストも毎回`render()`内の`updateEndTurnButton()`で更新する。
  - 座席⇔盤面辺の対応・7色の並び順に加え、時計回りの座席順を扱う`SEAT_ORDER`・座席ラベルの`SEAT_LABELS`も含め、これらは全て`board-layout.js`にまとめてあるため、ターン管理のような新しい機能追加でもmain.js/state.js/game-setup.js間でズレる心配がない。

- **手番グローの色を管理者モードから切り替え可能に**: `.lock-area.is-turn-player`の発光色をハードコードせず`--turn-glow-rgb`というCSS変数（`rgba(var(--turn-glow-rgb), 透明度)`という形でbox-shadowから参照）にし、admin.jsに「ロックエリアの手番グローを白色にする（オフ=黄色）」というチェックボックスを追加した（デフォルトは黄色寄り`255, 224, 130`、オンで`255, 255, 255`）。
- **「相手ゲート侵攻ボーナス」(docs/rulebook.md「Gate Invasion Bonus」)を自動処理化（[src/gate-invasion.js](src/gate-invasion.js) 新規）**: 「ターン終了」ボタンが押された瞬間、手番プレイヤーの駒が参加中の別プレイヤーのゲートに乗っていれば（自分のゲート・空席のゲートは対象外）、ルールブック通り①手札を半分（端数切り捨て）奪う→②エターナルカードを1枚無作為に獲得しロック→③自分のゲートのカードを回収してゲートに帰還、の3段階を、それぞれ内容を予告する確認ポップアップ（OK押下で実行）を挟みながら自動で順番に処理する。条件を満たさなければ何もせず通常通りすぐ次のプレイヤーへターンが渡る。
  - ②では、ロック先のスロットに既に何か（ファーストカードを除く）があれば先に手札へ加えるルールも再現した。エターナルカードの山が空の場合は「もう残っていません」という文言に切り替わる（ルール「無くなったら以降もらえない」）。
  - どの駒が誰のものかを判定するため、`SETUP_ASSIGN_FIRST_CARDS`が生成する駒トークンに新しく`player`フィールド（座席）を持たせるようにした（従来は`color`のみで、駒と座席の対応が取れていなかった）。
  - 新規state.jsアクション: `GATE_INVASION_STEAL_HAND`・`GATE_INVASION_ETERNAL`・`GATE_INVASION_RETURN_HOME`。乱数選択（奪うカードの抽選・エターナルの山の一番上）は呼び出し側(gate-invasion.js)が事前に行い、確認ポップアップの文言に反映してからreducerに渡す設計（reducer自体は受け取ったid通りに動くだけの純粋な処理）。
  - `board-layout.js`に、ゲートの辺からその持ち主の座席を逆引きする`SIDE_TO_SEAT`を追加。

- **駒のホバー/掴む当たり判定エリアの広さを管理者モードから微調整可能に**: 駒の見た目（立方体）はそのままに、判定だけを広げたり狭めたりしたいという要望に対応。`buildCubePiece()`が生成する`.piece`の中に、新しく透明な`.piece-hitbox`（`--piece-hitbox-scale`でサイズだけ独立に拡縮、中心基準）を追加した。findDraggableAt/findHoverTargetは`.closest(".piece")`で判定しているため、この子要素をクリック/ホバーしても正しく親の`.piece`（＝実際の駒トークン）に解決される。admin.jsに「駒の当たり判定（ホバーすると発光する範囲）」というスライダー（0.5〜2.5倍、デフォルト1＝見た目と同じ）を追加。
- **相手ゲート侵攻ボーナスの自動処理を「手番プレイヤー限定」から「参加中の全プレイヤー対象」に修正**: ルールブックに「あなたのターン、相手のターンどちらでも発生する」とある通り、何らかの効果で自分のターンでなくても相手ゲートに駒がいることはセブンではあり得るため、「ターン終了」ボタンを押した瞬間のチェック対象を手番プレイヤー1人に限定せず、参加している全プレイヤー（時計回り順）に広げた。該当者が複数いれば順番に1人ずつボーナス処理を行ってから、最後に１回だけターンを次のプレイヤーへ進める。`gate-invasion.js`の`runGateInvasionIfNeeded(attacker, done)`を`runGateInvasionsIfNeeded(done)`（引数なし、内部で全参加プレイヤーを走査）に変更。
- **手番プレイヤーの名前ラベルの点滅速度を、ロックエリアの明滅と同じ2.6秒に統一**: 別々のタイミングで点滅しているとちぐはぐに見えるため、`label-turn-blink`のアニメーション時間を`1.1s`→`2.6s`（`lock-area-turn-glow`と同じ）に変更。
- **カードが誰かの手札に加わった時、「何を得たか」を知らせるトースト通知を追加（[src/hand-announcer.js](src/hand-announcer.js) 新規）**: `announceHandPickups(player, pickups)`が、手札に入る直前にそのカードが「公開情報」（捨て場から取得した／既に表向きだった）だったか「非公開情報」（山札等から裏向きで引いた／相手の手札から奪った）だったかに応じて出し分ける。公開情報、または受け取った本人が自分(A)の場合は画像＋名前入りの詳細なトーストを、それ以外（本人以外が非公開情報を得た場合）は「プレイヤー●が非公開のカードをN枚手札に加えました」という事実だけのトーストにする（複数枚まとめて手に入る場合は公開/非公開が混在しても1つのトーストに収める）。3.2秒で自動的に消えるか、クリックで即座に消せる。
  - 呼び出し箇所: `main.js`の`onDragEnd`（山からのドロー・盤面/ロックからの移動、両方とも手札が行き先の時）、`gate-invasion.js`の3ステップ全て（①相手の手札を奪う＝常に非公開、②ロック先から弾かれた既存カード＝常に公開、③自ゲートのカード回収＝各カード自身のfaceUpに従う）。

- **ロックエリアに置いたカードは原則、表向きにするよう修正**: `faceUpForLocation(location)`が今まで`zone:"lock"`を「場」と同じ裏向き扱いにしていた（ルール「ロックする：カード1枚を...表向きで置くこと」に反していた）。ロック行き先は常に`faceUp:true`にするよう`MOVE_TOKEN`・`DRAW_FROM_PILE`双方の表裏決定ロジックを修正（`MOVE_TOKEN`は「場・ロック間の移動は表裏を変えない」という既存の例外より、ロック行きの表向き化を優先するよう条件を並べ替えた）。
- **駒の下にカードがあると駒の「下の方」が掴めなくなるバグを修正（重要なハマりどころ）**: `findDraggableAt`/`findHoverTarget`は`document.elementsFromPoint()`が返す要素配列を1回のループで「要素ごとに駒→カード→...の優先順位を判定して最初に一致したものを返す」実装だったため、たまたまその座標の一番手前（先頭）の要素が`.board-card`（駒の当たり判定を追加する前から存在する、セルにほぼ密着した大きな要素）で、かつその奥（配列の後ろの方）に`.piece`（今回追加した`.piece-hitbox`含む）があっても、ループが`.board-card`の時点で確定して返ってしまい、駒より優先度の低いはずのカードが先に選ばれてしまうことがあった。駒の可視な立方体は3D持ち上げ(`translateZ`)で上寄りにずれて描画されるため、見た目の「上の方」ではこの競合が起きにくく「下の方」でだけ再現していた。修正として、要素配列を種類（駒→盤面カード→手札カード→山）ごとに全体を舐めてから確定する2段階ループに変更し、描画順に関係なく常に優先順位通りに解決されるようにした。
- **駒の当たり判定の広さを管理者モードから調整可能に**: `buildCubePiece()`が生成する`.piece`の中に、見た目には影響しない透明な`.piece-hitbox`（`--piece-hitbox-scale`で中心基準に拡縮）を追加。管理者モードに「駒の当たり判定（ホバーすると発光する範囲）」スライダーを追加した。
- **管理者モードのパネルを大幅に整理**: 項目が増えて縦に長くなりすぎていたため、各セクションを`<details><summary>`で開閉できるようにし（デフォルトは全て閉じた状態）、パネルを開いた直後の見た目を大幅にコンパクトにした。
- **パネル/モーダルの閉じ方を「右上✕ボタン＋外クリックでも閉じる」に統一（[src/ui-helpers.js](src/ui-helpers.js) 新規、`createModalCloseX`/`createBackdrop`）**: 管理者モード・山札一覧・セットアップウィザード・重なりカード一覧モーダルの4つに適用（管理者モード・セットアップウィザードは常駐ツールなので背景は暗くしない透明backdrop、山札一覧は情報モーダルなので暗くするbackdropのまま）。以前の下部「閉じる」ボタンは撤去。今後追加するパネル/モーダルもこの部品を使って統一する方針。
- **カード獲得ポップアップの大きさ・表示時間を管理者モードから調整可能に、✕ボタンで即座に消せるように**: `--hand-pickup-toast-scale`（デフォルト1.3、以前より大きめ）・`--hand-pickup-toast-duration`（秒、デフォルト5秒、以前の3.2秒より長め）を追加し、管理者モードに「カード獲得ポップアップ」グループとして追加。トースト右上にも`.modal-close-x`を追加（クリックで即座に消せる。トースト全体クリックでの即消しも従来通り残している）。
- **画面右下に「1枚ドロー」ボタンを追加**: 手番プレイヤーが山札から1枚引いて自分の手札に加える簡易ショートカット。「ターンを次のプレイヤーへ渡す」ボタンと同じく、`state.turnPlayer`が無い間は非表示。
- **山札が空になった時の自動補充を実装（docs/rulebook.md「こんな時は」）**: 「捨て場のカードをそのまま裏向きにして山札とする。シャッフルはしない。」を再現。山札が空かつ捨て場にカードがある状態で「1枚ドロー」を押すと確認モーダルが出て、OKすると新規`REFILL_DECK_FROM_DISCARD`アクションが捨て場の山を（物理的に「ひっくり返す」動作にあたるため並び順を反転させて）山札に丸ごと移し、捨て場を空にしてからそのままドローを続行する。
- **7色ロック達成による勝利モーダルを実装（[src/victory.js](src/victory.js) 新規）**: `main.js`の`render()`終盤で毎回`checkForVictory()`を呼び、参加中の各プレイヤーについて自分のロックエリアの7色スロットが全て埋まっているかを判定、初めて達成した瞬間だけ金色に光るグロー＋トロフィー絵文字の勝利モーダルを表示する（一度出したプレイヤーは同じ対戦中は出し直さない。セットアップウィザードの手順1でリセットした時だけ`resetVictoryTracking()`で記録を忘れる）。

- **駒スキンをいつでも選び直せるように（[src/piece-skins.js](src/piece-skins.js) 新規）**: 画面右下に「🎨 駒スキン」ボタンを新設（自分の駒がまだ無い＝セットアップ未実施の間は非表示）。押すと自分の駒の色（ファーストカードで固定、変えられない）に対応する4種類（標準＋追加1〜3）のスキン画像をスワッチで選べるモーダルが開く。`画像素材/駒スキン追加/`にあった3セット×7色を`assets/pieces/${color}-{1,2,3}.png`としてコピーし（他の駒画像と同じ理由でgit管理外）、選択は色ごとに保持する（`skinIndexByColor`）ため、同じ色を引いた別のプレイヤーにもそのスキンが適用される。`buildCubePiece(color)`は`getSkinImagePath(color)`経由で画像パスを解決するよう変更。
- **山札・エターナル・ファーストの山が0枚でも満杯に見えるバグを修正**: `buildPileZone`が常に`config.backImage`をimagePathとして渡していたため（捨て場だけ0枚の時にnull扱いする特別分岐があったが、他の山には無かった）、0枚でも裏面画像がそのまま表示され続けていた。全ての山で0枚ならimagePathをnullにし、`buildCardStack`側でもnullの時は`background-image:none;background-color:transparent;`を明示設定してCSS側のフォールバック色（`.pile-deck`等の暗いグラデーション背景）を打ち消すようにした（捨て場が空の時と同じ「中身が無いと分かる」見た目に統一）。
- **山札切れの自動補充モーダルをOKを押すまで閉じないように変更**: 他のパネル/モーダルは「✕＋外クリックで閉じる」に統一したが、このモーダルはゲーム状態に関わる必須の確認のため、意図的にその対象から外し、`showConfirmModal`から✕ボタン・外クリックでの閉じる仕組みを削除した（`createBackdrop`のonClose引数にno-opを渡すことで、暗い背景は表示しつつクリックしても閉じないようにしている）。
- **相手の手札にカーソルを合わせると枚数だけ表示されるように**: 中身は非公開のまま、`getHandTooltipText(el)`（`.hand-card`かつ`is-self`でない時だけ）が「プレイヤー● 手札N枚」を返し、既存のピール用ツールチップ（`updatePileTooltip`）にそのまま乗せる形で統合した。
- **自分の手札枚数を画面左下に常時表示（`#self-hand-status`）**: 手札が扇状に重なって数えづらいため、自分（A）の手札枚数だけを常に見える小さなステータスとして追加した（他のプレイヤーの手札枚数はこれでは分からず、上記の相手手札ホバーでのみ分かる）。
- **自分のターン中、自分の駒も自分の駒の色でゆっくり柔らかく発光するように**: ロックエリア・名前ラベルの手番演出とは別に、`renderBoardTokens`で`token.player === "A" && turnPlayer === "A"`の時だけ駒に`.is-my-turn-glow`を付与し、`--piece-turn-glow-color`にその駒自身の色（`var(--color-${token.color})`）をJSから設定、CSSの`box-shadow`アニメーションで発光させる（ロックエリアと同じ2.6秒周期）。

- **「１〜３を一気に行う」完了時にセットアップウィザードを自動で閉じるように**: `game-setup.js`に`closePanel`という参照を持たせ、`initGameSetup()`内の`close()`をそこに登録、`runAll()`の最後に呼ぶようにした。
- **手番プレイヤーの駒の発光が自分(A)にしか効いていなかったバグを修正**: 前回「自ターンの時に自分の駒も発光」を実装した際、条件を`token.player === "A" && turnPlayer === "A"`と自分限定にしてしまっていた。ロックエリア/名前ラベルの手番演出と同じく「手番プレイヤー本人の駒」を対象にすべきだったため、`token.player === getState().turnPlayer`（座席を問わない）に修正。B/C/Dのターンでもそれぞれの駒が光ることを確認済み。
- **プレイヤー名・アバターを自由に変更可能に（[src/player-identity.js](src/player-identity.js) 新規）**: `getPlayerName`/`setPlayerName`・`getPlayerAvatar`/`setPlayerAvatar`を追加し、これまでコード各所にハードコードされていた`SEAT_LABELS[player]`の参照（main.js・game-setup.js・gate-invasion.js・hand-announcer.js・victory.js）を全てこれ経由に統一した。これにより名前を変更すると盤面のラベル・ターン終了ボタン・各種ポップアップ・トースト等、表示箇所すべてに反映される。アバターは画像素材が無いため、絵文字12種（🦊🐸🐙🦉🐲🦄🐧🦁🐺🐢🐬🦋）のダミーセットから選ぶ簡易実装（`AVATAR_OPTIONS`）。
  - 画面左下の自分専用ステータス（`#self-hand-status`）を、アバター（クリックで選択モーダル）＋名前（クリックでその場編集、Enter/blurで確定）＋手札枚数の3点セットに拡張した。
  - 盤面上にも各プレイヤーのアバターを追加（`buildPlayerZone`内、`.player-avatar`）。「相手のアバターは手札の後ろ側に」という要望に対応するため、DOM順を名前ラベル→アバター→手札の順にし（手札が後から描画される＝重なった時に手前）、位置・サイズを管理者モードの新グループ「プレイヤーアバターの位置・サイズ」から調整できるようにした（`--avatar-size`共通1つ＋`--avatar-{a,b,c,d}-pos-x/y`）。
- **「盤面拡大」ボタンを追加**: 画面右下に新設。押すと、プレイヤーA（手前）のロックエリアが画面下端、プレイヤーC（奥）のロックエリアが画面上端にほぼ収まる倍率まで拡大する。もう一度押すと通常の全体フィット表示に戻るトグル式。実装は「A側ロック〜C側ロックの中間点」を`transform-origin`に動的設定してからscale3d()で拡大する方式（中間点の画面上の位置を変えずに、その区間だけを上下対称に引き伸ばせる）。`fitTableToViewport()`を`applyNormalFit()`/`applyBoardZoomFit()`に分岐させ、`boardZoomed`フラグで管理。

- **「盤面拡大」ズームの位置を管理者モードから微調整可能に**: 理論上はA側ロック〜C側ロックの中間点を基準にscale3d()するだけでちょうど収まるはずだったが、手札・アバターの飛び出しやブラウザのレンダリング誤差で実際には手前のロックエリアが画面下端で見切れることがあった。`--board-zoom-margin`（拡大率の余白、小さいほど余白が増える）・`--board-zoom-offset-x/y`（transformにtranslateとして追加する微調整分）を新設し、管理者モードの新グループ「盤面拡大ボタンのズーム位置調整」から変更できるようにした。offset-yを変えると実際に見切れを解消できることを確認済み。
- **ロックエリアの色の存在感を弱める調整肢を拡充**: 枠線の太さ（既存の`--lock-slot-border-width`。範囲を0.02〜0.4remから0〜0.4rem・step0.01に広げ、枠線を完全に無くすことも選べるようにした）に加えて、新しく`--lock-slot-glow-scale`（色付きグローの強さ。枠線を細くしてもcurrentColorのbox-shadowグローだけ強いと結局目立ってしまっていたため）を追加。両方とも管理者モードの「ロックエリア」グループから調整できる。
- **プレイマットを複数用意し、セットアップ時に選べるように（[src/playmat.js](src/playmat.js) 新規）**: プレイマット画像の保存先が`画像素材/プレイマット/`に変わり、白・青・黒の3種類が用意されたため、`assets/playmats/{white,blue,black}.png`としてコピーした（他の実物画像素材と同じ理由でgit管理外、`.gitignore`に`/assets/playmats/`を追加）。セットアップウィザードの手順0のフォームに「プレイマット」選択（ラジオボタン、デフォルト白）を追加し、決定した瞬間に盤面へ即反映される（`.playmat-bg`の背景画像を固定のCSS指定からJS側のinline styleに変更）。
- **ロックしていても使えるカード（ファーストカード・エターナルカード）をロックエリア内で目立たせる**: 該当カードがロックゾーンにある間だけ`.is-usable-while-locked`を付与し、斜めの光の帯が定期的に（4秒周期で）カードを横切る「キラッ」演出と、同じタイミングでの控えめな金色グローの明滅を追加した（`buildFlatCard`でcardIdの接頭辞`first-`/`eternal-`かつ`location.zone === "lock"`の時だけ付与）。

- **管理者モードで調整された値をコードの新デフォルトとして反映**: ユーザーが管理者モードの「出力をコピー」で調整した内容（`--lock-slot-border-width: 0.01rem`・`--lock-slot-glow-scale: 0.25`・`--board-zoom-margin: 0.92`・`--board-zoom-offset-y: -1.7rem`）を、style.cssの`:root`デフォルト値とadmin.jsの各スライダーの`default`値の両方に反映した（「リセット」ボタンを押してもこの調整後の状態に戻るように）。なお「プレイヤーアバターの位置・サイズ」グループは前回のセッションで既に管理者モードに追加済みだったため、変更不要だった。
- **「盤面拡大」ボタンを2段階トグルに変更**: 押すたびに「盤面拡大」→「もっと拡大」→「元に戻す」と3段階を巡回するようにした。両レベルとも基準点（A側ロック〜C側ロックの中間点）は共通だが、倍率・位置の微調整分だけレベルごとに別のCSS変数（1段階目=`--board-zoom-{margin,offset-x,offset-y}`、2段階目=`--board-zoom-2-{margin,offset-x,offset-y}`）を持たせ、管理者モードにレベルごとの調整グループを分けて追加した（2段階目は「もっと拡大」なのでmarginのデフォルトを1.3＝画面より大きく引き伸ばす設定にしてある）。ボタンの見た目も2段階目だけ橙色にして1段階目（緑）と区別できるようにした。

- **駒スキンを左下の自分専用ステータスエリアに統合**: 以前は右下の独立したボタンだったが、アバター・名前と同じステータスエリアに移動した。実際の`buildCubePiece()`をそのまま使い、ドラッグ中のゴースト(`.drag-ghost-piece-outer/-inner`)と同じ「perspective+盤面と同じ傾きを持つ入れ子」のテクニックで、3D空間の外でも本物の駒を立方体のまま小さく表示する（クリックで従来通りのスキン選択モーダルが開く）。`piece-skins.js`から独自の呼び出しボタンを撤去し、データ管理とモーダルだけを持つ形にリファクタした。
- **画面左上にゲームタイトルを表示**: 「7 SHADES OF S:EVEN remake」（旧・管理者モードボタンがあった場所。オプションメニューへの統合で空いたスペースを使った）。
- **右上に「⚙ オプション」ボタンを新設し、その中に「管理者モード」を統合（[src/options-menu.js](src/options-menu.js) 新規）**: 以前は左上に専用の呼び出しボタンがあったが、オプションメニュー配下のドロップダウン項目にした。`admin.js`は自前の呼び出しボタンを持つのをやめ、`openAdminPanel()`という開閉トリガーだけを外部提供する形にリファクタした。
- **ロック中でも使えるカードの強調演出を2種類から選べるように**: 前回実装した「斜めに光る帯」は存在感が強すぎたため大幅に細く（35%→6%幅）した上で、新たに「そのロックスロットの色をした小さな球がカードのふちに沿ってゆっくり周回する」演出（`effect-orbit`、新デフォルト）を追加。球の動きはCSS`offset-path`等のモーションパスAPIに頼らず、4隅を等間隔のキーフレームにして直線的に結ぶ方式（確実に全ブラウザで同じ見た目になる）。管理者モードの「ロック中でも使えるカードの強調演出」でどちらか切り替えられる（`getUsableLockedEffect()`）。
- **管理者モードで新たに調整された値を反映**: `--board-zoom-2-offset-y: -2.5rem`、プレイヤーアバターの座席ごとの位置（A/B/C/Dそれぞれ）を、style.cssの`:root`デフォルトとadmin.jsの`default`値の両方に反映した。

> **注（コンテキスト削減のための整理・2026-08-28／2026-09-05／2026-09-06）**: 続き432以前（2026-07-19〜2026-09-05）の詳細な変更履歴は [docs/dev-log.md](docs/dev-log.md) へ移動しました（内容は削除せず移動しただけ）。過去の経緯・不具合の詳細を調べる時はそこを grep してください。以下は続き433以降（直近の作業）です。

### 2026-09-05（続き433）：#284 「このフェイズを終了する」の後にCPUがもう1枚使う／#283 前の効果のお知らせの上に次の効果／#280 奪う札を奪われた本人が選ばされる／#281 ホームにターン数が残る／#282 中央のお知らせの余白

続き426〜427（中央は一度に1つ・フェイズの開始を待たせる）の**副作用が2件**と、独立した3件。

- **#284「CPUが役人を使った後フェイズは終了するはずなのにドムスネロを使っていた」（原因確定・修正）**:
  ログで確定した——`effect-verb end_current_phase (blue-slum-official)` の**1.0秒後**に
  `hand-effect eternal-yellow`、その4.5秒後にようやく `diag-phase-deferred {phase:"move", via:"enter"}`。
  つまり `forceEndCurrentPhase()` → `advancePhase()` → `enterPhase("move")` が**中央が塞がっていて
  待たされている間**、`currentPhase` は "hand" のまま残り、その窓でCPUの手札効果スキャンが
  もう1枚使っていた。**続き427（#272の二重ロック）と全く同じ形**——「1回だけ／もう終わり」を
  **別の処理（フェイズの切り替え）が直後に走ることを前提に**担保していた。
  - 修正: phase-automation.js に `isPhaseTransitionPending()`（＝`pendingEnterPhase` があるか）を
    足し、main.js の `performPriorityTimeoutAutoAction` で **ロック/ハンドフェイズの新しい行動を
    始めない**。待つだけなので、enterPhase 側の上限（通常8秒・効果解決中30秒）で必ず前へ進む。
- **#283「CPUがドムスネロの諸々を処理している途中で試練の儀式を使用しました」（原因確定・修正）**:
  こちらは `handEffectBusy` は既に false（効果自体は終わっている）で、**お知らせの列がまだ
  掃けていなかった**（`diag-notice-wait pending:3` が続く中で次の `hand-effect` が始まっていた）。
  #266 の「画面の中央は一度に1つだけ」を**手札効果の開始**にも広げ、演出・お知らせの列・
  フェイズ告知のどれかが出ている間はCPUが次の効果を使い始めないようにした
  （`shouldWaitForCenterBeforeCpuHandEffect`、上限8秒）。
  - **止まらないための置き場所**: 判定は「使う効果を選んだ後」に置く。使う効果が無い時は
    そこまで来ないので、待つことでフェイズが終わらなくなる事故は起きない。
- **#280「これ、自分で選ばされたけど本来は相手が選ぶものです」（原因確定・修正）**:
  ゲート侵攻で手札を奪われる時、**奪われた本人（人間）**に「奪うカードを選んでください」の
  儀式モーダルが出ていた。原因は、この儀式が「今は誰が選ぶ番か」を **`isCpuSelectingNow()`＝
  優先権の持ち主**で判断していたこと。ゲート侵攻は**ターン終了時**に走るので、その時点で優先権は
  既に次の人へ渡っており（実測 `turnPlayer:"C"` なのに `priorityPlayer:"A"`）、CPUが奪う場面でも
  人間側が「選ぶ番」と判定されていた。
  - 修正: 儀式に `actingSeat`（＝奪う側）を渡し、表示するかどうかも自動代行の判断も**その席**で
    行う（`activeEffectPicker.owner`）。あわせて、CPUが奪う番なら**その場で無作為に選んで決着**
    させる（以前は「優先権の時間切れの自動代行」任せで、優先権が別の席にあると誰も解決しない）。
  - **教訓**: 「今この選択をしているのは誰か」を優先権から推測しない。**その効果の主語**（奪う側・
    使った側）を呼び出し側から明示的に渡す。ターン終了時・割り込み時は優先権が別の席にある。
- **#281「対戦が終わってホームに戻った時、右上にラウンドとターン数が残ってる」**: `#turn-round-counter`
  は「turnNumber が無い時だけ隠す」作りで、ホームへ戻っても状態は残るので表示されたままだった。
  全画面ページ（ホーム/マイページ/ランキング/図鑑/マイデッキ）が開いている間は隠す
  （`body.full-screen-page-active`。JS側が inline style で display を書くのでCSSは `!important`）。
- **#282「このモーダルの余白が気になる！」**: 画面中央のフラッシュ（`.hand-pickup-toast.is-flash`）は
  右下トーストのままの**左揃え**で、見出しや「〜の効果」の一行の方がカードより横に長いため、
  カード（4.2rem）だけが左端に寄って右に大きな空白ができていた。中央に出ている間だけ中身を
  中央揃えにし、カード名の折り返しも減らし、✕ボタンのぶん右だけ広かった余白も揃えた
  （右下へ畳んだ後のチップ・従来の右下トーストの見た目は変えない）。
- **検証**: `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` 56/56 PASS、
  `node test/smoke.mjs 2` PASS、CSSブレース平衡、お知らせ ja/en とも同数。ヘッドレス実測——
  #282: 左右の余白が同じ（33.28px / 33.28px）・カードが箱の中央（左右の隙間とも110px）・
  カード枠が 4.2rem → 94.6px に広がり名前が折り返しにくい。#281: 全画面ページを開くと
  `display:none`、閉じると `block` に戻る。CPU自己対戦での実測は下記コミット参照。

### 2026-09-06（続き434）：演出②ターンの始まり（席の方向から色の光・自分の番だけ一拍）／③「連鎖」と「コンボ」を分ける

ユーザーの選択（②「やりましょう！」／③「一旦『2 連鎖』と『2 コンボ』で！」）に沿って実装した。
⑥（山へ積まれる・手札へ差し込まれる）と⑧（各カードの効果ごとの演出）は次回。

- **② ターンの始まり**: 「モーダルを増やすと待ち時間が伸びる」ので、**今のモーダルの出方を変える**
  形にした（表示時間は2.2秒のまま）。
  - **その人の席の方向から光が走ってくる**（`.turn-announce-sweep`、1.1秒で消える全画面の一発。
    `data-from` に席の辺を入れ、CSS変数でグラデーションの角度を決める）。誰の番かが、名前を
    読む前に分かる。
  - **名前の枠と光をその人の駒の色に**（今までは全員おなじ金色だった）。
  - **自分の番の時だけ、名前が一拍だけ大きく脈打つ**（相手の番は静かなまま＝自分の番だと分かる）。
  - 席の辺・駒の色・自分かどうかは main.js が解決して渡す（`turnAnnounceLook`）。turn-announce.js は
    表示専用の部品のままにしておく（盤面の知識を持たせない）。
  - 「ずっと光り続ける演出をやめる」設定（`body.reduce-glow`）では光も脈動も止まる。
- **③ 連鎖とコンボ**: 到達の入れ子の深さは前から数えていたが、文言が常に「N 連鎖」だった。
  **駒が動いて次のカードに乗った到達＝連鎖／上のカードが取り除かれて下が露出したことで起きた
  到達＝コンボ**、と分ける。
  - 実装: `maybeTriggerCardArrivalForExposedCard` → `triggerCardArrivalIfFaceUp(..., fromExposure=true)`
    → `triggerCardArrival(..., { fromExposure })` と1つの真偽値を通し、オーラを出す**直前**に
    モジュール変数へ写して `spawnArrivalBurst__inner` が読む。演出の引数を増やすと呼び出し口が
    4か所あって漏れるため、この形にした（同期処理なので取り違えは起きない）。
  - 見た目も分ける（連鎖＝金／コンボ＝水色）。文言は `game.chain` / `game.combo`（ja/en）。
- **検証（ヘッドレス）**: ②——奥の席(C・紫)なら sweep の `data-from="top"`・角度180deg・
  グラデーションの色が紫(#915da3)・名前の枠も紫、自分(A・青)なら `turn-announce-self-beat` が動き
  枠は青(rgb(27,184,206))・sweep は下から。③——連鎖バッジは金(rgba(250,204,21,.85))で「2 連鎖」、
  コンボバッジは水色(rgba(56,189,248,.85))で「2 コンボ」。例外0件。
  翻訳キー ja/en とも 1793 で一致。
  - **未確認**: 実際の対局で「めくれての到達」が起きた時に `is-combo` が付くところ（露出到達は
    自己対戦180秒では発生しなかった）。配線は真偽値1つを2か所に通しただけなので、実機で
    「2 コンボ」と出るかを見てほしい。

### 2026-09-06（続き435）：演出⑥山へ積まれる「トン」・手札へ差し込まれる動き／⑧の1つ目「奪う」（相手の手札から自分へ光の筋）

- **⑥-1 山へ積まれる「トン」**: 山の厚みは前から枚数ぶん増えていたが、1枚は0.6pxなので**増えたことに
  気づけなかった**。積まれた瞬間だけ、天面を少し上から落として一度わずかに沈ませてから戻す
  （`.stack.is-settling .stack-top` ＋ `@keyframes stack-settle`）。
  - **注意**: 山の天面は `transform: translateZ(var(--stack-height))` で厚みのぶん持ち上げているので、
    キーフレーム側にも**必ずその持ち上げを含める**（含めないと天面が根元まで落ちる）。
  - 判定は「前回の描画時の枚数を覚えておいて、増えていたら」（`prevPileCounts`）。どの山でも同じ経路を
    通るので、山札・捨て場・エターナル・ファーストの全部に効く。
- **⑥-2 手札に差し込まれる動き**: 扇の位置はインラインの transform で決まるため、キーフレームでは
  上書きできない。そこで「**前回の位置をいったん入れてから、次のフレームで本来の位置へ戻す**」形にし、
  動きはCSSトランジションに任せた（`applyHandInsertMotion`）。結果として、
  **隣の札が「場所を空けるように」開き、新しい札が下から滑り込む**——扇は増えた枚数ぶん広がるので、
  前回の位置から始めるだけで自然にその動きになる。
  - **ハマりどころ**: `.hand-card.is-self` が「ひょこっと持ち上げ」用に
    `transition: transform 0.2s ease-out` を持っており、**同じ詳細度で後に書かれている方が勝つ**。
    最初この新ルールを前に置いてしまい、実測で `transitionProperty` が `transform` だけ（opacity が
    効かない・時間も0.2秒）になっていた。`.hand-card.is-self` の**後ろ**へ移して解決。
  - 「一番最初の描画は動かさない」判定は、**覚えている札が0枚か**ではなく「一度でも描いたか」で見る
    （手札が空の状態は普通にあるので、0枚判定だと最初の1枚が永久に動かない。実測で発覚）。
- **⑧-1「奪う」**: スリカエ・接触・ゲート侵攻——「相手の手札から自分の手札へ1枚移る」場面は
  **どれも儀式ピック（`requestOpponentHandRitualPick` / …MultiPick）を通る**ので、その決着（finish）
  1か所に入れるだけで3つ全部に演出が付く。奪われた側の手札エリアから奪った側の手札エリアへ、
  **奪った人の駒の色**の光の筋が走る（`playStealBeam` / `.steal-beam`）。
  - 座標は必ず `stageClientToLocal` を通す（実画面座標のままだとステージ変形が二重にかかる。#197/#198）。
  - 起点を左端・中心（`transform-origin: 0 50%`）に固定して `rotate` で向きを合わせるので、JS側は
    長さと角度を渡すだけでよい。盤面の外（body直下）なので**WebGL描画のON/OFFに左右されない**。
- **検証（ヘッドレス）**: ⑥-1——捨て場へ1枚送ると `.stack` に `is-settling` が付き `stack-settle` が動く
  （送る前は付いていない）。⑥-2——山から1枚引くと、その札に `is-hand-inserting` が付き
  `transition: transform, opacity` が効き、0.6秒後にはクラスが外れている。⑧-1——`.steal-beam` に
  `steal-beam` アニメーション・奪った人の色（青で確認）・`transform-origin: 0px 中心`。例外0件。
  - **未確認**: 実際の対局で光の筋が走るところ（奪う場面は自己対戦では起きなかった）。実機で
    スリカエ／接触／ゲート侵攻の時に見えるかを確認してほしい。
- **⑧の残り（次回）**: 入れ替える（マスチェンジ・コノハナサクヤ＝既存の電撃アークを流用）／
  ばらまく（増殖する樹々・合同建設・白の意思＝置く先へ順番に光が飛ぶ）／賭ける（ザ・ギャンブル・
  試練の儀式＝宣言した色が空中に並び、当たり/外れで割れる・輝く）。

### 2026-09-06（続き436）：#288/#286 マイデッキが1ロックフェイズに何枚でも引けた／#285 オンラインで移動演出が二重／#287 めくれより先に中身が分かる／#289 山の側面がモアレで太い縞

続き426〜427（中央は一度に1つ・フェイズの開始を待たせる）の**副作用がさらに2件**（#288/#286・#285）と、
前回入れた演出の作り込み不足が2件（#287・#289）。

- **#288「1回のロックフェイズでマイデッキボタン連打したらデッキが尽きるまでドローできた」／
  #286「ロック演出中にマイデッキボタンを押したらドローできた」（原因確定・修正）**:
  マイデッキから引くのは**ロックする代わり**なので1フェイズに1回だけ。ところがそれを
  「引いたら `advancePhase()` でフェイズが終わるから2回目は押せない」ことだけで担保していた。
  #266/#267 でフェイズの切り替えが（演出・お知らせが終わるまで）待つようになったため、
  その数秒の窓でボタンが残り、連打で山が尽きるまで引けた（実測ログ: T5 で `my-deck-draw` が6回）。
  ロックした直後（ロック演出中）も同じ窓で引けた。**続き427（#272の二重ロック）と全く同じ形**。
  - 修正: `myDeckDrawnThisPhase` を phase-automation に持ち、
    ①ボタンの click で**送信より前に**印を付ける（応答待ちの連打を止める）
    ②既にロック済み（`hasPlacedNewLockThisPhase`）なら受け付けない
    ③ボタン自体も出さない（`showMyDeck` の条件に追加）
    ④逆に、引いた後はロックも受け付けない（`performLockPhaseClick`。引いたのは「ロックの代わり」）
    ⑤CPUの自動ドローも同じ印を付ける（`noteMyDeckDrawThisPhase`）。
    リセットは `enterPhase("lock")` の1か所（`lockedIdsAtPhaseStart` と同じ場所）。
- **#285「オンラインで、移動時に古い移動演出が出てそのあと正規のよっこいしょ移動演出が出た」
  （原因確定・修正）**: `performPhaseMoveToCell` のオンライン経路が
  `await moveToken(...)` → `markSelfHandled([piece.id])` の順だった。**送信を待っている間に
  サーバーからの通知で hydrate が走る**ことがあり、その時点ではまだ印が無いので
  remote-move-animator が「他人の移動」と判断して**古い平たいゴースト**を飛ばす。そのあと
  自分の跳ねる演出も出るので二重に見えていた。印を**送信の前**に移した（印にはTTLがあるので
  送信が失敗しても実害は無い）。
  - あわせて、**他プレイヤーの駒の移動も同じ「立方体のまま跳ねる」動き**にした（続き406で
    「自分の移動と他人の移動が同じ見え方になる」ことを狙ったのに、続き428で自分側だけ
    立方体に作り変えたため、**他人の移動だけ平たい板のまま**残っていた）。remote-move-animator
    から `helpers.playPieceMove` 経由で main.js の演出を呼ぶ。
    - **注意**: あちら側は自前で「隠すトークンの集合」を管理しているので、演出側が
      `setSetupPendingTokenIds(new Set([...]))` で集合を**上書きすると他の飛翔中トークンが
      露出する**。`animOpts.skipPendingHide` を足して、隠す/戻すの操作だけを飛ばせるようにした。
- **#287「試練の儀式で移動するとき、モーダルでどのカードだったか分かった後に盤面のカードが
  オープンされた」（修正）**: めくれ演出（続き432の①）は `beginBoardAnimation()` を呼んで
  おらず、**「盤面の演出中」として数えられていなかった**。そのため #266 の「中央は一度に1つ・
  演出が終わるまでお知らせを待たせる」仕組みに乗らず、盤面がめくれる前に中央のお知らせが
  中身を明かしていた。ゴーストを出している間だけゲートを上げるようにした（二重解除しないよう
  `flipGateClosed` で1回だけ）。
- **#289「山札の側面だけど、太い縞々がある」（原因確定・修正）**: 縞の間隔を「カード1枚の厚み
  (0.6px)」そのものにしていた。**画面のピクセルより細かい繰り返しは、画面の格子と干渉して
  太い縞（モアレ）になる**——スクリーンショットでも5〜6本の太い帯になっていた。1枚ずつの線として
  描かれることは原理的に無いので、**実際に線として見える間隔（3px）まで広げた**（＝数枚ごとに
  1本の断面線）。線自体も 0.6px 幅にしてコントラストを下げた。枚数と本数は一致しなくなるが、
  狙いは「紙が重なっている質感」なので問題ない。
  - **教訓**: 「1枚 = 1本」のような**物理的に正しい数字**をそのまま描画の周期にすると、
    サブピクセルではモアレになって**かえって嘘の見た目**になる。見せたいのは質感であって
    枚数ではない、と割り切る。
- **検証**: `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` 56/56 PASS、
  `node test/smoke.mjs 2` PASS、CSSブレース平衡、お知らせ ja/en とも同数。ヘッドレス実測——
  #289: `--stack-stripe` が 0.9px → **3px**、山の厚み33.6px（＝約11本の線）。
  #288/#286: `hasDrawnMyDeckThisPhase()` が false → 印を付けると true。
  #287: めくれのゴーストが出ている間 `isBoardAnimationPlaying()` が **true**、
  0.9秒後に false でゴーストも消えている。例外0件。
  - **未確認（オンラインでしか通らない）**: #285 の二重解消と、他プレイヤーの駒が跳ねる動きに
    なること。ローカルのCPU戦は remote-move-animator を `blinkOnly`（点滅のみ・ゴースト無し）で
    通すため、この経路を手元で再現できない。実機での確認をお願いしたい。

### 2026-09-06（続き437）：#287 めくれは「中央と盤面で同時」に直した／「別の処理が直後に走るから大丈夫」に頼っている箇所の総点検（1手の二重実行を6か所で塞ぐ）／#285 と同じ形が実は19か所あった

ユーザーからの3点——①「この手の『何かが進むから大丈夫だろう』的なのって他にないか総点検してほしい」
②「#287 は盤のめくれとモーダルのめくれが**同時**でいい。今回は盤のめくれがモーダルのめくれの
**後**に来たので二回めくれたことになってしまっていた」③「#285 のこの手の問題って根治できてますか？」。

- **#287 の直し方が間違っていた（作り直した）**: 続き436では「めくれを盤面の演出として数え、
  中央のお知らせを待たせる」ようにしたが、**試練の儀式の経路には効いていなかった**。あの経路は
  engine 側が意図的に「①中央でじらしてフリップ（`announceSteppedCard`）→ ②盤面のカードを
  めくる（`flipCard`）」の順に書いてある（2026-08-08 のユーザー要望「盤面が先に開くとドキドキ感が
  無い」）。**続き432で盤面にも「めくれる動き」を付けたことで、同じカードが2回めくれて見える**
  ようになったのが #287 の正体だった（続き436の検証は「ゴーストの間ゲートが立つ」ことしか見て
  おらず、**順番そのものを測っていなかった**）。
  - **修正**: `playCenterCardFlipReveal` に `onFlip` を足し、**中央のカードが開く瞬間（じらしが
    終わってスケールXフリップに入るところ）で1回だけ**呼ぶ。engine 側はその `onFlip` で盤面を
    めくる。待たない（中央の動きを止めないため）ので、両方が同時にめくれる。
  - **演出が無い環境でも必ずめくれるようにした**: 演出オフ（`isArrivalEffectDisabled`）の分岐でも
    `onFlip` を呼び、engine 側は演出の後に**もう一度**同じ関数を呼ぶ（2回目は最初のPromiseを
    返すだけの no-op）。テストのスタブ `announceSteppedCard: () => {}` は onFlip を呼ばないので、
    この保険が無いと盤面が裏のままになる。
  - **実測（ヘッドレス・engineを直接叩くプローブ）**: 中央モーダル出現 74ms →
    **盤面 flipCard 381ms** → 中央が開く 397ms → 中央が消える 704ms。＝盤面のめくれが
    **中央が開く瞬間と同時**（修正前は「中央が消えた 704ms の後」）。盤面のカードは最終的に
    表向き（`faceUp: true`）、flipCard の呼び出しは**1回だけ**。

- **【総点検】「1回だけ」を、直後に走る別の処理に頼って担保していた箇所**（ユーザー指示）。
  今回で**5回目**の形（#272 二重ロック／#276／#284／#288 マイデッキ連打／今回）。共通の症状は
  「送信の往復や確認モーダルを待っている数百ms〜数秒の窓で、同じ1手がもう一度通る」。
  **窓の存在そのもの（＝いま送信中か）を印にして弾く**方針で、次の6か所を塞いだ。
  1. **ロック（`performLockPhaseClick`）**: 「このフェイズで既にロックしたか」
     (`hasPlacedNewLockThisPhase`) が真になるのは、カードが実際にロックエリアへ動いて
     サーバーから戻ってから。**確認モーダルを出している間と送信中は偽のまま**なので、その窓で
     2枚目が通る（#272 で塞いだのは「ロック後、フェイズが切り替わるまでの窓」だった）。
     `lockPhaseClickInFlight` で弾き、`diag-lock-click-skip {reason:"already-in-flight"}` を残す。
  2. **接触の申し込み（`submitContactProposal`）**: 「1手を使った」印
     (`markPhaseMoveActionTaken`) は requestContact の**成功後**に付ける（途中でキャンセルされた
     時にターンを消費しないため。これ自体は正しい）。その往復の間は `isMovePhaseActive()` が真の
     ままでハイライトも出ているので、2回目のタップで**接触をもう一度申し込めた**。
     `contactProposalInFlight` で弾く。
  3. **ドラッグでの1手（`onDragEnd` のドロップ送信）**: ロックも駒の移動も、印が付くのは送信が
     終わってから。送信中は `boardDropDispatchInFlight` で弾く（try/finally で必ず戻す）。
  4. **最後のロックの承認（`respondToFinalLock`）**: バナーは render() のたびに作り直されるので、
     押した直後に隠しても復活する。2回押すと**次の承認者の分まで自分が承認してしまう**。
     接触の応答（`respondToContact`、こちらは既に錠があった）と同じ形の錠を追加。
  5. **カウンターロックの使用（`useCounterLockOnContact`）**: 押してから捨て札が反映されるまでは
     「まだ使える」と判定されるため、モーダルが作り直されると二重に使えた。同じ錠を追加。
  6. **スキップボタン**: 次のフェイズの開始が（演出・お知らせ待ちで）予約されている間は
     `currentPhase` が前のフェイズのまま残るのでボタンが押せてしまう。`advancePhase()` は
     `currentPhase` から次を計算するので**二重に進みはしない**（ここは無害だった）が、持ち時間の
     再付与だけ走るので、`isPhaseTransitionPending()` の間は押せない・出さないようにした。
  - **問題無しと確認したもの（記録）**: ターン終了ボタン（`endTurnActionInProgress` ＋
    `isEndTurnAdvancePending` で二重に守られている）／タップでの移動（`markPhaseMoveActionTaken`
    を await の**前**に呼んでいる）／手札効果（`runAutoHandEffect` が同期的に `handEffectBusy` を
    立て、CPU側は `isHandEffectBusy()` で早期return）／ゴメンナサイ（`gomennasaiManualUseInFlight`
    と `setGomennasaiPicking(true)` を同期的に立てている）／接触の応答（`contactResponseInFlight`）。
    「1枚ドロー」「公開ドロー」は**回数制限のある行動ではない**ので対象外。

- **#285 は根治できていなかった（今回まとめて直した）**: 続き436で直したのは
  `performPhaseMoveToCell` の**1か所だけ**だった。同じ形——
  `await <送信>; markSelfHandled([id]); await fetchAndHydrate(...)`——を機械的に洗い出したところ、
  **main.js に19か所、card-dev-mode.js に1か所**残っていた（カードを置く・めくる・捨てる・
  ロックする・奪う・烙印を置く…ほぼ全ての操作）。全て**印を送信より前**へ移した。
  - 印には TTL（4秒）があるので、送信が失敗しても数秒で消えるだけ＝先に付けることによる実害は無い。
  - **例外はドロー系だけ**（引くまで新しいトークンidが分からないので、hydrate 後に
    `findNewHandTokenIds` で調べてから付ける。3か所）。
  - 二度と戻らないよう、**理由を `self-handled-tokens.js` の冒頭に○×付きで明記**し、各箇所には
    1行のコメント（「印は送信より前に付ける（理由は self-handled-tokens.js の冒頭）」）を添えた。
  - **教訓（今回の総まとめ）**: 「Aが済めばBは起きない」という担保は、**AとBの間に待ち時間が
    1msでもあれば破れる**。オンラインの送信・確認モーダル・演出の待ちは、どれもその待ち時間を
    後から増やす変更になる。制約は**その事実そのもの**（もうやったか／いまやっている最中か）で
    判定すること。

- **検証**: `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` **56/56 PASS**、
  `node test/smoke.mjs 2` PASS、構文チェック（変更した全ファイル）通過、お知らせ ja/en とも4件。
  ヘッドレスで①#287 の順番（上記の実測）②`isPhaseTransitionPending()` が中央を塞いだ瞬間だけ
  true になり、解放後にフェイズが進む（止まらない）③例外0件、を確認。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

### 2026-09-06（続き438）：#290 物語チュートリアルが1週間まるごと死んでいた（英語化の一括置換が引数 showBack を落としていた・続き411と同じ形）／#293 連鎖の数え方が1つずれる／#291・#292 は再現できず診断を追加

ユーザーから不具合4件（#290〜#293）と、基本設定を7項目に絞る提案。

- **#290「左上の『遊び方を知ってるからスキップ』等のボタンが押せない」＝真因は別のところ（原因確定・修正）**:
  実機のコンソールに **`Can't find variable: showBack`** が毎操作ごとに出ていた。
  `tutorial-battle-ui.js` の `showBlockingHint({...})` は本体で `showBack` / `onBack` を使うのに、
  **引数の分割代入からその2つが消えていた**——`git log -L` で追うと **UI英語化フェーズ13
  （`b6b14d6`）がこの1行を丸ごと差し替えた際に落ちていた**（続き411の online-ui.js とまったく同じ形。
  これで**5回目**）。この関数は説明モーダルを出す唯一の入口で、`showBack` を読む行は
  `scrimEl/calloutEl` を表示する行**より前**にあるため、**呼ばれるたびに ReferenceError で落ち、
  説明が一度も表示されない**＝物語チュートリアルが b6b14d6 以降ずっと進められない状態だった。
  引数を元に戻して修正。
  - **常設検査 `tools/check-undeclared.mjs` はなぜ見逃したか（重要）**: あの検査は
    **「未宣言の名前への*代入*」**（`name.prop = ...`）だけを見る。今回は
    `calloutBackEl.style.display = showBack ? …` ＝ **未宣言の名前の*読み取り***で、
    代入先(`calloutBackEl`)は正しく宣言されているため素通りする。
    読み取り側も静的に洗おうと試作したが、**正規表現だけでは誤検知が多すぎて（161ファイルで93件、
    その大半が正規表現リテラル・多行の分割代入引数の取りこぼし）実用にならなかった**ので、
    今回は見送った。**次に効きそうな案（申し送り）**: 「呼び出し側が渡しているのに、関数側の
    分割代入に無いキー」を突き合わせる検査。今回の事故も続き411もこの形なので、誤検知ほぼ0で
    拾えるはず（`...rest` や `opts.x` を読む関数は対象外にする）。

- **#293「ジャンプ台に到達は1連鎖のはず。ゴメンナサイ移動が連鎖にカウントされていない」
  （原因確定・修正）**: 連鎖の回数は `arrivalEffectProcessingDepth`（到達処理の入れ子の深さ）を
  そのまま表示していたが、**同じカウンタを `beginPostMoveArrivalGuard()` も +1 していた**
  （移動確定〜到達開始の空白で自動ターン終了が割り込むのを防ぐガード、続き…の #6 対策）。
  そのため**移動して最初に乗ったカードがいきなり「2 連鎖」**になり、その次の到達も
  （ガードが外れた分と相殺して）また 2 のままで、「ゴメンナサイの移動が数えられていない」ように
  見えていた。ガードが占めている分（`postMoveArrivalGuardDepth`）を別に数え、
  **連鎖の表示だけ差し引く**（busy 判定は今までどおり合計で見るので挙動は変えない）。
  - **実測**: CPU自己対戦200秒で到達8回・**バッジは1回だけ**（それも本物の2連鎖）。
    修正前ならログの `arrival depth:1` が示すとおり**8回すべてに「2 連鎖」**が出ていた。

- **#291「ランク戦でブーストカードが描画されていない」（再現できず・診断を追加）**:
  ローカルで `quickStart(2, true, true)`（ブーストON）を実際に走らせたところ、**ブーストの札4枚が
  作られ、4枚とも仮デザインのSVGで正しく描画され、404も0件**だった。
  - **立てた仮説と、その否定**: ブーストの札だけ `data:image/svg+xml;**utf8**,…` という
    **パラメータの書式が不正な**データURIを使っている（同じリポジトリの `fullscreen-toggle.js` は
    正しく `;charset=utf-8,`）。「WebKit が厳しくて弾いているのでは」と考え、**Playwright の
    WebKit を入れて実測**したが、**WebKit でも Chromium でも問題なく表示された**（naturalWidth 200）。
    仮説は否定。推測で直さずに済んだ。
  - そこで、次の報告で**「サーバーが札を作っていない」のか「作っているのに描けていない」のか**が
    一発で分かるよう、対局開始時に `diag-boost`（ロックエリアにあるブースト札の枚数・ロック札の
    総数・カードid）を残すようにした。0枚ならサーバー側（Edge Function の再デプロイ漏れを含む）、
    あるのに見えないなら描画側。

- **#292「ムーブフェイズが飛ばされました」（未解決・診断を追加）**: ログでは
  `diag-should-emphasize {isMovePhase:true, moveStillActive:false}` ＝**ムーブフェイズに入った
  時点で既に「もう動いた」印(`moveActionTaken`)が立っていた**ことまでは確定した。ところが印を
  立てる経路は6つ（タップ移動・ドラッグ移動・接触の成立・持ち時間切れの自動移動・救済の
  ターン終了・再読み込みからの復元）あり、**どれが立てたのかはログから区別できなかった**
  （報告者は `navType:"reload"` ＝再読み込み後なので #167 の復元経路が有力だが、断定できない）。
  `markPhaseMoveActionTaken(reason)` に呼び出し元の名前を渡し、`diag-move-action-taken`
  （reason / その時のフェイズ / 持ち主 / 既に立っていたか）を残すようにした。次の報告で確定できる。

- **検証**: `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` **56/56 PASS**、
  構文チェック（変更した全ファイル）通過、CSSブレース平衡、お知らせ ja/en とも2件。
  `node test/smoke.mjs 2` は**1回目が turn 5 で停止して FAIL、2回目は PASS**（今回の変更は連鎖の
  表示と診断ログだけで進行には触れていないが、続き407/408 と同じ「たまに止まる」がまだ残って
  いる可能性がある。次に FAIL したら `diag-move-action-taken` と合わせて読むこと）。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

### 2026-09-06（続き439）：基本設定を「7つのボタン」に（スマホで指1本分＋スクロール不要）／ランク戦が始まらない（対戦開始の押下を送り直す）／#290 スマホでボタンが端に寄りすぎ／カード文「上記の到達時の効果」→「上記の到達効果」

- **基本設定の1画面目を絞った（ユーザー承認済み・PCも共通）**: 以前は
  言語／効果音／BGM／カード拡大サイズ／確認2つ／振動 がベタ置きで **12行**並んでいた。
  中身は減らさず、性質でまとめて畳む——**①言語 ②🔊音量 ③💬案内表示 ④詳細設定▸ ⑤📋山札一覧
  ⑥（対局中は降参・未実装）⑦🏠タイトルに戻る**。カード拡大サイズは詳細設定の「画面の見え方」へ移した
  （見た目の微調整で毎回触る設定ではないため）。
  - **降参は「移動」ではなく新規機能**（コードに存在しない）。オンラインでの扱い（相手の勝ちとして
    レートに載せるか・放置離脱と何が違うか）を決める必要があるので、今回は入れていない。
- **スマホで各行を指1本分にした**: このアプリは 1600x900 の「ステージ」を画面に合わせて縮めて
  描くので、CSSの 1rem はスマホで実画面 7px 前後にしかならない。**実測（iPhone横向き 844x390・
  倍率0.433）で、チェックの行 11.7px／チェックボックス本体 5.6px／選択ボタン 11.1px／
  スライダーの帯 6.9px**＝目安（Apple 44pt / Google 48dp）の 1/4〜1/8 だった。
  - **パネル全体を transform で拡大するのは不可**（既存の `--options-menu-scale-phone` で 2.3倍に
    して実測: パネルが**画面外へ 826px はみ出し**、表示中の全行が画面外、しかも transform なので
    スクロールでも届かない）。**行そのものを大きくして、増えた分は縦スクロールに任せる**方式にした。
  - **実測（修正後）**: 言語48.4 / 音量43.8 / 案内表示43.8 / 詳細設定39.3 / 山札一覧36 /
    タイトルに戻る36 px ＝**すべて実画面36〜48px**。しかも項目を6つに減らしたので
    **中身の下端331px < 画面390px＝スクロール不要**で全部見える。PCは折りたたみになった分むしろ短い。
- **ランク戦で「お互い対戦開始を押したのに始まらない」（自力復帰を実装）**: 押すと
  `so7_ranked_ready` を1回だけ送り、null（まだ相手待ち）ならポーリングが `ingame` を拾うのを待つ、
  という作りだった。**1回目の送信が失敗しても、返り値が null なのと見分けが付かない**
  （`readyRanked` はエラーを握り潰して null を返す）＝「相手を待っています…」のまま永久に止まる。
  サーバー側の `so7_ranked_ready` は**冪等**（既に ready なら足さない・対局作成済みなら game_id を
  返す）なので、**押した後はポーリングのたびに送り直す**ようにした。全員が押した瞬間に対局が作られ、
  サーバーの締め切り処理（62秒）を待たなくても始まる。
  - 締め切り＋20秒を過ぎても始まらなければ、ボタンを押せる状態へ戻して案内を出す（`rm.readyStuck`）。
    押しっぱなしで固まったままにしない。
  - `diag-ranked-ready`（press / retry / stuck）を行動ログに残す。次に止まった時、
    「そもそも送れていない」のか「サーバーが対局を作らない」のかが読める。
  - **未確認**: 本番DBの `so7_ranked_ready` / `so7_ranked_poll` が最新版かは手元から確認できない。
    もし古ければ「押した人だけで開始」（poll の 62秒締め切り）が効かないが、今回のクライアント側の
    送り直しは**それとは独立に**効く（全員が押せば必ず始まる）。
- **#290 の続き（スマホでボタンが端に寄りすぎて押せない）**: チュートリアルの
  「遊び方を知っているのでスキップ」「はじめから」は `left: 1rem; top: 0.6rem`＝スマホでは
  実画面 6〜8px しか離れておらず、端末側のスワイプ操作とも取り合いになっていた。
  `body.is-phone-device` の時だけ端から離し、ボタン自体も大きくした。
  **実測（932x430・倍率0.478）: 高さ 41px・画面左端から 107px**（修正前は高さ約10px・端から8px）。
  - あわせて、この検証で**#290 の修正が実際に効いていること**も確認できた——
    `showBlockingHint({ showBack: true, … })` を実際に呼んで、説明モーダルが `display: block` で
    出て「戻る」ボタンも表示された（修正前はここで例外が出て一度も表示されなかった）。
- **カード文の言い換え（ユーザー提案）**: 「上記の**到達時の効果**を得る。」→「上記の**到達効果**を
  得る。」に統一。**削減（2文字×6枚）より用語の統一の方が大きい**——他の効果文は既に「到達効果」で
  書かれており（マスチェンジ「相手はこのカードの到達効果を得ない」、試練の儀式「移動先の到達効果は
  得ない」）、同じものが2通りに呼ばれていた。`docs/cards.md`・`card-text.ja.js`（表示）・
  `card-effects.js`（生成文）・`card-dev-mode.js`（生成文と実物の突き合わせ）の**41箇所**をまとめて統一。
  英語は元から "Gain the Arrival Effect above." で統一済みなので変更なし。
  - **さらに削れる候補（実測つき・ユーザー判断待ち）**: ①ファーストカード7種＋ノワールの基本効果が
    **全部同一の56文字**（合計448文字）で、「これは/これの/これが」と「これ」が4回出る。主語は自明
    なので「他のカードの効果の対象にならない。ロック中でも手札効果を使え、その時これは捨てない。」
    （39文字）にできる。②【追色１】の但し書き「（これと同色のカードを自分の手札から１枚捨てることで
    次の効果を得る。）」（31文字）が複数枚に重複——**全カード共通のルール**なので、カード文から外して
    【追色】の表記自体に説明を紐づける（長押し/ホバー）方が根本的。③「この効果は１ターンに１度のみ
    得られる。」（18文字）→「１ターンに１度。」（8文字）。
- **検証**: `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` **56/56 PASS**、
  構文チェック（変更した全ファイル）通過、CSSブレース平衡（2980）、翻訳キー ja/en とも 1795 で一致、
  お知らせ ja/en とも4件。基本設定はスマホ/PCの両方で実測（上記）。
  サーバー側（Supabase）のコード変更は無い＝SQLの実行・Edge Function の再デプロイは不要。
- **スモークテストの誤検知を直した（テスト側）**: 今日 `node test/smoke.mjs 2` が2度「30秒ターンが
  進まない＝STALLED」で失敗した。テストログを読むと**対局は正常に進んでいた**——ザ・ギャンブルの
  到達効果を解決中で、`arrivalProcessing:true` のまま**到達から色宣言まで23秒**かかっていた
  （続き425〜427で「中央は一度に1つ」にした分、色宣言・鼓動・じらしを挟む効果は1ターンが長い）。
  盤面が1つも動かないので活動署名(sig)も変わらず、30秒の見張りに引っかかっていた。
  **アプリが自分で「まだ処理中」と言っている間（行動ログの `diag-auto-action-nothing` の
  arrivalProcessing / handEffectBusy / picker）は停止と数えない**ようにし、それでも120秒続いたら
  従来どおり失敗にする。**泣き叫ぶテストは本物の停止を隠す**ので、閾値を上げるのではなく
  「処理中かどうか」で判定する形にした。

### 2026-09-06（続き440）：カード文の統一をテキストファイルにも反映／短縮案①②③はユーザー判断で見送り（再提案しないこと）

- 続き439 でアプリ側の41箇所を統一した「上記の到達時の効果を得る。」→「上記の到達効果を得る。」を、
  **フォルダ内のテキストファイルにも反映**した（ユーザー指示）——
  `カード効果　テキスト.txt`（6箇所・.gitignore対象なのでローカルのみ）と
  `説明書校正/説明書_校正版.txt`（6箇所）。書き込む前に「UTF-8として読んで書き戻すとバイト列が
  一致するか」を確認してから置換している（Shift_JIS のファイルを壊さないため）。
- **アートディレクターへの報告案件**（ユーザー指示「次回アートディレクターに報告する事案として
  メモしておいて。報告するタイミングで報告書をまとめてもらう」）。カード現物の文面に関わるので、
  紙・入稿データ側を持っている人への連絡が要る。控えは**メモ（memory/art-director-report-queue.md）**に
  置いた。今後**カード文・カード名・ルール用語をアプリ側で変えたら必ずそこへ1行足す**こと。
- **短縮案①②③はユーザーが見送りを決定（再提案しないこと）**:
  - ①ファーストカードの基本効果から「これ」を減らす案 → **却下**。「他のカードの効果の対象に
    ならない。」だけにすると、**対象がプレイヤー自身だと誤読される恐れ**があるため。現状維持。
  - ②【追色１】の但し書きをカード文から外して表記に紐づける案 → **却下**。現状維持。
  - ③「この効果は１ターンに１度のみ得られる。」の短縮 → **却下**。現状維持。
- **演出の現状確認（実測・次に何をやるかの材料）**: 「ほかに演出で強化できるものは？」への回答用に
  コードを確認した結果——
  - **カウンターロック／ゴメンナサイで「防いだ」瞬間に専用の演出は無い**（該当するCSSクラス0件・
    演出の呼び出しも無し）。逆転が決まる一番気持ちのいい瞬間がモーダルの文字だけになっている。
  - **山札切れ→捨て場をひっくり返して山札にする**は `refillDeckFromDiscard(); render();` だけで、
    **無言で入れ替わる**。ルールブックの見せ場なのに何も起きない。
  - **⑧の残り（承認済み・未着手）**: ばらまく（`scatter`系のCSS 0件）／賭ける（宣言色の常駐表示
    `declared-colors-indicator` はあるが、当たり/外れで割れる・輝く演出は無い）。入れ替えは
    マスチェンジの電撃アークが既にある（コノハナサクヤへの流用は未）。


### 2026-09-06（続き441）：演出の強化（承認されたおすすめ順に3つ）＋不具合4件（#295 対局中に募集バナー／#296 ブーストカードの絵／#297 着地で駒が一瞬消える／#298 捨てさせた札が裏面）

ユーザー指示「演出で強化できるところ→おすすめ順で実装しよう！」＋不具合報告 #295〜#298。

- **【#297】移動の着地で駒が一瞬消える（真因確定・修正）**: 盤面のWebGL描画は
  ①ゲーム状態が変わった時（`subscribe`）②500msごとの保険、の2つでしか作り直していなかった。
  ところが駒の着地で走る `render()` は**隠していた駒を戻すためだけ**で状態を何も変えない——
  ①では拾われず、②までの**最大0.5秒**、実物の駒は「箱はあるが絵が無い」状態になる。跳ねる
  ゴーストは着地の2フレーム後に消えるので、その差がそのまま「一瞬消える」に見えていた。
  `render()` から毎回「描き直したよ」の合図を送る形にした（`board-3d-setting.js` に
  `setBoard3dInvalidator`/`invalidateBoard3d` を新設。board-3d.js は three.js を静的import
  しているので、main.js から直接importできない——board-3d-setting.js を作ったのと同じ理由）。
  - **教訓**: 「DOMを描き直す」ことと「WebGLを描き直す」ことが**別の合図で動いている**設計では、
    片方だけが走る経路が必ず生まれる。DOMを作り直す入口（render）から必ず合図を送る。
- **【#295】対局中なのに「ランク戦で対戦相手を募集中の人がいます！」が出る（原因確定・修正）**:
  `tick()` は先頭で `isOnlineMode()` を見ているが、その後に `getCurrentUser()` と
  `pollRanked()` の**2回の通信**（数秒かかることがある）を挟んでから通知する。その間に対局が
  始まっていると、先頭の判定はもう古い。**続き437の総点検と全く同じ形**（「先頭で確かめたから
  大丈夫」は、間に待ち時間が1msでもあれば破れる）。出す直前にもう一度確かめるようにした。
  あわせて、**バナーは数十秒出しっぱなし**なので、表示中に対局が始まったら自分で引っ込む
  見張りを付けた（1秒ごと・バナーが消えたら止まる）。
- **【#298】セレスティアで「捨てさせた」モーダルのカードが裏面（原因確定・修正）**: オンラインでは
  相手の手札の中身はサーバー側で伏せられている（`cardId` が null）ため、儀式ピックで選んだ
  直後の時点では何の札か分からず裏面になっていた。**捨てた後なら捨て場の一番上＝公開情報**
  として実際の札が読める（engine 側は既にその方法で名前を出していた）。中央の公開だけを
  「捨てた後」まで持ち越す形にした（`deferReveal` に override 用の cardId を渡せるようにし、
  engine が `helpers.showForcedDiscardReveal(discardedCardId)` で呼ぶ）。ユーザー確認済み
  「捨てるカードは公開でいいです」。奪われた側の画面も同じ関数を通るので両方とも表向きに揃う。
- **【#296／#291】スマホでブーストカードが描画されない（再現できず・データ側の不備は修正）**:
  **Chromium/WebKit × PC幅/スマホ幅の4通り**で実際にブーストモードの盤面を作って測ったが、
  どれもDOM要素・インラインのデータURI・WebGLの板（visible:true / opacity:1）が揃っており
  再現しなかった。ただし画像データ自体に不備があった——`data:image/svg+xml;**utf8**,` の
  `utf8` は**正しくないMIMEパラメータ**（正しくは `charset=utf-8`）で、厳しめのパーサーは
  これを理由に読み込みを拒む。しかもこの文字列は CSSの `url()`・WebGLのテクスチャ読み込み・
  `<img>` の3経路すべてを通るので、パーセントエンコードのままだと経路ごとの解釈の違いを踏む
  余地が残る。**正しいcharset + base64** に変えた（日本語「効果なし」を含むので `btoa` には
  必ずUTF-8のバイト列を渡す）。これで直らなければ、次は画面のどの部分かを指してもらう。

- **【演出①・おすすめ1位】「防いだ！」の瞬間（カウンターロック／ゴメンナサイ）**: このゲームで
  一番の見せ場なのに専用の動きが**1つも無かった**（grepで該当CSS 0件・呼び出し0件を確認済み）。
  守った人のところ（駒のマス、駒が無ければ自分のロックエリア）に**六角形の盾**が張られ、
  **衝撃波**が2重に広がり、**攻めた側の駒がよろける**。画面中央に「防いだ！」が一瞬だけ出る。
  色は**守った人の色**（誰が防いだのかが色で分かる。奪う光の筋 playStealBeam と同じ考え方）。
  - 入れる場所は「止まったことが確定した瞬間」——カウンターロックは `respondToContact(false)`
    の直後、ゴメンナサイはコストと本体を払い終えた直後（奪う札の中央表示より前）。
  - `beginBoardAnimation`/`endBoardAnimation` で囲う。囲わないと #266 で決めた「画面の中央は
    一度に1つ・演出中はお知らせを待たせる」に乗らず、盾の上に無効化の告知モーダルが被る。
- **【演出②・おすすめ2位】山札切れ→捨て場をひっくり返して新しい山札にする**: ルール上とても
  大きな出来事なのに `refillDeckFromDiscard(); render();` と黙って入れ替わるだけだった。
  捨て場の山がまるごと裏返りながら山札の位置へ移り、着いたところで光の輪が広がる（山札の
  沈み込み「トン」は、枚数が増えたことで既存の `.stack.is-settling` が自動的に付く＝続き435）。
  - **見つけ方が要点**: 呼び出し口に演出を足すのではなく、**「捨て場が空になり山札が増えた」と
    いう事実**を `render()` の先頭で見て拾う。こうするとローカルの自動補充でも、サーバー側で
    補充されるオンライン（so7-apply-action.ts）でも同じ1か所で拾える。呼び出し口を1つずつ
    追いかけると必ずどれかを取りこぼす（このプロジェクトで何度も踏んでいる形）。
  - 呼ぶのは render() の**先頭**。この時点のDOMはまだ前回の状態（＝捨て場に山がある）なので、
    そこから位置と一番上の絵を測れる。状態の方はもう新しい。
  - めくれ演出の部品（`.card-flip-*`）をそのまま使い、`animation-direction: reverse` で
    向きだけ逆（表→裏）にした。
- **【演出③・おすすめ3位＝⑧の残り】**:
  - **ばらまく**（増殖する樹々・合同建設・白の意思ほか）: 着地したマスから**そのカードの色の粒が
    6個、四方へ弾ける**。カード1枚ごとに出るので、複数マスに配る効果では自然に「順番に光が
    飛ぶ」流れになる（配置は1枚ずつ await されるため）。入れた場所は `playCardCellLanding` の
    着地の瞬間1か所＝**カード効果でもドラッグ&ドロップでも同じように出る**。
  - **賭ける**（ザ・ギャンブル・試練の儀式）: 宣言色の丸（`declared-colors-indicator`）は
    「結果が出た瞬間に黙って消える」だけだった。**当たった色は大きく輝いて割れ、外れた色は
    ひび割れて灰色に沈む**。engine 側から `announceColorsResolved({ hit, color })` で結果を
    渡す（引数なしの従来の呼び方でも壊れない）。
- **入れ替え（マスチェンジの電撃アークをコノハナサクヤへ流用）は今回やっていない**（⑧の残りの
  うち、ユーザーが挙げた3つのうち2つを実装した）。

- **検証**: `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` 56/56 PASS、
  `node test/smoke.mjs 2` PASS、CSSブレース平衡（3032）、翻訳キー ja/en とも同数（片方にしか
  無いキー0件）、変更した全ファイルの構文チェック通過。ヘッドレス実測は下記コミット参照。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。


### 2026-09-06（続き442）：#301/#302 「置かれる瞬間に一瞬消える」の本当の原因（続き441の修正は**そもそもコードに入っていなかった**）／#303 パーティで取った表向きの札が裏面／#304 合同建設の選択モーダルがダーク

デプロイ直後に5件（#301〜#305）。**#301/#302 は、続き441 で「直した」と報告した #297 と同じ症状**で、
報告者の版（20260906-111655）はその修正が入った版だった。つまり私の修正が効いていなかった。

- **【最重要・自分の失敗】修正が1文字もファイルに入っていなかった**: 続き441 で board-3d.js へ
  「合図の受け口を登録する」2行（`setBoard3dInvalidator(markDirty)` と解除側）を足したつもりが、
  **CRLF のファイルに LF の複数行アンカーで `String.replace` を掛けたため、静かに何も置換されて
  いなかった**（`replace` は見つからなくてもエラーにならない）。import 行だけは1行アンカーだったので
  通っており、構文チェックも check-undeclared も npm test も素通りした。
  検証も「render() が合図を送るか」をスパイで見ただけで、**受け取る側が登録されているか**を
  見ていなかった（当時「3行なので読めば分かる」と判断したのが誤り）。
  - **今後の作法（このプロジェクトで徹底する）**: ①置換は必ず「見つかったか・一意か」を検査して
    **見つからなければ例外を投げる**ヘルパー経由で行う（今回から `rep(a,b)` を使っている）
    ②検証は「送る側」だけでなく**「受け取る側が繋がっているか」まで測る**。
- **【本当の原因・修正】仮に入っていても足りなかった**: 飛翔演出は **render() を通らない**。
  実物のカード・駒を `style.visibility` で直接隠して／見せているだけなので、render() から送る合図は
  一度も飛ばない（#302 のロックカードはまさにこれ）。しかも合図が飛んでも、実際に描き直されるのは
  **次のフレーム**——実機のログでは1フレームが **60〜200ms**（YGMのiPhoneで frameMs 100〜244）なので、
  その間ずっと「箱はあるが絵が無い」＝駒が枠だけ／カードが消える、が見えてしまう。
  - **対策**: `board-3d-setting.js` に `setBoard3dFlusher` / `flushBoard3d()` を新設し、board-3d 側は
    「その場で（同期的に）作り直して描く」`flushNow()` を登録する。実物を見せた**直後**に呼べば、
    DOMの変化とWebGLの絵が**同じ描画のタイミング**で入れ替わる。
  - 呼ぶ場所は「実物を `visibility` で隠した／見せた」全箇所＝追色コストの札／マス→手札の持ち上げ／
    カードの着地／手札への着地／ドラッグ開始／手札シャッフル（隠す2・戻す1）／駒の着地の render 直後、
    の計12か所。**めくれ演出は順番も直した**——以前は「ゴーストを消す→render()」の順で、実物が
    描かれるまでの数フレーム、カードが消えて見えていた。「render → flush → ゴーストを消す」にした。
  - ★演出を書く時のルールとしてファイル冒頭にも明記した: **盤面の実物を visibility で隠す／見せたら、
    その直後に flushBoard3d() を呼ぶ**。render() を呼んでいる場合も、ゴーストを消す前に呼ぶ。
  - **実測（今度は配線まで確認した）**: 静かな状態で `.board-card` を1枚 visibility:hidden にすると、
    **合図なしでは板の数が 198 のまま変わらず**（＝これが今までの「絵が無い」状態）、
    `flushBoard3d()` を呼ぶと **その場で 196 に減り、戻すとその場で 198 に戻る**。
    board-3d を停止→再開しても受け口が戻ることも確認（前回落ちた穴をふさぐため）。
- **#303「パーティで場の表向きのカードを手札に加えたのに、中央のお知らせが裏面」（対策＋診断）**:
  `runPartyOptionTask` は**動かす前に控えた古い写し**の `token.cardId` をお知らせに渡していた。
  オンラインでは札の中身がサーバー側で伏せられていることがあり、その場合 null＝裏面になる。
  手札に入った後なら本人の画面では必ず中身が分かるので、**最新の状態から読み直して**渡すようにした
  （#298 の「捨てた後なら捨て場から読める」と同じ考え方）。手元では再現できなかったので、
  `diag-party-pickup`（前後の cardId・表向きだったか）も残した。次の報告で確定できる。
- **#304「合同建設の『山札から／手札から』のモーダルがダークのまま」（修正）**: このモーダルは
  儀式ピック（`#sleight-ritual-modal`）の見た目を流用しているが、あちらは**裏向きのカードを並べて
  選ぶ場面なので暗いことに意味がある**。こちらはカードを見せない純粋な二択なので、目印のクラス
  （`.is-place-source-choice`）を付けて**ライトモードだけ**明るい地に切り替えた（儀式ピック本体は不変）。
- **#305「スマホでマイデッキの手札の印が出ない」（仕様の確認をユーザーへ依頼・コード変更なし）**:
  マイデッキ札の「誰の札か」の印（続き430）は、**他人のデッキ由来の札だけ・左上**に付ける設計。
  自分のデッキから引いた札には元から付かない（マイデッキ戦では手札のほとんどが自分の札なので、
  全部に付けると印の意味が無くなるため）。スマホ専用の分岐は無く、PCでも同じ挙動。
  「右上」という指定とも合わないので、どの印を指しているのかをユーザーに確認する。
- **検証**: `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` 56/56 PASS、
  `node test/smoke.mjs 2` PASS、CSSブレース平衡（3036）、構文チェック通過、お知らせ ja/en とも3件。
  ヘッドレスの実測は上記のとおり（板 198→196→198 が**同期的に**入れ替わる／合図なしでは 198 のまま）。
  あわせてCPU自己対戦345サンプルで「見えているのにWebGLの板が無い」要素を数え、飛翔ゴーストが
  出ているフレーム74件を含めて**残り 49/約19000 チェック（0.26%）**まで下がった（0にはなっていない。
  飛翔以外の普通の描き直しには1フレームの窓が残るが、そこは実物が動かないので見え方に出にくい）。
  **プローブ自身の欠陥も1つ潰した**——最初は要素自身の opacity しか見ておらず、親の `.piece` が
  opacity:0 で隠している駒まで「消えている」と数えていた（211件）。祖先まで遡る判定に直した。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。


### 2026-09-06（続き443）：#305 マイデッキ戦の手札の印を「他人の札だけ」から「3種類とも」へ（ユーザー判断で方針変更）

- **経緯**: 続き430 で「マイデッキ戦では手札のほとんどが自分の札なので、全部に印を付けると印の
  意味が無くなる」と判断し、**他人のデッキ由来の札にだけ**印を付けていた。これに対しユーザーから
  「マイデッキから引いたものと共有の山札から引いたものと他人のマイデッキのカード、**これらは裏面が
  すべて異なる**。なので自分の手札で見分けがついた方が良い」との指摘。**裏面が3種類ある以上、
  「印が無い＝どれ？」が分からない**というのが実態で、指摘の方が正しい。3種類とも印を付ける形に変えた。
- **実装**: `myDeckForeignOwnerOf`（他人の札か）はそのまま残し（並べ替えの判定で使う）、新しく
  `myDeckHandOriginOf(token, handOwner)` が `{ kind: "own" | "foreign" | "shared", owner }` を返す。
  角めくれは元から**その札の実際の裏面**（`cardBackImageForToken`）を敷くので、3種類がそのまま
  絵で見分けられる。折り目の線の色は持ち主の駒の色／共有の山札は**銀色**（どのプレイヤーの色でも
  ないことが色でも分かるように）。アバター版は共有だけ 🂠 にする（持ち主が居ないため）。
  拡大表示の注記も3種類に対応（手札・手札公開エリアの札だけ。盤面のカードは従来どおり持ち主だけ）。
  文言 `game.myDeck.ownNote` / `game.myDeck.sharedNote` を ja/en 追加。
- **検証（ヘッドレス）**: 自分(A)の手札に「自分の札／共有の山札／CPU(C)の札」を1枚ずつ置いて実測——
  角めくれが3枚とも付き、**裏面の画像が3種類とも別**（back-normal-2 / back-normal / back-normal-5）、
  折り目の色は 自分=赤(#c70025) / 共有=銀(rgba(226,232,240,.9)) / 相手=緑(#22ac38)、
  注記は「自分のマイデッキの札」「共有の山札の札」「CPU のマイデッキの札」。
  **管理者パネルの実際のセレクトを操作**して アバター版に切り替えると共有だけ `is-shared` の 🂠、
  「印を出さない」で **0個**、**マイデッキ戦でない普通の対戦では 0個**（通常戦で印が増えない）。例外0件。
  - 検証の作法メモ: この設定に外から呼べる setter は無い（管理者パネルのセレクトだけ）。
    最初 `admin.setMyDeckHandMarkStyle?.(...)` と書いたら **`?.` で静かに何もせず**、
    「アバター版が出ない・印を消せない」という**偽の失敗**を測ってしまった。実際のセレクトを
    操作する形に直した（続き430と同じやり方）。**存在しない関数を `?.` で呼ぶと失敗が見えない**。
- **検証**: `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` 56/56 PASS、
  `node test/smoke.mjs 2` PASS、CSSブレース平衡（3037）、翻訳キー ja/en とも同数。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。


### 2026-09-06（続き444）：共有の山札の札は「折り目」を出さない（続き443の微調整）

- ユーザー指示「共有の山札の場合は折り目無しでお願いします」。続き443では3種類とも角めくれ＋
  折り目の線（共有は銀色）にしていたが、**マイデッキの札（自分・相手）だけが「角が折れている」**
  形にすると、通常の札（共有の山札）が静かになり3種類がより見分けやすい。
  角からのぞく裏面自体はそのまま残す（それが何の札かを示す本体のため）。
- 実装: 角めくれに `is-shared` を付け、CSSで折り目（`::after`）だけを `display: none` にする。
- **実測**: 自分=折り目あり(display:block・橙) / 相手=折り目あり(赤) / **共有=折り目なし(display:none)**、
  裏面の画像は3種類とも別のまま。アバター版・「印を出さない」・通常戦0個も従来どおり。例外0件。
- お知らせ（changelog.js）は、数分前に配信した続き443の項目と**同じ機能の説明**なので、項目を
  増やさず最終形の文言に直した（プレイヤーには途中の形は見せない方が分かりやすいため）。
- **検証**: `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` 56/56 PASS、
  CSSブレース平衡（3038）、構文チェック通過。サーバー側（Supabase）の変更は無い＝SQLの実行・
  Edge Function の再デプロイは不要。


### 2026-09-06（続き445）：#307 ブーストカードだけ iPhone で描かれない＝**全カードで唯一のSVG**をWebGLのテクスチャに載せていた／#306 募集バナーの「参加する」がホームを開くだけだった

- **【#307・実機のスクリーンショットで確定】**（#291→#296→#307 と3回目の報告）。今回は画像が
  付いていたので**実物を見て**判断できた——盤面49マスのカード・ファーストカード・駒・左上の
  UIアイコン（これもインラインSVG）はすべて正しく描かれていて、**ブーストカードのスロットだけが空**。
  ブーストカードは**全カード中でただ1枚だけSVGで描いている仮デザイン**で、他は全部 webp/png。
  盤面はWebGL（three.js）で描いており、**SVG画像をWebGLのテクスチャに載せる経路は iOS Safari で
  昔から不安定**。UIアイコンのSVGはDOMのまま描かれるので無事、という切り分けとも一致する。
  - **私の以前の誤り**: 続き441で「Playwright の WebKit で再現しなかったのでSVGは無実」と判断したが、
    **Windows上の Playwright WebKit は iOS Safari とは描画の土台が別物**で、WebGLのテクスチャ周りの
    根拠にはならない。「WebKitで大丈夫だった」を根拠にしてはいけなかった。
  - **修正**: SVGをやめ、**同じ絵を canvas に描いて PNG のデータURIにして返す**（色ごと・言語ごとに
    1回だけ作って使い回す）。PNGはどの環境でも同じようにデコードされ、WebGLのテクスチャにも
    そのまま載る。見た目は今までと同じ仮デザインのまま。`ctx.roundRect` は iOS16未満に無いので
    角丸は手で描く。canvasが使えない環境（Node上のテスト）だけ従来のSVGへ落とす。
  - **ユーザー要望「原因を調査するログを仕込んでください」**: `diag-boost-render` を追加（対局開始の
    3秒後に1回）。札ごとに **el**（DOM要素があるか）/ **img**（入っている画像の種類。`data:image/png`
    なら今回の版）/ **decoded**（ブラウザが実際にデコードできたか）/ **quad**（WebGLの板があるか・
    見えているか）を残す。次に同じ報告が来たら、状態・画像データ・描画のどこで落ちているかが
    推測なしで分かる。
  - **実測**: Chromium と WebKit の両方で、返る文字列が `data:image/png;base64,` になり、
    **256x256 でデコードでき**、中身の画素が実際に色を持ち（青なら 27,184,206）、
    `texImage2D` が `ok`／`glError: none`、2回目の呼び出しは同じ文字列を使い回す。
- **【#306】募集バナーの「参加する」が効かない（原因確定・修正）**: この案内の「参加する」は
  **ホーム画面を開くだけ**で、そこからランク戦のタイルを押すのは利用者任せだった。ホームが
  既に開いていれば見た目に何も起きず、タイトル画面やオープニングが手前にある時もその裏で
  ホームが開くだけ＝やはり何も起きない（報告時は `mode:"title"`・再読み込み直後だった）。
  ボタンの文言どおり**ランク戦へ直行**する（オープニングが出ていれば閉じ、ホームも閉じてから
  `startRankedMatchmaking` を呼ぶ。キャンセル時はホームへ戻す）。既に対局中なら何もしない。
- **検証**: `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` 56/56 PASS、
  `node test/smoke.mjs 2` PASS、構文チェック通過、お知らせ ja/en とも2件。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。


### 2026-09-06（続き446）：#309 共有の山札の札は「角の折れ」自体を出さない（続き444は折り目の線しか消していなかった）

- ユーザー報告「共有の山札から引いたカードの右上がまだ折れてます」＋スクリーンショット。
  続き444では `.hand-card-mydeck-dogear.is-shared::after { display: none }` で**折り目の線だけ**を
  消し、三角に切り抜いた裏面（＝角がめくれて見える本体）はそのまま残していた。**折れて見える
  正体はその三角の切り抜きの方**なので、指示（「折り目無しで」）に応えられていなかった。
  `buildMyDeckHandMark` で `origin.kind === "shared"` なら**印そのものを作らない**ようにした。
  結果、**角が折れていない札＝共有の山札の札**という3種類の見分けになる（マイデッキの札は
  折り目の色で自分／相手が分かる）。拡大表示の注記「共有の山札の札」は別経路なのでそのまま残る。
  アバター版（管理者モードで切り替え）の 🂠 は折れて見えないのでそのまま。
- **画像を実際に見て確認した**（報告のスクリーンショットを5〜9倍に拡大）。手札2枚とも角に
  別の絵がのぞいており、報告時のログとも一致する——15:16:46 `my-deck-draw`（自分のマイデッキ
  から1枚）と 15:17:06 の公開ドロー（＝共有の山札から1枚）で、手札はちょうどその2枚だった。
  なお**印はカードの左上に置いている**（実測 relLeft/relTop ≒ 1px、`clip-path: polygon(0 0,
  100% 0, 0 100%)`）。ユーザーは #305 から一貫して「右上」と言うが、コード・実測ともに左上で、
  他に折れて見える要素は無い（`.hand-card-public-eye` も左上だが 👁 の四角）。呼び方の違いと
  判断してそのまま進めた。
- **副次的に直ったもの**: 公開ドローの札は必ず共有の山札由来なので、これまで左上の 👁 印が
  この角めくれ（z-index 3）に隠れていた。印が出なくなったので 👁 が見えるようになる。
- **検証**（ヘッドレス）: 自分(A)の手札に「自分の札／共有の山札／CPU(C)の札」を1枚ずつ置いて実測
  ——**共有だけ印が無く**（mark: false）、自分＝緑(#22ac38)・相手＝橙(#ee781f)の折り目付きで
  裏面画像も3種類とも別。管理者パネルの実際のセレクトを操作して**アバター版では共有だけ 🂠**、
  「印を出さない」で0個、**マイデッキ戦でない普通の対戦でも0個**。例外0件。
  `node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` 56/56 PASS、
  `node test/smoke.mjs 2` PASS、CSSブレース平衡（3038）、構文チェック通過。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。


### 2026-09-06（続き447）：CPUの打ち方3件（#310 ディメンションの二度撃ち／#313 ワイナウエアの狙い／#311 合同建設は自ゲートへ）／#312 どのマスを捨てたか分かるようにした

ユーザー報告4件。#312 以外はすべて**CPUの思考の質**についての要望で、不具合ではない。

- **#310「CPUが1ターンに2回ディメンションを使用しました。一回しか効果ないのに勿体無い」（修正）**:
  ディメンション（first-purple）は `usageLimit` を持たない＝ルール上は何度でも撃てるが、
  「このターンの移動が2マス」は重ねても増えないので、2回目は**追色コストで手札を1枚失うだけの丸損**。
  cpu-brain の評価に `isMovementBoostActiveThisTurn(seat)` を見る1行を足し、発動済みなら0点（＝撃たない）に
  した。**ルール側は変えていない**（人間は今までどおり2回でも撃てる）。
  cpu-brain.js → card-effect-engine.js の import は循環しない（あちらは cpu-brain を import しない）。

- **#313「ワイナウェアで破壊するのは相手が欲しそうな色のカードまたは相手の移動候補がいい」（修正）**:
  マス選択の自動判断は `chooseEffectCell` 1本で、**用途を区別していなかった**のが根っこ。この関数は元々
  「拾う／オープンする」前提で、優先1が**相手ゲート**、優先5が**自分がまだ要る色**——どちらも
  「壊す」では真逆（相手ゲートのカードを壊すと自分の侵攻の足場が消え、要る色を壊すのは自滅）。
  実測でも、purpose を渡さない従来の判断は20回中20回**相手ゲート**を選んでいた。
  `purpose` を受け取れるようにし、`"destroy"` の時だけ専用の採点（chooseCellToDestroy）に切り替える:
  ①相手の侵攻の踏み台（自ゲートへの次の一歩）+5 ②相手の“あと1色”の表向きカード+4／要る色+3
  ③相手の駒の隣（移動先の候補を減らす）+2 ④自ゲートは相手が近い時だけ+3、普段は−2
  ⑤積み重なっている分+1〜2 ⑥自滅の減点（自分が要る色−4／自分の次の一歩−3／相手ゲート−5）。
  purpose は engine（DISCARD_ALL_AT_CHOSEN_CELL）→ main.js の cell picker → 自動代行、と通す。

- **#311「ゲート侵攻ができそうなら合同建設などでは自分のゲートに置くのがいい。後で回収できるカード
  なので。しかし相手が自分のゲートの近くにいるならやらない方がいい」（修正）**: 従来は
  **「自分のゲートには置かない」と決め打ち**していた（空の自ゲートは着地不能で安全なのに、置くと
  相手に足場を与える＝自滅、という判断）。ユーザーの指摘どおり、**ゲート侵攻の3段階目で自ゲートの
  カードは自分の手札へ回収される**ので、侵攻が見込める時は自ゲートに置くのが一番得。条件を
  「①今まさに相手ゲートに乗っている or 1マスで乗れる ②相手の駒が自ゲートから3マス以内にいない」
  にして自ゲートへ置く。それ以外は従来どおり（自ゲートを避けて相手ゲートに近い空きマス）。
  判断は main.js に散らさず cpu-brain.js の `chooseEmptyCellToPlace` へ集約した（既存のゲート判定
  ヘルパーがそこに揃っているため）。

- **#312「ワイナウェアの効果でどのマスを捨てたのか分かりづらかった」（修正）**: 盤面からカードが
  消えるだけなので、どこが対象だったのかが分からなかった。配置系で実績のあるハイライトを流用し、
  **捨てている間はそのマスが光り（markPlacementTarget）、捨て終わった後も3秒残る（markPlacedLocation）**。
  お知らせの文言も「光っているマスのカード{n}枚をすべて捨てました。」に変えて枚数を添えた
  （`ce.winewareDiscarded` を ja/en 追加）。

- **検証（ヘッドレス・実測）**:
  ・#313: C=あと青だけ／A=あと黄だけの盤面で候補3つ（黄の表向き／青の表向き／Aのゲート）を与えると、
  　**20回中20回とも黄のマス**を選ぶ。purpose を渡さない従来の呼び方では**20回とも相手ゲート**
  　（＝用途の切り替えが効いていることの裏付け）。
  ・#311: 侵攻できて相手が遠い→**自ゲート(0,3)**／相手が自ゲートの目の前→**別のマス(4,5)**／
  　侵攻の見込み無し→**別のマス(4,5)**。
  ・#310: 同じ盤面で `chooseHandEffectCard` が **発動前 first-purple → 発動後 null**。発動は
  　**engine の本物の経路**（runHandEffect）で行い `isMovementBoostActiveThisTurn` が true に
  　なることも確認（送る側だけでなく効いているかを測る＝続き442の教訓）。
  ・#312/#313: engine の runHandEffect（本物）を stub helpers で走らせ、pickLocation へ
  　`{purpose:"destroy"}` が渡り、`markPlacementTarget`→`markPlacedLocation` が対象マス(1,1)で
  　呼ばれ、文言が「光っているマスのカード2枚をすべて捨てました。」になることを実測。
  　**受け取り側**（main.js が両ヘルパーを実際の markEffectPlacementTarget / markEffectJustPlaced へ
  　配線していること）も確認済み（手札効果・到達効果の両方の helpers に存在）。
  ・`node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` 56/56 PASS、
  　`node test/smoke.mjs 2` PASS、CSSブレース平衡（3038）、翻訳キー ja/en とも同数、構文チェック通過。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **申し送り（今回あえてやらなかったこと）**: ワイナウエアのハイライトを**実際の画面操作**で端から端まで
  通す検証は、カード開発モードから手札効果を呼ぶ経路（追色コストの札を選ぶ→マスを選ぶ→確認）で
  試したが、合成したポインタ操作が手札の当たり判定に届かず最後まで通せなかった（3D階層の当たり判定
  ＝elementsFromPoint 前提の作りのため）。engine 側の呼び出しと main.js 側の配線の両方を測って
  代替としたが、**実機での見え方の確認をお願いしたい**。


### 2026-09-06（続き448）：接触の見せ方を「タックル→手札選択→奪ったカード→ゲートへ」の順に／その過程で見つけた2つの実害（優先権から『今選んでいるのは誰か』を推測していた箇所）／黒のカードとなないろの欠片2枚ロックの演出

ユーザー指示2件——①「接触演出について、タックル演出→相手の手札選択→奪ったカードモーダル→相手は
自ゲートへワープの流れにできますか？今は手札選択が最初に来ています。」②「黒のカードがロックエリアに
置かれるときは演出があってもいいかも！なないろの欠片2枚ロックも壮大な演出でもいいかもね。」

- **①接触の順番（`respondToContactInner`）**: 従来は「奪う札を選ぶ→優先権を防御側へ→タックル→
  状態変更→ゲートへ飛ぶ→奪ったカードの表示」で、**突進する前に結果（何を奪うか）が分かって**いた。
  選ぶ処理を関数（`runContactStealPick`）にまとめ、**タックルの後**に呼ぶ形へ組み替えた。奪ったカードの
  中央表示も飛翔の後から**飛翔の前**へ移した。オンラインでは attacker へ「選んでいいよ」を送るのも
  この中なので、attacker の画面でも同じ順番になる。
  - **盤面の描き直しの止め方も変えた**: 以前はタックル開始〜飛翔終了までずっと止めていたが、間に
    「人が選ぶ」時間が入るようになったので、**タックルが終わったらいったん再開**し、状態変更の直前で
    また止める。止めっぱなしだと、選んでいる間にオンラインで届いた他の変化が画面に出ない。
  - **観戦側（attacker含む）の待ち時間を4秒→45秒に**。あちらは「タックル→状態変更が届くのを待つ→
    飛翔」という作りで、状態変更が届くのが人の操作の後になったため（待っている間は描き直しを止めてある）。
  - **保険タイマー（20秒で優先権を戻す）を、選び終えてから動かすようにした**。優先権の移譲は今までどおり
    早い段階で行う（その間ターンを保持したいため）が、タイマーまで一緒に動かすと**人が選んでいる時間が
    20秒の猶予に食い込み、まだ解決中なのに誤発火**する（実測で発生した）。
  - 選ばずに閉じた時（キャンセル）は、移した優先権と保険タイマーを元に戻してから抜ける。

- **【この順番変更で表に出た実害①】奪う札を選ぶのがCPUに乗っ取られた**: 儀式ピックは「今この選択を
  しているのは誰か」を、呼び出し側が `actingSeat` を渡さない時は**優先権の持ち主**で推測する。
  順番を変えたことで優先権は既に防御側（CPU）へ移った後になり、**人間が選ぶはずの場面をCPUが無作為に
  選んで**しまっていた（実測）。接触の呼び出しに `actingSeat: attacker` を明示した（#280 と同じ教訓）。

- **【実害②・こちらの方が重い】CPU戦で「カードを1枚も選べない」**: ローカルCPU戦には「CPUが選択待ちの
  モーダルを持っている間は人間が代わりに押せないよう、captureフェーズでクリックを握りつぶす」ガードが
  ある（main.js の document capture click）。この判定も**優先権**で「今は誰の選択か」を見ていたため、
  優先権が防御側（CPU）にある接触の解決中は、**人間が奪う札を選ぶクリックまで全部握りつぶされて**いた。
  ピッカーが持っている `owner`（＝actingSeat）を最優先で見るようにした。
  - **教訓（今回で3回目）**: 「今この選択をしているのは誰か」を**優先権から推測しない**。ターン終了時の
    ゲート侵攻・接触の解決中など、優先権が別の席にある場面は普通にある。
  - あわせて、承認/拒否が決まった時点で**接触の承認モーダルを必ず隠す**ようにした（人がボタンを押した
    時は隠れるが、CPU/AFK代行が決めた時は隠れず、z-index が儀式ピック(10621)より手前(10631)なので
    重なると選べなくなる）。

- **②黒のカードがロックエリアに置かれた時の演出（新規）**: 誘惑の黒の烙印・色落ちキャットは**ロックでは
  なく「置く」**なので `maybeAnnounceLock` が白黒を早期returnしており、**演出も音も一切出ていなかった**。
  ルール上の扱いはそのままに、暗い霧＋締まっていく封印のリング2枚＋紫の閃光（`.black-seal-*`）で
  「呪印が焼き付く」形にした。通常のロック（金色の刻印）と見た目が別物になるようにしてある。

- **②なないろの欠片の2枚ロックの演出（新規）**: LOCK_PAIR は `moveAndSync` で2枚まとめて動かすため
  こちらも `maybeAnnounceLock` を通らず**演出が無かった**。既にある虹色の柱（到達演出の `.is-rainbow`）を
  土台に、回る七色の光条＋外へ広がる七色のリング3枚＋白い芯（`.rainbow-lock-*`）、さらに**画面全体を
  横切る七色の帯**（`.rainbow-lock-sweep`、body直下＝盤面の3D階層の外。turn-announce の光の帯と同じ作り）。
  音は勝利演出の合成音を流用し、七色ぶん音が上がっていく（素材の追加は不要）。

- **見つけ方（重要）**: この2つは呼び出し口に演出を足すのではなく、**「ロックエリアに新しくカードが
  現れた」という事実**を render() の末尾で見て拾う（`maybeAnnounceSpecialLockPlacement`）。どちらも
  通常のロック経路を通らず、しかもローカル／オンライン（サーバーから届く差分）の両方で起こるため
  ——山札の作り直しの演出（続き441の `maybeAnnounceDeckRefill`）と同じ考え方。
  **一度に3枚以上が現れた時は出さない**（対局のセットアップ・途中参加の初回同期・リセットのような
  「まとめて入れ替わった」場合。実際の配置は1枚＝黒／2枚＝なないろの欠片）。

- **検証（ヘッドレス・実測）**:
  ・接触の順番: 実際にCPU戦で接触を起こし、**タックル 0.57秒 → 手札選択のモーダル 4.3秒 →
  　奪ったカードの表示 4.9秒 → ゲートへの飛翔 5.2秒**（行動ログも lunge-start → lunge-end →
  　state-move → flight-start → flight-end の順）。カードは**実際にクリックして**選んでいる。
  　保険タイマーの誤発火（`safety-return`）は0件。
  ・黒の演出: ロックスロットへ黒のカードを動かすと `.black-seal` が1つ・リング2枚・霧のアニメが動き、
  　リングの色が紫 rgb(168,85,247)。1.4秒後に片付いている。
  ・なないろの欠片: **1枚目では出ず（0件）**、2枚目で `.rainbow-lock` 1・リング3枚・画面全体の帯1・
  　光条と帯のアニメが動き、虹色の柱も1つ。2秒後に片付いている。
  ・回帰: 普通の色カードをロックしても新しい演出は0件／3枚以上まとめて置いた時も0件／例外0件。
  ・`node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` 56/56 PASS、
  　`node test/smoke.mjs 2` PASS、CSSブレース平衡（3080）、構文チェック通過、お知らせ ja/en とも4件。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **申し送り（検証のハマりどころ）**: ヘッドレスで「実際にカードをクリックする」検証をする時は、
  **物語チュートリアル（`so7-tutorial-completed` / `so7-eidos-progress-v1`）を先に既読にしておく**こと。
  操作チュートリアルの案内は document の capture フェーズでクリックを握りつぶすので、それが出ている限り
  何を押しても反応せず、**アプリ側の不具合と見分けが付かない**（今回それで長く迷った）。
  また `getState()` は毎回新しいオブジェクトを返すので、**dispatch を挟んだら取り直す**（古い写しに
  push したトークンは消える）。


### 2026-09-06（続き449）：#314 駒の奥の壁が描かれていた（backface-visibility を無視していた）／#317 設定画面ごしに盤面を触れていた／#318 基本設定の見た目を揃えた／#315 捨てるカードはまとめて1つ／#316 動けない時の救済を説明する

ユーザー報告5件（#314〜#318）。

- **#314「キューブの奥側上の辺の黒太線が気になります」（原因確定・修正）**: 駒の立方体の
  **奥の壁**（`.piece-wall-back`、CSSの `filter: brightness(0.35)` ＝5面で一番暗い）が、
  上面のさらに向こう側にそのまま見えていた。CSSでは `.piece-face` に
  `backface-visibility: hidden` があるので裏を向いた瞬間に消えるが、**WebGLの板は両面
  （DoubleSide）で描いていて、この指定を一切見ていなかった**。CSSと同じ判定——「その要素の
  表側（ローカル+Z）がカメラの方を向いているか」——を入れて、裏を向いた板は描かないようにした。
  Yを反転して置いてあるが、法線と視線の**両方**が同じように反転するので内積の符号は変わらず、
  three座標のまま計算してよい。
  ・実測: 駒1つあたり 上面=描く／前の壁=描く／**奥・左・右の壁=描かない**（左右の壁は法線が
  　立方体の内側を向いており、CSSでも同じく隠れている＝画面には元々出ていない）。

- **#317「基本設定画面で左側をタップしたらカードが出てきた！w」（原因確定・修正）**:
  盤面のタップ/ホバーは3D階層のネイティブ当たり判定が信用できないため `elementsFromPoint()` で
  自前に判定している＝**手前に何が乗っていようと、その下の盤面を触ってしまう**。返事待ちモーダル
  （`.so7-modal-backdrop`）は #250 で塞いだが、**全画面ページ**（ホーム/マイページ/ランキング/
  図鑑/マイデッキ）と**基本設定パネル**（常駐パネルなので dim 背景を持たない）は対象外だった。
  `isBoardCoveredByOtherScreen()` を足し、ホバー（拡大プレビュー・駒の名前）・タップ経路・
  ドラッグ開始の3か所で塞ぐ。ここでも**存在ではなく実際に出ているか**で見る（続き396/415の罠）。
  ・実測: 通常はホバーが効く → パネルを開くと**効かない・掴めない** → 閉じるとまた効く。

- **#318「スマホの基本設定画面のUIがダサい」（修正）**: ダサく見えていた正体は**同じ1画面に
  4種類の見た目が並んでいた**こと——言語だけ枠なし／音量・案内表示は細い枠／詳細設定は
  塗りつぶしの箱／山札一覧・タイトルに戻るは枠なしで左端の位置まで違う。中身は1つも変えず、
  **同じ形のカードが縦に並ぶ**だけに揃えた（`#options-menu-panel.is-basic-view` で基本設定の
  1画面目だけに限定。項目が多く性質も違う詳細設定ビューは従来どおり）。
  ・「押すと何かが開く」ものには右端に同じ矢印（›／開くと ⌄）を出す。文字に入っていた
  　「詳細設定 ▸」の ▸ は重複するので外した。言語の但し書きはカードの中へ入れて左端をそろえた。
  ・**ハマりどころ**: 矢印(::after)と「戻す」ボタンの**両方に margin-left:auto** を付けたため、
  　余白を半分ずつ分け合って「戻す」が行の真ん中に浮いた（実測）。見出しの行だけ矢印側の auto を
  　外す。**同じ詳細度なので、その打ち消しは矢印のルールより後ろに置くこと**（前に置いて効かず、
  　一度実測で気づいた）。

- **#315「ギャンブルで外した時、捨てるカードのミニモーダルは全てを一気に表示させたい」（修正）**:
  1枚ずつ「捨てました」を出しており、**中央は一度に1つ**（#266）なので手札の枚数ぶん順番待ちに
  なっていた。捨てる間は焼失演出だけを出し（`discardAndSync(id, { batchNotice: true })`）、
  捨て終わってから**1つのお知らせにまとめて**横並びで見せる（`announceCardsDiscarded`）。
  文言 `game.toast.discardedMany` / `game.chip.discardMany` を ja/en 追加。

- **#316「ムーブフェイズで動けなくて山札から隣に置いた時、その旨を知らしめるモーダルが欲しい」
  （実装）**: ルール上の救済（移動先も接触相手も無い＝隣へ山札から1枚裏向きで置いてターン終了）は、
  今までカードが1枚増えるだけで何の説明も無かった。置いた直後にお知らせを出す
  （`game.move.fallbackPlaced`）。phase-automation.js は main.js を import できないので、
  他のヘルパーと同じく `registerPhaseAutomationHelpers` で注入する。

- **検証（ヘッドレス・実測）**:
  ・#314: 駒の5面のうち **top / wall-front だけ visible:true**、wall-back / left / right は false。
  ・#317: 上記のとおり（ホバー・ドラッグ・復帰の3点）。
  ・#318: 基本設定を開いた実画面を修正前後で撮って比較（枠なし4種混在 → 同じカード6枚）。
  ・#315: **エンジンの本物**（runArrivalEffect でザ・ギャンブル）を stub helpers で走らせ、
  　`discardAndSync` が3枚とも `{batchNotice:true}` で呼ばれ、その後 `announceCardsDiscarded` が
  　**1回だけ・3枚分のidで**呼ばれることを確認。受け取り側も実際に出して**トースト1つ・カード3枚・
  　「プレイヤーAが3枚捨てました」**を実測（送る側だけでなく効いているかを測る＝続き442の教訓）。
  ・#316: **本物の** `runMoveFallbackNow("A")` を隣に空きマスがある盤面で走らせ、実際にお知らせが
  　出て文面が出ることを確認（空きマスが無い盤面では置かないので出ない＝正しい）。
  ・`node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` 56/56 PASS、
  　`node test/smoke.mjs 2` PASS、CSSブレース平衡（3093）、翻訳キー ja/en とも同数、構文チェック通過、
  　例外0件。サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **申し送り（実機で見てほしい）**: #314 は WebGL の描画なのでヘッドレスのスクリーンショットには
  写らない（`preserveDrawingBuffer` が既定オフ。続き412の限界）。「描く/描かない」の判定値は
  上のとおり実測したが、**駒の見え方そのもの**は実機で確認をお願いしたい。

- **未対応（ログから気づいたこと）**: 勝利演出のあと `diag-board3d` の `frameMs` が 740→1500ms まで
  上がり、しばらくして戻っている（報告 #318 のログ 17:19:40〜17:21:44）。勝利演出中は毎フレームの
  描画が極端に重くなっている可能性がある。報告は無いので今回は触っていないが、次に「勝った後
  カクつく」と言われたらここを見ること。


### 2026-09-06（続き450）：勝利演出が終わったあとも画面全体を重くし続けていた（報告 #318 のログで気づいた frameMs 740→1500 の正体）

ユーザー報告「確かに７色終結の画面になるのが遅いと感じています」。続き449で申し送りにしていた
「勝利演出のあと frameMs が 740→1500ms まで上がり、しばらく戻らない」を実測で切り分けた。

- **推測せず、まず切り分けた**: 実際に7色ロックさせて勝利演出を最後まで走らせ、①演出前 ②演出後 の
  **生のフレーム間隔（中央値）**を測り、そこから演出の要素を1つずつ止めて差分を見た（GPUの無い
  ヘッドレスなので絶対値は実機の参考にならないが、**何が効いているか**は比率で読める）。
  実測: 演出前 233ms → **演出後 333ms**（+43%）。止めると戻る要素は次の2つだった。

- **【原因①・最大】白の中の七色残光に掛けていた `filter: blur(18px)`**
  （`.vic-flash::after`）。全画面より一回り大きい板（`inset: -20%`）に七色のグラデーションを7枚
  重ね、それを**毎フレームぼかし直していた**。しかも `animation: … infinite alternate` で
  動き続ける。実測でこの1枚だけ止めると **333ms → 250ms**（＝演出前の水準）まで戻った。
  ・**直し方**: ぼかしをやめ、グラデーションを「中心からそのまま薄れる」形にして同じやわらかさを
  　出す（`#ff405c 0 12%, transparent 42%` → `#ff405c 0%, transparent 46%`）。不透明度0.1・白地の
  　上なので見分けは付かない。

- **【原因②】幕の `backdrop-filter: saturate(0.35) brightness(0.42)`**（`.vic-veil`、全画面）。
  これは演出中の「世界の色と光が引く」表現そのものなので**演出中は残す**。ただし白い光が画面を
  覆った後（`.is-flash`）は白の裏に隠れて見えないのに、**画面全体のフィルタを毎フレーム掛け続けて**
  いた。実測 333ms → 300ms。
  ・**直し方**: `.is-flash` で `backdrop-filter: none` にする。ただし外しただけだと**盤面が白の
  　向こうに薄く透けた**（白は 88% なので残り 12% に盤面が出る。実測: 背景の画素が
  　85,72,74＝盤面の色みを帯びる）。幕の**地の色そのもの**を `rgba(3,4,8,0.96)` にして盤面を隠す
  　（ただの塗りなので桁違いに軽い）。実測で 83,83,83 の無彩色になり、修正前と明るさは同じ。

- **【原因③・尾を引く方】演出が終わっても、これらが全部そのまま残り続けていた**。勝利演出は
  `keepWhite` で白い幕を残したまま **勝利モーダル→通貨→順位→個人結果→対戦終了パネル**の背景に
  なるので、**数分間**ずっと全画面のぼかしとフィルタが走っていた。報告 #318 のログで
  「17:19:40〜17:21:44 のあいだ frameMs が高いまま」だったのはこれ。
  ・**直し方**: 見せ場が終わった時点（`stage("RESULT")` の直前）で `root.classList.add("is-settled")`。
  　`.is-settled` では七色残光を出さない。**演出中（FLASH〜VICTORY の数秒）はそのまま見せる**ので、
  　見え方は変わらない。

- **あわせて（メモリ）**: 光の帯を描くキャンバスは、止めた後も**全画面ぶんの描画バッファ**
  （1600x900 × dpr の2乗＝最大20MB超）を抱えたままリザルトの背景として残っていた。止める時に
  `canvas.width = canvas.height = 0` で手放す（画像だけで既に苦しいiPhoneのGPUメモリ対策。#223 の系統）。

- **検証（実測）**:
  ・**演出後のフレーム間隔: 333ms → 267ms**（演出前 250ms・演出のDOMを丸ごと消した状態 267ms）
  　＝**演出が残していた負荷はほぼ0になった**（修正前は +80〜100ms）。
  ・段ごとの実測でも、リザルト段のフレーム間隔が 267ms → **233ms＝演出前と同じ**に戻った。
  ・**見た目が変わっていないこと**をスクリーンショットの画素で確認: 演出中（COLORS段）の背景は
  　修正前後で **完全に同一**（24,24,27 / 22,22,25 / 9,9,11 / 26,25,28 / 36,37,39 の5点とも一致）。
  　勝利表示の背景は 85,72,74 → 83,83,83（明るさは同じ・盤面の色みが乗らなくなっただけ）。
  ・段ごとの状態も実測: COLORS＝幕のフィルタON・残光なし／FLASH・VICTORY＝フィルタOFF・**残光あり
  　（filter: none）**・キャンバス 0x0／RESULT以降＝残光も消える。
  ・`node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` 56/56 PASS、
  　`node test/smoke.mjs 2` PASS、CSSブレース平衡（3095）、構文チェック通過、例外0件。
  　サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **教訓**: `filter: blur()` と `backdrop-filter` を**全画面の要素**に掛けると、GPUの弱い端末では
  1フレームまるごと持っていかれる。とくに**演出が終わった後も画面に残る幕**では、見せ場の数秒の
  ために数分間その負荷を払い続けることになる。「もう見せ場は終わった」時点で重い装飾を降ろす
  区切り（今回の `.is-settled`）を必ず用意すること。

- **申し送り（速さそのもの）**: 今回直したのは「重くて引っかかる」方で、演出の**設計上の長さ**
  （WAIT→RESULT で約8秒）は変えていない。重さが取れてもまだ長いと感じる場合は、管理者モードの
  「🏆 勝利演出シミュレーター（七色、集結）」で速さ・脈動の回数・見せる長さを詰められる。


### 2026-09-06（続き451）：演出の強化2件（①盤面・ロックエリアのカードが捨てられる瞬間／②コノハナサクヤの桜）＋ isCardDissolvePlaying() の取りこぼしを修正

ユーザー「演出強化をしよう！何する？」。**棚卸ししてから**提案し、①②を選んでもらった
（③ラウンドの切り替えは提案したがユーザー判断で取りやめ）。

- **【①】カードが「捨てられる」瞬間が、手札以外は無音だった（実装）**: 散る演出
  （`playHandCardBurn` → `playCardDissolve`）は **zone が hand / publicDraw のカードでなければ
  即 return** していたため、盤面とロックエリアのカードは音もなくパッと消えるだけだった。
  ・紅蓮の火山 ワイナウエア（マス1つのカードを全部捨てる）
  ・白の意思の覚醒（場の表向きのカードを一斉に捨てる）
  ・ロックエリアから1枚捨てる（＝そろえた色が1つ減る＝勝敗に直結する大事件）
  の3つとも**原因が同じ1か所**だったので、まとめて直した。
  - **見つけ方**: 呼び出し口を1つずつ追いかけると必ず取りこぼすので、**「盤面／ロックエリアに
    あったカードが、捨て場へ積まれた」という事実**を render() の先頭で拾う
    （`maybeAnnounceBoardCardDiscard`。山札の作り直し＝`maybeAnnounceDeckRefill` と同じ考え方）。
    この時点の DOM はまだ前回の描画なので、**消えたカードがどこに居たか**を測れる。
    カード効果でも手動のドラッグでも、ローカルでもオンラインでもこの1か所で全部かかる。
  - **誤爆しない作り**: 「捨て場が実際に増えた分」とだけ cardId で突き合わせる。盤面のリセットや
    セットアップの配り直しは**山札**へ戻るので捨て場は増えず、ここには入らない。手札へ拾われた
    場合はトークンが残るので、`alive` の判定で除外する。
  - **見せ方**: 白の意思の覚醒は一度に何枚も捨てるので、全画面のキャンバスを使う手札側の演出は
    **流用しない**（1枚ごとに全画面キャンバスを作ると端末が持たない）。その場で小さく散る軽い
    作り（1枚あたり DOM 数個）にして、端から順に 55ms ずつ遅らせて波にする。上限30枚。
  - **色は場面で変える**（今まさに処理中の効果カード `currentEffectCardIdForReason` から判断）:
    ワイナウエア＝赤い熱／白の意思の覚醒＝白い光／ロックエリア＝そのカードの色が砕ける／
    それ以外＝そのカード自身の色。音は専用素材を持たず `cardFlip` を1回だけ、4枚以上なら
    `playPulseThump(0.75)` を添える（枚数ぶん鳴らすと耳が痛いので必ず1回）。

- **【②】結ばれの一本桜 コノハナサクヤに演出が無かった（実装）**: マスチェンジには駒と駒を
  つなぐ電撃のアークがあるのに、こちらは説明モーダルが出るだけだった。桜のカードなので電撃は
  流用せず、**引き寄せ先から相手の駒へ桜の筋が伸び、花びらが巻き取られるように戻る**形にした
  （`playKonohanaPull`）。移動そのものは既存の跳ねる演出が担うので、桜は**先に**見せて
  重ならないようにする（`KONOHANA_PULL_LEAD_MS` = 420ms 待ってから駒が動く）。
  入れた場所は `moveAndSyncForEffect`＝engine の駒移動が必ず通る1か所（続き429）。
  - **ハマりどころ（CSSの%の罠）**: 花びらの位置を `translate(var(--start), 0)` で決めていたが、
    **translate() の % は「自分自身の大きさ」に対する割合**。花びらの箱は大きさを持たない
    （中の `.konohana-petal-inner` だけが大きさを持つ）ので常に 0px になり、**9枚全部が起点に
    重なって出ていなかった**（実測: 9枚中8枚が同じ座標）。箱に `width: 100%`（＝筋の長さ）を
    持たせて解決（実測: 筋 381px に対して 298px にわたって散る）。
  - あわせて `animation-fill-mode` を `forwards` → `both` に。順番待ちの花びらが起点に戻った
    状態で待たず、流れ出す位置で待つようになる。

- **【この作業で見つけた別の不具合・修正】`isCardDissolvePlaying()` が「呼ばれた直後」を
  取りこぼしていた**: あのカウンタが上がるのは **カードの画像を読み終わってから**
  （`await Promise.all([loadImage…])` の後）。そのため「捨てる処理を呼んだ直後」に見ると、
  演出はこれから始まるのに false が返る。実際、桜が「追色コストの霧散が終わるのを待つ」
  つもりで待たずに出て、**全画面の霧散（z-index 10660）の裏に隠れていた**（実測: 300ms 重なって
  いた。スクリーンショットで気づいた）。呼ばれた時刻を控えて、その直後の 600ms も「再生中」と
  して扱うようにした（時間で自然に切れるので、読み込みに失敗しても取り残されない）。
  実測で重なりは 300ms → 100ms（＝霧散の消え際だけ）になった。
  - この判定は「手札効果の使用中はホバー拡大を出さない」（#223対応）にも使われており、
    そちらも同じ取りこぼしがあった＝ついでに直っている。

- **検証（実測・すべて本物の経路）**:
  ・①: 盤面のカードを捨てる→ゴースト1・そのカードの色（青 27,184,206）／ロックエリアから捨てる
  　→`is-lock`・緑 34,172,56／ワイナウエア→`is-fire`・255,96,48／白の意思の覚醒（6枚同時）
  　→`is-light` 6個・白・位置がばらけている。**回帰**: 手札へ拾った時 0／別のマスへ動かした時 0／
  　盤面を配り直した時 0（40枚が山札へ戻っても誤爆しない）。
  ・②: **カード開発モードの実際のボタンをクリック**して本物の手札効果を発動し、追色コストの札も
  　**実際にクリックして**支払い、相手の駒が (6,6)→(2,2) へ移動、行動ログに
  　`move_chosen_opponent_adjacent_to_self` が残ることを確認。その最中に桜の筋1・花びら9・渦1 が
  　出て、3つのアニメーションとも動作、花びらの傾き打ち消し `rotate(-41.9deg)` も確認。
  ・`node tools/check-undeclared.mjs` 0件（161ファイル）、`npm test` 56/56 PASS、
  　`node test/smoke.mjs 2` PASS、CSSブレース平衡（3125）、構文チェック通過、例外0件。
  　サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **検証のハマりどころ（次回のために）**:
  1. **カード開発モードから手札効果を発動する時は、そのカードのトークンが盤面のどこかに
     存在している必要がある**（`state.tokens` から cardId で探す作り）。エターナルカードは
     獲得するまでトークンが無いので、先に1枚作ってから呼ぶ。
  2. **手番と優先権を必ず自分にしてから**発動する。相手のターンのままだと、CPUの選択待ちを
     守るガードにクリックを握り潰される（#280/#448 と同じ形）。
  3. **手札は扇状に重なっているので、箱の中心をクリックしても隣のカードが最前面**のことがある
     （実測でそうなっていて「クリックしたのに何も起きない」ように見えた）。
     `elementsFromPoint` で目的のカードが最前面になる点を探してから押す。
  4. **スクリーンショットを撮る前にカーソルを手札から離す**。そのままだとホバー拡大
     （`#card-preview`、z-index 10700）が出っぱなしで盤面の演出が隠れる（検証側の都合で、
     一度これを演出の不具合と読み違えた）。

- **申し送り（実機で見てほしい）**: ヘッドレスは盤面のWebGL描画を切って撮るためプレイマットが
  白っぽく、桜の花びら（桃色）が沈んで見える。実測では筋・花びら・渦とも出ていて動いているが、
  **見えやすさそのもの**は実機で確認をお願いしたい。小さいと感じたら花びらの大きさ
  （`.konohana-petal-inner` の width/height）と筋の太さ（`.konohana-thread` の height）で調整できる。


### 2026-09-06（続き452）：部外秘の原稿を公開リポジトリへ push してしまった（`git add -A` の事故）→ 履歴から消去。再発防止のルールを作業ルールへ追加

**私（Claude）の事故**。続き451 のコミットで `git add -A` を使ったため、作業フォルダに置かれていた
`ストーリー/STORY（部外秘）.txt`（10,840バイト）を**公開リポジトリへ一緒に push した**。

- **なぜ `.gitignore` で止まらなかったか**: `.gitignore` の2行目は `/*.txt` で、これは**リポジトリの
  直下の .txt しか除外しない**。`ストーリー/` の中の .txt は対象外だった。
  「テキストは除外してあるはず」という思い込みが抜け道になった。

- **対応（ユーザー承認のうえ実施）**:
  1. `git rm --cached` で最新の状態から外し、`/ストーリー/` を `.gitignore` へ追加。
     ※この時**`.gitignore` の追記をステージし忘れて**コミットに入っておらず、リポジトリ側では
     除外が効いていなかった（次のコミットで修正）。**`git rm --cached` と一緒に編集した
     `.gitignore` は自分で `git add` すること**。
  2. 影響範囲を先に測った——このファイルを含むのは**直近3コミットだけ**、ブランチは `main` 1本、
     タグ無し、未pushの作業無し。全履歴の書き換えにはならないと確認してから実行した。
  3. 原稿そのものと書き換え前の履歴の**控えを取ってから**、
     `git filter-branch --index-filter 'git rm -r --cached --ignore-unmatch "ストーリー"' -- 6443fa2~1..HEAD`
     で3コミットを書き換え、`git push --force-with-lease`。
     ・パスの指定は**フォルダ名だけ**にした（ファイル名に全角括弧が入っていて、シェルの引用でつまずく
     　余地を作らないため）。
  4. 手元も `git branch -D` → `refs/original` 削除 → `reflog expire` → `git gc --prune=now` で片付け。

- **検証（実測）**:
  ・書き換え後、`git log -- <そのファイル>` が **main / origin/main とも0件**。
  ・**`git diff <書き換え前> <書き換え後>` が空**＝配信されるファイルは1バイトも変わっていない
  　（GitHub Pages の利用者への影響なし。これが一番大事な確認）。
  ・コミット数 **1140 → 1140**（欠落なし）。原稿ファイルは手元に 10,840バイトのまま残っている。
  ・強制push後に GitHub 側を取り直して、リモートの履歴にも0件であることを確認。
  ・公開サイトが生きていることを実測（トップ HTTP 200 / `src/main.js` HTTP 200）、
  　原稿のURLは **HTTP 404**。

- **残る限界（ユーザーへ伝達済み）**: 強制push しても、**GitHub のサーバー側には古いコミットが
  しばらく残り、コミット番号を直接指定すれば見られる**。完全に消すには GitHub サポートへ
  削除を依頼する必要がある。また、公開されていた間（最新の状態に入っていたのは36秒、履歴には約45分）に
  複製された分は取り返せない。

- **再発防止（作業ルールへ追加済み）**: **`git add -A` / `git add .` を使わない。コミットする
  ファイルは1つずつ明示して `git add` する。コミット前に `git status --short` を読み、身に覚えの
  無いファイルが1つでもあれば手を止めてユーザーに確認する。**

- **ユーザーへの説明について（記録）**: ユーザーはコードを読まないので、「別のパソコンにコピーが
  あると作り直しになる」という説明が伝わらなかった。**ゲームのアカウント（YGM/SHO）やスマホでの
  プレイは一切関係ない**——聞きたいのは「このプロジェクトのフォルダを別のPCにも置いて編集して
  いるか」の一点だけ、と噛み砕いて初めて伝わった。危険の説明は、専門用語を外して
  **「何を聞かれているのか」が一文で分かる形**にすること。


### 2026-09-06（続き453）：CLAUDE.md を作業ルールどおりに整理（続き432以前を docs/dev-log.md へ移動）

作業ルールの「日付付き変更履歴が**およそ40エントリ**を超えたら古い方を [docs/dev-log.md](docs/dev-log.md) の
末尾へ移す」に従った。**67エントリ・3372行まで溜まっていた**（毎セッションこのファイルの全文が
読み込まれるので、そのぶん作業に使える余地が削られる）。

- **移したもの**: 続き386〜432 の **47エントリ**。dev-log.md は古い順（続き1が先頭・最新が末尾）なので、
  続き385 の直後へ時系列のまま追記した。本体には**続き433〜452 の20エントリ**を残した。
- **結果**: CLAUDE.md **3372行 → 1373行**（59%減）。dev-log.md 20472行 → 22471行。
- **検証（実測）**: ①移した47エントリの本文が dev-log.md に**1文字も変わらず**入っている
  ②dev-log.md の元の中身がそのまま先頭に残っている（末尾へ足しただけ）
  ③CLAUDE.md に残した続き433以降が元と**完全一致** ④注の1行（下記）以外、冒頭からルールまでが元と一致
  ⑤**続き番号の欠落0件**（1〜452 のうち存在するのは451件で、これは移動前と同じ。
  「続き1」は元から存在しない）。
- 冒頭の注記と、作業ルールの切り出し履歴の行も新しい範囲に更新した。

**注意（次に整理する人へ）**: `git show HEAD:CLAUDE.md` で取り出した内容と手元のファイルを
そのまま比べると、**改行コードの違い（git側はLF・手元はCRLF）だけで全部「不一致」になる**。
比べる時は必ず `
` を `
` に揃えてから照合すること（今回それで一度、正しい移動を
「NG」と読み違えた）。


### 2026-09-06（続き454）：オンラインの自動対戦を決着まで回して「86秒フェイズが進まない」停止を掴まえた——待ちに上限が無かった／何が中央をふさいでいるかを名前で残すようにした

ユーザー指示でオンラインの自動対戦（`node test/online-smoke.mjs 2 --full --isolated`）を決着まで
回したところ、**turn 9 で停止**して FAIL した。**このテストを回した目的そのものが果たされた形**。

- **掴まえた証拠**: `diag-phase-deferred {waited:86828, via:"reconcile"}` ＝**86.8秒**フェイズが
  進んでいない。同じ間の `diag-notice-wait` は 12283 / 14567 / 12935ms で、理由は**毎回 `modal`**
  （＝返事待ちのモーダルの背景が閉じずに残っている）。停止時の内訳は `pickerType:null` /
  `contactResultModals:0` / `gateInvasionPending:false` ＝**既知のモーダルはどれも出ていない**のに、
  `.so7-modal-backdrop` だけが見えている状態だった。

- **真因（構造）**: `reconcilePhaseAutomation()` の「演出・お知らせが終わるまで待つ」
  （#266/#267 で入れたもの）に**上限が無かった**。コメントには「演出もお知らせも上限付きで必ず
  終わるので、ここで対局が止まることはない」と書いてあったが、**その前提が崩れていた**——
  お知らせは1件ずつ10秒待って諦めるものの、閉じないモーダルがある限り**次のお知らせが積まれ続け**、
  `isNoticeQueueBusy()` は真のままになる。結果、待ちが延々と続いた。
  - **修正**: `RECONCILE_DEFER_MAX_MS = 16000` を入れ、上限に達したら諦めて先へ進める。
    16秒にしたのは、演出の自動解除（anim-gate の `STUCK_MS` = 15秒）より**後**に効かせるため
    （本当に演出が固まっている場合は、まずあちらの解除に任せたい）。この待ちは**見せる順番を
    整えるためだけ**のもので、破っても起きるのは「フェイズ告知がお知らせと重なる」という見た目の
    乱れだけ。**対局が止まる方が実害が桁違いに大きい**。

- **【教訓・今回の本質】「必ず終わるものを待っている」は、待っている間に新しい仕事が積まれる
  経路があると成立しない**。上限の無い待ちを書く時は、待つ対象が**外から増やされ得るか**を
  必ず確認する。今回は「1件ごとに上限があるから全体でも有限」と考えたのが誤りで、件数の方に
  上限が無かった。**待ちには例外なく上限を付ける**（enterPhase 側は前からそうしてあった）。

- **次に同じ停止が来た時に推測しないで済むようにした**: `describeCenterBlocker()`（anim-gate.js）を
  新設し、`diag-phase-defer-timeout` と `diag-notice-wait`（理由が modal の時）に**どのモーダルが
  ふさいでいるか**を名前で残す。背景(backdrop)自体は無名なので、**その直後の兄弟＝ダイアログ本体**の
  id とクラスを見る。出すのは要素の id / クラスだけで、**カード名や伏せ情報は一切入らない**
  （不具合報告にそのまま載るため）。実測の出力例:
  `backdrop>div#gate-invasion-modal.sleight-ritual-modal`。

- **検証（実測・受け取り側まで）**:
  ・`describeCenterBlocker()` が 何も無い時 `none` → 本物の `createBackdrop({dim:true})` を置くと
  　**そのダイアログの id を名指し** → 閉じると `none` に戻る。
  ・**実際のCPU自己対戦**で、閉じないモーダルを置きお知らせを積み続けて**60秒ふさいだまま**にしても、
  　`diag-phase-defer-timeout {waited:16103, notice:true, blocker:"backdrop>div#gate-invasion-modal…"}`
  　が出て**ターンが進んだ**（修正前ならここで永久に止まる）。例外0件。
  ・`npm test` **56/56 PASS**、`node test/smoke.mjs 2` PASS、`node tools/check-undeclared.mjs` 0件、
  　CSSブレース平衡（3125）、構文チェック通過、お知らせ ja/en とも1件。

- **未解決（次の材料あり）**: 実際に何のモーダルが閉じずに残っていたのかは**まだ分かっていない**
  （上の診断を入れる前の実行だったため）。もう一度オンラインの自動対戦を回し、
  `diag-phase-defer-timeout` の `blocker` を見れば一発で特定できる。

- **書き込みの事故（記録）**: この作業で `describeCenterBlocker()` の `split(/s+/)` が、
  ヒアドキュメント経由で書き込む途中に**バックスラッシュが落ちて `/s+/` になっていた**
  （＝クラス名を文字の `s` で分割し、意味不明な文字列を出していた）。構文エラーにならないので
  検査は素通りする。**ヒアドキュメントで .mjs を書く時はバックスラッシュを使わない**
  （空白区切りは `split(" ")` で足りる）。続き429で同じ形を踏んでいる。


### 2026-09-06（続き455）：停止の元凶が名指しで判明——**対局と無関係のお知らせ**（初回の音量設定・デイリーボーナス）がゲームの進行を止めていた

続き454 で入れた診断が、その日のうちに答えを出した。オンラインの自動対戦をもう一度回すと、
待たされている理由が**名前で**残った:

```
diag-phase-defer-timeout: {"waited":21025, "notice":true,
  "blocker":"backdrop>div#first-run-bgm-modal / backdrop>div#daily-bonus-modal"}
```

- **真因**: #225 で作った目印 `.so7-modal-backdrop` を、**2つの別の目的に流用していた**——
  ①盤面のタップを透かさない（本来の用途）②画面の中央がふさがっている間はゲームを進めない（#266）。
  ①はどんなモーダルにも必要だが、②は**対局の流れに属するモーダルにだけ**当てはまる。
  その結果、**初回の音量設定**や**デイリーボーナス**のような「アプリが勝手に出すお知らせ」が
  開いているだけでフェイズが進まなくなっていた。テストは毎回まっさらなゲストを作るので必ずこの2つが
  出る＝**毎回この状態に落ちていた**。
  - 実プレイでも起こる: 対局中に日付をまたいでデイリーボーナスが出た、起動直後の案内を閉じずに
    対局へ入った、等。閉じるまで自分の手番が進まない。
- **修正**: `createBackdrop(onClose, { dim, zIndex, blocksGame = true })` を足し、
  `blocksGame: false` の時だけ `.so7-modal-nonblocking` も付ける。判定側（anim-gate.js の
  `isBlockingModalVisible` / `describeCenterBlocker`）はその印の付いた背景を**数えない**。
  **タップの保護（①）は今までどおり全部のモーダルに効く**——外したのは②だけ。
  - `blocksGame: false` を付けたのは「**アプリが勝手に出すお知らせ**」7つ:
    初回の音量設定 / デイリーボーナス / 運営からのお知らせ / α版のお知らせ / ホーム画面に追加の案内 /
    更新情報 / 2D表示のすすめ。**プレイヤーが自分で開いたもの（ヘルプ・山札一覧等）と、
    対局の流れのモーダルは触っていない**（＝取りこぼしても従来どおり止まるだけで、安全側に転ぶ）。
- **教訓**: **1つの目印を2つの意味に使い回さない**。「タップを透かさない」と「ゲームを進めない」は
  別の判断で、後から片方だけ条件が変わると、関係のないものまで巻き込む。

- **検証（実測）**: ①`blocksGame:false` の背景を出しても `describeCenterBlocker()` は `none`、
  かつ `.so7-modal-backdrop` は付いたまま（タップ保護は維持） ②対局の流れのモーダル（従来どおり）は
  `backdrop>div#gate-invasion-modal` として**きちんと止める** ③例外0件。
  `npm test` **56/56 PASS**、`node tools/check-undeclared.mjs` 0件（161ファイル）、
  CSSブレース平衡（3125）、構文チェック（変更9ファイル）通過、お知らせ ja/en とも1件。

- **⚠️ スモークテストが現在 FAIL する（私の変更が原因ではない・未解決）**: `node test/smoke.mjs 2` が
  「60秒ターンが進まない」で落ちる。**今回の変更を一時的に外して（git stash）回しても同じく落ちた**
  （そちらは 300秒で turn 6 ＝もっと遅い）ので、原因は別。ログを読むと**対局自体は進んでおり**、
  `hand-effect first-orange` から `effect-verb pickup_to_hand` まで**50秒**かかっている——
  続き425〜427で「中央は一度に1つ」にした分の遅さ（あの時点で申し送りにしていたトレードオフ）が、
  テストの見張り時間に収まらなくなってきたと見える。今日1回は PASS しているので揺れもある。
  **次にやること**: ①どこで時間を使っているのかを実測で分解する（お知らせの表示時間
  `--hand-pickup-toast-duration` とフェイズ告知の2.6秒が中央の占有時間そのもの）
  ②実プレイで「もたつく」と感じるかをユーザーに確認する（数値ではなく体感の問題）。


### 2026-09-06（続き456）：降参を実装（オンラインの2人戦のみ・[src/resign.js](src/resign.js) 新規）／対局の終了処理を `concludeMatchWithWinner()` に共通化

**ユーザーの決定**（続き439 で保留にしていた仕様を確定）:
・成績は**普通の負けと同じ**（戦績システムもランクも）。「負けそうなら降参すればレートが減らない」
　という抜け道を作らないため。
・**CPU戦には出さない**（いつでもタイトルへ戻れるので要らない）。
・3人・4人戦は当面出さない（一人抜けた後の駒・カード・ロックの扱いを決める必要があるため）。

- **対局の終了処理を共通化した（ここが今回の肝）**: 「勝敗の確定→戦績への登録→勝利演出→通貨→
  順位→ランク反映→対戦終了パネル」の一連は、`checkForVictory()` の for ループの**中に直接**
  書かれていた＝**「7色そろえた」以外の理由で終わらせる手段が無かった**。この162行を
  `concludeMatchWithWinner(player, { reason })` として切り出し、`checkForVictory()` はそれを
  呼ぶだけにした。**降参は勝ち方が違うだけで、見せ方も記録も完全に同じ道を通る**ので
  「降参だけ記録が違う」という穴ができない。
  ・**中身は1行も変えていない**（インデントを4つ浅くしただけ）。切り出し前後を機械的に
  　突き合わせて **162行／差分0行**を確認済み。
  ・二重に走らないのは `announcedPlayers`（＝`hasAnyoneWon()` が見るもの）で担保。

- **サーバー側（Edge Function）は一切変更していない＝再デプロイ不要**。状態を変えるアクションを
  足すのではなく、**「降参した」という合図だけを Realtime で流し**（`broadcastResign` /
  `onResignEvents`、`ranked_forfeit` と同じ形）、受け取った側が自分で終了処理を走らせる。
  match-stats-tracker.js が採っているのと同じ考え方。SQLの追加も無し。
  ・合図が届かなかった場合でも、**降参した本人の画面では必ず終了処理が走る**
  　（記録は勝者・敗者どちらの画面からでも残せる作りになっている＝victory.js の保険）。

- **UI**: ⚙オプションの基本設定に⑥として「🏳️ 降参する」（赤字）。`canResignNow()` が真の時だけ
  出す——オンライン／CPU戦でない／観戦でない／まだ決着していない／参加2人／自分が座っている。
  押すと確認モーダル（**開いた直後の合成クリックを弾く** `createOpenGuard`＝#230/#236 と同じ）。
  ・確認の後に**もう一度 `canResignNow()` を見る**——待っている間に相手が7色そろえて終わって
  　いることがあるため（続き437「待ちを挟んだら確かめ直す」）。
  ・二重防止に専用フラグを置かず、**`hasAnyoneWon()` そのもの**で見る。フラグにすると対局を
  　またいで戻す必要があり、そのために victory.js から呼び返すと import が循環する。

- **検証（実測・本物のオンライン対戦で端から端まで）**: 実際に部屋を作って2人でゲストログインし、
  対戦を開始してから**画面の「オプション」ボタンを実際にクリック**して降参した——
  ①両者とも `canResignNow()` が true ②メニューに「🏳️ 降参する」が1件出る
  ③確認モーダルが出て**開いた直後のクリックは無視**され、400ms後のクリックで閉じる
  ④降参した側で `diag-resign {winner:"A",mine:true}` → `diag-match-concluded {reason:"resign"}`
  ⑤**相手の画面でも** `mine:false` で同じ勝者で終了（＝合図が届いている）⑥例外0件。
  CPU戦では `canResignNow()` が false でメニューにも出ないこと、文言が ja/en とも引けること
  （未定義キー0件）も実測。`npm test` **56/56 PASS**、`node tools/check-undeclared.mjs` 0件
  （162ファイル）、CSSブレース平衡（3136）、構文チェック通過、お知らせ ja/en とも1件。

- **検証で自分が踏んだ罠（続き443と同じものを繰り返した）**: 最初のプローブで
  `(await import("/src/options-menu.js")).openOptionsMenu?.()` と書いたが、**そんな export は無い**。
  `?.` なので静かに何も起きず、起動時に一度組まれたパネル（対局前＝降参の項目が無い状態）を
  読んで「メニューに出ない ❌」という**嘘の失敗**を測った。**存在しない関数を `?.` で呼ぶと
  失敗が見えない**——UIの検証は必ず**実際の要素をクリック**すること
  （今回は `document.getElementById("options-menu-button").click()`）。

- **申し送り**: 3人・4人戦の降参は未実装。やるなら「降参した人だけ抜けて残りで続行」の場合に
  **その人の駒・手札・ロックエリアをどうするか**をルールとして決める必要がある
  （盤面から消す／置き去りにする／山札へ戻す、で難易度も勝敗も変わる）。


### 2026-09-07（続き457）：降参で勝った時に「七色、集結」が出ていた（7色そろえていないのに集結演出＝嘘になる）

続き456 の直後に自分で見つけた穴。`concludeMatchWithWinner()` に相乗りさせた結果、**降参で勝った時も
勝利演出が走っていた**。あの演出は勝者のロックエリアの7色を集めて見せるもの（`findLockSlots`）なので、
3色しかそろっていない勝者で走らせると集まる色が足りず、しかも「七色、集結」という筋書き自体が事実と違う。

- **修正**: `reason === "resign"` の時は `playVictoryCelebration()` を呼ばない
  （`celebration?.dismiss?.()` は元から null 安全なのでそのまま通る）。あわせて勝利モーダルの副題を
  「相手が降参しました」に差し替えられるようにした（`showVictoryModal(player, onClose, { subtitleKey })`）。

- **【今回の書き込み事故・重要】置換が意図と違う場所に当たった**: 副題の引数を足す時、閉じ括弧
  `        });`（8スペース）をアンカーにしたが、続き456 の切り出しで**インデントが4つ浅くなっていた**
  ため、そこには当たらず**別の呼び出し（`showRankedResultModal`）の閉じ括弧に付いてしまった**。
  `editFile` は「1件でなければ例外」なので取りこぼしはしないが、**1件だけ別の場所に当たる**のは
  止められない。しかも余計な第2引数は無視されるだけなので **`npm test` 56/56 も構文チェックも通る**。
  置換の直後に**その行の前後を実際に読んで**気づいた。
  - **作法**: 閉じ括弧のような**どこにでもある文字列をアンカーにしない**。前後の一意な行と
    セットにするか、置換後に必ず周辺を読んで**狙った場所に入ったかを目で確かめる**
    （続き442の「受け取る側まで測る」と同じ話——“置換が成功した”は“正しい場所に入った”ではない）。

- **検証（実測）**: **本物のオンライン対戦**（部屋を作って2人でゲストログイン→対戦開始→
  画面の「オプション」ボタンを実際にクリックして降参）で——
  ⑥勝った側の画面に**七色集結の演出は出ず**、勝利モーダルが「プレイヤーC の勝利！／
  **相手が降参しました**」になる ④⑤両者とも同じ勝者で対局終了・例外0件。
  **回帰**: 通常の勝ち（reason 既定＝"seven"）では**今までどおり勝利演出が出る**ことも実測。
  `npm test` 56/56 PASS、`node tools/check-undeclared.mjs` 0件（162ファイル）。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。


### 2026-09-07（続き458）：テストの「停止」判定を、ターン番号ではなく**実際に手が進んだか**で見るようにした（アプリは正常なのにテストが落ちていた）

**ユーザー確認済みの前提**: 「適当タップで飛ばせるのでもたつき感はないです。初心者もゆっくり読めて
いいんじゃないかな？」——続き425〜427で「画面の中央は一度に1つ」にした分の遅さは**意図した仕様**で、
アプリ側は直さない。**直すのはテストの見張り方だけ**。

- **何が起きていたか**: `node test/smoke.mjs 2` が「60秒ターンが進まない＝停止」で落ちるようになった
  （続き455の作業中に3回中3回）。ところがログを読むと**対局は動いており**、
  `hand-effect first-orange` → `effect-verb pickup_to_hand` まで**50秒**かかっていただけだった。
  色宣言・鼓動・じらし・お知らせの順番待ちを挟む効果は、**盤面が1つも動かないまま**数十秒かかる。
  ・**変更を外して（git stash）回しても同じく落ちた**ので、私の修正が原因ではないことを先に確かめた。
  ・続き439で入れた「アプリが処理中と言っている間は数えない」判定は `diag-auto-action-nothing` に
  　依存しており、**この経路ではその記録が出ない**ため効いていなかった。

- **直し方（判定の材料を変えた）**: 行動ログのうち **`diag-` で始まらないエントリ**
  （`dispatch` / `lock` / `hand-effect` / `effect-verb` …＝実際にゲームが動いた記録）の数が
  増えていれば「活動あり」＝停止と数えない。
  ・**`diag-` を活動の証拠にしてはいけない**のが要点。心拍・再試行・待ち時間の記録は
  　**本物の停止でも出続ける**——実際、続き454で掴まえた86秒停止のログは `diag-` **だけ**だった。
  　この線引きなら、あの停止は今でもきちんと FAIL する。
  ・**堂々巡りは別に検出する**: 手は動いているのにターン番号だけが進まない状態が
  　`TURN_STALL_MAX_MS`（ローカル240秒／オンライン300秒）続いたら失敗にする。
  　これが無いと「無限に何かをし続けるが前へ進まない」不具合を永久に見逃す。
  ・制限時間も緩めた（ローカル 300秒→**480秒**、決着まで 720秒→**900秒**）。1ターンが実際に
  　長くなったので、8ターンに届く前に時間切れになっていた。

- **教訓（続き439で書いたことの再確認）**: **アプリは正常なのにテストが落ちる状態を放置しない**。
  「またあの失敗か」と見慣れてしまうと、**次に本物の停止が起きた時に埋もれる**。
  閾値を上げて誤魔化すのではなく、**何を根拠に「生きている」と判定するか**を作り直すこと。

- **検証（実測）**: `node test/smoke.mjs 2` が **PASS（8ターン到達）**。直前まで3回連続で
  同じ理由で落ちていたものが通るようになった。オンライン（`--full --isolated`）でも同じ判定に
  そろえた。`npm test` 56/56 PASS。アプリ側（src/）の変更は無し＝配信物は変わらない。


### 2026-09-07（続き459）：札の中身が分からない状態で**盤面まるごと描画が死ぬ**不具合を修正（`getCardDefinition(...).color` の無防備な参照7か所）

続き458 のテスト修正後にオンラインの決着まで自動対戦を回したところ、片方のゲストの session が
切れて（`_removeSession` → 送信が `401 unauthorized`）、その巻き添えで**描画が例外で落ちていた**:

```
TypeError: Cannot read properties of undefined (reading 'color')
  at buildPlayerZone (src/main.js:857) ← render() の中
```

- **原因**: `getCardDefinition(token.cardId).color` に `?.` が無かった。オンラインでは札の中身が
  サーバー側で伏せられる（`cardId` が null）ことがあり、`getCardDefinition(null)` は undefined を返す。
  **ここは `render()` の中なので、例外が出ると盤面まるごと描かれず操作不能になる**。
  同じ形が全部で**7か所**あった（card-effect-engine.js のコメントに、#166 で同じ罠を踏んだ記録が残っている）。

- **場所ごとに直し方を変えた**（一律 `?.` ではなく、その後どう使われるかを見て決めた）:
  ・手札のオーラ／ロック中カードの色 … `?.color` にして、色が無ければ演出を付けない（描画は続ける）。
  ・到達のオーラ3か所 … 同上（色の無い演出になるだけ）。
  ・自分のロック済みの色の集合 … `?.color` ＋ `.filter(Boolean)`（不明な色を集合に入れない）。
  ・**ロックの置き先（5349）だけは落ちる以上の問題があった** … 色が undefined だと
  　`COLORS.indexOf(undefined)` が **-1** になり、**存在しないスロットへカードを置いてしまう**。
  　色が引けなければロック自体を受け付けない形にし、`diag-lock-click-skip {reason:"unknown-card-color"}` を残す。

- **検証（修正前後のA/B・これが決定的）**: 自分の手札とロックエリアの札の `cardId` を null にした
  状態を流し込んで描画させた——**修正前は `main.js:857` でまさにその例外**（オンラインで出たものと同一）、
  **修正後は盤面49マスが正常に描かれ例外0件**。`npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、
  `node tools/check-undeclared.mjs` 0件（162ファイル）。無防備な `getCardDefinition(...).` は**残り0件**。

- **教訓**: `render()` の中では**1つの undefined 参照が画面全体を消す**。オンラインでは
  「相手の札」だけでなく**自分の札の中身が伏せられて届く**瞬間がある（通信の失敗・session切れの直後）ので、
  カードの定義を引く時は常に中身が無い場合を想定する。

- **未解決（診断だけ入れた）**: 別の回で、turn 7 に**ムーブフェイズのまま移動先も接触相手も無い**状態で
  止まるのを観測した（救済の分岐がどれも素通り）。止まった瞬間の内訳がテストのログで200文字に
  切られていて読めなかったため、①テストのログを切らずに出す ②`diag-auto-action-nothing` に
  **分岐の判断に使っている値そのもの**（`movePhaseActive` / 状態から数え直した移動・接触の候補数）を
  足した。次に再現した時に推測せず特定できる。


### 2026-09-07（続き460）：5秒ウォッチドッグに並べた見張りが**普段はまったく動いていなかった**（早期 return がタイマー全体を打ち切っていた）／接触の申し込みが宙ぶらりんで対局が永久に止まるのを塞いだ

オンラインの自動対戦（決着まで）を回して turn 12 で停止。**続き459 で足した診断が一発で当てた**:

```
diag-auto-action-nothing: {"phase":"move","movePhaseActive":false,"pendingContact":true}
```

- **表の症状**: 接触の申し込み（`pendingContact`）が解決されないまま残り、**どの経路も何もしなくなる**。
  ムーブフェイズの救済（動けないならターン終了）は「接触の申し込み中は手を出さない」という条件を
  持っているので**正しく素通り**する。その結果、誰も何もできない完全な停止になっていた。

- **【真因・こちらが本体】5秒ウォッチドッグの中身が、普段はまったく実行されていなかった**。
  `setInterval` のコールバックには4つの見張りが並んでいたが、**1つ目（手札効果の取り残し解除）の
  本体が setInterval の中へ直接書かれており、そこに早期 return が8個あった**。この `return` は
  **コールバック全体を終わらせる**ので、「取り残しは無い」という**普段の状態**（`getHandEffectBusyStuckMs()
  < 20000`）で毎回すぐ抜け、後ろに並んでいた
  ・**接触の45秒自動承認**（`checkContactApprovalTimeout`・続き393）
  ・**最後のロックの自動承認**（`checkFinalLockApprovalTimeout`）
  ・**フェイズが始まらない時の救済**（`rescuePhaseIfNeverStarted`・続き407）
  が**一度も動いていなかった**。続き408 で「`diag-phase-restart` が0件」だったのも、
  今回の停止で45秒の自動承認が効かなかったのも、すべてこれで説明が付く。
  - **修正**: 1つ目を `runHandEffectBusyWatchdog()` として関数に切り出し、`return` がそこだけで
    止まるようにした（中身は1行も変えていない・29行をそのまま移した）。

- **【教訓】1つのタイマーに複数の見張りを並べる時、早期 return を直接書かない**（それぞれ関数にする）。
  書いた本人には「この見張りを飛ばす」つもりでも、実際には**後ろに並んでいる全部を道連れにする**。
  しかも**何も起きないだけ**なので、構文チェックもテストも通り、誰も気づけない。

- **どうやって突き止めたか（推測せず計測した）**: ①`checkContactStuckCancel` の先頭で呼ばれた回数を
  数えたら **null＝0回** ②では見張り自体が動いていないのかと `setInterval` の中で回数を数えたら
  **10回** ③＝「タイマーは回っているのに、その先の関数には届いていない」→ 間にある早期 return が
  犯人、と確定できた。**どこまで届いているかを1段ずつ測る**のが一番速い。

- **あわせて最後の砦を追加**: `checkContactStuckCancel()`——席に関係なく**どのクライアントでも**、
  接触の申し込みが75秒残り続けたら**取り消す**。承認ではなく取り消しにしたのは、返事を見ていない
  相手からカードを奪ってしまわないため（相手の画面が生きていれば45秒の自動承認が先に走るので、
  ここに来る＝相手は見ていない）。サーバー側の `RESPOND_CONTACT` は**送り主を見ない**ので
  **Edge Function の変更は不要**。全員がほぼ同時に送っても、`pendingContact` が無ければ
  リデューサーは何もしない（冪等）。文言 `game.contact.stuckCancelled` を ja/en 追加。

- **検証（実測・本物のオンライン対戦）**: 部屋を作って2人でゲストログイン→対戦開始→片方が接触を
  申し込んだ**直後に相手のブラウザを閉じる**（＝通信が切れて誰も返事できない状況をそのまま再現）——
  **77秒で自動的に取り消され**、`diag-contact-stuck-cancel {selfSeat:"C", iAmDefender:false}` が残った。
  修正前は**180秒待っても取り消されなかった**（＝永久に停止）。
  `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、`node tools/check-undeclared.mjs` 0件、
  CSSブレース平衡（3136）、構文チェック通過、お知らせ ja/en とも2件。例外0件。


### 2026-09-07（続き461）：最後のロックの承認でも同じ停止を掴まえた——同じ最後の砦を入れた／停止調査の内訳に「誰待ちか」を足した

続き460 の修正後にオンラインの決着まで自動対戦を回すと、**turn 23 まで到達**した
（それまでは 12 → 9 → 7 → 4）。見張りの修復が効いている。そこで次の停止:

```
diag-lock-click-skip: {"reason":"pending-final-lock"} が10秒ごとに出続けて対局が進まない
```

- **接触とまったく同じ形**: `checkFinalLockApprovalTimeout`（45秒で自動承認）は
  **承認の順番が回っているクライアントでしか動かない**（`getSelfSeat() !== pending.queue[0]` で return）。
  その人の通信が切れていれば誰も解決できず、ロックしようとしている側は
  `pending-final-lock` で弾かれ続けて永久に止まる。
- **修正**: `checkFinalLockStuckApprove()` を追加。席に関係なく**どのクライアントでも**75秒で
  承認を進める。**取り消しではなく承認**にしたのは45秒の自動承認と同じ方針だから
  （相手が答えられないだけで、ロックした人の正当な勝ちを取り上げる理由が無い）。
  サーバー側の `RESPOND_FINAL_LOCK` は送り主を見ず queue の先頭を1つ進めるだけなので
  **Edge Function の変更は不要**。承認者が複数いる3・4人戦では順番に効いて最後まで進む。

- **停止調査の内訳に「誰待ちか」を足した**: 今回、承認待ちが残っているのは分かっても
  **誰待ちなのか**が読めず特定に時間がかかった（テストのログは末尾10行しか残らない）。
  `getStallDiagnostics()` に `pendingContact {attacker, defender}` / 
  `pendingFinalLock {attacker, queue}` / `selfSeat` を追加した。**席の記号だけで、
  カード名などの伏せ情報は含まない**。

- **検証（実測・本物のオンライン対戦）**: 最後のロックの承認待ちを作った直後に
  **承認する側のブラウザを閉じる**——**75秒で承認が進み**、
  `diag-final-lock-stuck-approve {attacker:"C", approver:"A", selfSeat:"C", iAmApprover:false}`
  が残った。`npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、
  `node tools/check-undeclared.mjs` 0件（162ファイル）、構文チェック通過、お知らせ ja/en とも3件。例外0件。

- **申し送り**: 「特定の1人しか解決できない待ち」は**この2つ以外にもある可能性が高い**
  （タイマーのオン/オフの承認 `REQUEST_TIMER_TOGGLE` / 自動処理の切り替え
  `REQUEST_AUTO_PROCESSING_TOGGLE` も同じ queue 方式）。ただしこの2つは**対局の進行を
  ブロックしない**（承認されなくても手は進められる）ので、今回は触っていない。
  進行を止める待ちを新しく足す時は、必ず**席に関係なく効く上限**をセットで入れること。


### 2026-09-07（続き462）：演出の強化7件（接触まわり一式／ゲート侵攻の締めくくり／ロックから引き抜く瞬間／細かい3つ）

ユーザー選択「接触まわり一式（おすすめ）／①ゲート侵攻の締めくくり／②ロックから札を引き抜く瞬間／
⑥⑦⑧細かいところ」＝**提案した全部**。棚卸し（サブエージェントによる調査）で見つけた
「まだ無音の場面」を、プレイヤーにとって大きい順に埋めた。

- **③「防いだ！」が防いだ本人の画面にしか出ていなかった**（このゲーム一番の見せ場を、
  攻めた側と観戦者は文字のモーダルでしか知れなかった）。`broadcastBlocked` / `onBlockedEvents` を
  online.js へ足し、**状態は変えず合図だけ**流す（降参・ranked_forfeit と同じ形＝**Edge Function の
  変更は不要**）。受け取った側は同じ演出を再生し、**自分が防いだ側なら二重に出さない**。
- **④ 接触の申し込み中、盤面が完全に平常運転だった**。手札を1枚奪われ強制送還される宣戦布告で、
  返事を待つ数秒がこのゲームで一番緊張する時間なのに、にらみ合う2つの駒に何も起きていなかった。
  攻めた駒は前のめりに脈打ち（`.is-contact-attacker`）、狙われた駒は自分の色で警戒する
  （`.is-contact-defender`）。**狙われた本人にだけ音**でも知らせる（`maybeAnnounceContactStandoff`——
  「起きた事実」を render() の先頭で拾う形なので、申し込みの経路が増えても取りこぼさない）。
- **⑤ 接触で飛ばされた駒の着地が無い**。通常の移動には元から着地の輪と着地音があるのに、
  **一番痛い目に遭った側の強制帰還だけ**抜けていた。`playContactFlight__inner` の末尾に足した。
- **① ゲート侵攻③「自ゲートへ帰還」だけ盤面演出が無かった**（①手札半分奪取＝光の筋、
  ②エターナル獲得＝3Dフリップ、はある）。数ターンかけた計画の締めくくりで駒が音もなく
  瞬間移動していた。**外から内へ締まっていく輪**（侵攻された側に出る `gate-intrusion-ring` とは
  逆向き＝役割の違いが動きで分かる）＋着地の輪＋音。ローカル(gate-invasion.js)・
  オンライン(gate-invasion-modal.js)の**両方**へ同じ実体を注入した。
- **② ゴメンナサイでロックエリアから1枚引き抜く瞬間**（7色目＝勝利宣言を止めたうえに相手の
  完成間近のロックを壊す、試合中で最大の逆転）が静かだった。スロットに亀裂が走り、その色の
  光の筋が奪った人の手札へ飛ぶ。**光の筋の描画は `spawnBeamBetween()` として切り出した**
  （playStealBeam の中身をそのまま移しただけ）。
- **⑥ 持ち時間が切れた瞬間が完全に無音**だった（turn-timer.js には効果音の呼び出しが1つも
  無かった）。警告が「出た瞬間」に1回だけ鳴らす。
- **⑦ 公開ドローが山札から飛ばず、その場に湧いていた**。カード効果の公開ドローと、
  **プレイヤーが実際に押す「公開ドロー」ボタン**の**両方**に飛翔を通した（後者は別経路で、
  こちらも同じく無かった）。
- **⑧ 手札が0枚でハンドフェイズが飛ぶ時**、文字のモーダルだけで手札エリア自体には何も
  起きていなかった。空の手札エリアを一度だけ淡く光らせる。

- **検証（実測・すべて本物の経路）**:
  ・③**オンラインで2人**つないで p1 から合図を流し、**p2 の画面に盾1・衝撃波2 が出た**。
  　送った側（＝防いだ本人）では**二重に出ない**ことも確認。
  ・④印が2つ付き、盤面が描き直された時点（実測4.5秒後）で**0個に戻る**。
  ・⑤**A/Bで決着**——修正後は**飛ばされた側のゲート(0,3)に着地の輪が1つ**、修正前は**0**。
  　（輪の総数だけでは他の経路の分と混ざって切り分けられなかった。**どのマスに出たか**で見た）
  ・①演出キュー経由（＝main.js の登録まで通す）で**輪が2回**。
  ・⑦**実際の「公開ドロー」ボタンをクリック**して、飛翔ゴースト1つ・**出発点が山札の位置(514,142)と一致**。
  ・②前提の実測（ロックの札→`.lock-slot` が引ける／光の筋の行き先の手札エリアが在る）＋CSS動作。
  ・⑧CSS動作。⑥は警告が出る状況を作れず**未検証**（配線は1か所・音は既存の `turnSwitch`）。
  ・`npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、`node tools/check-undeclared.mjs` 0件、
  　CSSブレース平衡（3161）、構文チェック通過、お知らせ ja/en とも7件。例外0件。

- **【今回3回踏んだ罠】`?.` で存在しない関数を呼ぶと、静かに何も起きず“嘘の失敗”を測る**。
  検証の中で `mainMod.render?.()`（**render は export されていない**）を呼び、
  「印が消えない ❌」という誤った結論を出しかけた。**UIの検証は必ず実際の要素をクリックする**か、
  **export の有無を先に確かめる**こと（続き443・456 と同じ罠でこれが3回目）。

- **【実測で見つけた設計の思い込み】公開エリアの札は `#game-table` の外にある**。
  `table.querySelector()` で探していて一度も見つからず、飛翔が丸ごと出ていなかった
  （引いた枚数1・ゴースト0で発覚）。**盤面の要素だと思い込まず、実際にどこに描かれているか
  を測ってから絞り込む**こと。

### 2026-09-07（続き463）：接触の見切りを「取り消し」から「承認」へ（**ユーザー指摘が正しかった**——取り消しは無条件の接触回避という抜け道になる）／見切りを75秒→60秒／決着まで回すテストの上限を延長

ユーザーからの指摘: 「接触された相手は承認するかカウンターロックを使うかの二択ですよね？
取り消しって作っちゃうのまずくないですか？接触しようとしてるのに無条件で拒否できてしまうのは
まずいと思います」「そもそも普通にタイマー制でタイムアップがあるから75秒自体いらないんじゃない？」

- **【指摘は正しい・修正】** 続き460 で入れた `checkContactStuckCancel()`（誰も返事しない接触を
  75秒で**取り消す**）は誤りだった。ルールブック（docs/rulebook.md 198行「接触とは: 相手プレイヤーの
  手札を無作為に1枚奪い、その相手は自分のゲートに強制移動すること」、332行、「こんな時は」）を
  読み直すと、**接触は申し込んだ側の一方的な行動**で、**接触された側に断る権利は無い**
  （唯一の対抗手段は275行のカウンターロック＝カードの効果）。
  つまり「返事をしなければ取り消される」＝**黙っているだけ・アプリを閉じるだけで、あらゆる接触を
  無条件に回避できる**。しかも接触は相手の手札を減らす主要な攻め手なので、影響は小さくない。
  - 私が「返事を見ていない相手からカードを奪ってしまわないため」と書いた配慮は**そもそも不要**
    だった。ルール上その札はもう攻撃側のもので、防御側に選択の余地は無い。
    **「優しい方に倒す」判断をする前に、そのルール上その選択肢が存在するのかを確かめること。**
  - `checkContactStuckApprove()` に改名し、`respondContact(true)` を送る。奪う札は指定せずに
    送るので**サーバー側が無作為に1枚選ぶ＝ルールどおり**。45秒の自動承認（防御側の画面で動く方）
    と結論がそろい、**放置しても不利になるだけで有利には決してならない**形になった。

- **「タイマー制があるから要らないのでは？」への回答（実測で確認した）**: **接触の承認は持ち時間の
  対象に入っていない**。`submitContactProposal` は優先権を防御側へ渡さない（渡すのは承認した後の
  `respondToContactInner`）ので、**防御側には時計が1つも動いていない**。実際、停止した回のログは
  防御側 p1 が `priority=C deadlineIn=-41s` ＝時計は攻撃側のもので既に切れており、
  `performPriorityTimeoutAutoAction` は picker とフェイズの行動しか見ないため
  `diag-auto-action-nothing` を出し続けるだけだった。**つまりこの見切りが無いと永久に止まる。**
  - 将来もし「承認も持ち時間で回す」ようにするなら、申し込みの時点で優先権を防御側へ移す必要が
    あるが、続き448 でその周辺（ピッカーの持ち主・クリックのガード）が優先権に依存していて
    事故が2件出ているので、**今回は触っていない**。

- **75秒→60秒**（ユーザー「75秒は長い」）。防御側の画面が生きていれば45秒の自動承認が先に走るので、
  ここに来るのは**防御側の通信が切れている時だけ**。カウンターロックを押した場合は
  `useCounterLockOnContactInner` の**先頭**で `respondToContact(false)` が走り pendingContact が
  即座に消える（捨てる処理・演出はその後）ので、**悩んでいる最中に横から承認される窓は無い**
  ——これを確かめた上で短くした。最後のロックの承認側（`FINAL_LOCK_STUCK_APPROVE_MS`）も同じく
  75秒→60秒（あちらは元から**承認**なので方針の変更は無い）。

- **検証（実測・本物のオンライン対戦／今回は「結果」まで測った）**: 部屋を作って2人でゲスト
  ログイン→対戦開始→**疑似CPUに数ターン打たせて防御側に札が入るまで待ってから**接触を申し込み、
  **直後に防御側のブラウザを閉じる**——**69秒で解決**し、
  `diag-contact-stuck-approve {attacker:"A",defender:"C",selfSeat:"A",iAmDefender:false}`。
  そのうえで**攻撃側の手札 0→1／防御側の手札 1→0／防御側の駒が自ゲートへ false→true**＝
  **接触が最後まで正しく成立**した（取り消しではない）。例外0件。
  - **最初の測定は無意味だった**（手札0枚・駒が最初からゲート上で、奪う対象も動く余地も無かった）。
    **「解決した」ことだけを見て満足しない**——何が起きたかを測る。
  - `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、`node tools/check-undeclared.mjs` 0件
    （162ファイル）、構文チェック通過。サーバー側（Supabase）の変更は無い＝SQLの実行・
    Edge Function の再デプロイは不要。
  - お知らせ（changelog.js）は、数時間前に配信した続き460の項目が「取り消して先に進みます」と
    **今と違う説明になっていた**ので、項目を増やさずその文言を直した（ja/en とも。続き444と同じ方針）。

- **決着まで回すテストの上限を 900秒→2700秒（45分）に**: 直前の実行は **turn 18 で「hard timeout」**
  ＝**停止ではなく、単に対局がまだ続いていた**（1ターンおよそ50秒）。7色そろえるには数十ターン
  かかるので、15分では最後まで行けない。停止の見張り（無活動100秒・同一ターン300秒）はそのまま
  なので、**本物の停止は今までどおり早期に捕まる**。
  - あわせて、この実行では**停止が1度も起きていない**（続き460/461 の修正が効いている。
    turn 4 → 12 → 23 → 18で時間切れ、と性質が変わった）。食い違い検知の再同期は4回入ったが
    いずれも「更新が遅れていただけ」で自動復帰している。

- **【同じ日に見つかった別の穴・ユーザー判断で修正】接触の承認モーダルに「🚫 拒否する」が出ていた**:
  カウンターロックを持っていない防御側にも出ており、押すと接触が完全に無効化される
  （`respondToContact(false)`＝「拒否された接触は何も起きていない」）。つまり**上のタイマーとは
  無関係に、オンラインでは誰でもあらゆる接触を無条件に断れる状態**だった。
  - **ユーザーの見立てがそのまま正解だった**——「多分『拒否する』は効果自動処理モードじゃないとき
    （ユドコネ風仕様）の仕様じゃないですか？その場合はあってもいい。効果自動処理モードでは
    無くすべき」。コードを確認すると**まさにそのとおり**で、「承認／拒否」の2択が出るのは
    `!hasCounterLock`＝**自動処理オフ（自己申告で進める形）**のUI。そこでは「カードの効果で接触を
    防いだ」等をプレイヤー自身が申告するための正当な選択肢。
  - **なぜ自動処理モードに漏れていたか**: 自動処理モードのローカル戦ではこのモーダル自体を出さない
    （main.js が自動承認する）ので表に出なかったが、**オンラインだけ #229「使えない人にも承認
    ボタンを出す」——早く通った人＝カウンターロックを持っていない、という情報漏れを防ぐため——で
    表示するようにしたときに、拒否まで一緒に出てしまっていた**。
  - **修正**: `if (!autoMode)` で囲い、拒否は自動処理オフの時だけ出す。承認ボタンは残るので
    #229 の狙い（全員が同じように1回押す＝かかった時間から何も分からない）はそのまま成立する。
  - **実測（本物のオンライン対戦・防御側の画面のボタンの文字をそのまま読んだ）**:
    **自動処理ON →「✅ 承認する」のみ**／**自動処理OFF →「✅ 承認する」「🚫 拒否する」の両方**
    （どちらもモーダルは出ており `pendingContact` も届いている）。例外0件。
  - **検証で1度空振りした（記録）**: 最初の測定は**両方とも「モーダルが出ない」**で、ON/OFF の差が
    測れていなかった。原因は自分の変更——このプローブは両席とも疑似CPU対象なので、
    続き462の「オンラインの疑似CPU席は即答する」が働いてモーダルが出る前に解決していた。
    防御側で `setPseudoCpuIncludeSelf(false)` して人間の席に戻してから測り直した。
    **両方が同じ結果になった時は「差が無い」ではなく「測れていない」を先に疑う。**
  - もう1つ: プローブで `import("/src/auto-processing.js")` と書きかけたが、その名前のモジュールは
    無い（正しくは `card-effect-engine.js` の `setAutoProcessingEnabled`）。今回は**呼ぶ前に
    export をgrepして**気づけた（続き462で3回踏んだ罠の再発を防げた）。

- **【ユーザー指示・同日】接触の返事をしている間、その人に優先権と持ち時間を渡すようにした**:
  「承認するかカウンタロックを使うかのモーダルが出ていて選択をするときは、そのプレイヤーにもちろん
  優先権が渡るべき。そのプレイヤーのタイマーが進むべき」。上で「今回は触っていない」と書いたが、
  ユーザーの判断で実施した。**心配していた2件の事故（誰が選ぶ番かの判定・クリックの取り合い）は
  続き448 で既に潰してあり**（儀式ピックは `actingSeat` を明示・クリックのガードは `picker.owner` を
  最優先で見る）、優先権に依存しなくなっていたので、今なら安全に渡せる。
  - **置き場所（重要）**: 申し込みの**呼び出し口**（`submitContactProposal`）ではなく、
    「pendingContact が新しく立った」という**事実**を render() の先頭で拾う既存の入口
    （`maybeAnnounceContactStandoff`＝続き462のにらみ合い演出）に相乗りさせた。
    最初は呼び出し口に書いたが、**検証用の経路（`state.requestContact` を直接叩く）を通らず
    測れなかった**——このプロジェクトで何度も学んだ「呼び出し口を追いかけると取りこぼす」形を
    自分でもう一度踏んだ。書き込むのは**申し込んだ側のクライアントだけ**なので競合しない。
  - **オンライン限定にした**。ローカルのCPU戦は1画面で全員を見ていて待たせる相手が居らず、しかも
    自席は持ち時間の対象外（`isSelfTimeLimitExempt`）。ここで時計を動かすと、人間が考えている
    最中に時間切れで勝手に承認される**改悪**になる。
  - **落とし穴（これを塞がないと逆に悪化する）**: turn-timer.js の tick には「**手番でない席が
    時間切れになったら、何もせず優先権を手番プレイヤーへ返す**」分岐がある（1211行）。ここに
    素通りさせると、**返事をしないまま優先権だけが返り、pendingContact が残ったまま堂々巡り**に
    なる。`owesContactAnswer`（＝優先権を持っているのが pendingContact の defender か）で
    この分岐から除外し、`performPriorityTimeoutAutoAction` に落とすようにした。
  - **時間切れ＝承認**（`performPriorityTimeoutAutoAction` に接触の分岐を追加。選択ピッカーの分岐より
    後・フェイズの分岐より前に置く——防御側は手番プレイヤーとは限らないのでフェイズの分岐には
    どれも当たらない）。カウンターロックを持っていても「使わなかった」だけ＝承認で正しい。
  - **60秒の最後の砦でも優先権を返すようにした**: 優先権を返せるのは「今まさに時間切れになった
    本人のクライアント」だけ（turn-timer.js の tick）。防御側が居なくなった状態であそこに来ると、
    承認はできても**優先権が誰にも返せなくなる**ので、あわせて手番プレイヤーへ返す。
  - **実測（本物のオンライン対戦）**: 申し込み後——**優先権が返事をする側へ渡り（実測6秒後。
    サーバー往復ぶん）**、その人の**持ち時間が動き（残り23秒）**、誰も押さないまま時間切れになると
    `diag-contact-timeout-approve` が出て**承認として解決**（防御側の手札 1→0・攻撃側 0→2・
    駒が自ゲートへ）、**優先権が手番プレイヤーへ戻る**。例外0件。
  - **検証で2回空振りした（記録）**: ①最初のプローブは `state.requestContact` を直接呼んでいて
    私の入れた箇所を通らなかった ②次は申し込みの**3秒後**に測ったが、実際の切り替わりは6秒後
    だった。どちらも「アプリが動いていない」ように見えるが**測り方の問題**。
    **0.5秒刻みで並べて見る**プローブに切り替えて初めて「6.0秒で C→A に切り替わっている」と
    分かった。**1点だけ見て結論を出さない。**

### 2026-09-07（続き464）：#319 **自分が数時間前に入れた60秒の砦が、正常な接触の解決に割り込んでいた**（＋その奥にあった「上限の無い待ち」も塞いだ）／#320 マイデッキボタンの残り／#323 連続タイムアップの見える化

ユーザーから不具合報告5件（#319〜#323）。うち #319 は**続き463で私が配信した変更の副作用**だった。

- **【#319・私の回帰】「相手のターン中に接触されて自ゲートのパーティに到達。なぜかもう自分の
  ターンになっちゃってます」（原因確定・修正）**。両者のログを突き合わせて確定できた——
  ・A(防御) 09:43:59 承認 → `respondToContactInner` 開始（優先権を A へ、タックル、そして
  　**攻撃側 C が奪う札を選ぶのを待つ**）。C はスマホの人間で、選ぶのに時間がかかっていた。
  ・A 09:44:34 **`diag-contact-stuck-approve`**（続き463で入れた60秒の砦）が発火。
  ・その中の「優先権を手番プレイヤーへ返す」が走り、**優先権が A → C へ戻った**。
  ・C 09:45:10 `diag-next-turn` ＝**A がまだ到達効果（パーティ）を処理している最中に C の
  　ターンが終わった**。A の画面では 09:46:27 まで処理が続いていた。
  - **見落とし**: 続き463のコメントに「カウンターロックを押した場合はその瞬間に pendingContact が
    消えるので、悩んでいる最中に横から承認される窓は無い」と書いたが、**承認の経路をまるごと
    考えていなかった**。承認は タックル → 相手が奪う札を選ぶ → 飛翔 と続き、その**全部の間
    pendingContact は立ったまま**。人間が相手なら60秒は普通に超える（実測 68秒）。
    「窓は無い」と書く時は、**その状態が続き得る経路を全部数え上げてから書くこと**。
  - **修正**: この砦は「**誰も解決できない**時」のためのものなので、**この画面が解決を進めて
    いる間（`contactResponseInFlight`）は時計を押し戻す**。
- **【#319の奥にあった本当の地雷】相手が奪う札を選ぶのを待つ処理に、上限が1つも無かった**
  （`waitForContactPickResolved` は素の Promise で、解決イベントが来なければ**永久に待つ**）。
  攻撃側が画面を閉じたらそこで対局が完全に停止する。しかも上の修正だけを入れると
  `contactResponseInFlight` が永久に真になり、**砦まで永久に効かなくなる**ところだった。
  90秒の上限を付け、超えたら**奪う札を指定せずに**先へ進める（＝サーバーが無作為に1枚選ぶ
  ＝ルールどおりの決着。取り消しではない）。`diag-contact-pick-timeout` を残す。
  - **教訓（続き454の再確認）**: **待ちには例外なく上限を付ける**。今回は「上限が無い待ち」の
    上に「上限付きの砦」を乗せていたので、砦の方が先に暴発していた。順番が逆だった。

- **【#320】「ロックする札を選んだあとも少しだけマイデッキボタンが表示される」（修正）**:
  ボタンを隠す条件が `hasPlacedNewLockThisPhase`（＝実際に札がロックエリアへ動いたか）で、
  オンラインではサーバーの往復のぶん真になるのが遅れる。**送信している最中かどうか**という
  事実で隠す（`noteLockSubmitInFlight`、phase-automation.js に持たせて main.js の
  `performLockPhaseClick` が印を付ける）。押せてしまう窓の方は #288 で既に塞いである。

- **【#323】「何回かタイムアップになると敗北しますが、あとなんか意味するとやばいよを見える化
  したいです」（実装）**: 調べると**ランク戦では連続タイムアップがしきい値（既定3回）に達した
  時点で本当に敗北**（`game.afk.youLost` / finishRankedForfeit）、ランク戦以外は自席がおまかせ
  （CPU代行）に切り替わる。ところが**今が何回目かはどこにも出ていなかった**＝気づかないうちに
  1回前まで来ていることがあった。
  - フェイズ案内板に `#turn-timeout-strike`（「⏳ 時間切れ 2/3・あと1回で敗北」）を追加。
    1回でも使ったら出て、**あと1回になったら赤**、増えた瞬間に一度跳ねる。在席の証拠
    （タップ/キー入力 → `resetTimeoutStreak`）でカウンタが0に戻れば自動的に消える。
  - **文言はランク戦かどうかで変える**（「敗北」か「おまかせに交代」か）。おまかせ交代を
    設定で切っている場合は**しきい値に達しても何も起きない**ので、数える意味が無い＝出さない。
  - バッジだけだと見落とすので、**あと1回になった時だけ**画面中央でも1回知らせる
    （turn-timer.js が `self-timeout-recorded` を飛ばし、main.js が受けて出す。turn-timer.js は
    main.js を import できないため、既存の `afk-cpu-threshold-reached` と同じ形にした）。

- **検証（実測・本物のオンライン対戦）**:
  ・#319: **攻撃側の画面を閉じて**再現——**砦は一度も割り込まず**（`contact-stuck` 0件）、
  　**90秒で `diag-contact-pick-timeout` が出て接触が正しく決着**（state-move → flight）、
  　優先権は最後まで防御側のまま。修正前はこの状況で**永久に止まる**（上限が無かった）。
  ・#323: 0回目＝出ない → 1回目「⏳ 時間切れ 1/3・あと2回でおまかせに交代」（琥珀）→
  　2回目「あと1回」で**赤**（rgba(127,29,29,.75)）→ 在席の証拠でリセットすると消える。
  ・`npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、`node tools/check-undeclared.mjs` 0件
  　（162ファイル）、CSSブレース平衡（3169）、翻訳キー ja/en とも同数、構文チェック通過、例外0件。
  　サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。
  - **検証で1度空振りした（記録）**: #319 のプローブは最初 6秒で終わってしまい「割り込んでいない ✅」
    と出たが、**防御側の手札が0枚**で「奪う札を選ぶ」経路自体を通っていなかった（＝待ちの上限を
    まったく測れていない）。札が配られるまで待ってから測り直した。**✅が出ても、狙った経路を
    通ったかを別の指標で確かめる**（今回は `diag-contact-pick-timeout` の有無）。

- **同じ日にユーザーの判断で一緒に直した2件**:
  ・**#321「相手のターンに色落ちキャットが出た。私の画面のミニモーダル群に捨てたログが残って
    いない」（ユーザー判断「次に直してほしい」→ 同じコミットで修正）**: 色落ちキャット・
    セレスティア等の「全員の手札を捨てさせる」効果は、**効果を使った人のクライアントだけ**が
    engine を回してお知らせのチップを作る。他の人の画面には「手札が入れ替わった結果」しか
    届かないので、何が起きたかの記録がどこにも残らなかった。
    - 呼び出し口（効果ごと）に足すと必ず取りこぼすので、**「自分の手札にあった札が捨て場へ
      積まれた」という事実**を render() で拾う（`maybeAnnounceSelfHandDiscard`。続き451の
      盤面・ロックの散り演出と同じ考え方）。ローカルでもオンラインでも、どの効果でも1か所で効く。
    - **二重に出さない作り**: 「捨てた」お知らせを出す唯一の入口（`discardFromHandReveal`）で
      tokenId に印を付け（TTL 15秒）、検出側はその印がある札を飛ばす。silent/batchNotice も
      「この画面の判断」なので印を付ける。**印を付けない捨て方は1つだけ**——手札を捨て場へ
      ドラッグした時（`onDragEnd` の `sendTokenToPile`）で、こちらは元から記録が残っていな
      かったので、記録が付くのはむしろ改善。
    - **実測**: 効果を回していない側の画面に、手札2枚が捨て場へ移る差分だけを流し込むと
      「プレイヤーAが2枚捨てた（パーティー、収穫と種まき）」のチップが残った。例外0件。
  ・**#322「このミニモーダルの『＋』しか書いてないカードは必要ですか？」（修正）**:
    最初「前のターンぶんを畳んでいる時の『あとN件』の表示だろう」と当たりを付けたが、**外れ**
    だった（あれは元からただの文字でカードの形をしていない）。実際は**お知らせチップ本体**で、
    `buildChip` は `cardId` も `cardBack` も無い出来事だと**札の形のまま中央に記号だけ**を出す。
    「＋」になる経路が2つあった——①「N枚引いた」（`announceDrawCount`。何を引いたかは伝えない
    出来事なので絵が無い）②「2枚以上まとめて手に入れた」（`cardId: visible.length === 1 ? … : null`
    ＝1枚の時しか絵を出していなかった）。どちらも**中身の無い札**に見えていた。
    ①は非公開ぶんの獲得チップと同じく**裏面**を見せ、②は**実際に手に入れた1枚目の絵**を出す
    （「＋」は隅のバッジに出るので情報は減らない。全部の中身はチップを押せば詳細で見られる）。
  - **実測**: ①`hasImage:true`（裏面）・記号だけの表示ではない・バッジ「＋」／②実際の札の絵が
    入り・バッジ「＋」。どちらも「＋だけの札」ではなくなった。
    - **検証で2度空振りした（記録）**: ①対局を始める前に測っていた（チップの帯は対局中しか
      組まれない）②トーストは**表示時間ぶん（既定5秒）中央に出てから**チップへ畳まれるのに、
      0.9秒で読んでいた。どちらも「見つからない＝直った」と読み違えかねなかった。
      **「無い」を確認する時ほど、そもそも出る条件を満たしているかを先に測る。**

### 2026-09-07（続き465）：タイトル右下のボタンが重なっていた（実測したら**2組**重なっていた）／勝利演出の1色ずつの音を到達効果音へ（暫定）／ランク戦の待機画面に豆知識を流す

ユーザー報告・要望3件。

- **【スクリーンショット付き報告】「スモークテストボタンがほかのボタンに被ってます」（修正）**:
  タイトル画面の右下には管理者向けのボタンが4つ縦に並ぶ（下から テストモード / 管理者パネル /
  ダッシュボード / スモークテスト）。**実測したところ重なりは1組ではなく2組**あった——
  「スモークテスト×ダッシュボード」18px と、**報告に無かった「管理者パネル×テストモード」2px**。
  - **なぜ重なっていたか（数字の読み違い）**: これらは `position: fixed` だが、**変形した祖先
    （1600x900のステージ）の中にある**ため fixed が absolute のように振る舞い、**1rem が実画面では
    約12.9px**にしかならない。一方ボタンの実測の高さは23px（＝約1.78rem。`border: 1px` は rem に
    連動しないぶん、縮むほど相対的に厚くなる）。つまり **rem で 1.7〜1.8 の間隔を空けても足りない**。
    ダッシュボードのCSSには「ボタンの高さ28px＜間隔28.8pxなので1pxも重ならない」というコメントが
    残っていたが、**16px/rem を前提にした机上の計算で、実測とは逆だった**。
  - **修正**: 間隔を 2.1rem にそろえ（テストモード1 / 管理者パネル3.1 / ダッシュボード5.2 /
    スモークテスト7.3）、スモークテストだけ 0.8rem だった right も 1rem にそろえて列を揃えた。
    管理者モードのスライダーの既定値（admin.js）も同じ数字に更新（style.css の fallback と
    一致させる決まり）。**間違ったコメントは消して、実測値と「計算で決めるな」を書き残した。**
  - **実測**: 修正前 → 重なり2組（18px / 2px）。修正後 → **重なり0**、4つとも right 13px で
    そろい、間の隙間は 3〜4px。例外0件。

- **【ユーザー指示】勝利演出で各ロックカードが光る瞬間の効果音を、暫定で到達時の効果音に**:
  `playSound("lock")` → `playSound("arrivalEffect")`（victory-celebration.js の COLORS 段のループ）。
  1段ずつ音が上がる `playVictoryChime(i)` は従来どおり別に鳴らす。専用の素材ができたら
  **この1行だけ**差し替えればよい形にしてある。

- **【ユーザー要望】ランク戦のマッチング待ち画面に豆知識を流す（[src/waiting-tips.js](src/waiting-tips.js) 新規）**:
  「添付の画面で暇なので、助かり情報や豆知識を流すのはどうかな？」。相手が見つかるまでの間、
  画面には回る輪と「今は他に探している人がいません」しか出ていなかった。
  - **内容は docs/rulebook.md と実装で確かめた事実だけ**にした（接触は断れない／接触と移動は別の
    行動／ゲート侵攻ボーナスの3段階／ロックは1ターン1枚／山札切れはシャッフルせず裏返す／
    ファースト・エターナルはロック中でも手札効果が使え捨てない／ゴメンナサイは7色目を止める／
    動けない時の救済／到達コンボ／連続タイムアップ／マイデッキのドロー／盤面拡大）。全12件・ja/en。
    **うろ覚えで書かない**——豆知識は間違えるとルールの誤解をそのまま広めてしまう。
  - **「カードを右クリックすると補足が読める」は入れなかった**。実装を見ると長押しの経路が無く
    （`touchstart` は接触の承認ボタンの保険だけ）、**スマホでは実行できない案内**になるため。
  - 置き場所は**一番下**（ボタン類の位置を動かさないため）。9秒ごとに、いったん薄くしてから
    差し替える（読んでいる途中で瞬間的に入れ替わらないように）。順番は毎回シャッフル。
  - **閉じたら必ず止める**（`closeWaitingScreen` で `waitingTips.stop()`）。タイマーを取り残すと
    画面が消えた後も回り続ける。
  - **実測**: 見出し「豆知識」が出て、**6回切り替わり**、`stop()` の後は2.5秒待っても文面が
    変わらない（タイマーの取り残し無し）。例外0件。

- **検証**: `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、`node tools/check-undeclared.mjs`
  0件（163ファイル）、CSSブレース平衡（3175）、翻訳キー ja/en とも同数（豆知識13キーも両方に在り）、
  構文チェック通過。サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **書き込みの作法（記録）**: 置換のアンカーが当たらず1度失敗した。原因は**行末の思い込み**——
  CLAUDE.md に「style.css は CRLF」と書いてあったが、**実際は LF** だった。以後この手の置換は
  「そのまま当たらなければ CRLF 版で当て直す」ヘルパーにして、**行末を決め打ちしない**ようにした。
  （検査が例外で止めてくれたので、続き442のような「静かに何も置換されない」事故にはならなかった。）

### 2026-09-07（続き466）：ランク戦で「対戦開始」を押さずに外された後、画面は**ぐるぐる回ったまま何も問い合わせていなかった**（相手が入り直しても永久にマッチしない）

ユーザー報告「フリーのランクマッチで対戦開始を押さなかった場合、『キューから外れました』と
なりますが、相方が再度入りなおしてもマッチしませんでした。なんで？」

- **サーバー側は仕様どおりだった**。`so7_ranked_poll` の (2-2) レディチェック締め切り（ロック後62秒）で、
  ready が2人未満なら解散し、**押した人は待機に戻す／押さなかった人はキューから削除**する
  （ユーザー要望2026-08-18「押した人だけで開始」の裏返し）。2人戦で誰も押さなければ**両方とも削除**される。
- **問題はクライアント側**。`handlePollResult` の `state === "none"` で `stopPolling()` するが、
  **待機オーバーレイはそのまま残り、ぐるぐる（`.ranked-waiting-spinner`）が回り続けていた**。
  つまり**キューにも入っていないし、サーバーへ一言も問い合わせていない**のに、画面は「探している」
  ように見える。しかも**入り直す手段が画面に無い**（残るボタンは CPU練習／通知ON／キャンセルだけ）。
  相手がキューに入り直しても、こちらは存在しないので**永久にマッチしない**——これが報告の正体。
- **実測（本物の画面。サーバーの応答だけを route で差し替え、waiting→matched→none と流した）**:
  none の後 **9秒間で問い合わせ0回**（通常は2.5秒間隔＝3〜4回）、オーバーレイは表示のまま、
  ぐるぐるは `ranked-spin` が回ったまま、押せるボタンは「CPUと練習する／お知らせをON／キャンセル」の3つ。
- **修正**: `showSearchStopped(true)` を追加し、**ぐるぐるを消す・文言を赤系にする・
  「🔁 もう一度さがす」ボタンをキャンセルの直前に出す**。押すと `enqueueRanked(myDeck)` で入り直し、
  `lastState`/`readyPressedMatchId` を捨てて `startPolling()` を再開する（見た目も探索中へ戻す）。
  文言 `rm.requeue` を ja/en 追加、`rm.L323` に「相手を探すには、もう一度さがしてください。」を追記。
  `diag-ranked-requeue` を行動ログに残す。
- **実測（修正後・同じ経路で最後までクリックして確認）**: none の後→**ぐるぐるが消え**、
  ボタンが4つ目に出る。**実際に「🔁 もう一度さがす」をクリック**すると **8秒で4回**問い合わせが再開し、
  ぐるぐるが戻り、文言が「対戦相手を探しています…」に戻り、ボタンとクラスが消える。例外0件。
- **ついでに見つけたが直していないもの（申し送り）**: `so7_ranked_queue.party_size` は
  enqueue で書き込むだけで**どこからも読まれていない**（「同じ party_size 同士だけがマッチする」という
  コメントは、その後「人数はおまかせ（2〜4人・段階的フィル）」に変わった時の取り残し）。
  全員が既定値2なので実害は無い。サーバー側のSQLを触る時にまとめて掃除するのが良い。
- **検証**: `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、`node tools/check-undeclared.mjs`
  0件（163ファイル）、CSSブレース平衡（3180）、翻訳キー ja/en とも同数、構文チェック通過。
  **サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。**

### 2026-09-07（続き467）：#324 2D表示の時、盤面がWebGLとDOMで**二重**になっていた（WebGL側が別の位置・半分の大きさで描いていた）

ユーザー報告「iPhone・ホームに追加・2D表示。ロックエリアの枠のエフェクトなどがずれてます。
フレンドリーマッチでもダメです。相手もiPhoneですが相手は大丈夫です」＋スクリーンショット。

- **実物の画像を見て、そのまま再現できた**（852x393・dpr3・2D表示でヘッドレスに再現。
  **iPhone固有ではなく2D表示なら必ず起きる**）——盤面が小さく左上に寄って描かれ、その周りに
  巨大なプレイマット、離れた場所に空の四角がいくつも散らばる。報告の画像と同じ絵になった。
- **真因**: [src/board-3d.js](src/board-3d.js) は冒頭に書いてあるとおり **「CSSの3D → WebGL の
  変換器」** で、「CSSならこう映るはず」を計算して同じ絵を1枚のキャンバスに描いている。
  ところが2D表示は style.css で
  `body.diagnostic-flatten-3d .scene { perspective: none; }` と
  `body.diagnostic-flatten-3d * { transform-style: flat !important; }`
  ＝**CSS側が3D合成そのものをやめる**。変換元が無くなるのに WebGL は動き続け、
  `syncCamera()` の `const P = parseFloat(cs.perspective) || 1000;` が
  **`"none"` → NaN → 既定値1000** を拾って**勝手な透視投影**で描き、さらに各要素の変形を
  preserve-3d 前提で合成する。結果、**盤面まるごとが別の位置・別の大きさ**になる。
  - **実測（2D表示）**: 盤面のDOMは (318,113) 214x200 なのに、WebGLは (212,42) 幅約102px
    ＝**半分の大きさで左上へずれて**描いていた。
  - プレイマット・カード・駒は**WebGLが描く**のでそちら側へ、ロックエリアの枠・各種エフェクト・
    タップ判定は**DOMのまま**なのでこちら側へ——**盤面が二重**になっていた。これが
    「ロックエリアの枠のエフェクトなどがずれてます」の正体。相手が無事だったのは3D表示だから。
- **修正**: 2D表示の間は WebGL描画を動かさない。`setBoard3dActive` を
  「やりたい(`board3dDesired`)」と「実際に動かす(`reconcileBoard3dActive`)」に分け、
  **設定がONかつ2D表示でない時だけ**動かす。`onFlatten2dModeChange` を購読して、
  2D表示のON/OFFを切り替えた瞬間にも追随する（2Dへ入ると止まり、3Dへ戻ると設定に従って再開）。
  2D表示はもともと「3D合成をやめて軽く・確実に描く」ための表示なので、WebGLを止めれば
  **素のCSS描画に戻るだけで失うものは無い**。
- **実測（修正後）**: 2D表示＝`board-3d-on` が付かない・盤面が中央に正しい大きさで描かれ、
  **ロックスロットがプレイマットに印刷された7色の帯の上に乗る**（修正前は帯の外側にずれていた）。
  3D表示＝今までどおり `board-3d-on` が付き、見た目も変化なし。例外0件。
- **注意（ユーザーへ伝達済み）**: 盤面のWebGL描画はiPhoneのちらつき・強制終了対策(#223)として
  入れたものだが、**2D表示も同じ問題への別の対策**（3D合成をやめる）なので、両方を同時に
  使う意味は元から無かった。オプションの「盤面のWebGL描画」のチェックは残るが、2D表示中は
  効かない（3D表示へ戻した時に効く）。
- **検証**: `npm test` 56/56 PASS、`node test/smoke.mjs 2` PASS、
  `node tools/check-undeclared.mjs` 0件（163ファイル）、構文チェック通過。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **調べ方の記録（次回のために）**: 最初は「ロックの位置合わせ値(`--lock-*-pos-*`)が2Dだと
  合わないのでは」と考え、位置合わせ値をいじる方向で2回測ったが、**どちらも本質ではなかった**
  （DOM同士＝スロットとカードのズレは 2D/3D とも0で、そこには問題が無かった）。
  決め手は**「盤面のカード・駒を隠して、実際に何が描かれているか」を撮ったこと**——
  WebGLが描いた盤面とDOMの枠が丸ごと別の場所にあることが一目で分かった。
  **「ずれている」報告では、まず"どの層が描いているのか"を分けて撮る。**

- **未解決（別件・診断は出ている）**: 同じログで、ブースト札4枚のうち2枚が
  `diag-boost-render` で `quad: null`（DOMも画像もあるのにWebGLの板が無い）だった。
  **これは2D/3D どちらの端末でも同じ2枚**（first-blank-red / first-blank-yellow）で起きており、
  今回の件とは別の原因。次にブースト札の見え方の報告が来たらここを見ること。

### 2026-09-07（続き468）：#325 接触を「承認せずに」終わらせると優先権が返らない（続き463の渡しっぱなし）／タイマーOFFの対局では渡さない／#326 スマホでマイページの下が永久に見切れる

ユーザー報告2件。#325 は**続き463で私が入れた「返事をする側へ優先権を渡す」の後始末漏れ**。

- **【#325・私の回帰】「私がゴメンナサイ打ったあと、相手に優先権があるままになってる」（原因確定・修正）**:
  両者のログで確定できた——12:39:45 A が接触を申し込み `diag-contact-priority
  {phase:"grant-to-defender-for-answer"}` で C へ優先権が渡る → 12:39:50 C がカウンターロックで
  止める（`diag-counter-lock use-start`）→ **以後、優先権を C から戻す記録が一切無い**。
  - **原因①（後始末漏れ）**: 優先権を返す `finishContactResolution()` は
    `respondToContactInner` の **`if (approve && defenderPieceId)` の中**にある。つまり
    **カウンターロックで止めた時と「拒否する」で断った時は、誰も返していなかった**。
    続き463で渡すようにするまでは渡していなかったので表に出なかった。**渡した分は必ず返す**。
    → `respondContact` の直後に「承認されなかった かつ 優先権が防御側にある」なら手番プレイヤーへ返す
    （`diag-contact-priority {phase:"return-after-decline"}`）。カウンターロックの後片付け
    （捨てる・告知・「1枚ロックしますか？」）の間にターンが自動で終わらないことは、呼び出し元が
    `setHandEffectBusy(true)` を握っていることで担保されている（#106）。
  - **原因②（こちらの方が根深い）・タイマーOFFの対局では渡してはいけなかった**:
    この対局は `syncedTimerConfig {enabled:false}` ＝**持ち時間タイマーOFF**。そういう対局では
    `state.priorityPlayer` は終始 null のまま進むのに、続き463のコードは
    **`initializeIfUnset: true` でわざわざ値を作っていた**。時間切れが来ないので
    `performPriorityTimeoutAutoAction` も turn-timer の「非手番の席が時間切れ→手番へ返す」分岐も
    動かず、**誰も戻せない優先権**になる。そもそも渡す理由は「返事をする人の時計を進めるため」
    （ユーザー指示）なので、時計が無い対局で渡す意味は無い。`isTurnTimerEnabled()` を条件に足した。
  - **実測（本物のオンライン対戦・実際の「🚫 拒否する」ボタンをクリック）**:
    ・タイマーON → 優先権が7.0秒で返事をする側へ渡り、**拒否の1.5秒後に手番プレイヤーへ戻る**
    　（修正前はここで戻らず、報告のとおり相手に残ったまま）。
    ・タイマーOFF → **そもそも渡さない**（priority は null のまま）。拒否も0.5秒で正常終了。
    ・例外0件。※カウンターロックも「拒否する」も同じ `respondToContactInner(false)` を通るので、
    　押しやすい方（拒否ボタン）で測った。
  - **検証で1度空振りした（記録）**: 最初の実行はタイマーONの方だけ「押したボタン: null」＝
    **モーダルが出る前に押していた**（優先権が2.5秒で渡ってループを抜けたため、クリックが早すぎた）。
    ボタンが出るまで待つループを足して測り直した。**「押せた」ことを確かめずに結果を読まない。**

- **【#326】「スマホのマイページの下が見切れてる」（原因確定・修正）**:
  `profile-layout-editor.js` が全画面のマイページへ**インラインで `overflow: hidden`** を書いていた
  （コメント: 「全画面版はステージ＝画面いっぱいなので、はみ出しは起き得ない。切れるのは画面端だけ」）。
  **その前提が縦だけ崩れていた**——スマホでは上端の安全余白（`--page-top-safe`＝オプションの帯のぶん、
  スマホでは約1.65倍）が加わるため、中身がステージの高さを超える。
  - **実測（iPhone・932x332・報告と同じ）**: 器の高さ **900 に対して中身 1007＝107はみ出し**、
    しかも `overflow: hidden` なので**指でスクロールしても届かない**＝下が永久に見切れる。
  - **修正**: 編集モードでない全画面版は **縦だけスクロールを許す**
    （`overflowX: hidden` / `overflowY: auto`）。横は従来どおりクリップする（レイアウト編集で負のxへ
    置いた装飾がステージの外へはみ出しても横スクロールを出さないため）。はみ出していない画面では
    `auto` は何も表示しないので**見た目は変わらない**。
  - **実測（修正後）**: `overflow-y: auto` / `overflow-x: hidden`、一番下までスクロールすると
    中身の下端が **360 → 320**＝画面（332）に収まる＝**届くようになった**。例外0件。
  - **見つけ方**: CSSには `overflow-y: auto` と書いてあるのに実測は `hidden` だった。
    どのルールが効いているかをブラウザに列挙させたら**該当は1件だけ**で辻褄が合わず、
    `el.getAttribute("style")` を見て**インライン指定**だと分かった。
    **CSSと実測が食い違う時は、まずインラインを疑う。**

- **検証**: `npm test` 56/56 PASS（exit 0）、`node test/smoke.mjs 2` PASS、
  `node tools/check-undeclared.mjs` 0件（163ファイル）、構文チェック通過、お知らせ ja/en とも3件。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **未解決（別件・診断は出ている）**: 同じログで、ブースト札4枚のうち2枚が `diag-boost-render` で
  `quad: null`（DOMも画像もあるのにWebGLの板が無い）。#324 と同じく**2台とも同じ2枚**で、
  今回は first-blank-green / first-blank-pink＝**C（相手）側のロックエリアの2枚**だった
  （#324 では first-blank-red / first-blank-yellow）。**「自分のロックエリアの2枚は板があり、
  相手側の2枚は無い」**という規則性が2件で一致している。次にブースト札の見え方の報告が来たら
  「相手のロックエリアのブースト札だけが見えないのでは」を最初に確かめること。

### 2026-09-07（続き469）：マイページはスクロールをやめて「収める」形に／#327 ゲート侵攻のエターナルのお知らせが案内より先に出ていた／捨てる順番を選べるように（フェニックス対策）／ランキングアイコンの2択

ユーザーからの指摘・要望5件。

- **【マイページ・続き468の作り直し】ユーザー指示「スクロールはなしにしたいです。以前はいい感じ
  でしたが、多分戻るボタンを下に下げるときに画面全体を下げちゃったんじゃないかな」**——
  **見立てのとおり**だった。上端の安全余白（`--page-top-safe`＝オプションの帯のぶん。スマホでは
  約1.65倍）を**ページ全体の padding-top** で足したので、中身がまるごと下へ押し出されていた。
  - **実測（ステージ単位・端末に関係なく同じ）**: 器 900 に対して中身 **1007**（はみ出し107）。
    内訳＝上余白54＋戻る34+16＋見出し29+22＋**カード820**＋下余白32。はみ出しの大半はカード。
  - 続き468では縦スクロールを許して届くようにしたが、指示に従い**スクロールは無し**へ戻し、
    代わりに `fitProfilePageContent()`（profile-page.js）で**カードだけを実測して縮めて収める**。
    縮めた分は `margin-bottom` の負値で打ち消す（transform は場所を空けないため）。
    画面の高さが変わったら測り直す（スマホはツールバーの出入りで実際に変わる）。
    レイアウト編集モード中は触らない。
  - **実測（修正後・932x332）**: 中身 **1007 → 900**（＝器と同じ）、一番下の要素の下端が 332＝
    画面ぴったりに収まる。スクロールは発生しない。

- **【#327】「ゲート侵攻時に先にエターナルがロックエリアにロックされちゃってる」（原因確定・修正）**:
  ゲート侵攻は**サーバー側で一度にまとめて適用**され、その後クライアントが5段の案内を流す。
  実測（両者のログ）——案内の1歩目「侵攻！」が 12:59:29.27、**その0.4秒後に lock**、
  エターナルの演出は**その19秒後**。つまり結果が案内より遥かに先に出ていた。
  - カードの絵自体は `suppressedEternalLockRender`（既存の先隠し）で隠せていたのに、
    **「ロックした」お知らせと金色の刻印の演出だけが素通りしていた**のが正体。
    `maybeAnnounceLock` に「今まさに先隠ししているエターナルなら announceCardLocked を出さない」
    を足した（`diag-gate-invasion-lock-defer` を残す）。案内の「エターナルカードを獲得！」の歩が
    そのお知らせを兼ねるので、情報は失われず**重複も消える**。
  - あわせて「手札{n}枚を無作為に**奪います**」（未来形）を「手札から無作為に{n}枚。」に直した。
    もう終わっていることを未来形で書くと「これから決まる」と誤解させるため。
  - **残っている限界（ユーザーへ伝達）**: 手札を奪う処理そのものは**サーバーが1手で確定**するので、
    「今まさに選んでいる」ようには見せられない。段階的に見せるにはEdge Functionを分割する必要がある。

- **【ユーザー要望】「複数枚捨てるときは捨てる順番を選べるようにしたい。フェニックスなどの効果が
  ある以上、捨てる順番は重要です」（実装）**: 赤のキューブ フェニックスは**捨て場の上から2番目**を
  拾うので、積む順がそのまま次の一手の価値になる。
  - **調べたら、複数枚捨てる効果のうち順番を選べなかったのは1つだけだった**——
    スラム上がりの役人（手札3枚まで捨てる）と選べる罠（手札の半分を捨てる）は元から
    1枚ずつ選ぶ作りで、**すでに選んだ順に積まれている**。順番を選べなかったのは
    **ザ・ギャンブルの「手札を全て捨てる」**（手札の並び順で機械的に捨てていた）。
  - 2枚以上ある時だけ「{n}番目に捨てるカードを選んでください（捨て場に積まれる順番）」と聞く。
    最後の1枚は自動（選ぶ意味が無い）。CPU・自動代行では pickHandCard 側が自動で選ぶ。
  - **色落ちキャットの「全員が手札を全て捨てる」は対象外**にした。全員ぶんを1人ずつ委任して
    順番を聞く形になり、対局が長くなりすぎるため。必要なら次に相談する。
  - **常設テストを1件足した**（`test/effect-cases.mjs`、期待の種類 `pileOrder` を新設）:
    手札3枚を「3枚目→1枚目」の順に選ぶと、捨て場が**その順で積まれる**ことを検査する。
    57/57 PASS。※最初この期待を書き間違えて FAIL したが、**失敗メッセージが実際の並びを
    見せてくれたおかげで「選んだ順は守られている／公開ドロー分の並びだけ想定と違った」と
    切り分けられた**（機能は最初から正しく、期待値の方が誤りだった）。

- **【ユーザー要望】ランキングアイコンの2択**: オプションエリアのランキングアイコンを押すと
  「🏆 ランキングを見る」「📊 戦績システムへ（別サイト）」を選べるモーダルを出す
  （ranking-page.js の `openRankingChoiceMenu`）。戦績システムは姉妹プロジェクト
  （BATTLE-log）なので新しいタブで開く。**実測**: 実際にアイコンをクリックして2択が出る、
  いきなりランキングは開かない、「ランキングを見る」でランキングが開きメニューが閉じる。

- **【ユーザーの質問】「カードを使用しますか？のモーダルの設定がデフォでオフになってない？」**:
  **なっていない**。`action-confirm-prefs.js` の既定は「表示する」で、明示的に "0" が保存されて
  いる時だけ非表示（＝モーダル内の「今後表示しない」を押した端末だけ）。オプションの基本設定の
  「💬 案内表示」から戻せる旨をユーザーへ伝えた。

- **検証**: `npm test` **57/57 PASS**（1件追加）、`node test/smoke.mjs 2` PASS、
  `node tools/check-undeclared.mjs` 0件（163ファイル）、CSSブレース平衡（3187）、
  翻訳キー ja/en とも同数、構文チェック通過。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **書き込みの作法（記録）**: 置換スクリプトをヒアドキュメントで書く時、**文字列の中の `\n` は
  ヒアドキュメントを通ると本物の改行になってしまう**（`ui-text.js` の "…！\n…" を狙った置換が
  当たらなかった）。バックスラッシュを含む文字列を置換する時は
  `String.fromCharCode(92)` で組み立てること（続き454の「ヒアドキュメントで .mjs を書く時は
  バックスラッシュを使わない」の具体例）。

### 2026-09-07（続き470）：ゲート侵攻を「案内どおりの順番」に見せる（[src/gate-invasion-stage.js](src/gate-invasion-stage.js) 新規）／#327 が何度直しても再発していた**構造上の理由**を特定して塞いだ

ユーザー指摘「この件（エターナルの最初のロックをちゃんと隠せていない）は過去に何度も修正して
もらっている気がします。改善しない原因はありますか？」＋「ちゃんとリアルな順番にしたいです」。

- **【真因・これが答え】隠す指示は、必ず一度描いたあとにしか出せなかった**。online.js の
  state_changed は ①合図を受け取る → ②`fetchAndHydrate()`（内部で hydrate → render まで同期的に
  走る）→ ③`.then()` でようやく gateInvasionEvents をリスナーへ渡す、という順。抑制
  （`suppressedEternalLockRender`）を立てられるのは③以降なので、**②で必ず一度描かれる**。
  これまでの修正（演出開始時 → キューに積んだ時点）は、どれも③の**内側**で前へ動かしただけで、
  ②との境界を一度も越えていなかった。しかも
  ・到達効果の処理中は③自体が数秒〜数十秒**先送り**される（#327 はまさにこれ。その間ずっと見える）
  ・侵攻が2件続くと2件目は `wasEmpty` の条件で**そもそも抑制が掛からない**
  という穴も同時に開いていた。**同じハンドラのすぐ上に `markSelfHandled(ids)` が「あとで付けたのでは
  遅い」という同じ理由のコメント付きで置いてある**——エターナルだけがその境界の外に取り残されていた。

- **やり方の選択（ユーザーと相談のうえ①）**: ①**見た目だけを据え置く** ②サーバー側を3つに分割。
  ②は「途中で通信が切れると侵攻が半分だけ適用されて対局が止まる」「駒が敵ゲートに残るので次の
  ターン終了で**二重に侵攻ボーナスが出る＝ルール違反**」という重い代償があり、救済の見張りと
  「この侵攻はもう使った」印を新設する必要がある。①なら通信が切れても**裏側は終わっているので
  何も起きない**（止まる絵が残るだけ）。プレイヤーからは見分けが付かない。

- **実装**: `gate-invasion-stage.js` に「動く前のトークンの写し」を控え、描画だけがそれを見る。
  ・仕掛ける場所は **`fetchAndHydrate()` より前**（＝上記の境界を越える。markSelfHandled と同じ場所）。
  ・描画の入口は2か所だけ（`renderBoardTokens` と `buildPlayerZone` の手札）に集約し、
  　`tokensForRender()` が写しに差し替える。**状態は1バイトも変えない**。
  ・解除は案内の段ごと（奪う → エターナル → 帰還）。帰還は据え置きを解く前に駒を隠してから
  　`playPieceMoveAnimation` で**敵ゲートから自ゲートへ跳ばす**（以前は無言のワープ＋19秒後に輪だけ）。
  ・保険は3段: 案内のキューが空になったら全解除／絶対上限120秒／状態から消えたトークンの控えは破棄。
  ・据え置き中は盤面を触らせない（`isBoardCoveredByOtherScreen` に相乗り＝#317 と同じ入口）。
  ・#327 のお知らせ抑制も据え置き側で判定するようにした（こちらは取り直しより前に立つので取りこぼさない）。
  ・**副産物**: 攻撃側が「奪う札をめくる」前に自分の手札を見れば中身が分かってしまっていたが、
  　据え置きの間は相手の手札に残った姿で描くので**ブラインドが本当にブラインドになった**。

- **検証（実測・自分のコードのバグを1つ見つけた）**: プローブで
  ・奪う: 状態は攻撃側の手札5枚なのに**画面は4枚のまま**／相手の手札は3枚のまま → 解除した瞬間に 5 と 2 へ。
  ・駒: 状態は自ゲート(0,3)なのに**画面は敵ゲート(6,3)のまま** → 解除で 0,3 へ。
  ・エターナル: 据え置き中は描かれない → 解除で現れる。お知らせの抑制判定も true。
  **1回目の測定でエターナルが隠れなかった**（`drawnWhileStaged: true`）。原因は自分のコードで、
  `isGateInvasionStageActive()` が `entries` しか数えておらず、**獲得エターナルの隠しだけが立っている
  侵攻**（奪う札も自ゲートのカードも無い場合）では描画側が据え置きを一切参照しなかった。
  ＝**#327 とまったく同じ「隠しているつもりで隠れていない」静かな失敗**を新規に作りかけていた。
  `hasAnything()` で両方を数えるよう修正し、再測定で `drawnWhileStaged: false` を確認。
  `npm test` **57/57 PASS**、`node test/smoke.mjs 2` PASS、`node tools/check-undeclared.mjs` 0件
  （164ファイル）、構文チェック通過、例外0件。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **教訓**: 「何度直しても再発する」時は、直し方ではなく**直せる場所が構造的に足りているか**を疑う。
  今回は「隠す指示を出せる一番早い瞬間」が、隠したい対象が描かれる瞬間より**後**にしか存在しなかった。

### 2026-09-07（続き471）：#331 当事者でない奪取の中身が見えていた（#217 と同じ穴の取り残し）／#332 出来事の帯が盤面に食い込む（vw をステージ座標で使っていた）／#329 ターン告知が中央の順番待ちに並んでいなかった

- **#331「CPU戦でCPUがCPUから奪ったカードが表向きでモーダルに出た」（原因確定・修正）**:
  `requestOpponentHandRitualPick` の中央公開は「自分が奪われる側」か「それ以外」の2分岐しかなく、
  **後者に“4人戦で他人が他人から奪う”場合まで含めて中身を表向きで見せていた**。CPU戦は1画面で
  全席を回すため、CPU同士のゲート侵攻でも人間の画面に出る＝**隠し情報の漏れ**。
  スリカエでは **#217 として 2026-09-03 に同じ判定（当事者だけに見せる）を入れてあり**、
  ゲート侵攻の経路だけ取り残されていた。単発・複数枚の両方に同じ判定を入れ、当事者でなければ
  **裏面＋「非公開のカード」**で「何が起きたか」だけ伝える（`getCardImagePath(null)` が裏面を返す）。
  ・あわせて、奪う側の判定を `getState().turnPlayer` から **`options.actingSeat` 優先**へ直した。
  　ゲート侵攻は**ターン終了時**に走るので turnPlayer は既に次の人であることがある（#280 と同じ罠）。
  ・**実測**: 本物のゲート侵攻を走らせて中央モーダルを読む——CがBから奪う（自分A）＝
  　名前「非公開のカード」・画像 back-normal.webp／自分Aが奪う＝「ジャンプ台」と実物どおり。例外0件。

- **#332「右下のミニモーダルが5枚以上で盤面に入ってくる」（原因確定・修正）**:
  `#turn-event-stock-list` の上限が `min(46rem, 62vw)` だった。**vw は実画面の幅で測られるのに、
  この帯は 1600x900 のステージの中にある**（変形した祖先の中では position:fixed が absolute の
  ように振る舞う）ため、vw の値がステージ座標では実質2倍前後に効き、帯が左へ伸びていた。
  実測: 盤面の右端はステージ座標で約1253px、帯の右端は約1581px＝空きは約20rem しかないのに
  上限は最大36rem相当＝**約6枚で盤面に届く**（報告の「5枚以上」と一致）。
  ステージは端末に関係なく同じ大きさなので**正しい単位は rem**。5枚ぶん(17rem)で打ち止めにし、
  それ以上は横スワイプ（`touch-action: pan-x` を明示。盤面側は touch-action:none のため）。
  前のターンの帯(`#turn-event-stock-prev-list`)にも同じ記述があったので一緒に直した。
  ・**実測**: 12枚積んで帯の幅 130px・左端709px（盤面の右端598pxから**111px空く**）。
  　修正前は上限まで伸びると左端が約573px＝**25px食い込む**。

- **#329「最後のミニモーダルが右に捌ける前に『◯◯のターンです』が出る」（原因確定・修正）**:
  ターン告知は `turnPlayer` の変化を検知した瞬間に無条件で出しており、**中央の順番待ちの列に
  一度も並んでいなかった**（#266 の「中央は一度に1つ」に enroll されていなかった）。
  獲得フラッシュは `waitForNoticeSlot(表示時間 + 飛翔0.42秒)` で中央を予約しているのに、
  告知だけがそれを無視して上に重なっていた。`waitForNoticeSlot(2200)` を通してから出す形にした。
  待ちは全て上限付き（演出9秒・返事待ちモーダル・列）なので対局は止まらない（続き454の原則）。
  ・予約の長さに **+200ms** の余裕も足した。予約は「順番が回った時点」から数え始めるのに、
  　実物がDOMに現れるのは中身を組み立てた数十ms後で、その差のぶん**飛翔の最後に約0.1秒**
  　重なっていた（実測）。
  ・**実測**（80ms間隔でDOMを見張る）: フラッシュ有り＝告知4421ms／フラッシュ最終確認4421ms
  　＝重なりは観測間隔以下。対照（フラッシュ無し）＝2197msに正常に出る。例外0件。

- **検証**: `npm test` 57/57 PASS、`node test/smoke.mjs 2` PASS、`node tools/check-undeclared.mjs`
  0件（164ファイル）、CSSブレース平衡（3187）、翻訳キー ja/en とも追加済み、構文チェック通過。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **検証で2度空振りした（記録）**: #329 のプローブが最初「告知が一度も出ない」と出た。
  原因は `quickStart(2, true)` では **turnPlayer が null のまま**で、そもそも告知の引き金が
  引かれていなかったこと（同じ理由で駒に `player` も入っていない）。**✅も❌も、狙った経路を
  通ったかを別の指標で確かめてから読む**（今回は turnPlayer の before→after を一緒に出した）。
  もう1つ: style.css は**行末が混在**している（CRLF の行と LF の行が両方ある）。行末を1つに
  決め打ちする置換ヘルパーは当たらない——LF・CRLF の両方を順に試す形にすること。

### 2026-09-07（続き472）：#333 コノハナサクヤで移動先の到達効果が起きなかった（「相手クライアントが処理してくれる」前提がCPU戦に無かった）／桜が小さすぎて見えなかった

- **#333「相手移動先のカードはオープンしたけど到達効果は発動しなかった」（原因確定・修正）**:
  カードの補足に明記がある——「効果の対象となった相手プレイヤーは『移動』扱いになるため、
  移動先のカードが裏向きであればオープンし**到達効果を得る**」（docs/cards.md）。
  ところが `MOVE_CHOSEN_OPPONENT_ADJACENT_TO_SELF` の実装は、コメントに書いてあるとおり
  **到達効果を「相手プレイヤー本人のクライアントの差分検知（remote-move-animator.js）」に
  任せる**設計だった。**CPU戦は1画面で全席を回すのでその経路が存在しない**＝カードがオープン
  するだけで何も起きない。オンラインでは差分検知が拾うので表に出ていなかった。
  ・マスチェンジ(MASS_CHANGE)は同じ場面で `helpers.triggerArrivalAtIfFaceUp?.(fromLoc, swapTargetPlayer)`
  　と**相手ぶんの到達を自分で発火**させている。そちらに揃えて1行足した。
  ・二重発火の心配は無い: `triggerCardArrival` は自動処理する席(getAutoDriveSeat)でなければ
  　拡大モーダルを出すだけなので、オンラインで両クライアントが処理することにはならない。
  ・**教訓（またこの形）**: 「別の仕組みが後で処理してくれる」という前提は、**その仕組みが
  　動かない経路**（今回はCPU戦）で必ず破れる。続き437で総点検した「Aが済めばBは起きる」の
  　親戚で、今回は“場所”ではなく“実行環境”の違いで破れた。

- **常設テストを1件追加（58/58 PASS）**: `test/effect-runner.mjs` の
  `triggerArrivalAtIfFaceUp` は**何もしない空スタブ**だったので、到達を起こし忘れてもテストが
  通っていた。呼び出しを callLog に記録するようにし、コノハナサクヤのケースで
  `{ kind: "called", name: "arrivalAt", arg: "B" }` を検査する。
  ・**この新テストが本当に効くことを確認した**: 修正の1行を外すと **57/58 FAIL**、戻すと 58/58 PASS。
  　（空スタブのまま増やしても「通るだけのテスト」になるので、必ず外して落ちることを見る。）

- **桜の演出が見えなかった（修正）**: 続き451 の申し送り「見えやすさは実機で確認をお願いしたい」
  への回答が今回の報告。**実測（932x430・ステージ縮小率約0.46）で、花びらが実画面 5.5x6.6px、
  光の筋が 1.4px** ——これでは見えない。花びら 0.72→1.25rem、筋 3→6px、渦 3.4→4.8rem、
  再生時間 900→1250ms、駒が動き出すまでの間 420→640ms にした。
  ・**実測（修正後）**: 花びら 9.6x11.5px・筋 2.9px・渦 36.7px。

- **検証**: `npm test` **58/58 PASS**、`node test/smoke.mjs 2` PASS、
  `node tools/check-undeclared.mjs` 0件（164ファイル）、CSSブレース平衡（3187）、構文チェック通過。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

### 2026-09-07（続き473）：#330 iPhoneのカクつき——**推測で直さず**、原因を1回の報告で確定できる内訳ログを入れた

- **報告ログから確定していること**（#330・#332・#333 のログを突き合わせ）:
  ・`texShape` が上限80に**張り付いたまま** `texEvicted` が 44 → 433 と増え続ける（4人戦・約7分）。
  　＝同じ枠のテクスチャを作り直してGPUへ送り直す動作が止まっていない。
  ・`rebuildMs` 50〜209ms、`frameMs` 400〜623ms（2人戦は 37〜276ms）。**人数が増えるほど悪化**。
  ・`imgMB` 51〜58（ピーク69）、起動時の `suspectedCrash: true`（5秒前に落ちて再読み込み）。

- **なぜここで直さなかったか**: 「色の明滅で毎フレーム新しい鍵ができている」のが**一番ありそう**
  だが、寸法の微揺れ（getBoundingClientRect の小数）でも同じ症状になる。丸める対象を間違えると
  効かないうえ、色を丸めれば明滅に段が出る可能性がある。**手元では再現できない**
  （ヘッドレスの4人戦は quads も shapes も実機の半分以下で texEvicted が 0 のまま。加えて
  Windows の Chromium は iOS の GPU 挙動の根拠にならない＝続き445 で踏んだ誤り）。

- **入れたもの**: `diag-board3d` に内訳を2つ追加した。
  ・`texNewColor` … 寸法（w x h | r | bw）が同じで**色だけ**違う鍵の生成数
  ・`texNewGeom`  … 寸法そのものが違う鍵の生成数
  次の報告でどちらが伸びているかを見れば、**丸めるべき対象が推測なしに決まる**
  （色なら 8/255 刻みへ、寸法なら実際に作るキャンバスの画素数へ丸める）。
  ・ヘッドレスの静かな盤面でも **色だけ違う鍵 8 / 寸法が違う鍵 1** と出ており、
  　色が主因である可能性は高い。ただし**実機で確認してから**直す。

- **検証**: `npm test` 58/58 PASS、`node test/smoke.mjs 2` PASS、
  `node tools/check-undeclared.mjs` 0件（164ファイル）、構文チェック通過、例外0件。
  プレイヤーから見える変更は無いので changelog は更新していない。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

### 2026-09-07（続き474）：ゲーム名で検索されないようにする（無料でできる範囲）／アナログ版の中身を公開対象から外した

ユーザー要望「Githubについてできるだけ『7 SHADES OF S:EVEN』などで検索されないようにしたい」＋
「Proへの加入はもう少し後にして、それ以外にできることを進めたい」。**非公開化（GitHub Pro）は先送り**し、
無料でできる対策だけを実施した。

- **前提として、まだ検索には出ていない**（実測）。「7 SHADES OF S:EVEN github」「リポジトリ名」
  「アカウント名」で検索しても、名前が似ているだけの無関係な海外プロジェクトしか出ない。
  GitHub API でも **フォーク0件・スター0件・ウォッチ0人**。HPからゲームへのリンクも無し（ユーザー確認）。
  ＝**傷が付く前**の状態なので、今から塞げば間に合う。

- **一番の穴はリポジトリの説明文だった**: `7 SHADES OF S:EVENリメイクのアプリ版の制作を行います。`
  が設定されており、**ゲーム名がそのまま日本語で入っていた**。GitHub内検索で一発で当たる。
  これはWeb画面からしか消せないのでユーザーに依頼した（手順書を提供）。
  README.md の1行目も同じくゲーム名だったので、ゲーム名を含まない最小限の内容に書き換えた。

- **アナログ版の中身そのものを公開対象から外した**（ユーザー承認済み）:
  `説明書校正/`（72ファイル・印刷物の校正データとカード画像）・`docs/rulebook.md`（ルール全文）・
  `docs/cards.md`（カード33種の効果テキスト全文）。**手元のファイルは消さず `.gitignore` で外しただけ**
  なので、コードのコメントからの参照も私の作業も一切影響を受けない（実行時には読み込まれないことを
  grep で確認済み——参照はすべてコメント内）。
  - **バックアップ**: `G:\マイドライブ\SEVEN非公開資料バックアップ\20260907\`
    （GitHubに置かなくなるぶんの控え。**Cloud Code 作業フォルダの中には置かない**——あそこは
    BATTLE-log の公開リポジトリそのものなので、続き452と同じ事故になる。実際に `.git` があることを
    確認したうえで、Googleドライブ直下の別フォルダにした）。
    75ファイル全部を **md5 で1件ずつ照合して完全一致**を確認してから `git rm --cached` した。

- **サイト側の検索避け**: `index.html` と `admin-dashboard.html` に
  `<meta name="robots" content="noindex, nofollow">` を追加。あわせて `robots.txt` を新規追加。
  - **robots.txt は今はまだ効かない**。あれは「ドメインの直下に置かれた1枚」しか読まれないので、
    現在の `.../7-SHADES-OF-S-EVEN-remake/robots.txt` は無視される。**独自ドメインを繋いで
    このリポジトリがドメイン直下になって初めて効く**（それまでは meta タグの方が効く）。
    この事情はファイル自身のコメントにも書いた。
  - **ハマりどころ**: `robots.txt` が `.gitignore` の **`/*.txt`** に飲まれて、`git status` に
    出てこなかった（続き452で事故った「ルート直下の `/*.txt`」がここでは逆に働いた）。
    `!/robots.txt` の除外解除を足して解決。**新しく追加したファイルが status に出ないときは、
    まず .gitignore を疑う**（黙って無視されるので、コミットしたつもりで入っていない事故になる）。

- **残る限界（ユーザーへ伝達済み）**: 上記はすべて**最新の状態**から外しただけで、**過去の履歴には
  残る**。＝「検索や偶然でたどり着く」は防げるが、「明確に探しに来た人が履歴を掘る」は防げない。
  完全に断つには非公開化（GitHub Pro）か履歴の書き換え。**Proに入る引き金は「宣伝を始める前／
  クラウドファンディングの告知前／アナログ版の発売前」のどれかが来た時**、とユーザーと合意した。
  - あわせて、**カードの絵・プレイマット・アバターは何をしても隠せない**ことも伝えた
    （ゲームが表示するために配信し続けるため。非公開にしても同じ）。守れるのは文書と履歴だけ。

- **テスターの人数（戦績システムを実測）**: 登録プレイヤー45人のうち、**アプリのアカウントと
  紐づいているのは17人**（対戦記録37件）。次にアドレスが変わる時の連絡対象はこの17人。
  なおアプリ側のテーブル（`so7_games` / `so7_bug_reports` / `so7_user_profiles` / `so7_visit_log`）は
  匿名キーで読みに行くと**すべて0件**が返る＝**RLSが正しく効いている**ことも確認できた。

- **検証**: `npm test` 58/58 PASS、`node test/smoke.mjs 2` PASS、`node tools/check-undeclared.mjs`
  0件（164ファイル）。プレイヤーから見える変更は無いので changelog は更新していない。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **次にやること（申し送り）**: リポジトリ名を `asobuzzasobazunihairarenai-dot.github.io` へ変更し、
  Wix（`asobuzz.net` の DNS も Wix 管理）で `seven` のCNAMEを1本足して `seven.asobuzz.net` を繋ぐ。
  これで①アドレスからGitHubのアカウント名が消える ②リポジトリ名からゲーム名が消える
  ③robots.txt が効くようになる ④戦績システムとの同一オリジン（自動ログイン）が保てる。

### 2026-09-07（続き475）：独自ドメイン（seven.asobuzz.net）への引っ越し準備——**移行で壊れる箇所が2つあった**（Googleログインと、戦績システムのランク章）

続き474の続き。ユーザーの決定は **`seven.asobuzz.net`**（`app`/`game` も提示したが「覚えやすさ・正式公開時にそのまま使えること」を優先）。
引っ越し先が決まったので、**先に壊れる箇所を洗い出してから**手を動かした。

- **【壊れる①・最重要】Googleログインが使えなくなる**: `signInWithOAuth` / `signInWithOtp` に渡す
  `redirectTo` は `cleanRedirectUrl()`＝**今開いているページのURL**を動的に組み立てている。
  このURLは **Supabaseダッシュボードの Authentication > URL Configuration の Redirect URLs で
  許可されているものだけ**が通る（online.js のコメントに既に明記されていた）。
  新しいアドレスを許可リストへ足さないと、切り替えた瞬間に**全員ログイン不能**になる。
  → **ユーザー作業**（追加するだけなので、切り替えの前にやっても既存の動作には影響しない）。

- **【壊れる②】戦績システムのランク章の画像が全部消える**: BATTLE-log 側が
  `RANK_BADGE_BASE = "https://….github.io/7-SHADES-OF-S-EVEN-remake/assets/rank-badges/"` と
  **ゲーム側のアドレスから直接**読み込んでいた（他にゲームへのリンクが1か所）。
  こちらは相対パスの形が引っ越し前後で変わる（`../7-SHADES-…/assets/` → `../assets/`）ため
  自動追従にできない。**切り替えの直後に手で直す**。

- **【対策1】ゲーム側から戦績システムへのURLを、引っ越し前後の両方で正しくなる形にした**:
  `post-game-panel.js` / `ranking-page.js` の `STATS_SITE_URL`（同じ定数が2か所にある）を
  直書きから `new URL("../BATTLE-log/", location.href).href` に変更。
  **URLの `..` はドメイン直下より上へは行かない**ので、
  `…/github.io/7-SHADES-…/` からは `…/github.io/BATTLE-log/`、`seven.asobuzz.net/` からは
  `seven.asobuzz.net/BATTLE-log/` と、**同じ1行で両方に解決される**（4パターンを実測して確認）。
  ローカルの確認用サーバーには戦績システムが無いので、localhost の時だけ公開アドレスへ逃がす。
  → これで、切り替え当日にゲーム側を触る必要が無くなった。

- **【対策2】`404.html` を新設**: 古いアドレス（ホーム画面に追加済み・ブックマーク）を持っている人が
  迷子にならないよう、案内を出して2秒後にサイト直下へ転送する。
  **GitHub Pages は存在しないパスに対してリポジトリ直下の `404.html` を返す**ので、
  **1枚置くだけで古いアドレスを全部まとめて拾える**。
  - **パスごとの転送ページを作らなかった理由**: 旧アドレスと同じ名前のフォルダを作ると、
    続き474でせっかく消したゲーム名を**リポジトリ内に書き戻す**ことになる。404.html なら
    ゲーム名を1文字も書かずに同じ効果が得られる（実際に「ゲーム名が含まれていないこと」を検査した）。
  - ルート自身が404になった場合に転送が無限に続かないよう、`pathname === "/"` は転送しない。

- **サブドメイン名について（記録）**: `seven` はゲーム名を連想させるので `app` / `game` も提示した。
  **新しく作るサブドメイン名は、証明書の透明性ログ（CT log）に公開記録として残るため隠せない**
  ——この事実を伝えたうえでユーザーが `seven` を選んだ。Google検索での発見は続き474の noindex で
  防げており、CT log を掘りに来る相手は想定読者ではない、という整理。

- **検証**: `npm test` 58/58 PASS、`node test/smoke.mjs 2` PASS、
  `node tools/check-undeclared.mjs` 0件（164ファイル）、構文チェック通過。
  URL解決は4パターン（引っ越し前後 × index.html の有無）を実測。
  プレイヤーから見える変更はまだ無いので changelog は更新していない
  （**アドレス変更のお知らせは、実際に切り替える時に1件出す**）。
  サーバー側（Supabase）のコード変更は無い＝SQLの実行・Edge Function の再デプロイは不要。
  **ただし Supabase ダッシュボードの Redirect URLs の追加だけは必要**（上記①）。

- **切り替えの順番（申し送り）**: ①Wix で `seven` の CNAME を追加（安全・まだ誰も使っていない）
  ②DNSが引けることを実測 ③Supabase に新URLを許可（追加だけなので既存に影響なし）
  ④GitHubでリポジトリ名を `asobuzzasobazunihairarenai-dot.github.io` へ変更
  ⑤Pages にカスタムドメインを設定＋HTTPSを強制 ⑥全部を実測で確認
  ⑦BATTLE-log の2か所を更新 ⑧テスター17人へお知らせ（アプリ内＋プッシュ）。
  **①②③は壊れないので先にやってよい。実際に切り替わるのは④**。

### 2026-09-07（続き476）：独自ドメイン **https://seven.asobuzz.net/** へ移行完了／「プロジェクトサイトはユーザーサイトのカスタムドメインの下に来る」を**実測で確定**／ゲーム名の入った別リポジトリが3つ残っていた

続き475の計画を実行した。**公開URLは `https://seven.asobuzz.net/`、戦績システムは `https://seven.asobuzz.net/BATTLE-log/`**。
リポジトリ名は `7-SHADES-OF-S-EVEN-remake` → **`asobuzzasobazunihairarenai-dot.github.io`**（＝アカウントの
ユーザーサイト）に変更した。

- **1回目の作業では「名前の変更だけが失敗していた」**（カスタムドメインの設定は成功）。この2つは独立した設定で、
  片方だけ成功し得る。結果として**プロジェクトリポジトリにカスタムドメインが付いた状態**になり、
  ・ゲームは `seven.asobuzz.net/` で正常に動く
  ・旧URL `…github.io/7-SHADES-…/` は **GitHubが301で新URLへ転送**
  ・しかし **`seven.asobuzz.net/BATTLE-log/` は404**（プロジェクトリポジトリのカスタムドメインは
  　**そのリポジトリ1つだけ**を配信するため）
  という中間状態になった。ここで「名前変更＝ユーザーサイト化」が戦績システムの同一オリジン化に
  **必須**だったことが実測で裏付けられた。
- **2回目で名前変更に成功し、仮説どおりになった（実測）**:
  `seven.asobuzz.net/` ＝ゲーム（title「7 SHADES OF S:EVEN デジタル版」）、
  `seven.asobuzz.net/BATTLE-log/` ＝戦績システム（title「7 SHADES OF S:EVEN 戦績管理システム」）。
  **＝ユーザーサイトにカスタムドメインを設定すると、同じアカウントのプロジェクトサイトも
  `<カスタムドメイン>/<リポジトリ名>` で配信される**。続き475で「私の推測でまだ実証できていない」と
  書いていた点が、これで確定した。**自動ログイン連携（同一オリジン）も復活**。
- **旧URLはGitHubが自動転送してくれる**（実測: ゲーム・戦績システムとも旧URL→新URLへ到達）。
  続き475で用意した `404.html` の出番は結果的に無かったが、**転送が効かない経路の保険**として残す。
- **HTTPSは自動で強制されている**（`http://` は301で `https://` へ。証明書の検証も正常）。
- **`robots.txt` が今日から本当に効くようになった**——ドメイン直下に来たため（続き474で「今はまだ
  効かない」と書いた状態が解消）。

- **【今回いちばんの見落とし】ゲーム名の入ったリポジトリが他に3つあった**。続き474で「説明文を消した」のは
  `-remake` だけで、アカウント全体を見ていなかった。
  - `7SHADES-OF-S-EVENtest`（今年2〜3月の試作版）… **Pagesが生きていて、title「7 SHADES OF S:EVEN」の
    ページを誰でも開ける状態**だった。中身にカードのルール定義のコメントまで入っている。
  - `7-SHADES-OF-S-EVEN-` … 説明文に「7 SHADES OF S:EVENリメイクのアプリ版の制作を行います。」が残存。
  - `BATTLE-log` … 説明文が「ユドコネ版セブンの戦績を記録します。」
  - **対応**: 前2つはユーザーが**非公開**にした（**リポジトリを非公開にするだけなら無料**。有料が要るのは
    「非公開のままPagesを配信し続ける」場合だけ）。API上も404になったことを確認。BATTLE-logの説明文も空にした。
  - **申し送り**: 非公開にしても**Pagesの配信は自動では止まらない**ことがある（実測で試作版サイトが
    HTTP 200 のまま残っていた）。**Settings > Pages で明示的に Unpublish する必要がある**。
  - **教訓**: 「このリポジトリの説明文を消した」で満足せず、**アカウント全体を一覧して**確認すること
    （`GET /users/<user>/repos` で名前・説明・Pagesの有無が一度に見える）。

- **BATTLE-log 側も更新した（別リポジトリ・push済み）**: ゲームへのリンクと `RANK_BADGE_BASE` の2箇所を
  `https://seven.asobuzz.net/…` へ。ランク章3種が新URLから取得できることを実測（HTTP 200）。
  相対パスにしなかったのは、**独自ドメインを取った目的が「もう二度とアドレスを変えないこと」**だから。
  なお `Cloud Code 作業フォルダ` は BATTLE-log の公開リポジトリそのものなので、**触るのは index.html だけ**
  （.gitignore で エイブン/ボードゲームレビューマン/7shades-digital は除外済みであることを確認してから作業した）。

- **検証**: `npm test` 58/58 PASS、`node test/smoke.mjs 2` PASS、`node tools/check-undeclared.mjs`
  0件（164ファイル）、構文チェック通過。お知らせ（changelog.js）にアドレス変更の案内を ja/en とも3件で追記。
  サーバー側（Supabase）のコード変更は無い＝SQLの実行・Edge Function の再デプロイは不要。
  **Supabase の Redirect URLs に `https://seven.asobuzz.net/**` を追加済み**（ユーザー作業）。
  許可されていないURLは Site URL へ弾かれることを対照実験で確かめたうえで、新URLが通ることを確認した。

- **残っていること（申し送り）**:
  ①試作版リポジトリの Pages を Unpublish する
  ②テスター17人へのお知らせ（アプリ内のお知らせ＋プッシュ通知。管理者ダッシュボードから送る）
  ③**新アドレスでの Google ログインの実機確認**（Supabaseの許可は実測済みだが、実際にログインを
  　通したわけではない）
  ④Supabase の Site URL が `http://localhost:3000` のまま（redirectTo を必ず明示しているので実害は無いが、
  　どこかで既定値に落ちると開発用のアドレスへ飛ぶ。次にSupabaseを触る時に直すとよい）
  ⑤GitHub Pro での非公開化は「宣伝を始める前／クラファン告知前／アナログ版発売前」に実施する約束。

### 2026-09-07（続き477）：移行後の後始末2件——試作版サイトは既に消えていた／**アバターの画像URLがDBに絶対URLで保存されていた**（画像だけが壊れる理由）

- **【①試作版サイトの Unpublish ボタンが無い】= 既に止まっていた**。ユーザーの画面には
  「Upgrade or make this repository public to enable Pages」＝**Pagesは既に無効**と出ており、
  だからボタンが無かった。続き476で「非公開にしてもPagesは自動では止まらない」と書いたが、
  **実際には止まっており、私が見ていたHTTP 200は配信網のキャッシュだった**（時間をおいて測り直したら
  301→`seven.asobuzz.net/7SHADES-OF-S-EVENtest/`→404 になっていた）。**訂正**: 非公開にすれば
  Pagesも止まる。ただし**しばらくキャッシュが残るので、直後の測定を結論にしないこと**。

- **【②戦績システムでアバターのサムネだけが壊れる】（原因確定・SQLで修正）**:
  `players.avatar_url` は **「保存した時に開いていたアドレス」を基準にした絶対URL**として保存される
  （online.js の `new URL(resolved, window.location.href).href` が4箇所）。移行前に保存された行は
  古いアドレスを指したままになる。
  - **なぜ「画像だけ」壊れるのか**: 旧アドレスは、末尾スラッシュ付きのトップ（＝ブックマークや
    ホーム画面の形）だけ GitHub が301で新アドレスへ転送してくれるが、**深いパスは転送されない**
    （実測: `…/7-SHADES-…/index.html` も `…/assets/avatars/x.webp` も404）。人が開くリンクは
    続き475の `404.html` が拾って新アドレスへ送るが、**画像の読み込みはJSを実行できないので拾えない**。
    ＝**人の導線は全部無事で、画像だけが壊れる**。今回の症状はこれで完全に説明が付く。
  - **影響範囲を測って確定させた**: アバターを設定している33人のうち、
    Supabaseストレージ28人・Google写真1人・**新アドレス2人**・**旧アドレス2人**。
    ＝**壊れているのは2件だけ**。しかも**ゲーム内のアバターは無事**
    （4箇所の絶対URL化は**すべて戦績システムの `players` テーブル向け**で、ゲーム内は相対パスと
    ストレージのURLを使っている）。**「他も壊れているのでは」と広げる前に、どこに保存され
    どこから読まれるかを両方たどって範囲を閉じた。**
  - **修正**: `supabase_setup_so7.sql` の末尾に UPDATE を追記（ユーザーがSQL Editorで実行）。
    `/assets/` 以降をそのまま新アドレスの下へ付け替える形にしたので、**SQL文にゲーム名は入らない**
    （リポジトリ名の部分は `%` で受ける）。条件に一致しなくなるので**再実行安全**。
    書き換わる2行の before→after を事前に実データで確認済み。
  - **放っておいても半分は直る**: 対局を登録するたびにこの列は書き直されるので、遊び続けている人の
    行は自然に新アドレスへ更新される。SQLは「しばらく遊んでいない人」のための一括修正。

- **検証**: 上記はすべて実データ・実URLで測定。`assets/avatars/orange-front.webp` が新アドレスで
  HTTP 200 であることも確認済み。アプリのコード変更は無し（＝再デプロイ不要）。
  **SQLの実行だけユーザー作業**。

### 2026-09-08（続き478）：オンラインの決着まで通すテストで**本物の停止**を掴まえた——#31対策のガードが広すぎて、CPUが優先権を持ったまま永久に止まっていた

続き458以降ずっと「決着まで到達しない」まま残っていたテストを回したところ、**turn 5 で完全停止**した。

- **掴まえた状態**: 手番はA、**優先権はCが持ったまま**、Cの持ち時間は**119秒前に切れている**。
  p1（A席）は `phase=hand` で優先権が無いので動けず、p2（C席）は `currentPhase: null`
  （フェイズは手番プレイヤーのクライアントでしか追跡されない値なので、非手番の席では常にnull）
  ＝`performPriorityTimeoutAutoAction()` が毎回 false を返し、`diag-auto-action-nothing` を
  4秒ごとに吐き続けるだけ。**誰も優先権を返さない。**

- **真因**: turn-timer.js の「手番でない席が時間切れになったら優先権を手番プレイヤーへ返す」分岐に
  付いていた `!isPseudoCpuTarget(state.priorityPlayer)` というガード。これは #31
  （委任効果の最中、結果モーダルを出している数秒だけ `isAnyEffectProcessingBusy()` が偽になる隙に
  優先権を奪い返すと、その後CPUが出すピッカーが誰にも解決されず止まる）への対策だった。
  ところが判定が**「委任が実際に走っているか」ではなく「持ち主が疑似CPUか」だけ**だったため、
  委任が何も無い場面でも一切返さなくなり、**永久に止まる**。
  - **これはテスト専用の問題ではない**: `cpu-battle.js` が `setPseudoCpuModeEnabled(true)` /
    `IncludeSelf(false)` を立てるので、**ローカルのCPU戦でも相手席は疑似CPU扱い**＝同じ条件が成立する。

- **修正**: `PSEUDO_CPU_PRIORITY_IDLE_MS = 30000` を新設し、**「処理中でない状態が連続して30秒続いた」
  時だけ**返す（`pseudoCpuIdleSince`）。カウントは ①`isAnyEffectProcessingBusy()` が真の間 ②優先権/期限が
  変わった時 ③まだ時間切れでない間 にリセットする。委任中の結果モーダルは数秒なので #31 は再発しない。
  発動時は `diag-pseudo-cpu-priority-stuck`（優先権の持ち主・手番・何もしていなかった時間）を残す。

- **教訓（この形は今回で何度目か分からない）**: 「ある状況で暴発しないように」と足したガードは、
  **その状況そのものを判定しているか**を確かめること。今回は「委任中か」を見たかったのに
  「持ち主がCPUか」で代用していた。代用の条件は必ずどこかで広すぎて、進行そのものを止める。
  そして**進行を止め得るガードには、必ず上限（最後の砦）をセットで置く**（続き454/460/461と同じ）。

- **置換ヘルパーが1件救った（記録）**: リセット箇所の目印が**2箇所**一致してヘルパーが例外で止めた。
  確認したら**2箇所とも直すべき場所**（まだ時間切れでない間／期限が動いた時）だったので両方に入れた。
  「1件でなければ止める」道具が、静かな取りこぼしと取り違えの両方を防いでいる。

- **検証**: `npm test` 58/58 PASS、`node test/smoke.mjs 2` PASS、`check-undeclared` 0件（164ファイル）。
  修正後に決着まで通すテストを再実行し、**停止していた turn 5 を越えて turn 7 まで進んだ**
  （そこで別の不具合が出た＝続き479）。サーバー側の変更は無い＝SQL実行・Edge Function 再デプロイは不要。

### 2026-09-08（続き479）：ザ・ギャンブル／試練の儀式が**昨日から壊れていた**（続き469で足した「捨てる順番を選ぶ」が配列を渡していた）／テストのスタブが**引数の並びを間違えていて絞り込みを一度も検査していなかった**

続き478の修正後にオンラインの通しテストを再実行したところ、turn 7 で**本物の例外**が出た:
`TypeError: tokenIdFilter.has is not a function`（動詞 `discard_hand_if_revealed_matches_declared`）。

- **真因（私の回帰・続き469）**: `requestHandCardChoiceForEffect(player, hint, tokenIdFilter, options)` の
  第3引数は **Set で渡す約束**（受け側が `.has()` を呼ぶ）。engine の他の2箇所は
  `new Set(...)` を渡しているのに、続き469で足した「捨てる順番を選ぶ」だけ
  `[...remaining.keys()]`＝**配列**を渡していた。配列に `.has` は無いので TypeError となり、
  **効果ごと停止**する。＝**宣言色に一致する手札が2枚以上ある時のザ・ギャンブル／試練の儀式が、
  昨日の配信からずっと動いていなかった**（1枚以下なら別の分岐なので気づけなかった）。

- **なぜ 58/58 PASS のまま素通りしたのか（こちらが本質）**: `test/effect-runner.mjs` のスタブが
  **`pickHandCard: async (player, filter) => {`** と宣言されていた。本物は
  `(player, hint, tokenIdFilter, options)` なので、**第2引数として受けていた `filter` は実際には
  見出しの文字列**だった。つまりスタブは**絞り込みを一度も見ていなかった**——続き469で足した
  常設テスト（捨てる順番）も、「順番」は検査していたが「候補の渡し方」は素通りしていた。
  **スタブが本物より甘いと、テストは通るのに本番だけ落ちる。**

- **修正（3点）**:
  1. engine 側: `new Set(remaining.keys())` を渡す（他の2箇所と揃えた）。
  2. main.js 側: `tokenIdFilter` が Set でなくても受けられるように正規化する
     （`instanceof Set ? … : new Set(…)`）。**同じ間違いが致命傷にならない一段**を足しておく。
  3. テストのスタブ: 引数の並びを本物に合わせ、**選ぼうとしたトークンが渡された候補に含まれるか**も
     検査して、含まれなければ例外にする。
  - **その検査が本当に効くことを確認した**: engine 側の候補をわざと空の Set にすると **57/58（1 FAIL）**、
    戻すと 58/58。空振りのテストを増やしていないことを実測で確かめてある。

- **教訓**: **スタブは本物と同じ引数の並び・同じ厳しさで書く**。並びがずれていても JS は何も言わないので、
  「テストはあるのに何も検査していない」状態が静かに成立する（続き472で `triggerArrivalAtIfFaceUp` が
  空スタブだったのと同じ形。これで2回目）。

- **検証**: `npm test` 58/58 PASS、`node test/smoke.mjs 2` PASS、`check-undeclared` 0件（164ファイル）、
  構文チェック通過。お知らせ（changelog.js）に ja/en とも2件で追記（**昨日の配信で壊していたので、
  プレイヤーに見える形で謝罪込みで告知した**）。サーバー側の変更は無い＝SQL実行・Edge Function 再デプロイは不要。

### 2026-09-08（続き480）：オンラインの通しテストが**初めて決着まで到達した（PASS）**／3回目の失敗は「テストが90秒の待ちを数え落としていた」だけだった

続き478・479の修正のあと、3回目は turn 3 で `arrivalProcessing: true` のまま100秒でFAILした。
相手側には `#sleight-ritual-modal` が開いたままで、`diag-notice-wait` が
`blocker: "backdrop>div#sleight-ritual-modal"` を繰り返していた。

- **原因はテスト側**: `STALL_MS = 100000` に対し、**アプリ自身の最長の立て直しは90秒**——
  委任の応答待ち（main.js の `giveUpTimer` 90秒）と接触で奪う札の選択待ち
  （`CONTACT_PICK_WAIT_MAX_MS` 90秒）。差が10秒しかなく、**90秒で諦めたアプリが実際に動き出す前に
  テストが先に失敗と決めていた**。
  - **同じ原則はすでにその場所のコメントに書いてあった**（続き463で私が書いた「アプリ側の最長の
    立て直しより必ず後に諦めること」）。**原則は正しかったが、当時の最長を75秒だと思っており、
    90秒の待ち2つを数え落としていた。** 原則を書くだけでは足りず、**その数字の根拠となる待ちを
    網羅できているか**を数え直す必要がある。
  - **修正**: `STALL_MS = 150000`。あわせて「アプリ側に90秒より長い待ちを足したら、ここも必ず伸ばす」と
    明記した。**閾値を上げて誤魔化したのではない**——「手は動いているのにターンだけ進まない」堂々巡りは
    `TURN_STALL_MAX_MS`（300秒）が別に見張っているので、本物の異常の検出力は落ちていない。

- **結果（4回目・実測）**: `node test/online-smoke.mjs 2 --full --isolated` が
  **turn 20 で決着して PASS**（`someone won at turn 20 — healthy finish`）。
  **このテストが決着まで到達したのは初めて**（続き458以降ずっと未達成だった）。
  途中2回「クライアント間の食い違い」を検知したが、いずれも再同期で「更新が遅れていただけ」と判明し
  自動復帰している（続き460以降と同じ健全な挙動）。

- **今回の一連（続き478〜480）で分かったこと**: 放置していた通しテストを1本通すだけで、
  ①**ローカルCPU戦でも起こり得る永久停止** ②**前日から壊れていたカード効果**
  ③**検査していなかったテストのスタブ** ④**テスト自身の見張り時間の誤り** が芋づるで出た。
  **「たまに落ちるから」と放置した検証は、本物の不具合を隠す**（続き458で書いた教訓の実例）。

- **未解決の手がかり（次に同じ停止が出たら見ること）**: 3回目の停止時、A側に
  `#sleight-ritual-modal` が**ピッカーの状態が無いまま（`pickerType: null`）開きっぱなし**だった。
  4回目はPASSしたので今回は追わないが、**「モーダルだけ取り残される」経路がある可能性**がある。
  再発したら `diag-notice-wait` の `blocker` と `pickerType` を突き合わせること。

- **検証**: `npm test` 58/58 PASS、`node test/smoke.mjs 2` PASS、`check-undeclared` 0件（164ファイル）、
  `node test/online-smoke.mjs 2 --full --isolated` **PASS（初）**。
  今回の変更はテストのしきい値のみ＝配信物（src/）に変更は無い。

### 2026-09-08（続き481）：#330 iPhoneのカクつき——**原因が実機ログで確定**（色だけが毎フレーム変わって鍵が増え続けていた）／推測で直さなかったのが効いた

続き473で「推測で直さず、1回の報告で確定できる内訳ログを入れる」と決めて仕込んだ `texNewColor` /
`texNewGeom` が、そのとおり1回の報告で答えを出した。

- **実測（4人戦・約17分・iPhone 18.7 / dpr3）**:
  ・**色だけ違う鍵の生成数 `texNewColor`: 67 → 416（増え続ける）**
  ・**寸法が違う鍵の生成数 `texNewGeom`: 4 → 4（一切増えない）**
  ・`texShape` は上限80に張り付き、`texEvicted` 0 → 340。`rebuildMs` 40〜138ms、`frameMs` 最大 571.9ms。
  ＝**丸めるべきは色**であって寸法ではない、と推測なしに確定した。
  （続き473の時点で「色が主因の可能性は高い」と見ていたが、丸める対象を間違えると効かないうえ
  　色を丸めれば明滅に段が出る恐れもあったので、実機の数字を待った判断は正しかった。）

- **真因**: `ensureShapeMesh` の鍵が `w x h | r | bw | 枠色 | 背景色 | グロー` で、**色を文字列のまま
  使っていた**。手番の明滅・グロー・ハイライトは透明度が連続的に変わるので、**毎フレーム別の鍵**になり、
  上限80枚の棚から追い出しては作り直す。その作り直し（canvasへの再描画＋GPUへの再転送）が
  1フレームを丸ごと持っていく。

- **修正**: `quantizeColor()` を新設し、**鍵にする前に色を粗い刻みへ丸める**
  （各チャンネル8/255刻み・透明度1/16刻み）。`rgb()`/`rgba()` だけを対象にし、
  16進や `transparent` などはそのまま通す（元から安定しているため）。
  **丸めた色は描画にもそのまま渡す**（鍵と絵がずれると「同じ鍵なのに違う色」になる）。
  変換結果は Map にキャッシュし、4000件を超えたら丸ごと捨てる。
  - **実測（丸めの効果）**: 手番グローの明滅40サンプルが **40種→7種**、色のわずかな揺れ30サンプルが
    **30種→4種**。色のずれは最大 4/255（透明度は1/32）で、白地に薄く重なる光では見分けが付かない。

- **書き込みの記録**: board-3d.js は **CRLF と LF が混在**（続き471の style.css と同じ）。
  LF 決め打ちのアンカーが 0 件で止まり、ヘルパーを「LF→CRLF の順に試す」形へ直してから適用した。
  **止まってくれたおかげで静かな取りこぼしにならなかった**（続き442の事故対策が2度目の仕事をした）。

- **検証**: `npm test` 58/58 PASS、`node test/smoke.mjs 2` PASS、
  `node tools/check-undeclared.mjs` 0件（164ファイル）、構文チェック通過。
  お知らせ（changelog.js）に ja/en とも1件追記。
  サーバー側の変更は無い＝SQL実行・Edge Function の再デプロイは不要。
  - **未確認（実機でしか測れない）**: ヘッドレスは盤面のWebGL描画を切って動かすため `texNewColor` を
    こちらで測れない。**次の4人戦のログで `texNewColor` の伸びが止まっているか**を必ず確認すること
    （止まっていなければ、丸めの刻みが足りないか、別に色を変えている経路がある）。

- **同じ報告で見つかった別件2つ（未着手・次にやる）**:
  - **#334「ゲート侵攻処理中にターンがCPU3に移ってしまった」**: T14 のログで、C のゲート侵攻の最中
    （steal-hand 23:50:01 → eternal 23:50:04）に、**C が2枚目のロックをしていた**
    （23:48:39 に pink-party をロック済みなのに、23:50:06 に奪ったばかりの card-60 をロック）。
    その後 `diag-lock-click-skip {reason:"already-locked-this-phase"}` が**6秒間に約30回**出続けている
    ＝ロックの自動処理がゲート侵攻の最中に走り、空回りしている。**1ターン2枚ロックはルール違反**。
    `diag-anytime-checkpoint` は `busy:true` だが `gateInvasionQueueActive:false` を報告しており、
    **ゲート侵攻中であることが自動処理側に伝わっていない**のが怪しい。
  - **#335「ワイナウェアで捨てたマスがどこかわからなかった」**: 続き447でハイライトを入れたが、
    ユーザーには伝わっていない。ユーザー提案は「**燃える演出が終わった後に**ミニモーダルを出す」。
    今は捨てる処理と同時にお知らせが出るので、演出とお知らせが同時に走って埋もれていると思われる。
